# 📝 Dokumentasi Fitur Edit Item - Shopping List App

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Perubahan pada CSS](#perubahan-css)
- [Perubahan pada JavaScript](#perubahan-javascript)
  - [Menambahkan Variable Global](#variable-global)
  - [Mengambil Element Button](#element-button)
  - [Membuat Handler untuk Edit](#handler-edit)
  - [Membuat Fungsi setItemToEdit](#fungsi-setitemtoedit)
  - [Penjelasan Lengkap setItemToEdit](#penjelasan-setitemtoedit)
- [Cara Kerja Fitur Edit](#cara-kerja-edit)
- [Hasil Akhir](#hasil-akhir)
- [Catatan Penting](#catatan-penting)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Pada dokumentasi ini, kita akan belajar cara membuat **fitur edit item** pada Shopping List App. Fitur ini memungkinkan kita untuk:

✅ **Klik pada item** untuk masuk ke mode edit  
✅ **Mengubah tampilan item** yang sedang diedit (menjadi abu-abu)  
✅ **Mengisi form input** dengan teks item yang diklik  
✅ **Mengubah tombol** menjadi "Update Item" dengan icon pen  

> 💡 **Catatan**: Pada tutorial ini kita hanya fokus untuk **masuk ke mode edit**. Fitur untuk benar-benar update item akan dibuat di video berikutnya.

---

<a name="perubahan-css"></a>
## 🎨 Perubahan pada CSS

Kita menambahkan **satu class CSS baru** untuk memberikan efek visual ketika item dalam mode edit.

### ➕ Kode yang Ditambahkan:

```css
.edit-mode {
  color: #ccc;
}
```

📌 **Penjelasan:**
- Class `.edit-mode` akan membuat warna teks menjadi **abu-abu terang** (#ccc)
- Class ini akan diterapkan pada item yang sedang dalam mode edit
- Memberikan **visual feedback** kepada user bahwa item sedang dipilih untuk diedit

---

<a name="perubahan-javascript"></a>
## 🔧 Perubahan pada JavaScript

<a name="variable-global"></a>
### 1️⃣ Menambahkan Variable Global `isEditMode`

**➕ Kode yang Ditambahkan:**

```javascript
let isEditMode = false;
```

📍 **Posisi**: Di bagian paling atas file, setelah deklarasi variable lainnya

📌 **Penjelasan:**
- Variable `isEditMode` digunakan untuk **menyimpan status** apakah aplikasi sedang dalam mode edit atau tidak
- Menggunakan `let` karena nilai variable ini akan **berubah-ubah**
- Default value adalah `false` (tidak dalam mode edit)

---

<a name="element-button"></a>
### 2️⃣ Mengambil Element Button Form

**➕ Kode yang Ditambahkan:**

```javascript
const formBtn = itemForm.querySelector('button');
```

📍 **Posisi**: Di bagian atas, bersama deklarasi element lainnya

📌 **Penjelasan:**
- `formBtn` menyimpan referensi ke **tombol submit** pada form
- Menggunakan `itemForm.querySelector('button')` untuk mencari button di dalam form
- Variable ini akan digunakan untuk **mengubah tampilan button** saat mode edit

---

<a name="handler-edit"></a>
### 3️⃣ Membuat Handler untuk Edit Item

**🔄 Kode yang Diubah pada fungsi `onClickItem`:**

```javascript
function onClickItem(e) {
  if (e.target.parentElement.classList.contains('remove-item')) {
    removeItem(e.target.parentElement.parentElement);
  } else {
    // ➕ BARIS BARU
    setItemToEdit(e.target);
  }
}
```

📌 **Penjelasan:**
- Kita menambahkan **bagian `else`** pada kondisi if
- **Logika kerja:**
  - **Jika** klik pada icon X → hapus item (`removeItem`)
  - **Jika tidak** (klik di area lain) → masuk mode edit (`setItemToEdit`)
- `e.target` adalah element yang diklik (dalam hal ini, `<li>` item)

---

<a name="fungsi-setitemtoedit"></a>
### 4️⃣ Membuat Fungsi `setItemToEdit` (BARU)

**➕ Kode Fungsi Baru:**

```javascript
function setItemToEdit(item) {
  isEditMode = true;

  itemList
    .querySelectorAll('li')
    .forEach((i) => i.classList.remove('edit-mode'));

  item.classList.add('edit-mode');
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  formBtn.style.backgroundColor = '#228B22';
  itemInput.value = item.textContent;
}
```

---

<a name="penjelasan-setitemtoedit"></a>
### 📖 Penjelasan Lengkap Fungsi `setItemToEdit`

#### 🔹 **Langkah 1: Set Mode Edit**
```javascript
isEditMode = true;
```
- Mengubah variable global `isEditMode` menjadi `true`
- Menandakan bahwa aplikasi sekarang dalam **mode edit**

---

#### 🔹 **Langkah 2: Hapus Class edit-mode dari Semua Item**
```javascript
itemList
  .querySelectorAll('li')
  .forEach((i) => i.classList.remove('edit-mode'));
```

📌 **Penjelasan detail:**
- `itemList.querySelectorAll('li')` → ambil **semua item** (`<li>`)
- `.forEach((i) => ...)` → looping untuk **setiap item**
- `i.classList.remove('edit-mode')` → **hapus class** `edit-mode`

💡 **Tujuan**: Memastikan hanya **satu item** yang berwarna abu-abu (mode edit)

**Contoh:**
- Klik "Juice" → Juice jadi abu-abu
- Klik "Eggs" → Eggs jadi abu-abu, Juice kembali hitam

---

#### 🔹 **Langkah 3: Tambah Class edit-mode pada Item yang Diklik**
```javascript
item.classList.add('edit-mode');
```

📌 **Penjelasan:**
- `item` adalah element `<li>` yang diklik
- `.classList.add('edit-mode')` → **tambahkan class** edit-mode
- Item akan **berubah warna** menjadi abu-abu (sesuai CSS)

---

#### 🔹 **Langkah 4: Ubah HTML Button (Icon & Text)**
```javascript
formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
```

📌 **Penjelasan:**
- Mengubah **isi HTML button** secara keseluruhan
- `<i class="fa-solid fa-pen"></i>` → icon **pen** (pensil)
- Teks berubah dari "➕ Add Item" menjadi "📝 Update Item"

---

#### 🔹 **Langkah 5: Ubah Warna Background Button**
```javascript
formBtn.style.backgroundColor = '#228B22';
```

📌 **Penjelasan:**
- Mengubah warna background button menjadi **hijau** (#228B22)
- Memberikan **visual feedback** bahwa mode berubah
- **Normal mode**: Hitam
- **Edit mode**: Hijau

---

#### 🔹 **Langkah 6: Isi Input dengan Text Item**
```javascript
itemInput.value = item.textContent;
```

📌 **Penjelasan:**
- `itemInput` → element input form
- `.value` → property untuk **mengisi nilai** input
- `item.textContent` → **teks dari item** yang diklik

💡 **Kenapa `.textContent`?**
- `item` adalah element `<li>` (bukan string)
- Kita perlu **ambil teks** dari element tersebut

**Contoh:**
- Klik item "Juice" → input terisi "Juice"
- Klik item "Eggs" → input terisi "Eggs"

---

<a name="cara-kerja-edit"></a>
## 🎬 Cara Kerja Fitur Edit

### Flow / Alur Kerja:

1. **User klik pada item** (misal: "Juice")
2. **Event click** ditangkap oleh `onClickItem(e)`
3. **Cek**: Apakah yang diklik icon X?
   - ❌ **Tidak** → Jalankan `setItemToEdit(e.target)`
4. **Fungsi `setItemToEdit`** dijalankan:
   - Set `isEditMode = true`
   - Hapus class `edit-mode` dari semua item
   - Tambah class `edit-mode` pada item yang diklik
   - Ubah button: icon → pen, text → "Update Item"
   - Ubah warna button → hijau
   - Isi input dengan teks item

---

<a name="hasil-akhir"></a>
## ✨ Hasil Akhir

### 🔴 Mode Normal (Sebelum Klik Item):
- ✅ Button berwarna hitam
- ✅ Icon: ➕ (plus)
- ✅ Text: "Add Item"
- ✅ Input kosong
- ✅ Semua item berwarna hitam

### 🟢 Mode Edit (Setelah Klik Item):
- ✅ Button berwarna **hijau** (#228B22)
- ✅ Icon: **📝** (pen)
- ✅ Text: **"Update Item"**
- ✅ Input terisi dengan teks item yang diklik
- ✅ Item yang diklik berwarna **abu-abu**
- ✅ Item lain tetap berwarna hitam

---

<a name="catatan-penting"></a>
## 📌 Catatan Penting

> ⚠️ **Fitur Belum Lengkap**
>
> Pada tutorial ini, kita baru membuat **tampilan mode edit**. Ketika klik button "Update Item", item akan **ditambahkan** (bukan diupdate).
>
> Fitur untuk **benar-benar update item** akan dibuat di video/tutorial berikutnya.

---

### 🔑 Poin-Poin Penting:

1. **Variable `isEditMode`**
   - Menyimpan status mode aplikasi
   - Akan digunakan untuk logika update di tutorial berikutnya

2. **Menggunakan Class CSS vs Style Inline**
   - Item → menggunakan **class** `.edit-mode`
   - Button → menggunakan **style inline** `.style.backgroundColor`
   - Keduanya valid, pilih sesuai kebutuhan

3. **Remove Class dari Semua Item**
   - Penting agar tidak ada **konflik visual**
   - Hanya satu item yang berwarna abu-abu

4. **Perbedaan `.textContent` vs `.value`**
   - `.textContent` → untuk **element HTML** (li, div, p, dll)
   - `.value` → untuk **input form** (input, textarea, select)

---

## 🎉 Selesai!

Sekarang aplikasi Shopping List kita sudah punya **mode edit** yang terlihat jelas! User bisa klik item dan melihat perubahan visual yang memberitahu bahwa item siap untuk diedit.

Di tutorial berikutnya, kita akan melanjutkan dengan **logika update** yang sebenarnya! 🚀

---

**Happy Coding! 💻✨**
