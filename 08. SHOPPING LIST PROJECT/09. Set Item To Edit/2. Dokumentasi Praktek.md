# 📝 Tutorial Membuat Fitur Edit Mode pada Shopping List App

> 🎓 **Untuk Pemula** - Ikuti langkah demi langkah dengan santai!

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan Awal](#persiapan-awal)
- [Langkah 1: Membuat Variable Edit Mode](#langkah-1)
- [Langkah 2: Menambahkan Handler Click](#langkah-2)
- [Langkah 3: Membuat Fungsi setItemToEdit](#langkah-3)
- [Langkah 4: Mengubah Tampilan Item](#langkah-4)
- [Langkah 5: Membuat Class CSS Edit Mode](#langkah-5)
- [Langkah 6: Mengubah Tampilan Button](#langkah-6)
- [Langkah 7: Mengisi Input dengan Text Item](#langkah-7)
- [Langkah 8: Mengubah Warna Button](#langkah-8)
- [Langkah 9: Menghapus Edit Mode dari Item Lain](#langkah-9)
- [Hasil Akhir](#hasil-akhir)
- [Full Code](#full-code)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan membuat fitur **Edit Mode** pada aplikasi Shopping List. Fitur ini memungkinkan user untuk:

✅ Klik pada item untuk mengeditnya  
✅ Item yang diklik akan berubah warna menjadi lebih terang  
✅ Button berubah dari "Add Item" menjadi "Update Item"  
✅ Text dari item otomatis muncul di input field  
✅ Button berubah warna menjadi hijau  

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

Pastikan kamu sudah memiliki:
- 📄 File `script.js` dengan kode dasar Shopping List
- 📄 File CSS untuk styling
- 🖥️ Browser untuk testing

---

<a name="langkah-1"></a>
## 📌 Langkah 1: Membuat Variable Edit Mode

### 🎯 Tujuan
Membuat variable global untuk menandai apakah aplikasi sedang dalam mode edit atau tidak.

### 💻 Kode

Tambahkan di bagian **paling atas** file `script.js`, setelah deklarasi variable lainnya:

```javascript
const itemForm = document.getElementById('item-form');
const itemInput = document.getElementById('item-input');
const itemList = document.getElementById('item-list');
const clearBtn = document.getElementById('clear');
const itemFilter = document.getElementById('filter');
const formBtn = itemForm.querySelector('button'); // ⭐ TAMBAHKAN INI
let isEditMode = false; // ⭐ TAMBAHKAN INI
```

### 📝 Penjelasan
- `formBtn` → Menyimpan referensi ke button dalam form
- `isEditMode` → Variable boolean untuk tracking mode (false = mode tambah, true = mode edit)
- Menggunakan `let` karena nilainya akan berubah-ubah

### ✅ Cek
Variable sudah dideklarasikan? Lanjut ke langkah berikutnya!

---

<a name="langkah-2"></a>
## 📌 Langkah 2: Menambahkan Handler Click

### 🎯 Tujuan
Membuat logika agar ketika item diklik (bukan tombol X), akan masuk ke mode edit.

### 💻 Kode

Ubah fungsi `onClickItem` yang sudah ada:

```javascript
function onClickItem(e) {
  if (e.target.parentElement.classList.contains('remove-item')) {
    removeItem(e.target.parentElement.parentElement);
  } else {
    setItemToEdit(e.target); // ⭐ TAMBAHKAN INI
  }
}
```

### 📝 Penjelasan
- **IF**: Jika yang diklik adalah button X → hapus item
- **ELSE**: Jika yang diklik adalah item itu sendiri → masuk mode edit
- `e.target` → Element yang diklik

### 🧪 Testing
Coba klik pada item (bukan tombol X). Belum ada perubahan karena fungsi `setItemToEdit` belum dibuat.

---

<a name="langkah-3"></a>
## 📌 Langkah 3: Membuat Fungsi setItemToEdit

### 🎯 Tujuan
Membuat fungsi yang akan mengatur semua perubahan saat masuk mode edit.

### 💻 Kode

Tambahkan fungsi baru (bisa diletakkan setelah fungsi `onClickItem`):

```javascript
function setItemToEdit(item) {
  isEditMode = true; // ⭐ Set mode menjadi edit
  
  // Kode lainnya akan ditambahkan di langkah berikutnya
}
```

### 📝 Penjelasan
- Fungsi menerima parameter `item` (element yang diklik)
- Langkah pertama: ubah `isEditMode` menjadi `true`

---

<a name="langkah-4"></a>
## 📌 Langkah 4: Mengubah Tampilan Item (Versi Pertama)

### 🎯 Tujuan
Membuat item yang diklik berubah warna menjadi lebih terang.

### 💻 Kode - Cara 1 (Menggunakan Style Langsung)

```javascript
function setItemToEdit(item) {
  isEditMode = true;
  
  // ⭐ TAMBAHKAN: Ubah warna text menjadi abu-abu
  item.style.color = '#CCC';
}
```

### 🧪 Testing
Klik pada item → item akan berubah warna menjadi abu-abu terang.

### ⚠️ Catatan
Cara ini langsung mengubah style. Kita akan ganti dengan cara yang lebih baik menggunakan CSS class.

---

<a name="langkah-5"></a>
## 📌 Langkah 5: Membuat Class CSS Edit Mode

### 🎯 Tujuan
Membuat class CSS khusus untuk mode edit (lebih professional dan mudah dikelola).

### 💻 Kode CSS

Buka file CSS dan tambahkan class baru:

```css
.edit-mode {
  color: #CCC; /* Abu-abu terang */
}
```

### 💻 Kode JavaScript

Ubah fungsi `setItemToEdit`:

```javascript
function setItemToEdit(item) {
  isEditMode = true;
  
  // ⭐ GANTI cara sebelumnya dengan ini:
  item.classList.add('edit-mode');
}
```

### 📝 Penjelasan
- `classList.add()` → Menambahkan class CSS ke element
- Lebih baik daripada mengubah style langsung
- Lebih mudah di-maintain

### 🧪 Testing
Klik pada item → hasilnya sama seperti sebelumnya, tapi dengan cara yang lebih baik!

---

<a name="langkah-6"></a>
## 📌 Langkah 6: Mengubah Tampilan Button

### 🎯 Tujuan
Mengubah icon dan text button dari "Add Item" menjadi "Update Item" dengan icon pen.

### 💻 Kode

Tambahkan di dalam fungsi `setItemToEdit`:

```javascript
function setItemToEdit(item) {
  isEditMode = true;
  
  item.classList.add('edit-mode');
  
  // ⭐ TAMBAHKAN: Ubah button content
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
}
```

### 📝 Penjelasan
- `innerHTML` → Mengubah isi HTML dari button
- `<i class="fa-solid fa-pen">` → Icon pen dari Font Awesome
- Spasi ditambahkan agar ada jarak antara icon dan text

### 🧪 Testing
Klik pada item → button berubah menjadi "Update Item" dengan icon pen!

---

<a name="langkah-7"></a>
## 📌 Langkah 7: Mengisi Input dengan Text Item

### 🎯 Tujuan
Ketika item diklik, text-nya otomatis muncul di input field untuk diedit.

### 💻 Kode

Tambahkan di dalam fungsi `setItemToEdit`:

```javascript
function setItemToEdit(item) {
  isEditMode = true;
  
  item.classList.add('edit-mode');
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  
  // ⭐ TAMBAHKAN: Isi input dengan text item
  itemInput.value = item.textContent;
}
```

### 📝 Penjelasan
- `itemInput.value` → Mengubah nilai input field
- `item.textContent` → Mengambil text dari item yang diklik
- Menggunakan `value` (bukan `textContent`) karena ini adalah input element

### 🧪 Testing
Klik pada item → text item muncul di input field!

---

<a name="langkah-8"></a>
## 📌 Langkah 8: Mengubah Warna Button

### 🎯 Tujuan
Mengubah warna background button menjadi hijau untuk membedakan mode edit.

### 💻 Kode

Tambahkan di dalam fungsi `setItemToEdit`:

```javascript
function setItemToEdit(item) {
  isEditMode = true;
  
  item.classList.add('edit-mode');
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  formBtn.style.backgroundColor = '#228B22'; // ⭐ TAMBAHKAN INI
  itemInput.value = item.textContent;
}
```

### 📝 Penjelasan
- `#228B22` → Kode warna hijau (Forest Green)
- Menggunakan `style.backgroundColor` untuk mengubah warna background

### 🧪 Testing
Klik pada item → button berubah warna menjadi hijau!

---

<a name="langkah-9"></a>
## 📌 Langkah 9: Menghapus Edit Mode dari Item Lain

### 🎯 Tujuan
Ketika kita klik item lain, item sebelumnya harus kembali normal (tidak abu-abu lagi).

### 💻 Kode

Tambahkan di **AWAL** fungsi `setItemToEdit` (sebelum `isEditMode = true`):

```javascript
function setItemToEdit(item) {
  // ⭐ TAMBAHKAN DI AWAL: Hapus edit mode dari semua item
  itemList
    .querySelectorAll('li')
    .forEach((i) => i.classList.remove('edit-mode'));
  
  isEditMode = true;
  
  item.classList.add('edit-mode');
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  formBtn.style.backgroundColor = '#228B22';
  itemInput.value = item.textContent;
}
```

### 📝 Penjelasan
- `querySelectorAll('li')` → Ambil semua item list
- `.forEach()` → Loop setiap item
- `.classList.remove('edit-mode')` → Hapus class edit-mode dari setiap item
- Dilakukan di awal agar semua item kembali normal dulu
- Baru kemudian item yang diklik ditambahkan class edit-mode

### 🧪 Testing
1. Klik item pertama → item jadi abu-abu
2. Klik item kedua → item kedua jadi abu-abu, item pertama kembali normal! ✅

---

<a name="hasil-akhir"></a>
## 🎉 Hasil Akhir - Kode Lengkap

### 📄 Fungsi setItemToEdit - Versi Final

```javascript
function setItemToEdit(item) {
  // Reset semua item
  itemList
    .querySelectorAll('li')
    .forEach((i) => i.classList.remove('edit-mode'));
  
  // Set mode edit
  isEditMode = true;
  
  // Tandai item yang sedang diedit
  item.classList.add('edit-mode');
  
  // Ubah button
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  formBtn.style.backgroundColor = '#228B22';
  
  // Isi input dengan text item
  itemInput.value = item.textContent;
}
```

### ✨ Fitur yang Berhasil Dibuat

✅ Klik item untuk masuk mode edit  
✅ Item yang dipilih berubah warna menjadi abu-abu terang  
✅ Button berubah menjadi "Update Item" dengan icon pen  
✅ Button berubah warna menjadi hijau  
✅ Text item otomatis muncul di input field  
✅ Hanya satu item yang bisa dalam mode edit  
✅ Item sebelumnya kembali normal saat item lain diklik  

---

## 🔍 Troubleshooting

### ❌ Problem: Item tidak berubah warna
**Solusi**: Pastikan class CSS `.edit-mode` sudah dibuat di file CSS

### ❌ Problem: Button tidak berubah
**Solusi**: Pastikan variable `formBtn` sudah dideklarasikan dengan benar

### ❌ Problem: Text tidak muncul di input
**Solusi**: Cek apakah menggunakan `item.textContent` (bukan `innerHTML`)

---
<a name="full-code"></a>
## 📦 Full Code - script.js

```javascript
const itemForm = document.getElementById('item-form');
const itemInput = document.getElementById('item-input');
const itemList = document.getElementById('item-list');
const clearBtn = document.getElementById('clear');
const itemFilter = document.getElementById('filter');
const formBtn = itemForm.querySelector('button'); // ⭐ BARU
let isEditMode = false; // ⭐ BARU

function displayItems() {
  const itemsFromStorage = getItemsFromStorage();
  itemsFromStorage.forEach((item) => addItemToDOM(item));
  checkUI();
}

function onAddItemSubmit(e) {
  e.preventDefault();

  const newItem = itemInput.value;

  // Validate Input
  if (newItem === '') {
    alert('Please add an item');
    return;
  }

  // Create item DOM element
  addItemToDOM(newItem);

  // Add item to local storage
  addItemToStorage(newItem);

  checkUI();

  itemInput.value = '';
}

function addItemToDOM(item) {
  // Create list item
  const li = document.createElement('li');
  li.appendChild(document.createTextNode(item));

  const button = createButton('remove-item btn-link text-red');
  li.appendChild(button);

  // Add li to the DOM
  itemList.appendChild(li);
}

function createButton(classes) {
  const button = document.createElement('button');
  button.className = classes;
  const icon = createIcon('fa-solid fa-xmark');
  button.appendChild(icon);
  return button;
}

function createIcon(classes) {
  const icon = document.createElement('i');
  icon.className = classes;
  return icon;
}

function addItemToStorage(item) {
  const itemsFromStorage = getItemsFromStorage();

  // Add new item to array
  itemsFromStorage.push(item);

  // Convert to JSON string and set to local storage
  localStorage.setItem('items', JSON.stringify(itemsFromStorage));
}

function getItemsFromStorage() {
  let itemsFromStorage;

  if (localStorage.getItem('items') === null) {
    itemsFromStorage = [];
  } else {
    itemsFromStorage = JSON.parse(localStorage.getItem('items'));
  }

  return itemsFromStorage;
}

function onClickItem(e) {
  if (e.target.parentElement.classList.contains('remove-item')) {
    removeItem(e.target.parentElement.parentElement);
  } else {
    setItemToEdit(e.target); // ⭐ BARU - Handler untuk edit mode
  }
}

// ⭐⭐⭐ FUNGSI BARU - INTI DARI TUTORIAL INI ⭐⭐⭐
function setItemToEdit(item) {
  isEditMode = true; // ⭐ Set mode ke edit

  // ⭐ Reset semua item ke mode normal
  itemList
    .querySelectorAll('li')
    .forEach((i) => i.classList.remove('edit-mode'));

  // ⭐ Tambahkan class edit-mode ke item yang diklik
  item.classList.add('edit-mode');
  
  // ⭐ Ubah button menjadi Update Item dengan icon pen
  formBtn.innerHTML = '<i class="fa-solid fa-pen"></i>   Update Item';
  
  // ⭐ Ubah warna button menjadi hijau
  formBtn.style.backgroundColor = '#228B22';
  
  // ⭐ Isi input dengan text dari item yang diklik
  itemInput.value = item.textContent;
}

function removeItem(item) {
  if (confirm('Are you sure?')) {
    // Remove item from DOM
    item.remove();

    // Remove item from storage
    removeItemFromStorage(item.textContent);

    checkUI();
  }
}

function removeItemFromStorage(item) {
  let itemsFromStorage = getItemsFromStorage();

  // Filter out item to be removed
  itemsFromStorage = itemsFromStorage.filter((i) => i !== item);

  // Re-set to localstorage
  localStorage.setItem('items', JSON.stringify(itemsFromStorage));
}

function clearItems() {
  while (itemList.firstChild) {
    itemList.removeChild(itemList.firstChild);
  }

  // Clear from localStorage
  localStorage.removeItem('items');

  checkUI();
}

function filterItems(e) {
  const items = itemList.querySelectorAll('li');
  const text = e.target.value.toLowerCase();

  items.forEach((item) => {
    const itemName = item.firstChild.textContent.toLowerCase();

    if (itemName.indexOf(text) != -1) {
      item.style.display = 'flex';
    } else {
      item.style.display = 'none';
    }
  });
}

function checkUI() {
  const items = itemList.querySelectorAll('li');

  if (items.length === 0) {
    clearBtn.style.display = 'none';
    itemFilter.style.display = 'none';
  } else {
    clearBtn.style.display = 'block';
    itemFilter.style.display = 'block';
  }
}

// Initialize app
function init() {
  // Event Listeners
  itemForm.addEventListener('submit', onAddItemSubmit);
  itemList.addEventListener('click', onClickItem);
  clearBtn.addEventListener('click', clearItems);
  itemFilter.addEventListener('input', filterItems);
  document.addEventListener('DOMContentLoaded', displayItems);

  checkUI();
}

init();
```

---

## 📝 CSS yang Dibutuhkan

```css
/* ⭐ Class baru untuk edit mode */
.edit-mode {
  color: #CCC;
}
```

---

## 📊 Ringkasan Perubahan

### ✨ Kode Baru yang Ditambahkan:

1. **Baris 6:** `const formBtn = itemForm.querySelector('button');`
2. **Baris 7:** `let isEditMode = false;`
3. **Baris 93:** `setItemToEdit(e.target);` di dalam else block
4. **Baris 96-113:** Seluruh fungsi `setItemToEdit(item)` 

### 🎯 Total: 1 variable, 1 konstanta, dan 1 fungsi baru!

---

## 🎯 Langkah Selanjutnya

Di video berikutnya, kita akan membuat fungsi **Update** yang sebenarnya, sehingga ketika button "Update Item" diklik, item akan benar-benar terupdate (bukan menambah item baru).

---

## 📚 Apa yang Sudah Dipelajari

🎓 Menggunakan variable boolean untuk tracking state  
🎓 Manipulasi DOM dengan `classList.add()` dan `remove()`  
🎓 Mengubah innerHTML dari element  
🎓 Mengubah style CSS dengan JavaScript  
🎓 Menggunakan `forEach()` untuk loop element  
🎓 Menggunakan `querySelectorAll()` untuk select multiple element  

---

## 💡 Tips

> 💎 **Best Practice**: Gunakan CSS class untuk styling daripada inline style langsung  
> 💎 **Best Practice**: Buat variable untuk element yang sering digunakan  
> 💎 **Best Practice**: Pisahkan fungsi-fungsi agar kode lebih rapi dan mudah di-maintain  

---

**🎊 Selamat! Kamu sudah berhasil membuat fitur Edit Mode!** 🎊

*Keep coding and happy learning! 🚀*
