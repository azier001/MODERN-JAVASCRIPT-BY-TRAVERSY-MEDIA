# 📝 Shopping List Application - Dokumentasi Lengkap

> 🎓 **Dokumentasi Pribadi untuk Pemula** - Belajar membuat aplikasi Shopping List dengan JavaScript langkah demi langkah

---

## 📑 Daftar Isi

- [Pengenalan Proyek](#pengenalan-proyek)
- [Struktur File Proyek](#struktur-file-proyek)
- [Penjelasan Kode HTML](#penjelasan-kode-html)
  - [Head Section](#head-section)
  - [Body Section](#body-section)
  - [Form Input Item](#form-input-item)
  - [Filter Input](#filter-input)
  - [List Items](#list-items)
  - [Clear Button](#clear-button)
- [Kode CSS](#kode-css)
- [Penjelasan Kode JavaScript](#penjelasan-kode-javascript)
  - [Mendapatkan Element dari DOM](#mendapatkan-element-dari-dom)
  - [Function addItem()](#function-additem)
  - [Function createButton()](#function-createbutton)
  - [Function createIcon()](#function-createicon)
  - [Event Listener](#event-listener)
- [Cara Kerja Aplikasi](#cara-kerja-aplikasi)
- [Catatan Penting](#catatan-penting)
- [Rencana Pengembangan](#rencana-pengembangan)

---

<a name="pengenalan-proyek"></a>
## 🎯 Pengenalan Proyek

**Shopping List Application** adalah aplikasi sederhana untuk membuat daftar belanja. Pada tahap awal ini, kita akan belajar cara:

✅ Menambahkan item baru ke dalam list  
✅ Validasi input (tidak boleh kosong)  
✅ Membuat element HTML secara dinamis dengan JavaScript  
✅ Memanipulasi DOM (Document Object Model)

> ⚠️ **Catatan**: Pada tahap ini, data belum tersimpan secara permanen. Jika halaman di-refresh, data akan hilang. Fitur penyimpanan menggunakan Local Storage akan ditambahkan nanti.

---

<a name="struktur-file-proyek"></a>
## 📂 Struktur File Proyek

```
shopping-list/
│
├── index.html          # File HTML utama
├── style.css           # File CSS untuk styling (tidak dibahas di sini)
├── script.js           # File JavaScript utama
└── images/
    └── note.png        # Icon gambar untuk header
```

---

<a name="penjelasan-kode-html"></a>
## 📄 Penjelasan Kode HTML

<a name="head-section"></a>
### 🔖 Head Section

```html
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" />
    <link rel="stylesheet" href="style.css" />
    <title>Shopping List</title>
</head>
```

**Penjelasan:**
- `<meta charset="UTF-8" />` → Mengatur encoding karakter
- `viewport` → Membuat tampilan responsive di mobile
- **Font Awesome CDN** → Library icon gratis untuk tombol delete (icon X)
- `style.css` → File CSS untuk styling aplikasi

---

<a name="body-section"></a>
### 🎨 Body Section

Semua konten dibungkus dalam `<div class="container">` untuk memudahkan styling.

<a name="form-input-item"></a>
### ✍️ Form Input Item

```html
<form id="item-form">
    <div class="form-control">
        <input
            type="text"
            class="form-input"
            id="item-input"
            name="item"
            placeholder="Enter Item"
        />
    </div>
    <div class="form-control">
        <button type="submit" class="btn">
            <i class="fa-solid fa-plus"></i> Add Item
        </button>
    </div>
</form>
```

**Penjelasan:**
- `id="item-form"` → ID untuk menangkap event submit
- `id="item-input"` → ID untuk mengambil nilai input
- `type="submit"` → Tombol submit form
- `<i class="fa-solid fa-plus"></i>` → Icon plus dari Font Awesome

> 💡 **Catatan**: ID sangat penting karena akan digunakan di JavaScript untuk mengakses element

---

<a name="filter-input"></a>
### 🔍 Filter Input

```html
<div class="filter">
    <input
        type="text"
        class="form-input-filter"
        id="filter"
        placeholder="Filter Items"
    />
</div>
```

**Penjelasan:**
- Input ini **tidak** dibungkus `<form>` karena tidak akan di-submit
- Fungsinya untuk filter/mencari item (akan dibuat nanti)

---

<a name="list-items"></a>
### 📋 List Items

```html
<ul id="item-list" class="items">
    <li>
        Apples
        <button class="remove-item btn-link text-red">
            <i class="fa-solid fa-xmark"></i>
        </button>
    </li>
    <!-- Item lainnya... -->
</ul>
```

**Penjelasan:**
- `id="item-list"` → Container untuk semua item
- Setiap `<li>` berisi:
  - **Text item** (contoh: "Apples")
  - **Button delete** dengan icon X

> ⚠️ **Penting**: Item-item ini adalah hardcoded (tulis langsung di HTML) untuk contoh tampilan. Nantinya akan dihapus dan diganti dengan item yang dibuat via JavaScript.

---

<a name="clear-button"></a>
### 🗑️ Clear Button

```html
<button id="clear" class="btn-clear">Clear All</button>
```

**Penjelasan:**
- Tombol untuk menghapus semua item sekaligus
- Fungsinya akan dibuat di tutorial berikutnya

---

<a name="kode-css"></a>
## 📄 Kode CSS

```css
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;700&display=swap');

*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Poppins', sans-serif;
  font-size: 16px;
  line-height: 1.5;
  color: #333;
  background-color: #f5f5f5;
}

header {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  margin-bottom: 20px;
}

header h1 {
  font-weight: 300;
  margin-left: 10px;
}

.container {
  max-width: 500px;
  margin: 30px auto;
  padding: 20px;
}

/* Form & Input */
.form-input {
  width: 100%;
  font-size: 18px;
  margin-bottom: 20px;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  outline: none;
}

.form-input-filter {
  margin-top: 20px;
  width: 100%;
  font-size: 18px;
  margin-bottom: 20px;
  padding: 10px;
  border: none;
  border-bottom: 1px solid #ccc;
  background: transparent;
  outline: none;
}

/* Buttons */
.btn {
  background-color: #333;
  color: #fff;
  border: none;
  border-radius: 5px;
  padding: 10px 20px;
  cursor: pointer;
}

.btn:hover {
  background-color: #444;
}

.btn-link {
  font-size: 16px;
  background-color: transparent;
  color: #333;
  border: none;
  padding: 0;
  cursor: pointer;
}

.btn-clear {
  margin-top: 20px;
  width: 100%;
  font-size: 16px;
  background-color: transparent;
  color: #333;
  border: 1px solid #ccc;
  border-radius: 5px;
  padding: 10px 20px;
  cursor: pointer;
}

.btn-clear:hover {
  background-color: #f1f1f1;
}

.text-red {
  color: red;
}

/* Items */

.items {
  margin-top: 20px;
  display: flex;
  flex-wrap: wrap;
}

.items li {
  display: flex;
  justify-content: space-between;
  width: 45%;
  border: 1px solid #ccc;
  border-radius: 5px;
  padding: 10px 15px;
  margin: 0 5px 20px;
  font-weight: 700;
}

@media (max-width: 500px) {
  .items li {
    width: 100%;
  }
}

```

---

<a name="penjelasan-kode-javascript"></a>
## 💻 Penjelasan Kode JavaScript

<a name="mendapatkan-element-dari-dom"></a>
### 🎯 Mendapatkan Element dari DOM

```javascript
const itemForm = document.getElementById('item-form');
const itemInput = document.getElementById('item-input');
const itemList = document.getElementById('item-list');
```

**Penjelasan:**

| Variable | Fungsi | HTML Element |
|----------|--------|--------------|
| `itemForm` | Mengakses form untuk event submit | `<form id="item-form">` |
| `itemInput` | Mengakses input untuk ambil nilai | `<input id="item-input">` |
| `itemList` | Mengakses list untuk menambah item | `<ul id="item-list">` |

> 💡 **Tips**: Variable ini diletakkan di atas (global scope) agar bisa diakses dari berbagai function.

**Alternatif Cara:**
```javascript
// Menggunakan querySelector
const itemForm = document.querySelector('#item-form');
```

Keduanya bisa digunakan, tapi `getElementById()` lebih cepat untuk mengakses element berdasarkan ID.

---

<a name="function-additem"></a>
### ➕ Function addItem()

Function ini dipanggil saat form di-submit. Berikut penjelasan per bagian:

#### 1️⃣ Prevent Default Behavior

```javascript
function addItem(e) {
  e.preventDefault();
```

**Penjelasan:**
- `e` → Event object dari submit
- `e.preventDefault()` → Mencegah form melakukan submit default (refresh halaman)

> ⚠️ **Penting**: Tanpa ini, halaman akan refresh dan data hilang!

---

#### 2️⃣ Mengambil Nilai Input

```javascript
const newItem = itemInput.value;
```

**Penjelasan:**
- `itemInput.value` → Mengambil text yang diketik user
- Disimpan dalam variable `newItem`

**Contoh:**
- User ketik "Milk" → `newItem = "Milk"`

---

#### 3️⃣ Validasi Input

```javascript
// Validate Input
if (newItem === '') {
    alert('Please add an item');
    return;
}
```

**Penjelasan:**
- Cek apakah input kosong (`''`)
- Jika kosong:
  - Tampilkan alert
  - `return` → Keluar dari function (tidak lanjut ke bawah)

**Diagram Alur:**
```
Input kosong? 
   ├─ Ya  → Tampilkan alert → Berhenti
   └─ Tidak → Lanjut ke bawah
```

---

#### 4️⃣ Membuat List Item

```javascript
// Create list item
const li = document.createElement('li');
li.appendChild(document.createTextNode(newItem));
```

**Penjelasan Baris per Baris:**

**Baris 1:** `document.createElement('li')`
- Membuat element `<li>` baru
- Masih kosong, belum ada isi

**Baris 2:** `li.appendChild(document.createTextNode(newItem))`
- Membuat text node dari `newItem`
- Menambahkan text tersebut ke dalam `<li>`

**Visualisasi:**
```html
<!-- Sebelum -->
<li></li>

<!-- Setelah (jika newItem = "Milk") -->
<li>Milk</li>
```

---

#### 5️⃣ Membuat Button Delete

```javascript
const button = createButton('remove-item btn-link text-red');
li.appendChild(button);
```

**Penjelasan:**
- Memanggil function `createButton()` dengan parameter classes CSS
- Menambahkan button ke dalam `<li>`

**Hasil:**
```html
<li>
    Milk
    <button class="remove-item btn-link text-red">
        <i class="fa-solid fa-xmark"></i>
    </button>
</li>
```

---

#### 6️⃣ Menambahkan ke DOM

```javascript
itemList.appendChild(li);
```

**Penjelasan:**
- Menambahkan `<li>` yang sudah lengkap ke dalam `<ul id="item-list">`
- Item akan muncul di halaman

---

#### 7️⃣ Clear Input Field

```javascript
itemInput.value = '';
```

**Penjelasan:**
- Mengosongkan input field setelah item ditambahkan
- User bisa langsung ketik item baru

---

<a name="function-createbutton"></a>
### 🔘 Function createButton()

```javascript
function createButton(classes) {
  const button = document.createElement('button');
  button.className = classes;
  const icon = createIcon('fa-solid fa-xmark');
  button.appendChild(icon);

  return button;
}
```

**Penjelasan Step by Step:**

| Step | Code | Penjelasan |
|------|------|------------|
| 1 | `document.createElement('button')` | Buat element button |
| 2 | `button.className = classes` | Tambahkan CSS classes |
| 3 | `createIcon(...)` | Buat icon X |
| 4 | `button.appendChild(icon)` | Masukkan icon ke button |
| 5 | `return button` | Kembalikan button yang sudah jadi |

**Input & Output:**
```javascript
// Input
createButton('remove-item btn-link text-red')

// Output
<button class="remove-item btn-link text-red">
    <i class="fa-solid fa-xmark"></i>
</button>
```

---

<a name="function-createicon"></a>
### 🎨 Function createIcon()

```javascript
function createIcon(classes) {
  const icon = document.createElement('i');
  icon.className = classes;

  return icon;
}
```

**Penjelasan:**
- Membuat element `<i>` (italic tag, digunakan Font Awesome untuk icon)
- Menambahkan classes untuk menampilkan icon X

**Input & Output:**
```javascript
// Input
createIcon('fa-solid fa-xmark')

// Output
<i class="fa-solid fa-xmark"></i>
```

> 💡 **Fun Fact**: Tag `<i>` dulunya untuk italic text, tapi sekarang sering dipakai untuk icon!

---

<a name="event-listener"></a>
### 👂 Event Listener

```javascript
// Event Listeners
itemForm.addEventListener('submit', addItem);
```

**Penjelasan:**
- Mendengarkan event `submit` pada form
- Saat form di-submit (klik tombol atau tekan Enter), jalankan function `addItem()`

**Diagram Alur:**
```
User klik "Add Item" 
    ↓
Form submit
    ↓
Event listener menangkap
    ↓
Jalankan addItem()
```

---

<a name="cara-kerja-aplikasi"></a>
## 🔄 Cara Kerja Aplikasi (Flow Lengkap)

```
1. User ketik item → "Milk"
         ↓
2. User klik "Add Item" atau tekan Enter
         ↓
3. Form submit → Event listener menangkap
         ↓
4. addItem() dipanggil
         ↓
5. Cek validasi → Jika kosong, alert & berhenti
         ↓
6. Buat element <li> dengan text "Milk"
         ↓
7. Buat button delete dengan icon X
         ↓
8. Gabungkan: <li>Milk<button>X</button></li>
         ↓
9. Tambahkan ke <ul id="item-list">
         ↓
10. Clear input field
         ↓
11. Item muncul di halaman! ✅
```

---

<a name="catatan-penting"></a>
## 📌 Catatan Penting

### ✅ Yang Sudah Bisa Dilakukan:
- ✅ Menambahkan item baru
- ✅ Validasi input kosong
- ✅ Membersihkan input setelah submit
- ✅ Menampilkan button delete (meski belum berfungsi)

### ❌ Yang Belum Bisa:
- ❌ Menghapus item (button X belum berfungsi)
- ❌ Clear all items
- ❌ Filter/search items
- ❌ Data tersimpan permanen (akan hilang jika refresh)

### 🔍 Mengapa Data Hilang Saat Refresh?

Karena item hanya disimpan di **memori sementara** (DOM). Solusinya:
- Gunakan **Local Storage** untuk menyimpan data
- Data akan tetap ada meski browser ditutup

---

<a name="rencana-pengembangan"></a>
## 🚀 Rencana Pengembangan Selanjutnya

### 📅 Tahap 2: Git & GitHub
- Setup Git version control
- Push project ke GitHub

### 📅 Tahap 3: Delete & Clear
- Function untuk hapus item satuan
- Function untuk hapus semua item

### 📅 Tahap 4: Filter Items
- Membuat fitur pencarian/filter

### 📅 Tahap 5: Local Storage
- Menyimpan data permanen
- Load data saat halaman dibuka

### 📅 Tahap 6: Edit Items
- Mengubah item yang sudah ada

### 📅 Tahap 7: Refactoring
- Membuat code lebih rapi dan efisien
- Menggunakan IIFE (Immediately Invoked Function Expression)

---

## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah belajar:

1. ✅ Struktur HTML untuk aplikasi Shopping List
2. ✅ Cara mengambil element dari DOM
3. ✅ Membuat element HTML dengan JavaScript
4. ✅ Manipulasi DOM (appendChild, createElement)
5. ✅ Event listener untuk form submit
6. ✅ Validasi input sederhana
7. ✅ Function-function helper (createButton, createIcon)

**Keep coding and happy learning!** 💪🚀

---

> 📝 **Catatan**: Dokumentasi ini dibuat untuk keperluan belajar pribadi. Terus praktek dan jangan takut untuk bereksperimen dengan kode!
