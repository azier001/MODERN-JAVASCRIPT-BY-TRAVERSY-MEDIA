# 📝 Dokumentasi Filter Items - Shopping List App

> 🎓 **Catatan untuk Pemula**: Dokumentasi ini akan memandu kamu membuat fitur filter untuk aplikasi shopping list langkah demi langkah. Ikuti setiap tahap dengan teliti!

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa yang Akan Kita Buat?](#apa-yang-akan-kita-buat)
- [Persiapan Awal](#persiapan-awal)
- [Langkah 1: Membuat Event Listener](#langkah-1-membuat-event-listener)
- [Langkah 2: Membuat Fungsi filterItems](#langkah-2-membuat-fungsi-filteritems)
- [Langkah 3: Mengambil Teks Input](#langkah-3-mengambil-teks-input)
- [Langkah 4: Mendapatkan Semua List Items](#langkah-4-mendapatkan-semua-list-items)
- [Langkah 5: Loop Melalui Items](#langkah-5-loop-melalui-items)
- [Langkah 6: Membandingkan Teks](#langkah-6-membandingkan-teks)
- [Langkah 7: Menampilkan/Menyembunyikan Items](#langkah-7-menampilkanmenyembunyikan-items)
- [Testing & Hasil Akhir](#testing--hasil-akhir)
- [Penjelasan Konsep Penting](#penjelasan-konsep-penting)
- [Tips & Trik](#tips--trik)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Halo! 👋 Selamat datang di dokumentasi pribadi untuk membuat fitur **Filter Items**. Fitur ini akan memungkinkan kamu untuk mencari item dalam shopping list dengan mengetik di input field, dan item yang cocok akan ditampilkan secara real-time!

**Skill yang akan kamu pelajari:**
- ✅ Event handling dengan `input` event
- ✅ Manipulasi DOM
- ✅ String manipulation (`toLowerCase()`, `indexOf()`)
- ✅ Loop dengan `forEach()`
- ✅ Styling dinamis dengan JavaScript

---

<a name="apa-yang-akan-kita-buat"></a>
## 🎨 Apa yang Akan Kita Buat?

Kita akan membuat fitur filter yang bisa:
- 🔍 Mencari item saat kamu mengetik
- ⚡ Update hasil pencarian secara real-time (tanpa tombol submit)
- 🎯 Menampilkan item yang cocok, menyembunyikan yang tidak cocok
- 🔤 Case-insensitive (tidak peduli huruf besar/kecil)

**Contoh:**
- Ketik "egg" → Tampilkan: "eggs", "eggnog"
- Ketik "app" → Tampilkan: "apples"

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

### ✨ Variabel yang Sudah Ada

Pastikan kamu sudah punya variabel ini di bagian atas `script.js`:

```javascript
const itemFilter = document.getElementById('filter');
const itemList = document.getElementById('item-list');
```

### 📝 HTML Structure yang Dibutuhkan

```html
<!-- Input untuk filter -->
<input type="text" id="filter" placeholder="Filter Items">

<!-- List items -->
<ul id="item-list">
  <li>
    Apples
    <button class="remove-item btn-link text-red">
      <i class="fa-solid fa-xmark"></i>
    </button>
  </li>
  <!-- more items... -->
</ul>
```

---

<a name="langkah-1-membuat-event-listener"></a>
## 🎧 Langkah 1: Membuat Event Listener

### 📍 Lokasi Kode
Tempatkan di bagian **Event Listeners** (biasanya di bawah kode, sebelum `checkUI()`)

### 💻 Kode yang Ditambahkan

```javascript
// Event Listeners
itemForm.addEventListener('submit', addItem);
itemList.addEventListener('click', removeItem);
clearBtn.addEventListener('click', clearItems);
itemFilter.addEventListener('input', filterItems); // ← TAMBAH INI!

checkUI();
```

### 🤔 Penjelasan

| Bagian | Penjelasan |
|--------|-----------|
| `itemFilter` | Element input dengan id="filter" |
| `'input'` | Event yang trigger setiap ada perubahan input |
| `filterItems` | Fungsi yang akan dijalankan (kita buat di langkah berikutnya) |

> 💡 **Kenapa `input` bukan `keyup`?**  
> Event `input` lebih universal - bisa menangkap typing, paste, dan cara input lainnya!

---

<a name="langkah-2-membuat-fungsi-filteritems"></a>
## 🔧 Langkah 2: Membuat Fungsi filterItems

### 📍 Lokasi Kode
Letakkan **di atas** fungsi `checkUI()` (agar terorganisir)

### 💻 Kode Awal

```javascript
function filterItems(e) {
  // Kode akan kita isi bertahap
}

function checkUI() {
  // fungsi yang sudah ada...
}
```

### 🤔 Penjelasan

- `e` = event object yang otomatis dikirim oleh event listener
- Fungsi ini akan dipanggil setiap kali user mengetik di input filter

---

<a name="langkah-3-mengambil-teks-input"></a>
## 📝 Langkah 3: Mengambil Teks Input

### 💻 Kode yang Ditambahkan

```javascript
function filterItems(e) {
  const text = e.target.value.toLowerCase();
  
  console.log(text); // ← Testing dulu!
}
```

### 🔍 Testing

1. Buka **Console** di browser (F12)
2. Ketik di input filter
3. Lihat hasilnya di console

**Hasil yang diharapkan:**
```
// Ketik "Apple"
apple  // ← huruf kecil semua!

// Ketik "EGGS"
eggs   // ← huruf kecil semua!
```

### 🤔 Penjelasan Step by Step

```javascript
const text = e.target.value.toLowerCase();
//        │   │       │      │
//        │   │       │      └─ Ubah jadi huruf kecil
//        │   │       └──────── Ambil nilai input
//        │   └──────────────── Element yang di-trigger (input filter)
//        └──────────────────── Event object
```

> 💡 **Kenapa pakai `toLowerCase()`?**  
> Supaya pencarian tidak case-sensitive. "Apple", "apple", "APPLE" semuanya dianggap sama!

---

<a name="langkah-4-mendapatkan-semua-list-items"></a>
## 📦 Langkah 4: Mendapatkan Semua List Items

### 💻 Kode yang Ditambahkan

```javascript
function filterItems(e) {
  const text = e.target.value.toLowerCase();
  
  const items = itemList.querySelectorAll('li'); // ← TAMBAH INI!
  
  console.log(items); // ← Testing
}
```

### 🔍 Testing

Ketik sesuatu di input, lalu cek console:

```
NodeList(5) [li, li, li, li, li]
// ← Kamu dapat semua <li> yang ada!
```

### 🤔 Penjelasan

| Kode | Penjelasan |
|------|-----------|
| `itemList` | Element `<ul id="item-list">` |
| `.querySelectorAll('li')` | Ambil SEMUA element `<li>` di dalamnya |
| Hasilnya | NodeList (seperti array) berisi semua `<li>` |

> 📚 **NodeList vs HTMLCollection**  
> - `querySelectorAll()` → NodeList (bisa pakai `forEach`)
> - `getElementsByTagName()` → HTMLCollection (harus diubah ke array dulu)

---

<a name="langkah-5-loop-melalui-items"></a>
## 🔄 Langkah 5: Loop Melalui Items

### 💻 Kode yang Ditambahkan

```javascript
function filterItems(e) {
  const text = e.target.value.toLowerCase();
  const items = itemList.querySelectorAll('li');
  
  items.forEach((item) => { // ← MULAI LOOP!
    console.log(item); // ← Testing dulu
  });
}
```

### 🔍 Testing

Ketik di input, cek console:

```html
<li>
  Apples
  <button class="remove-item btn-link text-red">...</button>
</li>
<li>
  Orange Juice
  <button class="remove-item btn-link text-red">...</button>
</li>
<!-- dst... semua <li> akan muncul satu per satu -->
```

### 🤔 Penjelasan

```javascript
items.forEach((item) => {
//     │        │
//     │        └─ Setiap <li> (satu per satu)
//     └────────── Loop melalui semua items
  // Kode di sini dijalankan untuk SETIAP item
});
```

---

<a name="langkah-6-membandingkan-teks"></a>
## 🔍 Langkah 6: Membandingkan Teks

### 💻 Step 6.1: Ambil Nama Item

```javascript
items.forEach((item) => {
  const itemName = item.firstChild.textContent.toLowerCase();
  
  console.log(itemName); // ← Testing
});
```

**Testing - Ketik sesuatu:**
```
apples
orange juice
eggs
meat
cheese
```

### 🤔 Penjelasan Struktur HTML

```html
<li>
  Apples                    ← firstChild (text node)
  <button>...</button>      ← secondChild
</li>
```

```javascript
item.firstChild              // ← Text node "Apples"
   .textContent              // ← Ambil teksnya: "Apples"
   .toLowerCase()            // ← Ubah jadi: "apples"
```

---

### 💻 Step 6.2: Gunakan indexOf() untuk Mencocokkan

```javascript
items.forEach((item) => {
  const itemName = item.firstChild.textContent.toLowerCase();
  
  if (itemName.indexOf(text) != -1) {
    console.log('true');  // ← Cocok!
  } else {
    console.log('false'); // ← Tidak cocok
  }
});
```

### 🔍 Testing

**Ketik "a":**
```
true   // apples (ada huruf 'a')
true   // orange juice (ada huruf 'a')
false  // eggs (tidak ada 'a')
false  // meat (tidak ada 'a')
false  // cheese (tidak ada 'a')
```

**Ketik "egg":**
```
false  // apples
false  // orange juice
true   // eggs (cocok!)
false  // meat
false  // cheese
```

### 🤔 Penjelasan indexOf()

```javascript
'apples'.indexOf('a')    // → 0 (ketemu di index 0)
'apples'.indexOf('p')    // → 1 (ketemu di index 1)
'apples'.indexOf('xyz')  // → -1 (tidak ketemu)

// Jadi kita cek:
if (itemName.indexOf(text) != -1) {
  // Artinya: kalau KETEMU (bukan -1)
}
```

> 💡 **indexOf() vs includes()**  
> Bisa juga pakai `itemName.includes(text)` yang lebih modern dan readable!

---

<a name="langkah-7-menampilkanmenyembunyikan-items"></a>
## 👁️ Langkah 7: Menampilkan/Menyembunyikan Items

### 💻 Kode Final

```javascript
function filterItems(e) {
  const text = e.target.value.toLowerCase();
  const items = itemList.querySelectorAll('li');
  
  items.forEach((item) => {
    const itemName = item.firstChild.textContent.toLowerCase();
    
    if (itemName.indexOf(text) != -1) {
      item.style.display = 'flex'; // ← TAMPILKAN!
    } else {
      item.style.display = 'none'; // ← SEMBUNYIKAN!
    }
  });
}
```

### 🤔 Penjelasan Display Properties

| Property | Efek |
|----------|------|
| `display: 'flex'` | Item terlihat (default style dari CSS) |
| `display: 'none'` | Item disembunyikan |

> ⚠️ **Kenapa 'flex' bukan 'block'?**  
> Karena di CSS, `<li>` kita sudah di-style dengan `display: flex` untuk layout button-nya!

---

<a name="testing--hasil-akhir"></a>
## 🎉 Testing & Hasil Akhir

### ✅ Cara Testing

1. **Tambahkan beberapa items** ke shopping list:
   - eggs
   - eggnog
   - apples
   - meat
   - cheese

2. **Coba filter dengan berbagai input:**

| Input | Hasil yang Ditampilkan |
|-------|------------------------|
| `a` | apples, meat, cheese |
| `e` | eggs, eggnog, cheese, meat |
| `egg` | eggs, eggnog |
| `eggo` | eggnog saja |
| `xyz` | (tidak ada - semua tersembunyi) |

### 🎊 Fitur yang Sudah Selesai

- ✅ Filter bekerja real-time
- ✅ Case-insensitive
- ✅ Mencari di seluruh teks (tidak hanya awal kata)
- ✅ Smooth & responsive

---

<a name="penjelasan-konsep-penting"></a>
## 📚 Penjelasan Konsep Penting

### 🎯 1. Event Object (`e`)

```javascript
function filterItems(e) {
  console.log(e);          // Event object lengkap
  console.log(e.target);   // Element yang di-trigger
  console.log(e.target.value); // Nilai input
}
```

### 🎯 2. String Methods

```javascript
const text = "Hello World";

text.toLowerCase()    // "hello world"
text.toUpperCase()    // "HELLO WORLD"
text.indexOf("World") // 6 (ketemu di index 6)
text.indexOf("xyz")   // -1 (tidak ketemu)
```

### 🎯 3. firstChild vs textContent

```html
<li>
  Apples              ← firstChild (text node)
  <button>X</button>  ← tidak termasuk
</li>
```

```javascript
item.firstChild           // Text node
item.firstChild.textContent // "Apples"
item.textContent          // "Apples X" (semua teks!)
```

### 🎯 4. NodeList & forEach()

```javascript
const items = document.querySelectorAll('li'); // NodeList

// ✅ Bisa langsung forEach
items.forEach((item) => {
  console.log(item);
});

// ❌ Kalau HTMLCollection, harus begini:
const oldWay = document.getElementsByTagName('li');
Array.from(oldWay).forEach((item) => {
  console.log(item);
});
```

---

<a name="tips--trik"></a>
## 💡 Tips & Trik

### 🚀 Peningkatan yang Bisa Ditambahkan

#### 1️⃣ **Gunakan includes() (Lebih Modern)**

```javascript
// Cara lama
if (itemName.indexOf(text) != -1) { }

// Cara baru (lebih readable)
if (itemName.includes(text)) { }
```

#### 2️⃣ **Tambahkan Message "No Results"**

```javascript
function filterItems(e) {
  const text = e.target.value.toLowerCase();
  const items = itemList.querySelectorAll('li');
  let hasResults = false;
  
  items.forEach((item) => {
    const itemName = item.firstChild.textContent.toLowerCase();
    
    if (itemName.includes(text)) {
      item.style.display = 'flex';
      hasResults = true;
    } else {
      item.style.display = 'none';
    }
  });
  
  // Tampilkan pesan jika tidak ada hasil
  if (!hasResults && text !== '') {
    console.log('No items found!');
  }
}
```

#### 3️⃣ **Highlight Text yang Cocok**

Untuk level advanced, bisa tambahkan highlight pada teks yang cocok menggunakan `innerHTML` dan `<mark>` tag!

### 🐛 Troubleshooting Common Issues

| Masalah | Solusi |
|---------|--------|
| Filter tidak jalan | Cek apakah event listener sudah ditambahkan |
| Item tidak muncul kembali | Pastikan pakai `display: 'flex'` bukan `'block'` |
| Case-sensitive | Jangan lupa `toLowerCase()` di kedua sisi |
| Error "forEach is not a function" | Pastikan pakai `querySelectorAll`, bukan `getElementsByTagName` |

### 📖 Best Practices

1. **Selalu lowercase kedua sisi** (input dan item name)
2. **Gunakan const untuk variabel yang tidak berubah**
3. **Testing bertahap** dengan `console.log()`
4. **Komentar kode** untuk referensi di masa depan

---

## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah berhasil membuat fitur filter yang:
- ⚡ Real-time dan responsive
- 🎯 Akurat dalam pencarian
- 💪 Mudah dipahami dan di-maintain

**Yang sudah kamu pelajari:**
- Event handling dengan `input` event
- DOM manipulation dan traversal
- String methods (`toLowerCase()`, `indexOf()`)
- Array methods (`forEach()`)
- Dynamic styling dengan JavaScript

---

## 📝 Kode Lengkap Final

```javascript
function filterItems(e) {
  const items = itemList.querySelectorAll('li');
  const text = e.target.value.toLowerCase();

  items.forEach((item) => {
    const itemName = item.firstChild.textContent.toLowerCase();

    if (itemName.indexOf(text) != -1) {
      item.style.display = 'flex';
    } else {
      item.style.display = 'none';
    }
  });
}

// Event Listener
itemFilter.addEventListener('input', filterItems);
```

---

> 💪 **Next Steps**: Coba tambahkan localStorage untuk menyimpan items, atau buat fitur edit item!

**Happy Coding!** 🚀✨
