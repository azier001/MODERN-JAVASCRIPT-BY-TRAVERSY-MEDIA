# 📚 Panduan Lengkap Number Methods & Properties JavaScript

> 🎯 **Dokumentasi untuk Pemula** - Belajar sedikit demi sedikit tentang cara kerja angka di JavaScript

---

## 🔢 Pengenalan Number

### Membuat Number di JavaScript

Ada dua cara membuat number di JavaScript:

#### 1️⃣ **Number Primitive** (Cara Umum)
```javascript
let num = 5;
console.log(num); // Output: 5
console.log(typeof num); // Output: "number"
```

#### 2️⃣ **Number Object** (Menggunakan Constructor)
```javascript
const num = new Number(5);
console.log(num); // Output: Number {5}
console.log(typeof num); // Output: "object"
```

> 💡 **Catatan Penting**: Biasanya kita menggunakan cara pertama (primitive). JavaScript secara otomatis akan membungkus number primitive menjadi object ketika kita menggunakan methods.

---

## 🛠️ Number Methods

### 1. `toString()` - Mengubah ke String

> 🎯 **Fungsi**: Mengubah angka menjadi teks (string)

```javascript
let x;
const num = new Number(5);

x = num.toString();
console.log(x); // Output: "5"
console.log(typeof x); // Output: "string"
```

#### 📌 Bonus: Menghitung Panjang Digit
```javascript
// Number tidak punya property .length
x = num.length; // ❌ undefined

// Solusi: Ubah ke string dulu, baru hitung length
x = num.toString().length;
console.log(x); // Output: 1

// Contoh dengan angka lebih besar
const bigNum = 500;
x = bigNum.toString().length;
console.log(x); // Output: 3 (karena 500 punya 3 digit)
```

---

### 2. `toFixed()` - Menentukan Jumlah Desimal

> 🎯 **Fungsi**: Memformat angka dengan jumlah desimal tertentu

```javascript
const num = new Number(5);

x = num.toFixed(2);
console.log(x); // Output: "5.00"
console.log(typeof x); // Output: "string"
```

#### 📊 Contoh dengan Desimal:
```javascript
const decimal = 5.4567;

x = decimal.toFixed(2);
console.log(x); // Output: "5.46" (dibulatkan ke atas)

const decimal2 = 4.5234;
x = decimal2.toFixed(2);
console.log(x); // Output: "4.52" (dibulatkan ke bawah)
```

> ⚠️ **Penting**: Hasil dari `toFixed()` adalah **string**, bukan number!

---

### 3. `toPrecision()` - Menentukan Total Digit

> 🎯 **Fungsi**: Memformat angka dengan total digit tertentu (termasuk angka sebelum desimal)

```javascript
const num = 5.4234;

x = num.toPrecision(2);
console.log(x); // Output: "5.4" (total 2 digit)

x = num.toPrecision(3);
console.log(x); // Output: "5.42" (total 3 digit)

x = num.toPrecision(4);
console.log(x); // Output: "5.423" (total 4 digit)
```

#### 🔍 Perbedaan `toFixed()` vs `toPrecision()`:
| Method | Menghitung | Contoh `5.4234` |
|--------|------------|-----------------|
| `toFixed(2)` | Hanya desimal | `"5.42"` |
| `toPrecision(2)` | **Semua digit** | `"5.4"` |

---

### 4. `toExponential()` - Notasi Eksponensial

> 🎯 **Fungsi**: Mengubah angka ke notasi ilmiah (scientific notation)

```javascript
const num = new Number(5);

x = num.toExponential(2);
console.log(x); // Output: "5.00e+0"
```

> 📝 **Catatan**: Method ini jarang digunakan dalam pengembangan web biasa, lebih sering dipakai dalam perhitungan ilmiah.

---

### 5. `toLocaleString()` - Format Lokal

> 🎯 **Fungsi**: Memformat angka sesuai standar negara/bahasa tertentu

```javascript
const num = new Number(5);

// Format Amerika Serikat (default untuk banyak developer)
x = num.toLocaleString('en-US');
console.log(x); // Output: "5"

// Format Mesir (Arab)
x = num.toLocaleString('ar-EG');
console.log(x); // Output: "٥" (angka Arab)

// Format Indonesia
x = num.toLocaleString('id-ID');
console.log(x); // Output: "5"
```

#### 💰 Contoh Praktis dengan Mata Uang:
```javascript
const harga = 1234567.89;

// Format Rupiah
console.log(harga.toLocaleString('id-ID', {
  style: 'currency',
  currency: 'IDR'
}));
// Output: "Rp1.234.567,89"

// Format Dollar
console.log(harga.toLocaleString('en-US', {
  style: 'currency',
  currency: 'USD'
}));
// Output: "$1,234,567.89"
```

---

### 6. `valueOf()` - Mendapatkan Nilai Primitif

> 🎯 **Fungsi**: Mengembalikan nilai primitif dari Number object

```javascript
const num = new Number(5);

x = num.valueOf();
console.log(x); // Output: 5
console.log(typeof x); // Output: "number"
```

> 💡 **Kapan digunakan?** Method ini otomatis dipanggil JavaScript saat kita melakukan operasi matematika. Jarang digunakan secara manual.

---

## 📊 Number Properties

> 🏷️ **Catatan**: Properties ini dipanggil dari `Number` (huruf kapital), bukan dari variabel!

### 1. `Number.MAX_VALUE` - Angka Terbesar

> 🎯 **Fungsi**: Mendapatkan angka terbesar yang bisa direpresentasikan di JavaScript

```javascript
x = Number.MAX_VALUE;
console.log(x); 
// Output: 1.7976931348623157e+308
```

> 🔢 **Fakta Menarik**: Ini adalah angka terbesar yang bisa disimpan dalam tipe data number JavaScript!

---

### 2. `Number.MIN_VALUE` - Angka Terkecil Positif

> 🎯 **Fungsi**: Mendapatkan angka **positif** terkecil yang bisa direpresentasikan

```javascript
x = Number.MIN_VALUE;
console.log(x); 
// Output: 5e-324
```

> ⚠️ **Perhatian**: Ini bukan angka negatif terbesar! Ini adalah angka positif terkecil (mendekati nol).

---

## 📝 Ringkasan

### 🎨 Method yang Sering Digunakan:
| Method | Kegunaan | Return Type |
|--------|----------|-------------|
| ✅ `toString()` | Ubah ke string | String |
| ✅ `toFixed()` | Format desimal | String |
| ⚡ `toLocaleString()` | Format lokal | String |
| 🔧 `toPrecision()` | Total digit | String |

### 🏆 Method yang Jarang Digunakan:
- `toExponential()` - Notasi ilmiah
- `valueOf()` - Nilai primitif (otomatis dipanggil JS)

### 📦 Properties Penting:
- `Number.MAX_VALUE` - Angka terbesar
- `Number.MIN_VALUE` - Angka positif terkecil

---

## 🎓 Tips Belajar

1. **Praktik**: Coba setiap method di console browser (tekan F12)
2. **Eksperimen**: Ubah-ubah nilai dan parameter untuk memahami perbedaan
3. **Catat**: Method mana yang mengembalikan string vs number
4. **Project Kecil**: Buat kalkulator sederhana untuk mempraktikkan method-method ini

---

## 🚀 Langkah Selanjutnya

Setelah memahami Number Methods, selanjutnya pelajari:
- **Math Object** - Operasi matematika lanjutan
- **Date Object** - Bekerja dengan tanggal dan waktu
- **Array Methods** - Manipulasi data array

---

> 💪 **Semangat Belajar!** Coding adalah skill yang dipelajari sedikit demi sedikit. Keep practicing!

**Created with ❤️ for JavaScript Beginners**
