# 🔄 Type Conversion di JavaScript

## 📚 Pengenalan

**Type Conversion** (atau **Type Casting**) adalah proses mengubah nilai dari satu tipe data ke tipe data lainnya secara eksplisit.

### Contoh Sederhana:
- String `"5"` → Number `5`
- Number `100` → String `"100"`

---

## 🆚 Type Conversion vs Type Coercion

| Type Conversion | Type Coercion |
|----------------|---------------|
| ✅ **Eksplisit** - Anda yang mengubahnya | ⚡ **Implisit** - JavaScript yang mengubahnya |
| Contoh: `Number("5")` | Contoh: `"5" + 1` hasilnya `"51"` |

> 💡 **Catatan**: Dalam dokumentasi ini kita fokus pada Type Conversion (perubahan eksplisit)

---

## 🔢 Mengubah String ke Number

Ada **3 cara** untuk mengubah string menjadi number:

### 1️⃣ Menggunakan `parseInt()`

```javascript
let amount = "100";
amount = parseInt(amount);

console.log(amount);        // Output: 100
console.log(typeof amount); // Output: number
```

**Kapan digunakan?** 
- ✅ Untuk bilangan bulat (integer)
- ✅ Ketika menerima input dari form
- ❌ Tidak cocok untuk angka desimal

---

### 2️⃣ Menggunakan Unary Operator `+`

```javascript
let amount = "100";
amount = +amount;

console.log(amount);        // Output: 100
console.log(typeof amount); // Output: number
```

**Keunggulan:**
- 🚀 Cara paling modern dan cepat
- ✨ Sintaks yang simpel
- 💯 Direkomendasikan untuk digunakan

---

### 3️⃣ Menggunakan `Number()`

```javascript
let amount = "100";
amount = Number(amount);

console.log(amount);        // Output: 100
console.log(typeof amount); // Output: number
```

**Karakteristik:**
- 📦 Constructor method
- 🔧 Lebih eksplisit dan mudah dibaca
- ✅ Cocok untuk kode yang perlu kejelasan

---

## 📝 Mengubah Number ke String

Ada **2 cara** untuk mengubah number menjadi string:

### 1️⃣ Menggunakan `.toString()`

```javascript
let amount = 100;
amount = amount.toString();

console.log(amount);        // Output: "100"
console.log(typeof amount); // Output: string
```

**🤔 Pertanyaan Menarik:**
> Mengapa method `.toString()` bisa bekerja pada primitive type (number)?

**💡 Jawaban:**
JavaScript secara otomatis membuat **temporary wrapper object** ketika kita memanggil method pada primitive type. Jadi meskipun `100` adalah primitive, JavaScript sementara membungkusnya sebagai object agar bisa menggunakan method `.toString()`.

---

### 2️⃣ Menggunakan `String()`

```javascript
let amount = 100;
amount = String(amount);

console.log(amount);        // Output: "100"
console.log(typeof amount); // Output: string
```

---

## 🎯 Mengubah String ke Decimal (Float)

Untuk angka desimal, gunakan `parseFloat()` bukan `parseInt()`:

### ✅ Cara Yang Benar - `parseFloat()`

```javascript
let amount = "99.5";
amount = parseFloat(amount);

console.log(amount);        // Output: 99.5
console.log(typeof amount); // Output: number
```

---

### ❌ Kesalahan Umum - Menggunakan `parseInt()`

```javascript
let amount = "99.5";
amount = parseInt(amount);

console.log(amount);        // Output: 99 ⚠️ Desimal hilang!
console.log(typeof amount); // Output: number
```

**⚠️ Perhatian:** `parseInt()` akan membuang angka di belakang koma!

---

## 🔘 Mengubah Number ke Boolean

Menggunakan `Boolean()` constructor:

```javascript
let amount = 1;
amount = Boolean(amount);

console.log(amount);        // Output: true
console.log(typeof amount); // Output: boolean
```

---

## ✨ Truthy dan Falsy Values

Di JavaScript, setiap nilai bisa dianggap sebagai `true` atau `false` ketika dikonversi ke Boolean:

### ✅ Truthy Values (Menjadi `true`)

```javascript
Boolean(1);        // true
Boolean(100);      // true
Boolean(-5);       // true
Boolean("hello");  // true
Boolean(" ");      // true (string dengan spasi)
```

---

### ❌ Falsy Values (Menjadi `false`)

```javascript
Boolean(0);          // false ⚠️
Boolean("");         // false (string kosong)
Boolean(null);       // false
Boolean(undefined);  // false
Boolean(NaN);        // false
```

**🎯 Ingat:** `0` adalah satu-satunya **angka** yang bernilai `false`!

---

## 🚫 NaN - Not a Number

### Apa itu NaN?

**NaN** adalah nilai spesial yang berarti "Not a Number" (Bukan Angka).

```javascript
let amount = "hello";
amount = Number(amount);

console.log(amount);        // Output: NaN
console.log(typeof amount); // Output: number ⚠️
```

**🤯 Fakta Aneh:** `NaN` memiliki tipe data `number`!

---

### 5️⃣ Cara Mendapatkan NaN

#### 1. String Non-Angka Diparse ke Number

```javascript
let result = Number("hello");
console.log(result); // NaN
```

---

#### 2. Operasi Math Tidak Valid

```javascript
let result = Math.sqrt(-1);
console.log(result); // NaN
// Akar kuadrat dari bilangan negatif tidak valid
```

---

#### 3. Operasi dengan NaN

```javascript
let result = 1 + NaN;
console.log(result); // NaN
// Operasi apapun dengan NaN menghasilkan NaN
```

---

#### 4. Operasi dengan Undefined

```javascript
let result = undefined + undefined;
console.log(result); // NaN
```

---

#### 5. Operasi String Selain Penjumlahan

```javascript
let result = "foo" / 3;
console.log(result); // NaN

// Penjumlahan string adalah exception:
console.log("foo" + 3); // "foo3" (bukan NaN)
```

---

## 📋 Kode Praktek Lengkap

```javascript
// 🔢 CONVERT STRING TO NUMBER
let amount = "100";

// Cara 1: parseInt
amount = parseInt(amount);
console.log(amount, typeof amount); // 100 "number"

// Cara 2: Unary operator + (Recommended!)
amount = +amount;
console.log(amount, typeof amount); // 100 "number"

// Cara 3: Number constructor
amount = Number(amount);
console.log(amount, typeof amount); // 100 "number"

// =====================================

// 📝 CONVERT NUMBER TO STRING
amount = 100;

// Cara 1: toString method
amount = amount.toString();
console.log(amount, typeof amount); // "100" "string"

// Cara 2: String constructor
amount = String(amount);
console.log(amount, typeof amount); // "100" "string"

// =====================================

// 🎯 CONVERT STRING TO DECIMAL
amount = "99.5";

// Gunakan parseFloat untuk desimal
amount = parseFloat(amount);
console.log(amount, typeof amount); // 99.5 "number"

// ❌ JANGAN gunakan parseInt untuk desimal!
amount = parseInt("99.5");
console.log(amount); // 99 (desimal hilang!)

// =====================================

// 🔘 CONVERT NUMBER TO BOOLEAN
amount = 1;
amount = Boolean(amount);
console.log(amount, typeof amount); // true "boolean"

amount = 0;
amount = Boolean(amount);
console.log(amount); // false (0 adalah falsy!)

// =====================================

// 🚫 WAYS TO GET NaN

// 1. Parse non-numeric string
console.log(Number("hello")); // NaN

// 2. Invalid math operations
console.log(Math.sqrt(-1)); // NaN

// 3. Operations with NaN
console.log(1 + NaN); // NaN

// 4. Operations with undefined
console.log(undefined + undefined); // NaN

// 5. String operations (except addition)
console.log("foo" / 3); // NaN
```

---

## 🎓 Ringkasan

### 🔢 String → Number
- `parseInt()` - untuk integer
- `+` unary operator - cara tercepat ⭐
- `Number()` - paling eksplisit

### 📝 Number → String
- `.toString()` - method pada number
- `String()` - constructor function

### 🎯 String → Decimal
- `parseFloat()` - untuk angka desimal ✅
- ❌ Jangan gunakan `parseInt()` untuk desimal

### 🔘 Number → Boolean
- `Boolean()` - konversi ke true/false
- Ingat: `0` = false, angka lain = true

### 🚫 NaN (Not a Number)
- Hasil dari konversi yang gagal
- Tipe datanya tetap `number`!
- 5 cara mendapatkan NaN

---

## 💪 Latihan

Coba prediksi output dari kode berikut:

```javascript
console.log(Number("42"));
console.log(Number("42.5"));
console.log(parseInt("42.9"));
console.log(parseFloat("42.9"));
console.log(Boolean(0));
console.log(Boolean(1));
console.log(String(123));
console.log(Number("hello"));
```

**🎯 Tips:** Praktikkan setiap contoh di browser console untuk memahami lebih dalam!

---

## 🎉 Selamat Belajar!

Ingat: **Praktik membuat sempurna!** Coba berbagai kombinasi konversi untuk memahami perilaku JavaScript lebih dalam.

**Next Topic:** Type Coercion (Konversi Implisit) 🚀
