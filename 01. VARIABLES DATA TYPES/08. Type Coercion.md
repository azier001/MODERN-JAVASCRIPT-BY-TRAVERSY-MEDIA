# 🔄 Type Coercion dalam JavaScript

## 📖 Pengenalan

**Type Coercion** adalah konversi tipe data yang terjadi **secara otomatis (implisit)** oleh JavaScript ketika kita menggunakan operator pada nilai dengan tipe data yang berbeda.

### 🆚 Perbedaan dengan Type Conversion

| Type Conversion | Type Coercion |
|----------------|---------------|
| ✍️ **Eksplisit** - Dilakukan oleh developer | 🤖 **Implisit** - Dilakukan otomatis oleh JavaScript |
| Contoh: `Number("5")` | Contoh: `5 + "5"` |

---

## 🎯 Kapan Type Coercion Terjadi?

Type coercion biasanya terjadi ketika kita **mengaplikasikan operator** (seperti `+`, `*`, `-`) pada nilai dengan **tipe data berbeda**.

---

## 💡 Contoh-Contoh Type Coercion

### 1️⃣ String Coercion dengan Operator `+`

```javascript
let x;

// Coerced to a string
x = 5 + '5';
console.log(x, typeof x);
// Output: "55" string
```

#### 📝 Penjelasan:
- Ketika menggunakan operator `+` dengan **number** dan **string**
- JavaScript akan **mengubah number menjadi string**
- Lalu melakukan **concatenation (penggabungan string)**
- Hasil: `"55"` (tipe: string)

#### ✅ Cara Memperbaiki (Konversi Eksplisit):

```javascript
x = 5 + Number('5');
console.log(x, typeof x);
// Output: 10 number
```

**Penjelasan:** Dengan menggunakan `Number()`, kita mengkonversi string `"5"` menjadi angka `5` terlebih dahulu, sehingga hasilnya adalah penjumlahan matematika: `5 + 5 = 10`.

---

### 2️⃣ Number Coercion dengan Operator `*`

```javascript
// Coerced to a number
x = 5 * '5';
console.log(x, typeof x);
// Output: 25 number
```

#### 📝 Penjelasan:
- Ketika menggunakan operator `*` (perkalian) dengan string
- JavaScript **tidak bisa mengalikan string**
- Maka JavaScript **mengubah string menjadi number**
- Hasil: `25` (tipe: number)

#### 💭 Logika JavaScript:
> "Perkalian string tidak masuk akal, jadi saya akan ubah string ini menjadi angka!"

---

### 3️⃣ Coercion dengan `null`

```javascript
// null is coerced to 0 as it is a `falsy` value
x = 5 + null;
console.log(x, typeof x);
// Output: 5 number
```

#### 📝 Penjelasan:
- `null` diubah menjadi angka `0`
- Maka: `5 + 0 = 5`

#### 🔍 Membuktikannya:

```javascript
x = Number(null);
console.log(x, typeof x);
// Output: 0 number
```

**Kesimpulan:** `null` sebagai angka adalah `0`.

---

### 4️⃣ Coercion dengan Boolean Values

#### 🟢 Boolean `true`

```javascript
x = Number(true);
console.log(x, typeof x);
// Output: 1 number
```

**Penjelasan:** `true` sebagai angka adalah `1`.

```javascript
// true is coerced to a 1
x = 5 + true;
console.log(x, typeof x);
// Output: 6 number
```

**Penjelasan:** `5 + 1 = 6` (karena `true` menjadi `1`).

---

#### 🔴 Boolean `false`

```javascript
x = Number(false);
console.log(x, typeof x);
// Output: 0 number
```

**Penjelasan:** `false` sebagai angka adalah `0`.

```javascript
// false is coerced to 0 (falsy)
x = 5 + false;
console.log(x, typeof x);
// Output: 5 number
```

**Penjelasan:** `5 + 0 = 5` (karena `false` menjadi `0`).

---

### 5️⃣ Coercion dengan `undefined`

```javascript
x = 5 + undefined;
console.log(x, typeof x);
// Output: NaN number
```

#### 📝 Penjelasan:
- `undefined` **tidak bisa dikonversi** menjadi angka yang valid
- Hasilnya adalah `NaN` (Not a Number)
- **Penting:** Tipe dari `NaN` tetap `number`! 🤯

#### ⚠️ Perhatian:
Operasi apapun dengan `undefined` akan menghasilkan `NaN`, termasuk dengan dirinya sendiri.

---

## 📊 Tabel Ringkasan Coercion

| Nilai | Sebagai Number | Sebagai String |
|-------|---------------|----------------|
| `null` | `0` | `"null"` |
| `undefined` | `NaN` | `"undefined"` |
| `true` | `1` | `"true"` |
| `false` | `0` | `"false"` |
| `""` (empty string) | `0` | `""` |
| `"5"` | `5` | `"5"` |

---

## 🎓 Aturan Penting

### ✨ Operator `+` (Plus)
- Jika **salah satu operand adalah string** → **Coercion ke string**
- Jika **kedua operand adalah number** → **Penjumlahan matematika**

### ✨ Operator Matematika Lain (`*`, `-`, `/`, `%`)
- Selalu mencoba **coercion ke number**
- Jika gagal → Hasilnya `NaN`

---

## 🚀 Tips untuk Pemula

1. **🎯 Gunakan Konversi Eksplisit:** Lebih aman menggunakan `Number()`, `String()`, atau `Boolean()` untuk menghindari bug.

2. **⚠️ Hati-hati dengan Operator `+`:** Bisa berarti penjumlahan ATAU penggabungan string.

3. **🔍 Selalu Cek Tipe Data:** Gunakan `typeof` untuk memastikan tipe data yang Anda kerjakan.

4. **📚 Pelajari Truthy & Falsy Values:** Topik selanjutnya yang penting untuk dipahami!

---

## 🧪 Latihan Praktik

Coba prediksi hasilnya sebelum menjalankan kode:

```javascript
// Latihan 1
console.log(10 + "20");      // ❓

// Latihan 2
console.log(10 - "5");       // ❓

// Latihan 3
console.log(true + true);    // ❓

// Latihan 4
console.log(false + "10");   // ❓

// Latihan 5
console.log(null * 5);       // ❓
```

### ✅ Jawaban:
1. `"1020"` (string)
2. `5` (number)
3. `2` (number, karena `1 + 1`)
4. `"false10"` (string)
5. `0` (number, karena `0 * 5`)

---

## 🎉 Kesimpulan

Type Coercion adalah fitur JavaScript yang **powerful** tapi juga bisa **membingungkan**. Dengan memahami aturan-aturannya, Anda bisa:

- ✅ Menulis kode yang lebih predictable
- ✅ Menghindari bug yang sulit dilacak
- ✅ Memahami behavior JavaScript dengan lebih baik

---

## 📚 Topik Selanjutnya

Setelah memahami Type Coercion, Anda siap untuk belajar:
- 🎯 **Truthy & Falsy Values**
- 🔤 **String Methods & Manipulation**
- 🔢 **Number Methods & Math Operations**

---

**Happy Coding! 💻✨**
