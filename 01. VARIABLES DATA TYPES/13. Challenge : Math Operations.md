# 🎯 JavaScript Math Operations Challenge

> **Dokumentasi Pembelajaran untuk Pemula** - Belajar Operasi Matematika & Random Numbers di JavaScript

---

## 📋 Daftar Isi
- [Pendahuluan](#pendahuluan)
- [Konsep Dasar](#konsep-dasar)
- [Challenge](#challenge)
- [Solusi Step-by-Step](#solusi-step-by-step)
- [Kode Lengkap](#kode-lengkap)
- [Output Contoh](#output-contoh)
- [Tips & Catatan](#tips-catatan)
- [Variasi Challenge](#variasi-challenge)
- [Next Steps](#next-steps)
- [Kesimpulan](#kesimpulan)

---

<a name="pendahuluan"></a>
## 🌟 Pendahuluan

Challenge ini dirancang untuk **pemula** yang baru belajar JavaScript. Kita hanya menggunakan konsep-konsep dasar yang sudah dipelajari sebelumnya:
- ✅ Variables (const)
- ✅ Math functions
- ✅ Template literals
- ✅ Console.log

> 💡 **Catatan Penting**: Challenge ini mungkin terlihat sedikit aneh karena terbatas pada materi yang sudah dipelajari. Jika belum paham sepenuhnya, itu wajar! Yang penting praktik terus.

---

<a name="konsep-dasar"></a>
## 🧠 Konsep Dasar

### 1️⃣ **Math.random()**
```javascript
Math.random()
```
- 📌 Menghasilkan angka **desimal acak** antara `0` dan `kurang dari 1`
- 📌 Contoh: `0.543`, `0.891`, `0.012`

### 2️⃣ **Math.floor()**
```javascript
Math.floor(angka)
```
- 📌 Membulatkan angka **ke bawah** ke bilangan bulat terdekat
- 📌 Contoh: `Math.floor(5.9)` → `5`

### 3️⃣ **Membuat Random Number dengan Range**
```javascript
Math.floor(Math.random() * 100 + 1)
```
- 📌 `Math.random()` → angka 0 sampai <1
- 📌 `* 100` → angka 0 sampai <100
- 📌 `+ 1` → angka 1 sampai 100 (menghindari 0)
- 📌 `Math.floor()` → bulatkan ke bawah jadi bilangan bulat

---

<a name="challenge"></a>
## 🎯 Challenge

### 📝 **Tugas:**
1. Buat variable `x` dengan **angka random 1-100**
2. Buat variable `y` dengan **angka random 1-50**
3. Hitung dan tampilkan:
   - ➕ **Sum** (penjumlahan)
   - ➖ **Difference** (pengurangan)
   - ✖️ **Product** (perkalian)
   - ➗ **Quotient** (pembagian)
   - 🔢 **Remainder** (sisa bagi/modulus)

### 📤 **Format Output:**
```
31 + 15 = 46
31 - 15 = 16
31 * 15 = 465
31 / 15 = 2.066666666666667
31 % 15 = 1
```

---

<a name="solusi-step-by-step"></a>
## 🔧 Solusi Step-by-Step

### **LANGKAH 1: Generate Random Numbers**

```javascript
// Random number 1-100
const x = Math.floor(Math.random() * 100 + 1);

// Random number 1-50
const y = Math.floor(Math.random() * 50 + 1);
```

#### 🔍 **Penjelasan Detail:**
- **Tanpa `Math.floor()`**: Akan dapat angka desimal (misal: `45.7892`)
- **Dengan `Math.floor()`**: Dapat bilangan bulat (misal: `45`)
- **`* 100 + 1`**: Membuat range dari 1 sampai 100
- **`* 50 + 1`**: Membuat range dari 1 sampai 50

---

### **LANGKAH 2: Hitung Penjumlahan (Sum) ➕**

```javascript
// Get the sum
const sum = x + y;
const sumOutput = `${x} + ${y} = ${sum}`;
console.log(sumOutput);
```

#### 🔍 **Penjelasan:**
- **`const sum = x + y`**: Menjumlahkan x dan y
- **Template Literal** (` `` `): Memudahkan gabungkan string dan variable
- **`${}`**: Syntax untuk memasukkan variable ke dalam string
- **Output**: `34 + 12 = 46`

---

### **LANGKAH 3: Hitung Pengurangan (Difference) ➖**

```javascript
// Get the difference
const diff = x - y;
const diffOutput = `${x} - ${y} = ${diff}`;
console.log(diffOutput);
```

#### 🔍 **Penjelasan:**
- **`x - y`**: Operator pengurangan
- **Hasil bisa negatif** jika y lebih besar dari x
- **Output**: `78 - 43 = 35`

---

### **LANGKAH 4: Hitung Perkalian (Product) ✖️**

```javascript
// Get the product
const prod = x * y;
const prodOutput = `${x} * ${y} = ${prod}`;
console.log(prodOutput);
```

#### 🔍 **Penjelasan:**
- **`x * y`**: Operator perkalian
- **Hasil bisa sangat besar** (misal: `100 * 50 = 5000`)
- **Output**: `45 * 23 = 1035`

---

### **LANGKAH 5: Hitung Pembagian (Quotient) ➗**

```javascript
// Get the quotient
const quot = x / y;
const quotOutput = `${x} / ${y} = ${quot}`;
console.log(quotOutput);
```

#### 🔍 **Penjelasan:**
- **`x / y`**: Operator pembagian
- **Hasil bisa desimal** (misal: `1 / 12 = 0.083333...`)
- **Output**: `68 / 12 = 5.666666666666667`

---

### **LANGKAH 6: Hitung Sisa Bagi (Remainder) 🔢**

```javascript
// Get the remainder
const rm = x % y;
const rmOutput = `${x} % ${y} = ${rm}`;
console.log(rmOutput);
```

#### 🔍 **Penjelasan:**
- **`x % y`**: Operator **modulus** (sisa bagi)
- **Contoh**: `543 % 5 = 3` karena `543 = 5 × 108 + 3`
- **Kegunaan**: Cek genap/ganjil, siklus berulang, dll
- **Output**: `43 % 5 = 3`

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap

```javascript
// Generate random numbers
const x = Math.floor(Math.random() * 100 + 1);
const y = Math.floor(Math.random() * 50 + 1);

// Get the sum ➕
const sum = x + y;
const sumOutput = `${x} + ${y} = ${sum}`;
console.log(sumOutput);

// Get the difference ➖
const diff = x - y;
const diffOutput = `${x} - ${y} = ${diff}`;
console.log(diffOutput);

// Get the product ✖️
const prod = x * y;
const prodOutput = `${x} * ${y} = ${prod}`;
console.log(prodOutput);

// Get the quotient ➗
const quot = x / y;
const quotOutput = `${x} / ${y} = ${quot}`;
console.log(quotOutput);

// Get the remainder 🔢
const rm = x % y;
const rmOutput = `${x} % ${y} = ${rm}`;
console.log(rmOutput);
```

---

<a name="output-contoh"></a>
## 💻 Output Contoh

### **Run 1:**
```
34 + 12 = 46
34 - 12 = 22
34 * 12 = 408
34 / 12 = 2.8333333333333335
34 % 12 = 10
```

### **Run 2:**
```
78 + 43 = 121
78 - 43 = 35
78 * 43 = 3354
78 / 43 = 1.813953488372093
78 % 43 = 35
```

### **Run 3:**
```
1 + 12 = 13
1 - 12 = -11
1 * 12 = 12
1 / 12 = 0.08333333333333333
1 % 12 = 1
```

> 🔄 **Setiap kali refresh**, angka akan berubah karena menggunakan `Math.random()`

---

<a name="tips-catatan"></a>
## 💡 Tips & Catatan

### ✅ **Yang Perlu Diingat:**

1. **Template Literal vs String Biasa**
   ```javascript
   // ❌ Cara lama (ribet)
   const output = x + " + " + y + " = " + sum;
   
   // ✅ Cara modern (mudah)
   const output = `${x} + ${y} = ${sum}`;
   ```

2. **Operator Matematika**
   | Operator | Simbol | Contoh | Hasil |
   |----------|--------|--------|-------|
   | Tambah   | `+`    | `5 + 3` | `8` |
   | Kurang   | `-`    | `5 - 3` | `2` |
   | Kali     | `*`    | `5 * 3` | `15` |
   | Bagi     | `/`    | `5 / 2` | `2.5` |
   | Modulus  | `%`    | `5 % 2` | `1` |

3. **Debugging Tips** 🐛
   ```javascript
   // Cek nilai x dan y dulu
   console.log("x:", x);  // x: 45
   console.log("y:", y);  // y: 23
   ```

---

<a name="variasi-challenge"></a>
## 🎓 Variasi Challenge

### **Level 1: Tambah Pengecekan**
```javascript
if (x > y) {
  console.log(`${x} lebih besar dari ${y}`);
} else {
  console.log(`${y} lebih besar dari ${x}`);
}
```

### **Level 2: Format Angka**
```javascript
// Bulatkan pembagian jadi 2 desimal
const quotFormatted = quot.toFixed(2);
console.log(`${x} / ${y} = ${quotFormatted}`);
// Output: 34 / 12 = 2.83
```

---

<a name="next-steps"></a>
## 🚀 Next Steps

Setelah memahami challenge ini, kamu sudah siap untuk:
- 📅 **Belajar Date & Time** di JavaScript
- 🔄 **Control Flow** (if-else, loops)
- 🎯 **Functions** untuk kode yang reusable

---

<a name="kesimpulan"></a>
## 📌 Kesimpulan

Challenge ini mengajarkan:
- ✨ Cara generate **random numbers**
- 🧮 **Operasi matematika** dasar
- 📝 Penggunaan **template literals**
- 🖥️ **Console logging** yang informatif

> 💪 **Ingat**: Tidak apa-apa jika belum 100% paham. Yang penting **praktik terus**! Setiap programmer pernah di tahap ini.

---

<div align="center">

**Happy Coding! 🎉**

*Dokumentasi ini dibuat untuk pembelajaran bertahap. Jangan terburu-buru, santai saja!* 😊

</div>
