# 📚 Dokumentasi JavaScript: Tipe Data untuk Pemula

---

## 🎯 Pengenalan Tipe Data

Ketika kamu bekerja dengan kode (bahasa pemrograman apapun), setiap data yang kamu gunakan pasti memiliki **tipe data**. Setiap bahasa pemrograman memiliki sistem tipe data sendiri, meskipun konsep dasarnya mirip.

> 💡 **Penting:** Memahami tipe data adalah fondasi penting dalam programming!

---

## 🏗️ Struktur Tipe Data di JavaScript

JavaScript memiliki **2 kategori utama** tipe data:

### 1️⃣ **Primitive Types** (Tipe Primitif)
Data sederhana yang disimpan langsung di memori

### 2️⃣ **Reference Types** (Tipe Referensi / Objects)
Data kompleks yang disimpan sebagai referensi

---

## 📝 Tipe Data Primitif

### 1. **String** 🔤

**Definisi:** Urutan karakter yang harus diapit dengan tanda kutip

```javascript
const firstName = 'Sara';
console.log(firstName, typeof firstName);
// Output: Sara string
```

**✨ Cara Penulisan String:**
- `'Sara'` - Single quotes (kutip tunggal)
- `"Sara"` - Double quotes (kutip ganda)
- `` `Sara` `` - Backticks (template literals)

---

### 2. **Number** 🔢

**Definisi:** Semua jenis angka (integer, decimal, float)

```javascript
// Integer (bilangan bulat)
const age = 30;
console.log(age, typeof age);
// Output: 30 number

// Decimal/Float (bilangan desimal)
const temp = 98.9;
console.log(temp, typeof temp);
// Output: 98.9 number
```

**⚠️ Catatan Penting:**
> Di JavaScript, SEMUA angka adalah tipe `number` - tidak ada perbedaan antara integer, float, atau decimal seperti di bahasa lain (C, Java, dll)

---

### 3. **Boolean** ✅❌

**Definisi:** Nilai logika yang hanya bisa `true` atau `false`

```javascript
const hasKids = true;
console.log(hasKids, typeof hasKids);
// Output: true boolean
```

**🎯 Kegunaan:**
- Untuk kondisi (if/else)
- Validasi
- Toggle status

---

### 4. **Null** 🗑️

**Definisi:** Pengosongan nilai yang **disengaja**

```javascript
const aptNumber = null;
console.log(aptNumber, typeof aptNumber);
// Output: null object
```

**🤔 Kenapa `typeof null` menghasilkan `object`?**

Ini adalah **bug historis** di JavaScript! Penjelasannya:

📖 **Info dari MDN Documentation:**
- Di implementasi JavaScript pertama, nilai direpresentasikan sebagai *type tag* dan *value*
- Type tag untuk object adalah `0`
- `null` direpresentasikan sebagai NULL pointer (0x00)
- Karena itu, `null` memiliki type tag `0`, sehingga `typeof` mengembalikan `"object"`

> ⚠️ Meskipun `typeof null` menghasilkan `"object"`, **null adalah tipe primitif**, bukan object!

**🔗 Referensi:** [MDN - typeof null](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof#typeof_null)

---

### 5. **Undefined** ❓

**Definisi:** Variabel yang belum diberi nilai

```javascript
// Cara 1: Deklarasi tanpa nilai
let score;
console.log(score, typeof score);
// Output: undefined undefined

// Cara 2: Eksplisit set undefined (jarang digunakan)
const score2 = undefined;
console.log(score2, typeof score2);
// Output: undefined undefined
```

**🆚 Perbedaan Null vs Undefined:**

| Aspek | Null | Undefined |
|-------|------|-----------|
| Sifat | Disengaja (purposeful) | Tidak disengaja |
| Penggunaan | Kosongkan nilai dengan sengaja | Variabel belum diisi |
| Contoh | `const apt = null` | `let score;` |

---

### 6. **Symbol** 🔑

**Definisi:** Nilai unik yang digunakan sebagai identifier

```javascript
const id = Symbol('id');
console.log(id, typeof id);
// Output: Symbol(id) symbol
```

**📌 Karakteristik:**
- Diperkenalkan di ES2015 (ES6)
- Setiap Symbol adalah **unik**
- Jarang digunakan untuk pemula
- Biasanya untuk properti object yang private

**💭 Kapan Digunakan?**
- Advanced use case
- Membuat property identifier yang unik
- Kamu akan mempelajarinya lebih dalam nanti!

---

### 7. **BigInt** 🔢++

**Definisi:** Angka yang sangat besar yang tidak bisa ditangani oleh tipe `number`

```javascript
const n = 9007199254740991n;
console.log(n, typeof n);
// Output: 9007199254740991n bigint
```

**✨ Cara Penulisan:**
- Tambahkan huruf `n` di akhir angka
- Contoh: `123456789n`

**🎯 Kegunaan:**
- Operasi matematika dengan angka sangat besar
- Kriptografi
- Perhitungan presisi tinggi

---

## 🎨 Tipe Data Referensi (Reference Types)

Reference types adalah **non-primitive** dan ketika kita assign ke variabel, variabel tersebut diberi **referensi** ke nilai tersebut (bukan nilai langsung).

### 1. **Array** 📋

**Definisi:** Kumpulan data dalam satu variabel

```javascript
const numbers = [1, 2, 3, 4];
console.log(numbers, typeof numbers);
// Output: [1, 2, 3, 4] object
```

---

### 2. **Object Literal** 📦

**Definisi:** Kumpulan pasangan key-value

```javascript
const person = {
  name: 'Brad',
};
console.log(person, typeof person);
// Output: { name: 'Brad' } object
```

---

### 3. **Function** ⚙️

**Definisi:** Block code yang bisa dipanggil berulang kali

```javascript
function sayHello() {
  console.log('Hello');
}

console.log(sayHello, typeof sayHello);
// Output: [Function: sayHello] function
```

**🤔 Kenapa `typeof function` hasilnya `function`, bukan `object`?**

📖 **Info dari ECMA Specification:**
- Function sebenarnya adalah **object** (function object)
- Tapi `typeof` memberikan hasil khusus `"function"` untuk kemudahan identifikasi
- Secara teknis, function adalah **callable object**

**🔗 Referensi:** [ECMA-262 Specification](https://262.ecma-international.org/5.1/#sec-11.4.3)

> ✅ **Semua reference types adalah object!** Array, object literal, dan function semuanya adalah object.

---

## 🔍 Operator `typeof`

Operator `typeof` digunakan untuk **mengecek tipe data** dari sebuah nilai atau variabel.

**📝 Syntax:**
```javascript
typeof value
typeof(value)  // dengan parentheses juga bisa
```

**💡 Contoh Penggunaan:**
```javascript
const output = firstName;
console.log(output, typeof output);
```

**📊 Tabel Hasil `typeof`:**

| Tipe Data | Hasil typeof |
|-----------|--------------|
| String | `"string"` |
| Number | `"number"` |
| Boolean | `"boolean"` |
| Undefined | `"undefined"` |
| Null | `"object"` ⚠️ |
| Symbol | `"symbol"` |
| BigInt | `"bigint"` |
| Array | `"object"` |
| Object | `"object"` |
| Function | `"function"` |

---

## 🔄 Static vs Dynamic Typing

### 🎭 Dynamic Typing (JavaScript)

JavaScript adalah bahasa **dynamically typed** - artinya kita **tidak perlu** mendefinisikan tipe data secara eksplisit.

```javascript
// JavaScript - Tidak perlu tulis tipe
const name = 'Brad';  // ✅ Otomatis tahu ini string
```

### 🏷️ Static Typing (C, C++, Java)

Bahasa statically typed **harus** mendefinisikan tipe data secara eksplisit.

```java
// Java - Harus tulis tipe
String name = "Brad";  // Harus tulis "String"
```

---

## 📘 TypeScript: Static Typing untuk JavaScript

**TypeScript** adalah superset dari JavaScript yang menambahkan **static typing**.

**🎯 Keuntungan TypeScript:**
- ✅ Kode lebih verbose (jelas)
- ✅ Mengurangi error
- ✅ Better IDE support
- ✅ Easier debugging

**📝 Contoh TypeScript:**
```typescript
const name: string = 'Brad';
const age: number = 30;
```

**💭 Kapan Belajar TypeScript?**
> Pelajari TypeScript setelah kamu benar-benar menguasai dasar-dasar JavaScript!

---

## 🎓 Latihan Pemahaman

### 📝 Quiz 1: Tebak Tipe Data

```javascript
const a = "Hello";           // ?
const b = 42;                // ?
const c = true;              // ?
const d = null;              // ?
const e = undefined;         // ?
const f = [1, 2, 3];         // ?
const g = { name: "John" };  // ?
```

**Jawaban:**
- a: string
- b: number
- c: boolean
- d: object (tapi sebenarnya null)
- e: undefined
- f: object (array)
- g: object

---

## 💡 Tips & Tricks

### ⌨️ VS Code Shortcuts

**1. Multi-cursor Selection:**
- Letakkan cursor di kata yang ingin diganti
- Hold `Shift` dan highlight
- Tekan `Cmd/Ctrl + D` untuk select next instance
- Edit semuanya sekaligus!

**2. Contoh:**
```javascript
// Ingin ganti semua 'firstName' jadi 'output'
const firstName = 'Sara';
console.log(firstName, typeof firstName);

// Select 'firstName' → Cmd+D → ketik 'output'
```

---

## 📋 Kode Lengkap Praktek

```javascript
// ===== PRIMITIVE TYPES =====

// 1. String
const firstName = 'Sara';

// 2. Number
const age = 30;
const temp = 98.9;

// 3. Boolean
const hasKids = true;

// 4. Null
const aptNumber = null;

// 5. Undefined
// let score;
const score = undefined;

// 6. Symbol
const id = Symbol('id');

// 7. BigInt
const n = 9007199254740991n;

// ===== REFERENCE TYPES =====

// 1. Array
const numbers = [1, 2, 3, 4];

// 2. Object Literal
const person = {
  name: 'Brad',
};

// 3. Function
function sayHello() {
  console.log('Hello');
}

// ===== TESTING =====
const output = sayHello;
console.log(output, typeof output);
```

---

## 🎯 Ringkasan

### ✅ Yang Sudah Kamu Pelajari:

1. ✨ **7 Tipe Primitif:**
   - String, Number, Boolean, Null, Undefined, Symbol, BigInt

2. 🎨 **3 Tipe Referensi:**
   - Array, Object, Function

3. 🔍 **Operator typeof:**
   - Cara mengecek tipe data

4. 🎭 **Dynamic vs Static Typing:**
   - JavaScript = Dynamic
   - TypeScript = Static

5. 🐛 **Bug Historis:**
   - `typeof null === "object"`
   - Function adalah object tapi typeof = "function"

---

## 📚 Referensi & Link Berguna

- 📖 [MDN - typeof null explained](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof#typeof_null)
- 📖 [ECMA - Function Object Type](https://262.ecma-international.org/5.1/#sec-11.4.3)
- 📖 [MDN - JavaScript Data Types](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures)

---

## 🚀 Langkah Selanjutnya

Di video/materi berikutnya, kamu akan belajar:

🔜 **Bagaimana Primitive dan Reference Types disimpan di memori**
- Stack vs Heap memory
- Pass by value vs Pass by reference
- Kenapa ini penting?

---

## 💬 Catatan Penutup

> 🎓 **Untuk Pemula:** Jangan terburu-buru! Pahami setiap tipe data satu per satu. Praktekkan dengan menulis kode sendiri. Tipe data adalah fondasi yang akan kamu gunakan setiap hari dalam programming!

> 💪 **Keep practicing!** Coding adalah skill yang diasah dengan praktek, bukan hanya teori.

---

**Happy Coding! 🚀✨**
