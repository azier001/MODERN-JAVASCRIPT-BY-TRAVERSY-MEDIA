# 📐 Dokumentasi JavaScript Math Object

> **Panduan Lengkap untuk Pemula** - Belajar sedikit demi sedikit tentang objek Math di JavaScript

---

## 📚 Pengenalan

**Math** adalah objek bawaan JavaScript yang menyediakan berbagai metode untuk operasi matematika. Objek ini sangat berguna ketika kita perlu melakukan perhitungan seperti:
- Mencari akar kuadrat
- Membulatkan angka
- Menghasilkan angka random
- Dan banyak lagi!

> 💡 **Tips:** Objek Math ditulis dengan huruf **M besar** (Math, bukan math)

---

## 🔢 Metode-Metode Math Object

### 1. **Math.sqrt()** - Akar Kuadrat

Metode ini digunakan untuk mencari **akar kuadrat** dari sebuah angka.

```javascript
let x;
x = Math.sqrt(9);
console.log(x); // Output: 3
```

**📖 Penjelasan:**
- `Math.sqrt(9)` mencari akar kuadrat dari 9
- Hasilnya adalah **3** karena 3 × 3 = 9

---

### 2. **Math.abs()** - Nilai Absolut

Metode ini mengubah angka negatif menjadi **positif** (nilai absolut).

```javascript
x = Math.abs(-5);
console.log(x); // Output: 5

x = Math.abs(5);
console.log(x); // Output: 5
```

**📖 Penjelasan:**
- `Math.abs(-5)` menghasilkan **5**
- `Math.abs(5)` juga menghasilkan **5**
- Nilai absolut selalu positif!

---

### 3. **Math.round()** - Pembulatan Normal

Membulatkan angka ke bilangan bulat **terdekat**.

```javascript
x = Math.round(4.6);
console.log(x); // Output: 5

x = Math.round(4.2);
console.log(x); // Output: 4
```

**📖 Penjelasan:**
- Jika desimal **≥ 0.5** → dibulatkan ke **atas**
- Jika desimal **< 0.5** → dibulatkan ke **bawah**

---

### 4. **Math.ceil()** - Pembulatan ke Atas ⬆️

Membulatkan angka ke atas **tanpa peduli** nilai desimalnya.

```javascript
x = Math.ceil(4.2);
console.log(x); // Output: 5

x = Math.ceil(4.6);
console.log(x); // Output: 5
```

**📖 Penjelasan:**
- `ceil` berasal dari kata "ceiling" (langit-langit)
- Selalu membulatkan **ke atas** meskipun desimalnya kecil
- `4.2` → **5**, `4.9` → **5**

---

### 5. **Math.floor()** - Pembulatan ke Bawah ⬇️

Membulatkan angka ke bawah **tanpa peduli** nilai desimalnya.

```javascript
x = Math.floor(4.9);
console.log(x); // Output: 4

x = Math.floor(4.2);
console.log(x); // Output: 4
```

**📖 Penjelasan:**
- `floor` berasal dari kata "lantai"
- Selalu membulatkan **ke bawah** meskipun desimalnya besar
- `4.9` → **4**, `4.2` → **4**

> ⚡ **Penting:** `Math.ceil()` dan `Math.floor()` sangat sering digunakan dalam berbagai situasi pemrograman!

---

### 6. **Math.pow()** - Pangkat

Menghitung hasil **pemangkatan** sebuah angka.

```javascript
x = Math.pow(2, 3);
console.log(x); // Output: 8
```

**📖 Penjelasan:**
- `Math.pow(2, 3)` artinya 2 pangkat 3
- Hasilnya: 2 × 2 × 2 = **8**
- Format: `Math.pow(angka, pangkat)`

---

### 7. **Math.min()** - Nilai Terkecil

Mengembalikan angka **terkecil** dari sekumpulan angka.

```javascript
x = Math.min(4, 5, 3);
console.log(x); // Output: 3

x = Math.min(10, 25, 8, 15);
console.log(x); // Output: 8
```

**📖 Penjelasan:**
- Bisa membandingkan 2 angka atau lebih
- Mengembalikan nilai yang **paling kecil**

---

### 8. **Math.max()** - Nilai Terbesar

Mengembalikan angka **terbesar** dari sekumpulan angka.

```javascript
x = Math.max(4, 5, 3);
console.log(x); // Output: 5

x = Math.max(10, 25, 8, 15);
console.log(x); // Output: 25
```

**📖 Penjelasan:**
- Bisa membandingkan 2 angka atau lebih
- Mengembalikan nilai yang **paling besar**

---

## 🎲 Math.random() - Angka Random (PENTING!)

Ini adalah metode yang **sangat sering digunakan** dalam pemrograman!

### Penggunaan Dasar

```javascript
x = Math.random();
console.log(x); // Output: 0.xxxxx (angka desimal antara 0 dan 1)
```

**📖 Penjelasan:**
- Menghasilkan angka desimal **acak** antara **0** dan **1**
- Contoh: `0.7482`, `0.2156`, `0.9834`
- Setiap kali dijalankan hasilnya **berbeda**

---

### 🎯 Membuat Angka Random 1-10

Biasanya kita ingin angka random dalam range tertentu, bukan 0-1. Berikut caranya:

#### **Langkah 1:** Random 0-9 (desimal)

```javascript
x = Math.random() * 10;
console.log(x); // Output: 0.xxx hingga 9.xxx
```

**📖 Penjelasan:**
- `Math.random()` menghasilkan 0-1
- Dikali 10 menjadi 0-10 (tapi belum termasuk 10)

---

#### **Langkah 2:** Random 1-10 (desimal)

```javascript
x = Math.random() * 10 + 1;
console.log(x); // Output: 1.xxx hingga 10.xxx
```

**📖 Penjelasan:**
- Ditambah 1 untuk menggeser range dari 0-10 menjadi **1-10**

---

#### **Langkah 3:** Random 1-10 (bilangan bulat) ✅

```javascript
x = Math.floor(Math.random() * 10 + 1);
console.log(x); // Output: 1, 2, 3, 4, 5, 6, 7, 8, 9, atau 10
```

**📖 Penjelasan:**
- `Math.floor()` membulatkan ke bawah
- Hasilnya bilangan bulat antara **1 sampai 10**

> 🎉 **Ini adalah rumus yang akan kamu lihat berkali-kali dalam pemrograman!**

---

### 🎯 Membuat Angka Random 1-100

Gunakan prinsip yang sama, tinggal ganti angka 10 dengan 100:

```javascript
x = Math.floor(Math.random() * 100 + 1);
console.log(x); // Output: 1 hingga 100
```

**📖 Penjelasan:**
- `* 100` untuk range 0-100
- `+ 1` untuk menggeser menjadi 1-100
- `Math.floor()` untuk membuat bilangan bulat

---

## 📝 Kode Praktik Lengkap

Berikut adalah kode lengkap yang bisa kamu coba:

```javascript
let x;

// 1. Akar kuadrat
x = Math.sqrt(9);
console.log('Akar kuadrat dari 9:', x); // 3

// 2. Nilai absolut
x = Math.abs(-5);
console.log('Nilai absolut dari -5:', x); // 5

// 3. Pembulatan normal
x = Math.round(4.2);
console.log('Pembulatan 4.2:', x); // 4

// 4. Pembulatan ke atas
x = Math.ceil(4.2);
console.log('Pembulatan ke atas 4.2:', x); // 5

// 5. Pembulatan ke bawah
x = Math.floor(4.9);
console.log('Pembulatan ke bawah 4.9:', x); // 4

// 6. Pangkat
x = Math.pow(2, 3);
console.log('2 pangkat 3:', x); // 8

// 7. Nilai minimum
x = Math.min(4, 5, 3);
console.log('Nilai terkecil:', x); // 3

// 8. Nilai maksimum
x = Math.max(4, 5, 3);
console.log('Nilai terbesar:', x); // 5

// 9. Random 0-1
x = Math.random();
console.log('Random 0-1:', x);

// 10. Random 1-100 (bilangan bulat)
x = Math.floor(Math.random() * 100 + 1);
console.log('Random 1-100:', x);
```

---

## 🎓 Kesimpulan

Selamat! Kamu sudah mempelajari metode-metode penting dalam **Math Object**:

✅ `Math.sqrt()` - Akar kuadrat  
✅ `Math.abs()` - Nilai absolut  
✅ `Math.round()` - Pembulatan normal  
✅ `Math.ceil()` - Pembulatan ke atas  
✅ `Math.floor()` - Pembulatan ke bawah  
✅ `Math.pow()` - Pangkat  
✅ `Math.min()` - Nilai terkecil  
✅ `Math.max()` - Nilai terbesar  
✅ `Math.random()` - Angka random  

---

## 💪 Tips Belajar

1. **Praktikkan** setiap metode satu per satu
2. **Ubah angka-angkanya** untuk melihat hasilnya
3. **Kombinasikan** beberapa metode seperti contoh `Math.floor(Math.random()...)`
4. Metode yang paling sering dipakai: `Math.floor()`, `Math.ceil()`, dan `Math.random()`

---

## 🚀 Langkah Selanjutnya

Sekarang kamu sudah paham Math Object! Saatnya untuk:
- Mencoba membuat **mini project** dengan angka random
- Berlatih membuat **calculator sederhana**
- Eksplorasi metode Math lainnya

> 💡 **Ingat:** Objek Math masih punya banyak metode lain. Tapi yang kamu pelajari di sini adalah yang **paling sering digunakan**!

---

**Happy Coding! 🎉👨‍💻**
