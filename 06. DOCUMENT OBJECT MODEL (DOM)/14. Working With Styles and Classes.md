# 🎨 Dokumentasi Manipulasi CSS & Classes dengan JavaScript

> **Dokumentasi Pribadi untuk Pemula** 📚  
> Panduan lengkap cara memanipulasi CSS dan classes menggunakan JavaScript

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Setup Project](#setup-project)
- [Property className](#property-classname)
  - [Mengambil Nama Class](#mengambil-nama-class)
  - [Mengubah Class](#mengubah-class)
  - [Kekurangan className](#kekurangan-classname)
- [Property classList](#property-classlist)
  - [Apa itu classList?](#apa-itu-classlist)
  - [Looping Class dengan forEach](#looping-class-dengan-foreach)
  - [Method add() - Menambah Class](#method-add-menambah-class)
  - [Method remove() - Menghapus Class](#method-remove-menghapus-class)
  - [Method toggle() - Toggle Class](#method-toggle-toggle-class)
  - [Method replace() - Mengganti Class](#method-replace-mengganti-class)
- [Property style](#property-style)
  - [Mengubah Style Langsung](#mengubah-style-langsung)
  - [Styling Multiple Elements](#styling-multiple-elements)
  - [Conditional Styling](#conditional-styling)
- [Kapan Menggunakan JavaScript untuk CSS?](#kapan-menggunakan-javascript-untuk-css)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan mempelajari cara **memanipulasi CSS classes dan styles** menggunakan JavaScript. Ini sangat berguna untuk membuat website yang **dinamis dan interaktif**! 🚀

### 🤔 Mengapa Perlu JavaScript untuk CSS?

- ✅ Untuk membuat perubahan style **saat event terjadi** (click, hover, dll)
- ✅ Untuk **toggle** class (dark mode, show/hide menu)
- ✅ Untuk membuat **animasi dan interaksi** yang dinamis
- ✅ CSS biasa hanya untuk style statis saat page load

> **💡 Catatan Penting:**  
> 99% waktu kita memanipulasi CSS dengan JavaScript adalah saat ada **event** (seperti button click), bukan saat page load.

---

<a name="setup-project"></a>
## 🛠️ Setup Project

### 📄 File HTML (`index.html`)

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Adding & Removing Styles & Classes</title>
  </head>
  <body>
    <h1>Adding & Removing Styles & Classes</h1>

    <!-- Paragraph dengan class 'card' -->
    <p class="card">
      Lorem ipsum, dolor sit amet consectetur adipisicing elit. Dignissimos
      quasi odio dicta nisi voluptate tempora corporis reprehenderit optio qui
      eos officiis nostrum cupiditate delectus, accusamus adipisci non, amet
      quis provident.
    </p>

    <!-- UL dengan MULTIPLE classes -->
    <ul class="item-list items">
      <li>Item 1</li>
      <li>Item 2</li>
      <li>Item 3</li>
      <li>Item 4</li>
      <li>Item 5</li>
    </ul>

    <!-- Button untuk trigger event -->
    <button>Run</button>

    <script src="script.js"></script>
  </body>
</html>
```

**📝 Penjelasan HTML:**
- `<p class="card">` - Paragraph dengan 1 class
- `<ul class="item-list items">` - List dengan **2 classes** (untuk demo multiple classes)
- `<button>` - Tombol untuk trigger function

---

### 🎨 File CSS (`style.css`)

```css
body {
  font-family: 'Arial', sans-serif;
  line-height: 1.3;
}

/* Class untuk hide element */
.hidden {
  display: none;
}

/* Class untuk card style */
.card {
  background: #f4f4f4;
  border: 1px #ccc solid;
  padding: 15px;
}

/* Class untuk dark mode */
.dark {
  background: #333;
  color: #fff;
}
```

**📝 Penjelasan CSS:**
- `.hidden` - Class untuk menyembunyikan element
- `.card` - Class untuk styling card (background, border, padding)
- `.dark` - Class untuk dark mode (background gelap, text putih)

---

### ⚙️ File JavaScript Dasar (`script.js`)

```javascript
// Ambil element-element yang dibutuhkan
const text = document.querySelector('p');
const itemList = document.querySelector('.item-list');
const items = itemList.querySelectorAll('li');

// Function yang akan dijalankan saat button diklik
function run() {
  console.log('Button diklik!');
}

// Event listener untuk button
document.querySelector('button').onclick = run;
```

**📝 Penjelasan Setup JavaScript:**
- `text` - Menyimpan element `<p>` (paragraph)
- `itemList` - Menyimpan element `<ul>` dengan class `.item-list`
- `items` - Menyimpan SEMUA `<li>` dalam bentuk NodeList
- `document.querySelector('button').onclick = run` - Saat button diklik, jalankan function `run()`

> **⚠️ Perhatian:**  
> Jangan gunakan `run()` dengan parentheses pada event listener! Itu akan langsung execute function-nya.

---

<a name="property-classname"></a>
## 📦 Property `className`

<a name="mengambil-nama-class"></a>
### 📖 Mengambil Nama Class

Property `className` digunakan untuk **mendapatkan atau mengubah class** dari sebuah element.

```javascript
function run() {
  // Mengambil semua class names sebagai STRING
  console.log(itemList.className);
  // Output: "item-list items"
}
```

**📝 Penjelasan:**
- `className` mengembalikan **string** yang berisi semua class
- Jika ada multiple classes, akan digabung dengan spasi

---

<a name="mengubah-class"></a>
### ✏️ Mengubah Class

Kita bisa **mengubah class** dengan assignment:

```javascript
function run() {
  // Mengubah class paragraph menjadi 'card dark'
  text.className = 'card dark';
}
```

**📝 Penjelasan:**
- Class paragraph akan diubah menjadi `card` dan `dark`
- Background akan menjadi gelap karena class `dark` applied

---

<a name="kekurangan-classname"></a>
### ⚠️ Kekurangan `className`

```javascript
function run() {
  // Ini akan MENGHAPUS class 'card' yang sudah ada!
  text.className = 'dark';
}
```

**🚨 MASALAH:**
- `className` akan **OVERWRITE** semua class yang ada
- Class `card` akan hilang, hanya `dark` yang tersisa
- Jika ingin keep class yang ada, harus ditulis manual: `text.className = 'card dark';`

**💡 Solusi:**  
Gunakan `classList` untuk manipulasi yang lebih aman! 👇

---

<a name="property-classlist"></a>
## 📚 Property `classList`

<a name="apa-itu-classlist"></a>
### 🎯 Apa itu `classList`?

`classList` adalah **object** yang berisi **array-like collection** dari class names dengan **methods** untuk manipulasi.

```javascript
function run() {
  console.log(itemList.classList);
  // Output: DOMTokenList(2) ['item-list', 'items']
}
```

**📝 Penjelasan:**
- `classList` mengembalikan `DOMTokenList` (mirip array)
- Lebih powerful dari `className`
- Punya methods: `add()`, `remove()`, `toggle()`, `replace()`

---

<a name="looping-class-dengan-foreach"></a>
### 🔄 Looping Class dengan `forEach`

Karena `classList` mirip array, kita bisa loop dengan `forEach`:

```javascript
function run() {
  // Loop setiap class dan log
  itemList.classList.forEach((c) => {
    console.log(c);
  });
  // Output:
  // "item-list"
  // "items"
}
```

**📝 Penjelasan:**
- `forEach` iterate semua class
- `c` = current class name
- Berguna untuk checking atau processing multiple classes

---

<a name="method-add-menambah-class"></a>
### ➕ Method `add()` - Menambah Class

**BEST WAY** untuk menambah class tanpa menghapus yang lama:

```javascript
function run() {
  // Menambah class 'dark' ke paragraph
  text.classList.add('dark');
}
```

**📝 Penjelasan:**
- Class `dark` ditambahkan ke paragraph
- Class `card` tetap ada (tidak dihapus!)
- Background jadi gelap, tapi padding & border tetap ada

**🎯 Keuntungan:**
- ✅ Tidak overwrite class yang ada
- ✅ Lebih aman dari `className`
- ✅ Bisa add multiple classes: `text.classList.add('dark', 'another-class')`

---

<a name="method-remove-menghapus-class"></a>
### ➖ Method `remove()` - Menghapus Class

Menghapus specific class dari element:

```javascript
function run() {
  // Tambah dark, lalu hapus card
  text.classList.add('dark');
  text.classList.remove('card');
}
```

**📝 Penjelasan:**
- Pertama: Add class `dark` (background gelap)
- Kedua: Remove class `card` (hilang padding & border)
- Hanya class `dark` yang tersisa

**💡 Use Case:**
- Menghapus styling tertentu
- Reset ke default state
- Conditional styling

---

<a name="method-toggle-toggle-class"></a>
### 🔄 Method `toggle()` - Toggle Class

**SUPER USEFUL!** Toggle class on/off seperti switch:

```javascript
function run() {
  // Toggle class 'dark'
  text.classList.toggle('dark');
}
```

**📝 Penjelasan:**
- **Pertama kali klik**: Class `dark` ditambahkan (background gelap) ⚫
- **Klik lagi**: Class `dark` dihapus (background normal) ⚪
- **Klik lagi**: Class `dark` ditambahkan lagi ⚫
- Dan seterusnya...

**🎯 Use Case:**
- 🌙 Dark mode toggle
- 📱 Hamburger menu (show/hide)
- 👁️ Show/Hide password
- 🎨 Theme switcher

**💡 Contoh Toggle Hidden:**

```javascript
function run() {
  // Toggle class 'hidden' untuk hide/show element
  text.classList.toggle('hidden');
}
```

- **Klik pertama**: Element hilang (display: none)
- **Klik kedua**: Element muncul lagi

---

<a name="method-replace-mengganti-class"></a>
### 🔁 Method `replace()` - Mengganti Class

Mengganti satu class dengan class lain:

```javascript
function run() {
  // Ganti class 'card' dengan 'dark'
  text.classList.replace('card', 'dark');
}
```

**📝 Penjelasan:**
- Parameter 1: Class yang mau diganti (`card`)
- Parameter 2: Class pengganti (`dark`)
- Class `card` hilang, diganti dengan `dark`

**⚠️ Catatan:**
- Jarang digunakan dalam praktik
- Biasanya lebih baik pakai kombinasi `remove()` + `add()`

---

<a name="property-style"></a>
## 🎨 Property `style`

<a name="mengubah-style-langsung"></a>
### 🖌️ Mengubah Style Langsung

Untuk mengubah **inline CSS** langsung dari JavaScript:

```javascript
function run() {
  // Mengubah line-height menjadi 3
  itemList.style.lineHeight = '3';
}
```

**📝 Penjelasan:**
- `style` property untuk akses inline styles
- `lineHeight` dalam **camelCase** (bukan `line-height`)
- Nilai harus dalam **string** (`'3'` bukan `3`)

**⚠️ Perbedaan dengan CSS:**

| CSS Property | JavaScript Style |
|--------------|------------------|
| `line-height` | `lineHeight` |
| `background-color` | `backgroundColor` |
| `font-size` | `fontSize` |
| `margin-top` | `marginTop` |

> **💡 Rule:**  
> Jika CSS property punya **dash** (`-`), di JavaScript jadi **camelCase** tanpa dash!

---

<a name="styling-multiple-elements"></a>
### 🎯 Styling Multiple Elements

Untuk style banyak element sekaligus, gunakan **loop**:

```javascript
function run() {
  // Loop semua <li> dan ubah color jadi red
  items.forEach((item) => {
    item.style.color = 'red';
  });
}
```

**📝 Penjelasan:**
- `items` berisi semua `<li>` elements
- `forEach` loop setiap item
- Semua text dalam `<li>` jadi warna merah 🔴

---

<a name="conditional-styling"></a>
### 🎭 Conditional Styling

Style element berdasarkan **kondisi tertentu**:

```javascript
function run() {
  items.forEach((item, index) => {
    // Semua item jadi merah
    item.style.color = 'red';
    
    // Kecuali item index 2 (item ke-3) jadi biru
    if (index === 2) {
      item.style.color = 'blue';
    }
  });
}
```

**📝 Penjelasan:**
- Parameter `index` = posisi item (0, 1, 2, 3, 4)
- Semua item default merah 🔴
- Item index 2 (item ke-3) jadi biru 🔵

**💡 Output:**
1. Item 1 → 🔴 Red
2. Item 2 → 🔴 Red
3. Item 3 → 🔵 Blue ⭐
4. Item 4 → 🔴 Red
5. Item 5 → 🔴 Red

---

<a name="kapan-menggunakan-javascript-untuk-css"></a>
## 🤔 Kapan Menggunakan JavaScript untuk CSS?

### ✅ Gunakan JavaScript Jika:

- 🖱️ **Event-driven**: Perubahan terjadi saat user interaction (click, hover, scroll)
- 🔄 **Dynamic**: Styling berubah-ubah based on conditions
- 🎮 **Interactive**: Menu toggle, dark mode, show/hide
- 📊 **Conditional**: Style berbeda untuk element berbeda

### ❌ Jangan Gunakan JavaScript Jika:

- 📄 **Static**: Style tidak berubah
- 🎨 **Initial Load**: Style langsung saat page load
- 🎯 **Simple**: Bisa dicapai dengan CSS saja

> **💡 Golden Rule:**  
> Gunakan **CSS** untuk styling statis, gunakan **JavaScript** untuk styling dinamis!

---

<a name="rangkuman"></a>
## 📝 Rangkuman

### 🎯 Property `className`
- ✅ Mendapat/set class sebagai string
- ❌ Overwrite semua class yang ada
- 👎 Kurang fleksibel

```javascript
text.className = 'card dark'; // Overwrite semua
```

---

### 🎯 Property `classList` ⭐ RECOMMENDED!

| Method | Fungsi | Contoh |
|--------|--------|--------|
| `add()` | Tambah class | `text.classList.add('dark')` |
| `remove()` | Hapus class | `text.classList.remove('card')` |
| `toggle()` | Toggle class on/off | `text.classList.toggle('hidden')` |
| `replace()` | Ganti class | `text.classList.replace('card', 'dark')` |
| `forEach()` | Loop semua class | `itemList.classList.forEach(c => {...})` |

---

### 🎯 Property `style`
- ✅ Mengubah inline CSS langsung
- ⚠️ Gunakan **camelCase** untuk property dengan dash
- 🔄 Bisa dikombinasikan dengan loop untuk multiple elements

```javascript
element.style.lineHeight = '3';
element.style.backgroundColor = '#333';
```

---

### 🎓 Best Practices

1. **Gunakan `classList` daripada `className`**  
   ✅ Lebih aman dan fleksibel

2. **Prefer CSS classes over inline styles**  
   ✅ Lebih maintainable dan organized

3. **Toggle untuk show/hide**  
   ✅ Gunakan `toggle('hidden')` untuk display on/off

4. **Event-driven styling**  
   ✅ Selalu gunakan event (click, hover) untuk trigger changes

5. **Conditional styling**  
   ✅ Combine dengan loop dan if statement untuk dynamic styling

---

## 🎉 Selamat!

Kamu sudah mempelajari cara manipulasi CSS dengan JavaScript! Sekarang kamu bisa:

- ✅ Mengubah classes dengan `classList`
- ✅ Toggle dark mode dan hide/show elements
- ✅ Mengubah inline styles dengan `style`
- ✅ Loop dan apply conditional styling
- ✅ Membuat website yang dinamis dan interaktif!

**💪 Next Step:**  
Praktek membuat dark mode toggle atau hamburger menu sendiri!

---

**📚 Dokumentasi Pribadi** | Dibuat untuk pembelajaran JavaScript DOM Manipulation
