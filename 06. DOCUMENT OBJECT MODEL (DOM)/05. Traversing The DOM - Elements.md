# 🌲 Panduan Lengkap DOM Traversal untuk Pemula

> **Dokumentasi Pribadi** - Belajar cara navigasi dan memanipulasi elemen DOM dengan mudah

---

## 📋 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu DOM Node?](#apa-itu-dom-node)
- [Apa itu Element Node?](#apa-itu-element-node)
- [Hubungan Antar Element](#hubungan-antar-element)
- [Setup Project](#setup-project)
- [Mengakses Child Elements](#mengakses-child-elements)
  - [Property children](#property-children)
  - [Property firstElementChild](#property-firstelementchild)
  - [Property lastElementChild](#property-lastelementchild)
- [Mengakses Parent Element](#mengakses-parent-element)
- [Mengakses Sibling Elements](#mengakses-sibling-elements)
  - [Property nextElementSibling](#property-nextelementsibiling)
  - [Property previousElementSibling](#property-previouselementsibiling)
- [Ringkasan Properties](#ringkasan-properties)
- [Tips dan Best Practices](#tips-dan-best-practices)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

**DOM Traversal** adalah teknik untuk **berpindah** atau **navigasi** antar elemen dalam struktur DOM (Document Object Model). Bayangkan DOM seperti pohon keluarga, dimana kita bisa berpindah dari anak ke orang tua, atau dari satu saudara ke saudara lainnya.

### 🤔 Mengapa DOM Traversal Penting?

- ✅ Memudahkan akses elemen tanpa harus `querySelector` berulang kali
- ✅ Membuat kode lebih efisien dan dinamis
- ✅ Memahami hubungan antar elemen dalam HTML

---

<a name="apa-itu-dom-node"></a>
## 📖 Apa itu DOM Node?

**Node** adalah istilah untuk setiap "bagian" dalam struktur DOM. Ada beberapa tipe node:

- 🏷️ **Element Node** - Tag HTML seperti `<div>`, `<p>`, `<span>`
- 📝 **Text Node** - Teks di dalam element
- 💬 **Comment Node** - Komentar HTML `<!-- komentar -->`

> ⚠️ **Catatan**: Di tutorial ini kita fokus pada **Element Node** saja!

---

<a name="apa-itu-element-node"></a>
## 🧩 Apa itu Element Node?

**Element Node** adalah representasi dari **tag HTML** dalam DOM. Setiap tag HTML di halaman web adalah sebuah element node.

**Contoh Element Node:**
```html
<div>    ← Element Node (div)
<p>      ← Element Node (p)
<button> ← Element Node (button)
```

---

<a name="hubungan-antar-element"></a>
## 👨‍👩‍👧‍👦 Hubungan Antar Element

Seperti pohon keluarga, element dalam DOM memiliki hubungan:

```
         🎯 PARENT
         └── div.parent
              ├── 👶 CHILD 1
              ├── 👶 CHILD 2  ← SIBLING (saudara kandung)
              └── 👶 CHILD 3
```

### 📌 Istilah Penting:

| Istilah | Penjelasan | Emoji |
|---------|------------|-------|
| **Parent** | Element yang "membungkus" element lain | 👨‍👩‍👧 |
| **Child** | Element yang ada di "dalam" parent | 👶 |
| **Sibling** | Element yang sejajar/setingkat | 👫 |

---

<a name="setup-project"></a>
## 🛠️ Setup Project

### 📄 File: `index.html`

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Traversing The DOM</title>
  </head>
  <body>
    <div class="container">
      <h1>Traversing The DOM</h1>
      
      <!-- 👇 INI PARENT ELEMENT -->
      <div class="parent">
        <div class="child">Child 1</div>
        <div class="child">Child 2</div>
        <div class="child">Child 3</div>
      </div>
      
    </div>

    <script src="script.js"></script>
  </body>
</html>
```

### 🎨 Struktur Visual:

```
div.parent (PARENT)
├── div.child: "Child 1" (CHILD pertama)
├── div.child: "Child 2" (CHILD kedua)
└── div.child: "Child 3" (CHILD terakhir)
```

---

<a name="mengakses-child-elements"></a>
## 👶 Mengakses Child Elements

<a name="property-children"></a>
### 1️⃣ Property `children`

Property `children` memberikan kita **HTMLCollection** berisi **SEMUA** child elements.

#### 📝 Kode:

```javascript
let output;

// Ambil element parent
const parent = document.querySelector('.parent');

// Ambil SEMUA children
output = parent.children;

console.log(output);
```

#### 📤 Output:
```
HTMLCollection(3) [div.child, div.child, div.child]
```

> 💡 **Penjelasan**: Kita mendapatkan **array-like object** berisi 3 div dengan class "child"

---

### 🎯 Mengakses Child Tertentu

Karena `children` seperti array, kita bisa akses menggunakan **index** (dimulai dari 0):

```javascript
// Akses child KEDUA (index 1)
output = parent.children[1];

console.log(output);
// Output: <div class="child">Child 2</div>
```

#### 📊 Index Mapping:

| Index | Element |
|-------|---------|
| `[0]` | Child 1 (pertama) |
| `[1]` | Child 2 (kedua) |
| `[2]` | Child 3 (ketiga) |

---

### 🔍 Mengakses Property dari Child

```javascript
// Ambil teks dari child kedua
output = parent.children[1].innerText;
console.log(output); // Output: "Child 2"

// Ambil class name
output = parent.children[1].className;
console.log(output); // Output: "child"

// Ambil nama tag (node)
output = parent.children[1].nodeName;
console.log(output); // Output: "DIV"
```

---

### ✏️ Mengubah Child Element

```javascript
// Ubah teks child kedua
parent.children[1].innerText = 'Child Two';

// Ubah warna teks jadi merah
parent.children[1].style.color = 'red';
```

#### 🎨 Hasil Visual:
```
Child 1
Child Two (teks merah 🔴)
Child 3
```

---

<a name="property-firstelementchild"></a>
### 2️⃣ Property `firstElementChild`

Untuk mengakses child **PERTAMA** tanpa menggunakan index.

#### 📝 Kode:

```javascript
// Akses child pertama
parent.firstElementChild.innerText = 'Child One';

console.log(parent.firstElementChild);
// Output: <div class="child">Child One</div>
```

> ✨ **Keuntungan**: Lebih jelas dan mudah dibaca daripada `parent.children[0]`

---

<a name="property-lastelementchild"></a>
### 3️⃣ Property `lastElementChild`

Untuk mengakses child **TERAKHIR**.

#### 📝 Kode:

```javascript
// Akses child terakhir
parent.lastElementChild.innerText = 'Child Three';

console.log(parent.lastElementChild);
// Output: <div class="child">Child Three</div>
```

#### 🎯 Hasil Akhir:
```
Child One
Child Two (merah 🔴)
Child Three
```

---

<a name="mengakses-parent-element"></a>
## 👨‍👩‍👧 Mengakses Parent Element

Kita juga bisa **naik ke atas** dari child ke parent!

### 📝 Kode:

```javascript
// Ambil SALAH SATU child (yang pertama)
const child = document.querySelector('.child');

// Akses parent-nya
output = child.parentElement;

console.log(output);
// Output: <div class="parent">...</div>
```

---

### 🎨 Styling Parent dari Child

```javascript
// Tambahkan border pada parent
child.parentElement.style.border = '1px solid #ccc';

// Tambahkan padding
child.parentElement.style.padding = '10px';
```

#### 📦 Hasil Visual:

```
┌─────────────────────────┐
│  (padding 10px)         │
│  Child One              │
│  Child Two (red)        │
│  Child Three            │
│                         │
└─────────────────────────┘
   ↑ border abu-abu
```

> 💡 **Catatan**: Kita memanipulasi parent **tanpa** harus `querySelector` langsung ke parent!

---

<a name="mengakses-sibling-elements"></a>
## 👫 Mengakses Sibling Elements

**Sibling** = Saudara kandung (element yang sejajar)

<a name="property-nextelementsibiling"></a>
### 1️⃣ Property `nextElementSibling`

Untuk mengakses sibling **BERIKUTNYA** (ke kanan/bawah).

#### 📝 Kode:

```javascript
// Ambil child KEDUA
const secondItem = document.querySelector('.child:nth-child(2)');

output = secondItem;
console.log(output); // <div class="child">Child Two</div>

// Ambil sibling BERIKUTNYA (Child 3)
output = secondItem.nextElementSibling;
console.log(output); // <div class="child">Child Three</div>
```

---

### 🎨 Styling Next Sibling

```javascript
// Ubah warna sibling berikutnya jadi hijau
secondItem.nextElementSibling.style.color = 'green';
```

#### 🌈 Hasil:
```
Child One
Child Two (red 🔴)
Child Three (green 🟢) ← ini yang berubah!
```

---

<a name="property-previouselementsibiling"></a>
### 2️⃣ Property `previousElementSibling`

Untuk mengakses sibling **SEBELUMNYA** (ke kiri/atas).

#### 📝 Kode:

```javascript
// Dari Child 2, akses sibling SEBELUMNYA (Child 1)
secondItem.previousElementSibling.style.color = 'orange';
```

#### 🌈 Hasil Final:
```
Child One (orange 🟠) ← ini yang berubah!
Child Two (red 🔴)
Child Three (green 🟢)
```

---

<a name="ringkasan-properties"></a>
## 📚 Ringkasan Properties

### 🔽 Dari Parent ke Child:

| Property | Fungsi | Contoh |
|----------|--------|--------|
| `.children` | Semua child (HTMLCollection) | `parent.children` |
| `.children[index]` | Child tertentu | `parent.children[1]` |
| `.firstElementChild` | Child pertama | `parent.firstElementChild` |
| `.lastElementChild` | Child terakhir | `parent.lastElementChild` |

### 🔼 Dari Child ke Parent:

| Property | Fungsi | Contoh |
|----------|--------|--------|
| `.parentElement` | Parent element | `child.parentElement` |

### ↔️ Antar Sibling:

| Property | Fungsi | Contoh |
|----------|--------|--------|
| `.nextElementSibling` | Sibling berikutnya | `element.nextElementSibling` |
| `.previousElementSibling` | Sibling sebelumnya | `element.previousElementSibling` |

---

<a name="tips-dan-best-practices"></a>
## 💡 Tips dan Best Practices

### ✅ DO (Yang Benar):

```javascript
// ✅ Gunakan property, BUKAN method (tanpa tanda kurung)
parent.children

// ✅ Gunakan *ElementSibling untuk element saja
element.nextElementSibling

// ✅ Gunakan console.dir() untuk lihat semua property
console.dir(parent.children[0]);
```

### ❌ DON'T (Yang Salah):

```javascript
// ❌ Jangan pakai tanda kurung (ini property, bukan method)
parent.children()

// ❌ Hindari nextSibling (ini ambil semua node termasuk text)
element.nextSibling // Bisa return text node!

// ❌ Jangan lupa index dimulai dari 0
parent.children[3] // Kalau cuma ada 3 child, ini undefined!
```

---

### 🎯 Kapan Menggunakan Apa?

| Situasi | Gunakan |
|---------|---------|
| Tahu posisi pasti child | `.children[index]` |
| Butuh child pertama | `.firstElementChild` |
| Butuh child terakhir | `.lastElementChild` |
| Dari child ke parent | `.parentElement` |
| Ke element setelahnya | `.nextElementSibling` |
| Ke element sebelumnya | `.previousElementSibling` |

---

### 🔥 Kombinasi Power Moves:

```javascript
// Ambil parent dari sibling berikutnya 🤯
element.nextElementSibling.parentElement

// Akses child kedua dari parent element 🚀
element.parentElement.children[1]

// Chain property untuk navigasi kompleks ⚡
element.previousElementSibling.firstElementChild
```

---

## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah belajar:

- ✅ Cara mengakses child dari parent
- ✅ Cara mengakses parent dari child
- ✅ Cara navigasi antar sibling
- ✅ Cara memanipulasi element dengan traversal

### 📝 Next Steps:

1. Praktik kode di atas berkali-kali
2. Coba buat struktur HTML sendiri
3. Eksperimen dengan chain property
4. Lanjut belajar tentang **Node types** (text node, comment node)

---

> 💪 **Keep Learning!** Practice makes perfect!

---

**📅 Dokumentasi Pribadi** | Terakhir diperbarui: 25 Desember 2025
