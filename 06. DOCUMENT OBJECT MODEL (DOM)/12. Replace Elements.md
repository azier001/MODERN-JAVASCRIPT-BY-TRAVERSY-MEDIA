# 📝 Dokumentasi DOM: Mengganti Elemen (Replace Elements)

> **Dokumentasi Pribadi untuk Pemula** 🎓  
> Belajar cara mengganti elemen-elemen dalam DOM dengan berbagai metode

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan Project](#persiapan-project)
- [Apa itu Replace Elements?](#apa-itu-replace-elements)
- [Mengapa Perlu Mengganti Elemen?](#mengapa-perlu-mengganti-elemen)
- [Metode 1: replaceWith()](#metode-1-replacewith)
- [Metode 2: outerHTML](#metode-2-outerhtml)
- [Metode 3: Replace All Items dengan Loop](#metode-3-replace-all-items-dengan-loop)
- [Metode 4: replaceChild()](#metode-4-replacechild)
- [Perbedaan innerHTML vs outerHTML](#perbedaan-innerhtml-vs-outerhtml)
- [Tips dan Trik](#tips-dan-trik)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Setelah belajar cara **memasukkan** elemen baru ke dalam DOM, sekarang kita akan belajar cara **mengganti** elemen yang sudah ada. Ada beberapa cara untuk melakukan ini, dan masing-masing punya kelebihan tersendiri! 

**Contoh Kasus:** Kita akan bekerja dengan Shopping List UI dan mengganti item-item di dalamnya.

---

<a name="persiapan-project"></a>
## 🛠️ Persiapan Project

### 📄 Struktur HTML (index.html)

Kita akan menggunakan contoh **Shopping List** sederhana:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <title>Shopping List</title>
  </head>
  <body>
    <div class="container">
      <header>
        <img src="images/note.png" alt="" />
        <h1>Shopping List</h1>
      </header>

      <!-- Form untuk menambah item -->
      <form id="item-form">
        <div class="form-control">
          <input
            type="text"
            class="form-input"
            id="item-input"
            placeholder="Enter Item"
          />
        </div>
        <div class="form-control">
          <button type="submit" class="btn">Add Item</button>
        </div>
      </form>

      <!-- Filter input -->
      <div class="filter">
        <input
          type="text"
          class="form-input-filter"
          id="filter"
          placeholder="Filter Items"
        />
      </div>

      <!-- Daftar belanja -->
      <ul id="item-list" class="items">
        <li>Apples</li>
        <li>Orange Juice</li>
        <li>Oreos</li>
        <li>Milk</li>
      </ul>

      <!-- Tombol clear -->
      <button id="clear" class="btn-clear">Clear All</button>
    </div>

    <script src="script.js"></script>
  </body>
</html>
```

### 🎨 Penjelasan Struktur:
- 📦 **Container**: Pembungkus utama aplikasi
- 📝 **Form**: Untuk menambah item baru
- 🔍 **Filter**: Input untuk mencari item
- 📋 **List (ul)**: Daftar belanjaan
- 🗑️ **Clear Button**: Tombol untuk menghapus semua

---

<a name="apa-itu-replace-elements"></a>
## 📖 Apa itu Replace Elements?

**Replace Elements** adalah proses mengganti satu elemen HTML dengan elemen HTML lainnya. Bayangkan seperti mengganti kartu lama dengan kartu baru! 🃏

**Ilustrasi:**
```
SEBELUM:  <li>Apples</li>
SESUDAH:  <li>Replaced First</li>
```

---

<a name="mengapa-perlu-mengganti-elemen"></a>
## 🤔 Mengapa Perlu Mengganti Elemen?

- ✅ **Update konten dinamis** - Mengganti data yang sudah tidak relevan
- ✅ **Mengubah struktur HTML** - Misalnya dari `<h1>` ke `<h2>`
- ✅ **Refresh tampilan** - Mengganti seluruh komponen dengan yang baru
- ✅ **Interaksi user** - Merespon aksi pengguna dengan update visual

---

<a name="metode-1-replacewith"></a>
## 🔄 Metode 1: `replaceWith()`

### 📌 Penjelasan
Metode paling **modern dan simpel**! Langsung memanggil `.replaceWith()` pada elemen yang mau diganti.

### 🎯 Cara Kerja
1. **Pilih** elemen yang mau diganti
2. **Buat** elemen baru
3. **Panggil** `replaceWith()` dengan elemen baru

### 💻 Kode Praktek

```javascript
// Mengganti item pertama dalam list
function replaceFirstItem() {
  // 1️⃣ PILIH elemen pertama yang akan diganti
  const firstItem = document.querySelector('li:first-child');

  // 2️⃣ BUAT elemen baru (list item baru)
  const li = document.createElement('li');
  li.textContent = 'Replaced First';

  // 3️⃣ GANTI elemen lama dengan yang baru
  firstItem.replaceWith(li);
}

// Jalankan fungsi
replaceFirstItem();
```

### 🔍 Penjelasan Detail

| Langkah | Kode | Penjelasan |
|---------|------|------------|
| **Step 1** | `document.querySelector('li:first-child')` | Mengambil elemen `<li>` pertama dalam dokumen |
| **Step 2** | `document.createElement('li')` | Membuat elemen `<li>` baru di memori |
| **Step 3** | `li.textContent = 'Replaced First'` | Mengisi teks dalam elemen baru |
| **Step 4** | `firstItem.replaceWith(li)` | Mengganti elemen lama dengan yang baru |

### ✨ Kelebihan
- ✅ Syntax paling **mudah dipahami**
- ✅ Hanya **1 baris** untuk mengganti
- ✅ Metode **modern** (ES6+)

---

<a name="metode-2-outerhtml"></a>
## 🏷️ Metode 2: `outerHTML`

### 📌 Penjelasan
Metode ini menggunakan **property `outerHTML`** yang mengganti **keseluruhan elemen** termasuk tag pembukanya.

### 🎯 Cara Kerja
1. **Pilih** elemen yang mau diganti
2. **Set** property `outerHTML` dengan HTML string baru

### 💻 Kode Praktek

```javascript
// Mengganti item kedua dengan outerHTML
function replaceSecondItem() {
  // 1️⃣ PILIH elemen kedua
  const secondItem = document.querySelector('li:nth-child(2)');

  // 2️⃣ SET outerHTML dengan HTML baru (termasuk tag <li>)
  secondItem.outerHTML = '<li>Replaced Second</li>';
}

// Jalankan fungsi
replaceSecondItem();
```

### 🔍 Penjelasan Detail

**Selector `li:nth-child(2)`** berarti:
- `li` = Pilih elemen list item
- `:nth-child(2)` = Ambil yang ke-2

**`outerHTML`** mengganti **SELURUH** elemen termasuk tag-nya:
```html
<!-- SEBELUM -->
<li>Orange Juice</li>

<!-- SESUDAH -->
<li>Replaced Second</li>
```

### 💡 Catatan Penting
> **Perbedaan `innerHTML` vs `outerHTML`:**
> - `innerHTML` → Hanya isi **DALAM** tag
> - `outerHTML` → **SELURUH** elemen termasuk tag-nya

---

<a name="metode-3-replace-all-items-dengan-loop"></a>
## 🔁 Metode 3: Replace All Items dengan Loop

### 📌 Penjelasan
Ketika ingin mengganti **BANYAK elemen sekaligus**, kita bisa loop dengan `forEach()`.

### 🎯 Cara Kerja
1. **Ambil** semua elemen dengan `querySelectorAll()`
2. **Loop** setiap elemen dengan `forEach()`
3. **Ganti** satu per satu

### 💻 Kode Praktek - Versi Panjang

```javascript
function replaceAllItems() {
  // 1️⃣ AMBIL semua elemen <li>
  const lis = document.querySelectorAll('li');

  // 2️⃣ LOOP setiap item
  lis.forEach((item, index) => {
    // 3️⃣ KONDISI: jika index = 1 (item kedua)
    if (index === 1) {
      item.innerHTML = 'Second Item';
    } else {
      // Item lainnya
      item.innerHTML = 'Replace All';
    }
  });
}
```

### 💻 Kode Praktek - Versi Pendek (dengan Ternary)

```javascript
function replaceAllItems() {
  const lis = document.querySelectorAll('li');

  // ⚡ SHORTHAND dengan ternary operator
  lis.forEach((item, index) =>
    item.outerHTML = index === 1 
      ? '<li>Second Item</li>'  // Jika index = 1
      : '<li>Item</li>'         // Jika bukan
  );
}

// Jalankan fungsi
replaceAllItems();
```

### 🔍 Penjelasan Detail

**Ternary Operator:**
```javascript
kondisi ? nilaiJikaTrue : nilaiJikaFalse
```

**Breakdown kode:**
```javascript
item.outerHTML = index === 1 ? '<li>Second Item</li>' : '<li>Item</li>'
//               └─────┬─────┘   └────────┬─────────┘   └──────┬──────┘
//                  Kondisi         Jika TRUE           Jika FALSE
```

### 📊 Perbandingan Versi

| Aspek | Versi Panjang | Versi Pendek |
|-------|--------------|--------------|
| **Baris Kode** | ~8 baris | ~3 baris |
| **Readability** | ⭐⭐⭐⭐⭐ Mudah dibaca | ⭐⭐⭐ Perlu terbiasa |
| **Performance** | ✅ Sama | ✅ Sama |
| **Untuk Pemula** | ✅ Recommended | ⚠️ Setelah paham if-else |

---

<a name="metode-4-replacechild"></a>
## 👨‍👦 Metode 4: `replaceChild()`

### 📌 Penjelasan
Metode ini bekerja dari **sudut pandang parent** (elemen pembungkus). Parent-nya yang melakukan penggantian terhadap child-nya.

### 🎯 Cara Kerja
1. **Pilih** elemen parent (pembungkus)
2. **Pilih** elemen child yang mau diganti
3. **Buat** elemen baru
4. **Panggil** `replaceChild(elemenBaru, elemenLama)` dari parent

### 💻 Kode Praktek

```javascript
// Mengganti <h1> menjadi <h2> di dalam <header>
function replaceChildHeading() {
  // 1️⃣ PILIH elemen PARENT
  const header = document.querySelector('header');
  
  // 2️⃣ PILIH elemen CHILD yang akan diganti
  const h1 = document.querySelector('header h1');

  // 3️⃣ BUAT elemen baru
  const h2 = document.createElement('h2');
  h2.id = 'app-title';  // Tambahkan ID
  h2.textContent = 'Shopping List';  // Tambahkan teks

  // 4️⃣ PARENT mengganti child lama dengan child baru
  header.replaceChild(h2, h1);
  //                  ↑    ↑
  //                BARU LAMA
}

// Jalankan fungsi
replaceChildHeading();
```

### 🔍 Penjelasan Detail

**Struktur HTML:**
```html
<!-- SEBELUM -->
<header>
  <h1>Shopping List</h1>  ← Yang akan diganti
</header>

<!-- SESUDAH -->
<header>
  <h2 id="app-title">Shopping List</h2>  ← Hasil penggantian
</header>
```

**Urutan Parameter:**
```javascript
parent.replaceChild(newChild, oldChild)
//                  └──┬───┘  └──┬───┘
//                  Elemen    Elemen
//                   BARU      LAMA
```

### ⚠️ Catatan Penting
> Urutan parameter sangat penting!  
> **BARU dulu**, **LAMA kemudian** ✅  
> Bukan kebalik! ❌

---

<a name="perbedaan-innerhtml-vs-outerhtml"></a>
## 🆚 Perbedaan `innerHTML` vs `outerHTML`

### 📊 Tabel Perbandingan

| Aspek | `innerHTML` | `outerHTML` |
|-------|-------------|-------------|
| **Yang Diganti** | Isi DALAM tag | SELURUH elemen + tag |
| **Tag Target** | Tidak ikut diganti | Ikut diganti |
| **Use Case** | Ganti konten saja | Ganti elemen sepenuhnya |

### 💻 Contoh Visual

```javascript
// Setup HTML
<div id="box">
  <p>Hello World</p>
</div>

// Menggunakan innerHTML
box.innerHTML = '<span>New Content</span>';
// Hasil: <div id="box"><span>New Content</span></div>
//        └─── Tag DIV tetap ada ───┘

// Menggunakan outerHTML
box.outerHTML = '<section>New Content</section>';
// Hasil: <section>New Content</section>
//        └─── DIV hilang, diganti SECTION ───┘
```

### 🎯 Kapan Menggunakan?

**Gunakan `innerHTML` ketika:**
- ✅ Hanya ingin mengubah **isi** elemen
- ✅ Tag pembungkus tetap sama
- ✅ Contoh: Update teks, gambar, atau konten dalam div

**Gunakan `outerHTML` ketika:**
- ✅ Ingin mengganti **seluruh elemen**
- ✅ Tag pembungkus juga perlu diganti
- ✅ Contoh: Ganti `<div>` menjadi `<section>`

---

<a name="tips-dan-trik"></a>
## 💡 Tips dan Trik

### 1. 🎯 Pilih Metode yang Tepat

```javascript
// ✅ SIMPLE: Ganti 1 elemen
element.replaceWith(newElement);

// ✅ QUICK: Ganti dengan HTML string
element.outerHTML = '<div>New</div>';

// ✅ BULK: Ganti banyak elemen
elements.forEach(el => el.outerHTML = '...');

// ✅ HIERARCHICAL: Parent mengganti child
parent.replaceChild(newChild, oldChild);
```

### 2. 🔍 Selector Tips

```javascript
// First child
document.querySelector('li:first-child')

// Nth child (dimulai dari 1)
document.querySelector('li:nth-child(2)')  // Elemen ke-2

// Last child
document.querySelector('li:last-child')

// Semua elemen
document.querySelectorAll('li')
```

### 3. ⚡ Performance Tips

| ❌ Kurang Efisien | ✅ Lebih Efisien |
|-------------------|------------------|
| Loop besar dengan `outerHTML` | Gunakan `replaceWith()` |
| Query selector di dalam loop | Query sekali, simpan di variabel |
| Banyak manipulasi DOM | Gabung manipulasi, execute sekali |

### 4. 🐛 Debugging Tips

```javascript
// Cek elemen sebelum replace
console.log('Before:', element);
console.log('HTML:', element.outerHTML);

// Lakukan replace
element.replaceWith(newElement);

// Cek hasil
console.log('After:', newElement);
```

---

<a name="rangkuman"></a>
## 📚 Rangkuman

### 🎯 4 Metode Replace Elements

| No | Metode | Syntax | Kapan Digunakan |
|----|--------|--------|-----------------|
| 1️⃣ | **replaceWith()** | `oldEl.replaceWith(newEl)` | Ganti 1 elemen, paling simple |
| 2️⃣ | **outerHTML** | `el.outerHTML = '<div>...</div>'` | Ganti dengan HTML string |
| 3️⃣ | **forEach + outerHTML** | `els.forEach(el => ...)` | Ganti banyak elemen sekaligus |
| 4️⃣ | **replaceChild()** | `parent.replaceChild(new, old)` | Parent mengganti child-nya |

### ✅ Checklist Belajar

- [ ] Paham perbedaan `innerHTML` vs `outerHTML`
- [ ] Bisa pakai `replaceWith()` untuk 1 elemen
- [ ] Bisa pakai `outerHTML` untuk replace
- [ ] Bisa loop dan replace banyak elemen
- [ ] Paham `replaceChild()` dari sudut pandang parent
- [ ] Tahu kapan menggunakan metode mana

### 🎓 Langkah Selanjutnya

Setelah menguasai **Replace**, selanjutnya belajar:
1. ✅ Insert Elements (Sudah ✓)
2. ✅ Replace Elements (Saat ini ✓)
3. ⏭️ **Remove Elements** (Next!)

---

### 🌟 Motivasi

> "Setiap kali kamu mengganti elemen dengan benar, kamu selangkah lebih dekat menjadi JavaScript Master!" 🚀

**Keep coding! Keep learning!** 💪✨

---

**📅 Catatan Dibuat:** Dokumentasi Pribadi  
**🎯 Level:** Pemula  
**📝 Topik:** DOM Manipulation - Replace Elements
