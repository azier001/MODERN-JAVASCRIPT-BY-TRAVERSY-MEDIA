# 📚 Dokumentasi Lengkap JavaScript Promises untuk Pemula

> 🎓 **Catatan Pribadi** - Dokumentasi ini dibuat untuk memudahkan pembelajaran JavaScript Promises secara bertahap dan mudah dipahami.

---

## 📑 Daftar Isi

- [Pengenalan Promises](#pengenalan-promises)
- [Apa itu Promise?](#apa-itu-promise)
- [Mengapa Menggunakan Promises?](#mengapa-menggunakan-promises)
- [Setup Project](#setup-project)
- [Membuat Promise Pertama](#membuat-promise-pertama)
- [Menangani Promise dengan .then()](#menangani-promise-dengan-then)
- [Memahami Asynchronous](#memahami-asynchronous)
- [Mengirim Data dari Promise](#mengirim-data-dari-promise)
- [Menangani Error dengan .catch()](#menangani-error-dengan-catch)
- [Menggunakan .finally()](#menggunakan-finally)
- [Kode Lengkap](#kode-lengkap)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan-promises"></a>
## 🎯 Pengenalan Promises

Selamat datang di dokumentasi belajar JavaScript Promises! Dalam tutorial ini, kita akan belajar tentang salah satu konsep paling penting dalam JavaScript modern.

### 🎬 Yang Akan Dipelajari:
- ✅ Apa itu Promise
- ✅ Cara membuat Promise
- ✅ Cara menangani Promise dengan `.then()`, `.catch()`, dan `.finally()`
- ✅ Memahami konsep asynchronous programming

---

<a name="apa-itu-promise"></a>
## 📖 Apa itu Promise?

**Promise** adalah sebuah objek yang merepresentasikan **penyelesaian** atau **kegagalan** dari sebuah operasi asynchronous (operasi yang membutuhkan waktu).

### 🤝 Analogi Sederhana:
Bayangkan kamu memesan makanan secara online:
- 📱 Kamu membuat pesanan (membuat Promise)
- ⏳ Restoran sedang memasak (proses asynchronous)
- ✅ Makanan diantar (Promise **resolved** - berhasil)
- ❌ Atau pesanan dibatalkan (Promise **rejected** - gagal)

Sementara menunggu makanan, kamu bisa melakukan aktivitas lain (non-blocking).

---

<a name="mengapa-menggunakan-promises"></a>
## 💡 Mengapa Menggunakan Promises?

### Keuntungan Promises:

1. **🚫 Menghindari Callback Hell**
   - Callback Hell = banyak callback bersarang yang sulit dibaca
   
2. **⚡ Non-blocking & Asynchronous**
   - Kode lain tetap berjalan sambil menunggu operasi selesai
   
3. **🎯 Mudah Menangani Error**
   - Error handling yang lebih clean dengan `.catch()`

---

<a name="setup-project"></a>
## 🛠️ Setup Project

### Langkah 1: Buat File HTML

Buat file `index.html`:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Promises</title>
  </head>
  <body>
    <h1>Promises</h1>

    <script src="./script.js"></script>
  </body>
</html>
```

### Langkah 2: Buat File JavaScript

Buat file `script.js` (kosong dulu, kita akan isi bertahap).

---

<a name="membuat-promise-pertama"></a>
## 🎨 Membuat Promise Pertama

### 📝 Langkah 1: Sintaks Dasar Promise

Buka file `script.js` dan tulis kode berikut:

```javascript
// Create a promise
const promise = new Promise((resolve, reject) => {
  // Do some async task
  setTimeout(() => {
    console.log('Async task complete');
    resolve();
  }, 1000);
});
```

### 🔍 Penjelasan Kode:

| Bagian | Penjelasan |
|--------|-----------|
| `new Promise()` | Constructor untuk membuat Promise baru |
| `(resolve, reject)` | Dua parameter fungsi: `resolve` untuk sukses, `reject` untuk gagal |
| `setTimeout()` | Simulasi operasi asynchronous (menunggu 1 detik) |
| `resolve()` | Memanggil fungsi ini saat Promise berhasil |

### ⏱️ Mengapa setTimeout?
`setTimeout()` digunakan untuk mensimulasikan operasi yang membutuhkan waktu, seperti:
- 🌐 Mengambil data dari server
- 📁 Membaca file
- 💾 Menyimpan data ke database

---

<a name="menangani-promise-dengan-then"></a>
## 🎯 Menangani Promise dengan .then()

### 📝 Langkah 2: Menambahkan .then()

Tambahkan kode ini di bawah Promise:

```javascript
promise.then(() => {
  console.log('Promise consumed..');
});
```

### 🔍 Penjelasan:
- `.then()` dijalankan **setelah** Promise berhasil (resolved)
- Menerima callback function yang akan dieksekusi

### 🧪 Test Kode:
1. Buka `index.html` di browser
2. Buka Console (F12)
3. Lihat output:
   ```
   Async task complete
   Promise consumed..
   ```

---

<a name="memahami-asynchronous"></a>
## ⚡ Memahami Asynchronous

### 📝 Langkah 3: Tambahkan Console.log Global

Tambahkan di **akhir** file `script.js`:

```javascript
console.log('Hello from global scope');
```

### 🧪 Test Kode Lengkap:

```javascript
// Create a promise
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    console.log('Async task complete');
    resolve();
  }, 1000);
});

promise.then(() => {
  console.log('Promise consumed..');
});

console.log('Hello from global scope');
```

### 📊 Output di Console:
```
Hello from global scope        👈 Muncul PERTAMA!
Async task complete            👈 Muncul setelah 1 detik
Promise consumed..             👈 Muncul setelah Promise resolved
```

### 🤔 Mengapa Urutan Seperti Ini?

1. **"Hello from global scope"** muncul dulu karena **tidak menunggu** Promise
2. Promise berjalan di background (non-blocking)
3. Setelah 1 detik, Promise selesai dan `.then()` dijalankan

> 💡 **Ini adalah inti dari Asynchronous Programming!**

---

<a name="mengirim-data-dari-promise"></a>
## 📦 Mengirim Data dari Promise

### 📝 Langkah 4: Promise dengan Data

Sekarang kita akan membuat Promise yang mengirim data. **Ganti** kode dengan:

```javascript
const getUser = new Promise((resolve, reject) => {
  setTimeout(() => {
    let error = false;

    if (!error) {
      resolve({ name: 'John', age: 30 });
    } else {
      reject('Error: Something went wrong');
    }
  }, 1000);
});

getUser.then((user) => {
  console.log(user);
});

console.log('Hello from global scope');
```

### 🔍 Penjelasan Kode:

| Bagian | Penjelasan |
|--------|-----------|
| `let error = false` | Flag untuk simulasi error |
| `resolve({ name: 'John', age: 30 })` | Mengirim objek user saat sukses |
| `reject('Error: ...')` | Mengirim pesan error saat gagal |
| `.then((user) => ...)` | Menerima data yang dikirim `resolve()` |

### 📊 Output di Console:
```
Hello from global scope
{ name: 'John', age: 30 }
```

### ✨ Poin Penting:
- Data yang dimasukkan ke `resolve()` akan diterima di `.then()`
- Bisa mengirim string, number, object, array, dll

---

<a name="menangani-error-dengan-catch"></a>
## 🛡️ Menangani Error dengan .catch()

### 📝 Langkah 5: Menambahkan Error Handling

Ubah kode menjadi:

```javascript
const getUser = new Promise((resolve, reject) => {
  setTimeout(() => {
    let error = true;  // 👈 Ubah menjadi true untuk simulasi error

    if (!error) {
      resolve({ name: 'John', age: 30 });
    } else {
      reject('Error: Something went wrong');
    }
  }, 1000);
});

getUser
  .then((user) => console.log(user))
  .catch((error) => console.log(error));

console.log('Hello from global scope');
```

### 🔍 Penjelasan:
- `.catch()` menangkap error dari `reject()`
- Tanpa `.catch()`, akan muncul **"Uncaught Promise"** error di console

### 🧪 Test dengan Error:

**Ketika `error = true`:**
```
Hello from global scope
Error: Something went wrong
```

**Ketika `error = false`:**
```
Hello from global scope
{ name: 'John', age: 30 }
```

### 💡 Best Practice:
**Selalu** gunakan `.catch()` untuk menangani error di Promise!

---

<a name="menggunakan-finally"></a>
## 🏁 Menggunakan .finally()

### 📝 Langkah 6: Tambahkan .finally()

Tambahkan `.finally()` di akhir chain:

```javascript
const getUser = new Promise((resolve, reject) => {
  setTimeout(() => {
    let error = false;

    if (!error) {
      resolve({ name: 'John', age: 30 });
    } else {
      reject('Error: Something went wrong');
    }
  }, 1000);
});

getUser
  .then((user) => console.log(user))
  .catch((error) => console.log(error))
  .finally(() => console.log('The promise has been resolved or rejected'));

console.log('Hello from global scope');
```

### 🔍 Kapan .finally() Digunakan?

`.finally()` akan **SELALU** dijalankan, apapun hasilnya:
- ✅ Dijalankan saat Promise **berhasil** (resolved)
- ❌ Dijalankan saat Promise **gagal** (rejected)

### 🎯 Kegunaan .finally():
- Menutup koneksi database
- Menyembunyikan loading spinner
- Cleanup operations

---

<a name="kode-lengkap"></a>
## 📄 Kode Lengkap

### File: `index.html`

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Promises</title>
  </head>
  <body>
    <h1>Promises</h1>

    <script src="./script.js"></script>
  </body>
</html>
```

### File: `script.js`

```javascript
// Create a promise
const promise = new Promise((resolve, reject) => {
  // Do some async task
  setTimeout(() => {
    console.log('Async task complete');
    resolve();
  }, 1000);
});

const getUser = new Promise((resolve, reject) => {
  setTimeout(() => {
    let error = false;

    if (!error) {
      resolve({ name: 'John', age: 30 });
    } else {
      reject('Error: Something went wrong');
    }
  }, 1000);
});

getUser
  .then((user) => console.log(user))
  .catch((error) => console.log(error))
  .finally(() => console.log('The promise has been resolved or rejected'));

console.log('Hello from global scope');
```

---

<a name="rangkuman"></a>
## 📌 Rangkuman

### ✨ Konsep Utama Promise:

1. **Promise memiliki 3 state:**
   - ⏳ **Pending** - Sedang dalam proses
   - ✅ **Fulfilled** - Berhasil (resolved)
   - ❌ **Rejected** - Gagal

2. **Cara Membuat Promise:**
   ```javascript
   new Promise((resolve, reject) => {
     // kode asynchronous
   })
   ```

3. **Cara Menangani Promise:**
   ```javascript
   promise
     .then(data => { /* sukses */ })
     .catch(error => { /* gagal */ })
     .finally(() => { /* selalu dijalankan */ })
   ```

### 🎯 Poin Penting:

| Konsep | Penjelasan |
|--------|-----------|
| **resolve()** | Dipanggil saat operasi berhasil |
| **reject()** | Dipanggil saat operasi gagal |
| **.then()** | Menangani hasil sukses |
| **.catch()** | Menangani error |
| **.finally()** | Selalu dijalankan (opsional) |

### 🚀 Kapan Menggunakan Promises?

- 🌐 Fetching data dari API
- 📁 Membaca/menulis file
- 💾 Operasi database
- ⏱️ Operasi yang membutuhkan waktu

### 📚 Langkah Selanjutnya:

1. ✅ Praktik membuat berbagai Promise
2. ✅ Pelajari **async/await** (cara modern menulis Promise)
3. ✅ Gunakan **Fetch API** untuk request HTTP
4. ✅ Pelajari **Promise.all()** dan **Promise.race()**

---

## 🎉 Selamat!

Kamu sudah memahami dasar-dasar JavaScript Promises! Terus praktik dan eksplorasi lebih dalam.

> 💡 **Tips:** Kebanyakan library modern JavaScript menggunakan Promises, jadi memahami konsep ini sangat penting!

---

**📅 Terakhir diperbarui:** 2026
**👨‍💻 Dibuat untuk:** Pembelajaran pribadi
