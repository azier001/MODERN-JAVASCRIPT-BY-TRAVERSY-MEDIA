# 🎬 Tutorial Membuat Movie Slider dengan Swiper.js

> **Dokumentasi Pribadi** - Panduan lengkap implementasi Swiper slider untuk menampilkan film yang sedang tayang (Now Playing)

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan Awal](#persiapan-awal)
- [Struktur HTML yang Dibutuhkan](#struktur-html)
- [Langkah 1: Membuat Fungsi Display Slider](#langkah-1)
- [Langkah 2: Fetch Data dari API](#langkah-2)
- [Langkah 3: Loop Data dan Buat Elemen Slide](#langkah-3)
- [Langkah 4: Inisialisasi Swiper](#langkah-4)
- [Langkah 5: Konfigurasi Swiper Options](#langkah-5)
- [Langkah 6: Tambahkan Breakpoints](#langkah-6)
- [Langkah 7: Jalankan di Homepage](#langkah-7)
- [Hasil Akhir](#hasil-akhir)
- [Kode Lengkap](#kode-lengkap)
- [Tips dan Catatan](#tips-catatan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan belajar cara membuat **slider movie** menggunakan library **Swiper.js versi 8**. Slider ini akan menampilkan film-film yang sedang tayang (Now Playing) dari API TMDB dengan fitur:

✨ **Fitur Utama:**
- Auto-play dengan delay 4 detik
- Free mode (bisa di-drag manual)
- Responsive dengan breakpoints
- Loop infinite
- Smooth transitions

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

### Library yang Diperlukan

Pastikan sudah include Swiper di `index.html`:

```html
<!-- CSS Swiper -->
<link rel="stylesheet" href="lib/swiper.css" />

<!-- JavaScript Swiper -->
<script src="lib/swiper.js"></script>
```

> 💡 **Catatan:** Library Swiper sudah tersedia di folder `lib/`

---

<a name="struktur-html"></a>
## 📦 Struktur HTML yang Dibutuhkan

Swiper memerlukan struktur HTML khusus. Berikut struktur yang sudah ada di `index.html`:

```html
<!-- Now Playing Section -->
<section class="now-playing">
  <h2>Now Playing</h2>
  <div class="swiper">
    <div class="swiper-wrapper">
      <!-- Slide akan ditambahkan via JavaScript -->
    </div>
  </div>
</section>
```

**Penjelasan Struktur:**
- 🎯 `.swiper` - Container utama
- 🎯 `.swiper-wrapper` - Wrapper untuk semua slide
- 🎯 `.swiper-slide` - Setiap slide individual (akan dibuat via JS)

---

<a name="langkah-1"></a>
## 📝 Langkah 1: Membuat Fungsi Display Slider

### Buka file `script.js`

Tambahkan fungsi baru di bawah fungsi `displayBackgroundImage()`:

```javascript
// 🆕 Display Slider Movies
async function displaySlider() {
  // Kode akan ditambahkan di langkah berikutnya
}
```

**Penjelasan:**
- ✅ `async` - Karena kita akan fetch data dari API
- ✅ Fungsi ini khusus untuk menampilkan slider

---

<a name="langkah-2"></a>
## 🌐 Langkah 2: Fetch Data dari API

### Tambahkan kode fetch di dalam fungsi `displaySlider()`:

```javascript
// Display Slider Movies
async function displaySlider() {
  // 🆕 Destructure hasil dari API
  const { results } = await fetchAPIData('movie/now_playing');
  
  // 🆕 Console log untuk cek data (opsional)
  console.log(results);
}
```

**Penjelasan:**
- 🎬 `movie/now_playing` - Endpoint API untuk film yang sedang tayang
- 📦 `results` - Array berisi daftar film
- 🔍 `console.log()` - Untuk debugging (bisa dihapus nanti)

**Hasil yang didapat:**
```javascript
// Array berisi objek film seperti:
[
  {
    id: 123,
    title: "Movie Title",
    poster_path: "/path-to-poster.jpg",
    vote_average: 8.5,
    // ... data lainnya
  }
]
```

---

<a name="langkah-3"></a>
## 🔄 Langkah 3: Loop Data dan Buat Elemen Slide

### Update fungsi `displaySlider()`:

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  // 🆕 Loop setiap movie
  results.forEach((movie) => {
    // 🆕 Buat element div
    const div = document.createElement('div');
    
    // 🆕 Tambahkan class 'swiper-slide'
    div.classList.add('swiper-slide');

    // 🆕 Isi HTML
    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    // 🆕 Append ke swiper-wrapper
    document.querySelector('.swiper-wrapper').appendChild(div);
  });
}
```

**Penjelasan Detail:**

1. **Loop forEach:**
   ```javascript
   results.forEach((movie) => { ... })
   ```
   - Mengambil setiap film dari array `results`

2. **Buat Element Div:**
   ```javascript
   const div = document.createElement('div');
   div.classList.add('swiper-slide');
   ```
   - 📌 Setiap slide HARUS punya class `swiper-slide`

3. **Template HTML:**
   - 🖼️ `movie.poster_path` - Path gambar poster
   - 🆔 `movie.id` - ID untuk link ke detail
   - ⭐ `movie.vote_average` - Rating film
   - 📝 `movie.title` - Judul untuk alt text

4. **Append ke DOM:**
   ```javascript
   document.querySelector('.swiper-wrapper').appendChild(div);
   ```
   - Menambahkan slide ke dalam wrapper

---

<a name="langkah-4"></a>
## ⚙️ Langkah 4: Inisialisasi Swiper

### Buat fungsi baru `initSwiper()` di bawah `displaySlider()`:

```javascript
// 🆕 Initialize Swiper
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    // Options akan ditambahkan di langkah berikutnya
  });
}
```

### Panggil fungsi ini di dalam `displaySlider()`:

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    document.querySelector('.swiper-wrapper').appendChild(div);

    // 🆕 Panggil inisialisasi Swiper
    initSwiper();
  });
}
```

> ⚠️ **Penting:** `initSwiper()` dipanggil SETELAH semua slide ditambahkan ke DOM

---

<a name="langkah-5"></a>
## 🎨 Langkah 5: Konfigurasi Swiper Options

### Tambahkan options di fungsi `initSwiper()`:

```javascript
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    // 🆕 Jumlah slide yang terlihat
    slidesPerView: 1,
    
    // 🆕 Jarak antar slide (pixels)
    spaceBetween: 30,
    
    // 🆕 Free mode - bisa drag manual
    freeMode: true,
    
    // 🆕 Loop infinite
    loop: true,
    
    // 🆕 Autoplay configuration
    autoplay: {
      delay: 4000,                    // ⏱️ 4 detik per slide
      disableOnInteraction: false,    // ✋ Tetap autoplay meski di-hover
    },
  });
}
```

**Penjelasan Options:**

| Option | Value | Fungsi |
|--------|-------|--------|
| `slidesPerView` | `1` | Tampilkan 1 slide (mobile) |
| `spaceBetween` | `30` | Jarak 30px antar slide |
| `freeMode` | `true` | Bisa di-drag bebas |
| `loop` | `true` | Loop tak terbatas |
| `autoplay.delay` | `4000` | Auto-slide tiap 4 detik |
| `autoplay.disableOnInteraction` | `false` | Tetap autoplay saat interaksi |

---

<a name="langkah-6"></a>
## 📱 Langkah 6: Tambahkan Breakpoints (Responsive)

### Update fungsi `initSwiper()` dengan breakpoints:

```javascript
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    slidesPerView: 1,
    spaceBetween: 30,
    freeMode: true,
    loop: true,
    autoplay: {
      delay: 4000,
      disableOnInteraction: false,
    },
    // 🆕 Breakpoints untuk responsive
    breakpoints: {
      500: {
        slidesPerView: 2,    // 📱 2 slide di 500px+
      },
      700: {
        slidesPerView: 3,    // 💻 3 slide di 700px+
      },
      1200: {
        slidesPerView: 4,    // 🖥️ 4 slide di 1200px+
      },
    },
  });
}
```

**Penjelasan Breakpoints:**

```
Mobile (< 500px)    ➜  1 slide
Tablet (500px+)     ➜  2 slides
Laptop (700px+)     ➜  3 slides
Desktop (1200px+)   ➜  4 slides
```

> 💡 **Tip:** Breakpoints bisa disesuaikan dengan kebutuhan desain Anda

---

<a name="langkah-7"></a>
## 🚀 Langkah 7: Jalankan di Homepage

### Update fungsi `init()` untuk memanggil `displaySlider()`:

Cari fungsi `init()` yang sudah ada, lalu update bagian homepage:

```javascript
// Init App
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displaySlider();        // 🆕 TAMBAHKAN INI
      displayPopularMovies();
      break;
    case '/shows.html':
      displayPopularShows();
      break;
    case '/movie-details.html':
      displayMovieDetails();
      break;
    case '/tv-details.html':
      displayShowDetails();
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}
```

**Penjelasan:**
- ✅ `displaySlider()` dipanggil HANYA di homepage
- ✅ Dipanggil SEBELUM `displayPopularMovies()`

---

<a name="hasil-akhir"></a>
## 🎉 Hasil Akhir

### Kode Lengkap `script.js`

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    document.querySelector('.swiper-wrapper').appendChild(div);

    initSwiper();
  });
}

function initSwiper() {
  const swiper = new Swiper('.swiper', {
    slidesPerView: 1,
    spaceBetween: 30,
    freeMode: true,
    loop: true,
    autoplay: {
      delay: 4000,
      disableOnInteraction: false,
    },
    breakpoints: {
      500: {
        slidesPerView: 2,
      },
      700: {
        slidesPerView: 3,
      },
      1200: {
        slidesPerView: 4,
      },
    },
  });
}
```

### Update di `init()`:

```javascript
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displaySlider();        // ✅ Tambahkan ini
      displayPopularMovies();
      break;
    // ... kode lainnya
  }

  highlightActiveLink();
}
```

---

<a name="hasil-akhir"></a>
## 🎉 Hasil Akhir

### ✅ Fitur yang Berhasil Diimplementasikan:

1. ✨ **Slider otomatis** dengan 20 film "Now Playing"
2. 🔄 **Autoplay** setiap 4 detik
3. 🎯 **Free mode** - bisa drag manual
4. 🔁 **Loop** tanpa henti
5. 📱 **Responsive**:
   - Mobile: 1 slide
   - Tablet: 2-3 slide
   - Desktop: 4 slide
6. ⭐ **Rating** di setiap slide
7. 🔗 **Link** ke halaman detail film

### 🖱️ Cara Menggunakan:

- **Hover** di atas slider untuk pause autoplay (jika `disableOnInteraction: true`)
- **Drag** slider untuk navigasi manual (free mode)
- **Klik** pada poster untuk masuk ke halaman detail film
- **Resize** browser untuk melihat perubahan responsif

---

<a name="kode-lengkap"></a>
## 📄 Kode Lengkap

### 🔵 script.js (Perubahan dan Penambahan)

```javascript
const global = {
  currentPage: window.location.pathname,
};

// Display 20 most popular movies
async function displayPopularMovies() {
  const { results } = await fetchAPIData('movie/popular');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('card');
    div.innerHTML = `
          <a href="movie-details.html?id=${movie.id}">
            ${
              movie.poster_path
                ? `<img
              src="https://image.tmdb.org/t/p/w500${movie.poster_path}"
              class="card-img-top"
              alt="${movie.title}"
            />`
                : `<img
            src="../images/no-image.jpg"
            class="card-img-top"
            alt="${movie.title}"
          />`
            }
          </a>
          <div class="card-body">
            <h5 class="card-title">${movie.title}</h5>
            <p class="card-text">
              <small class="text-muted">Release: ${movie.release_date}</small>
            </p>
          </div>
        `;

    document.querySelector('#popular-movies').appendChild(div);
  });
}

// Display 20 most popular tv shows
async function displayPopularShows() {
  const { results } = await fetchAPIData('tv/popular');

  results.forEach((show) => {
    const div = document.createElement('div');
    div.classList.add('card');
    div.innerHTML = `
          <a href="tv-details.html?id=${show.id}">
            ${
              show.poster_path
                ? `<img
              src="https://image.tmdb.org/t/p/w500${show.poster_path}"
              class="card-img-top"
              alt="${show.name}"
            />`
                : `<img
            src="../images/no-image.jpg"
            class="card-img-top"
            alt="${show.name}"
          />`
            }
          </a>
          <div class="card-body">
            <h5 class="card-title">${show.name}</h5>
            <p class="card-text">
              <small class="text-muted">Air Date: ${show.first_air_date}</small>
            </p>
          </div>
        `;

    document.querySelector('#popular-shows').appendChild(div);
  });
}

// Display Movie Details
async function displayMovieDetails() {
  const movieId = window.location.search.split('=')[1];

  const movie = await fetchAPIData(`movie/${movieId}`);

  // Overlay for background image
  displayBackgroundImage('movie', movie.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    movie.poster_path
      ? `<img
    src="https://image.tmdb.org/t/p/w500${movie.poster_path}"
    class="card-img-top"
    alt="${movie.title}"
  />`
      : `<img
  src="../images/no-image.jpg"
  class="card-img-top"
  alt="${movie.title}"
/>`
  }
  </div>
  <div>
    <h2>${movie.title}</h2>
    <p>
      <i class="fas fa-star text-primary"></i>
      ${movie.vote_average.toFixed(1)} / 10
    </p>
    <p class="text-muted">Release Date: ${movie.release_date}</p>
    <p>
      ${movie.overview}
    </p>
    <h5>Genres</h5>
    <ul class="list-group">
      ${movie.genres.map((genre) => `<li>${genre.name}</li>`).join('')}
    </ul>
    <a href="${
      movie.homepage
    }" target="_blank" class="btn">Visit Movie Homepage</a>
  </div>
</div>
<div class="details-bottom">
  <h2>Movie Info</h2>
  <ul>
    <li><span class="text-secondary">Budget:</span> $${addCommasToNumber(
      movie.budget
    )}</li>
    <li><span class="text-secondary">Revenue:</span> $${addCommasToNumber(
      movie.revenue
    )}</li>
    <li><span class="text-secondary">Runtime:</span> ${
      movie.runtime
    } minutes</li>
    <li><span class="text-secondary">Status:</span> ${movie.status}</li>
  </ul>
  <h4>Production Companies</h4>
  <div class="list-group">
    ${movie.production_companies
      .map((company) => `<span>${company.name}</span>`)
      .join(', ')}
  </div>
</div>
  `;

  document.querySelector('#movie-details').appendChild(div);
}

// Display Show Details
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];

  const show = await fetchAPIData(`tv/${showId}`);

  // Overlay for background image
  displayBackgroundImage('tv', show.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    show.poster_path
      ? `<img
    src="https://image.tmdb.org/t/p/w500${show.poster_path}"
    class="card-img-top"
    alt="${show.name}"
  />`
      : `<img
  src="../images/no-image.jpg"
  class="card-img-top"
  alt="${show.name}"
/>`
  }
  </div>
  <div>
    <h2>${show.name}</h2>
    <p>
      <i class="fas fa-star text-primary"></i>
      ${show.vote_average.toFixed(1)} / 10
    </p>
    <p class="text-muted">Last Air Date: ${show.last_air_date}</p>
    <p>
      ${show.overview}
    </p>
    <h5>Genres</h5>
    <ul class="list-group">
      ${show.genres.map((genre) => `<li>${genre.name}</li>`).join('')}
    </ul>
    <a href="${
      show.homepage
    }" target="_blank" class="btn">Visit show Homepage</a>
  </div>
</div>
<div class="details-bottom">
  <h2>Show Info</h2>
  <ul>
    <li><span class="text-secondary">Number of Episodes:</span> ${
      show.number_of_episodes
    }</li>
    <li><span class="text-secondary">Last Episode To Air:</span> ${
      show.last_episode_to_air.name
    }</li>
    <li><span class="text-secondary">Status:</span> ${show.status}</li>
  </ul>
  <h4>Production Companies</h4>
  <div class="list-group">
    ${show.production_companies
      .map((company) => `<span>${company.name}</span>`)
      .join(', ')}
  </div>
</div>
  `;

  document.querySelector('#show-details').appendChild(div);
}

// Display Backdrop On Details Pages
function displayBackgroundImage(type, backgroundPath) {
  const overlayDiv = document.createElement('div');
  overlayDiv.style.backgroundImage = `url(https://image.tmdb.org/t/p/original/${backgroundPath})`;
  overlayDiv.style.backgroundSize = 'cover';
  overlayDiv.style.backgroundPosition = 'center';
  overlayDiv.style.backgroundRepeat = 'no-repeat';
  overlayDiv.style.height = '100vh';
  overlayDiv.style.width = '100vw';
  overlayDiv.style.position = 'absolute';
  overlayDiv.style.top = '0';
  overlayDiv.style.left = '0';
  overlayDiv.style.zIndex = '-1';
  overlayDiv.style.opacity = '0.1';

  if (type === 'movie') {
    document.querySelector('#movie-details').appendChild(overlayDiv);
  } else {
    document.querySelector('#show-details').appendChild(overlayDiv);
  }
}

// ✨ ============================================
// ✨ FUNGSI BARU: Display Slider Movies
// ✨ ============================================
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    document.querySelector('.swiper-wrapper').appendChild(div);
  });
  
  initSwiper();
}

// ✨ ============================================
// ✨ FUNGSI BARU: Initialize Swiper
// ✨ ============================================
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    slidesPerView: 1,
    spaceBetween: 30,
    freeMode: true,
    loop: true,
    autoplay: {
      delay: 4000,
      disableOnInteraction: false,
    },
    breakpoints: {
      500: {
        slidesPerView: 2,
      },
      700: {
        slidesPerView: 3,
      },
      1200: {
        slidesPerView: 4,
      },
    },
  });
}

// Fetch data from TMDB API
async function fetchAPIData(endpoint) {
  // Register your key at https://www.themoviedb.org/settings/api and enter here
  // Only use this for development or very small projects. You should store your key and make requests from a server
  const API_KEY = '';
  const API_URL = 'https://api.themoviedb.org/3/';

  showSpinner();

  const response = await fetch(
    `${API_URL}${endpoint}?api_key=${API_KEY}&language=en-US`
  );

  const data = await response.json();

  hideSpinner();

  return data;
}

function showSpinner() {
  document.querySelector('.spinner').classList.add('show');
}

function hideSpinner() {
  document.querySelector('.spinner').classList.remove('show');
}

// Highlight active link
function highlightActiveLink() {
  const links = document.querySelectorAll('.nav-link');
  links.forEach((link) => {
    if (link.getAttribute('href') === global.currentPage) {
      link.classList.add('active');
    }
  });
}

function addCommasToNumber(number) {
  return number.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ',');
}

// Init App
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displaySlider();          // ✨ TAMBAHKAN BARIS INI
      displayPopularMovies();
      break;
    case '/shows.html':
      displayPopularShows();
      break;
    case '/movie-details.html':
      displayMovieDetails();
      break;
    case '/tv-details.html':
      displayShowDetails();
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}

document.addEventListener('DOMContentLoaded', init);
```

---

## 📌 Ringkasan Perubahan

### ✅ Yang Ditambahkan:

1. ✨ **Fungsi `displaySlider()`**
   - Fetch data dari `movie/now_playing`
   - Loop dan buat slide untuk setiap film
   - Append slide ke `.swiper-wrapper`

2. ✨ **Fungsi `initSwiper()`**
   - Inisialisasi Swiper dengan konfigurasi lengkap
   - Setup autoplay, loop, free mode
   - Konfigurasi breakpoints untuk responsive

3. ✨ **Update `init()`**
   - Panggil `displaySlider()` di homepage

### 🔧 File yang Dimodifikasi:
- ✅ `script.js` (2 fungsi baru + 1 modifikasi)
- ✅ `index.html` (sudah ada struktur yang benar)

---

<a name="tips-catatan"></a>
## 💡 Tips dan Catatan

### ✅ Checklist Testing

- [ ] Slider muncul di homepage
- [ ] Auto-play berjalan setiap 4 detik
- [ ] Bisa di-drag manual (free mode)
- [ ] Loop berfungsi (tidak berhenti di akhir)
- [ ] Responsive breakpoints bekerja:
  - Mobile: 1 slide
  - 500px+: 2 slides
  - 700px+: 3 slides
  - 1200px+: 4 slides
- [ ] Klik movie menuju detail page

### 🐛 Troubleshooting

**Problem:** Slider tidak muncul
- ✅ Cek apakah Swiper library sudah di-include
- ✅ Cek console untuk error
- ✅ Pastikan API key sudah diisi di `fetchAPIData()`

**Problem:** Slider muncul tapi tidak bisa slide
- ✅ Pastikan `initSwiper()` dipanggil setelah append
- ✅ Cek class `swiper-slide` sudah ditambahkan

**Problem:** Responsive tidak bekerja
- ✅ Cek nilai breakpoints di `initSwiper()`
- ✅ Test dengan resize browser

### 📚 Resources Tambahan

- [Swiper API Documentation](https://swiperjs.com/swiper-api)
- [TMDB API Documentation](https://developers.themoviedb.org/3)

### 🎯 Customization Ideas

Anda bisa modifikasi:
- 🎨 Delay autoplay (ubah nilai `delay`)
- 📏 Spacing antar slide (ubah `spaceBetween`)
- 📱 Breakpoint values
- 🎭 Tambahkan pagination/navigation
- 🌈 Custom styling di CSS

---

## 🎊 Selamat!

Anda telah berhasil membuat slider movie menggunakan Swiper.js! 

**Next Steps:**
- Coba tambahkan slider untuk TV Shows
- Explore options lain dari Swiper
- Customize styling sesuai kreativitas Anda

---

> **📝 Catatan Akhir:** Dokumentasi ini dibuat untuk pembelajaran pribadi. Swiper adalah library yang powerful dengan banyak options. Jangan ragu untuk bereksperimen!

**Happy Coding! 🚀**
