# 📚 Dokumentasi Implementasi Swiper Slider - Movie App

> **Catatan Penting**: Dokumentasi ini dibuat untuk pemula yang ingin belajar mengimplementasikan slider menggunakan library Swiper JS pada aplikasi web movie.

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Swiper?](#apa-itu-swiper)
- [Persiapan Awal](#persiapan-awal)
- [Struktur HTML](#struktur-html)
- [Membuat Fungsi displaySlider()](#membuat-fungsi-displayslider)
- [Menampilkan Data dari API](#menampilkan-data-dari-api)
- [Membuat Elemen Slide](#membuat-elemen-slide)
- [Mengisi Konten Slide](#mengisi-konten-slide)
- [Fungsi initSwiper()](#fungsi-initswiper)
- [Konfigurasi Swiper](#konfigurasi-swiper)
- [Breakpoints (Responsive Design)](#breakpoints-responsive-design)
- [Menjalankan displaySlider()](#menjalankan-displayslider)
- [Hasil Akhir](#hasil-akhir)
- [Kode Lengkap](#kode-lengkap)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan belajar cara membuat **slider interaktif** untuk menampilkan film-film yang sedang tayang (Now Playing) menggunakan library **Swiper JS**. Slider ini akan responsif dan dapat menyesuaikan jumlah tampilan berdasarkan ukuran layar.

---

<a name="apa-itu-swiper"></a>
## 📖 Apa itu Swiper?

**Swiper** adalah library JavaScript modern untuk membuat slider/carousel yang:
- ✨ Mudah digunakan
- 📱 Responsif dan mobile-friendly
- ⚙️ Memiliki banyak opsi konfigurasi
- 🎨 Tampilan yang menarik
- 🔄 Mendukung autoplay, loop, dan banyak fitur lainnya

**Versi yang digunakan**: Swiper 8

**Official Website**: [https://swiperjs.com/](https://swiperjs.com/)

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

### 1️⃣ File yang Diperlukan

Pastikan Anda sudah menyertakan file Swiper di project Anda:

```html
<!-- Di dalam <head> tag -->
<link rel="stylesheet" href="lib/swiper.css" />

<!-- Sebelum penutup </body> tag -->
<script src="lib/swiper.js"></script>
```

> 💡 **Catatan**: File Swiper sudah disertakan di folder `lib/` dalam resource project.

### 2️⃣ Endpoint API yang Digunakan

Kita akan menggunakan endpoint **`movie/now_playing`** dari TMDB API untuk mendapatkan data film yang sedang tayang.

---

<a name="struktur-html"></a>
## 🏗️ Struktur HTML

Swiper memerlukan struktur HTML khusus agar berfungsi dengan baik:

```html
<!-- Now Playing Section -->
<section class="now-playing">
  <h2>Now Playing</h2>
  <div class="swiper">                    👈 Container utama dengan class "swiper"
    <div class="swiper-wrapper">          👈 Wrapper untuk semua slide
      <!-- Slide akan ditambahkan di sini via JavaScript -->
    </div>
  </div>
</section>
```

### 📌 Penjelasan Struktur:

| Element | Class | Fungsi |
|---------|-------|--------|
| `<div>` | `.swiper` | Container utama slider |
| `<div>` | `.swiper-wrapper` | Pembungkus untuk semua slide |
| `<div>` | `.swiper-slide` | Setiap slide individual (akan dibuat via JS) |

---

<a name="membuat-fungsi-displayslider"></a>
## ⚡ Membuat Fungsi displaySlider()

Kita akan membuat fungsi async untuk menampilkan slider. Letakkan fungsi ini di `script.js`:

```javascript
// ✨ TAMBAHKAN KODE INI (Letakkan setelah fungsi displayBackgroundImage)

// Display Slider Movies
async function displaySlider() {
  // Kode akan ditambahkan di sini
}
```

### 📝 Penjelasan:
- **`async`**: Fungsi ini bersifat asynchronous karena akan melakukan fetch data dari API
- Kita akan mengambil data film dan menampilkannya dalam bentuk slider

---

<a name="menampilkan-data-dari-api"></a>
## 🌐 Menampilkan Data dari API

Tambahkan kode untuk mengambil data dari API:

```javascript
// Display Slider Movies
async function displaySlider() {
  // ✨ Ambil data film dari endpoint 'movie/now_playing'
  const { results } = await fetchAPIData('movie/now_playing');
  
  console.log(results); // 👈 Untuk mengecek data yang diterima
}
```

### 📝 Penjelasan:
- **`{ results }`**: Destructuring untuk mengambil array `results` dari response API
- **`await fetchAPIData()`**: Menunggu data dari API selesai di-fetch
- **`'movie/now_playing'`**: Endpoint untuk mendapatkan film yang sedang tayang

### 🔍 Isi Data `results`:
Setiap item dalam array `results` berisi informasi film seperti:
- `id`: ID unik film
- `title`: Judul film
- `poster_path`: Path untuk gambar poster
- `vote_average`: Rating film (0-10)

---

<a name="membuat-elemen-slide"></a>
## 🎨 Membuat Elemen Slide

Sekarang kita akan loop melalui setiap film dan membuat slide untuk masing-masing:

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  // ✨ Loop melalui setiap film
  results.forEach((movie) => {
    // ✨ Buat elemen div untuk slide
    const div = document.createElement('div');
    
    // ✨ Tambahkan class 'swiper-slide' (WAJIB untuk Swiper)
    div.classList.add('swiper-slide');
    
    // Konten slide akan ditambahkan di sini
  });
}
```

### 📝 Penjelasan:
- **`forEach()`**: Melakukan iterasi untuk setiap film dalam array `results`
- **`document.createElement('div')`**: Membuat element `<div>` baru
- **`classList.add('swiper-slide')`**: Menambahkan class yang diperlukan Swiper
- **`movie`**: Parameter yang merepresentasikan setiap film dalam loop

---

<a name="mengisi-konten-slide"></a>
## 📦 Mengisi Konten Slide

Tambahkan HTML ke dalam setiap slide:

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    // ✨ Isi konten slide dengan template string
    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">                           👈 Link ke halaman detail
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}"       👈 Gambar poster
             alt="${movie.title}" />                                          👈 Alt text
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10  👈 Rating film
      </h4>
    `;

    // ✨ Tambahkan slide ke dalam swiper-wrapper
    document.querySelector('.swiper-wrapper').appendChild(div);
  });
}
```

### 📝 Penjelasan Detail:

#### 1. **Link ke Detail Film**
```javascript
<a href="movie-details.html?id=${movie.id}">
```
- Mengarahkan user ke halaman detail dengan ID film

#### 2. **Gambar Poster**
```javascript
<img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
```
- **Base URL**: `https://image.tmdb.org/t/p/`
- **Ukuran**: `w500` (width 500px)
- **Path**: Dari `movie.poster_path`

#### 3. **Rating Film**
```javascript
${movie.vote_average} / 10
```
- Menampilkan rating dari 0-10

#### 4. **Append ke DOM**
```javascript
document.querySelector('.swiper-wrapper').appendChild(div);
```
- Menambahkan slide ke dalam wrapper

---

<a name="fungsi-initswiper"></a>
## 🚀 Fungsi initSwiper()

Setelah semua slide ditambahkan, kita perlu **menginisialisasi Swiper**. Buat fungsi baru:

```javascript
// ✨ TAMBAHKAN FUNGSI BARU INI

function initSwiper() {
  const swiper = new Swiper('.swiper', {
    // Konfigurasi akan ditambahkan di sini
  });
}
```

### 📝 Penjelasan:
- **`new Swiper()`**: Membuat instance Swiper baru
- **`.swiper`**: Selector untuk container Swiper
- Parameter kedua berisi object konfigurasi

---

<a name="konfigurasi-swiper"></a>
## ⚙️ Konfigurasi Swiper

Tambahkan berbagai opsi konfigurasi:

```javascript
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    // ✨ Jumlah slide yang ditampilkan (default untuk mobile)
    slidesPerView: 1,
    
    // ✨ Jarak antar slide (dalam pixel)
    spaceBetween: 30,
    
    // ✨ Mode bebas (bisa drag manual)
    freeMode: true,
    
    // ✨ Loop terus menerus
    loop: true,
    
    // ✨ Autoplay otomatis
    autoplay: {
      delay: 4000,                    // ⏱️ Delay 4 detik (4000ms)
      disableOnInteraction: false,    // 🔄 Tetap autoplay meski di-hover
    },
    
    // Breakpoints akan ditambahkan di sini
  });
}
```

### 📝 Penjelasan Opsi:

| Opsi | Nilai | Penjelasan |
|------|-------|------------|
| `slidesPerView` | `1` | Tampilkan 1 slide di layar kecil |
| `spaceBetween` | `30` | Jarak 30px antar slide |
| `freeMode` | `true` | User bisa drag slider secara bebas |
| `loop` | `true` | Slider berputar tanpa henti |
| `autoplay.delay` | `4000` | Ganti slide otomatis tiap 4 detik |
| `autoplay.disableOnInteraction` | `false` | Autoplay tetap jalan meski di-hover |

---

<a name="breakpoints-responsive-design"></a>
## 📱 Breakpoints (Responsive Design)

Tambahkan breakpoints agar slider responsif:

```javascript
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    slidesPerView: 1,
    spaceBetween: 30,
    freeMode: true,
    loop: true,
    autoplay: {
      delay: 4000,
      disableOnInteraction: false,
    },
    
    // ✨ Breakpoints untuk berbagai ukuran layar
    breakpoints: {
      500: {                    // 📱 Layar ≥ 500px
        slidesPerView: 2,       // Tampilkan 2 slide
      },
      700: {                    // 💻 Layar ≥ 700px
        slidesPerView: 3,       // Tampilkan 3 slide
      },
      1200: {                   // 🖥️ Layar ≥ 1200px
        slidesPerView: 4,       // Tampilkan 4 slide
      },
    },
  });
}
```

### 📝 Penjelasan Breakpoints:

| Ukuran Layar | Jumlah Slide | Device |
|--------------|--------------|--------|
| < 500px | 1 slide | 📱 Mobile portrait |
| 500px - 699px | 2 slide | 📱 Mobile landscape |
| 700px - 1199px | 3 slide | 💻 Tablet |
| ≥ 1200px | 4 slide | 🖥️ Desktop |

---

<a name="menjalankan-displayslider"></a>
## ▶️ Menjalankan displaySlider()

### 1️⃣ Panggil initSwiper() di Akhir Loop

Update fungsi `displaySlider()`:

```javascript
// Display Slider Movies
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    document.querySelector('.swiper-wrapper').appendChild(div);
  });
  
  // ✨ PENTING: Panggil initSwiper() setelah semua slide ditambahkan
  initSwiper();
}
```

> ⚠️ **Catatan Penting**: `initSwiper()` harus dipanggil **SETELAH** semua slide ditambahkan ke DOM, bukan di dalam loop!

### 2️⃣ Panggil displaySlider() di Homepage

Update fungsi `init()`:

```javascript
// Init App
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displaySlider();          // ✨ TAMBAHKAN BARIS INI
      displayPopularMovies();
      break;
    case '/shows.html':
      displayPopularShows();
      break;
    case '/movie-details.html':
      displayMovieDetails();
      break;
    case '/tv-details.html':
      displayShowDetails();
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}
```

### 📝 Penjelasan:
- `displaySlider()` hanya dijalankan di **homepage** (`/` atau `/index.html`)
- Fungsi akan otomatis dipanggil saat halaman dimuat berkat event listener `DOMContentLoaded`

---

<a name="hasil-akhir"></a>
## 🎉 Hasil Akhir

### ✅ Fitur yang Berhasil Diimplementasikan:

1. ✨ **Slider otomatis** dengan 20 film "Now Playing"
2. 🔄 **Autoplay** setiap 4 detik
3. 🎯 **Free mode** - bisa drag manual
4. 🔁 **Loop** tanpa henti
5. 📱 **Responsive**:
   - Mobile: 1 slide
   - Tablet: 2-3 slide
   - Desktop: 4 slide
6. ⭐ **Rating** di setiap slide
7. 🔗 **Link** ke halaman detail film

### 🖱️ Cara Menggunakan:

- **Hover** di atas slider untuk pause autoplay (jika `disableOnInteraction: true`)
- **Drag** slider untuk navigasi manual (free mode)
- **Klik** pada poster untuk masuk ke halaman detail film
- **Resize** browser untuk melihat perubahan responsif

---

<a name="kode-lengkap"></a>
## 📄 Kode Lengkap

### 🔵 script.js (Perubahan dan Penambahan)

```javascript
const global = {
  currentPage: window.location.pathname,
};

// Display 20 most popular movies
async function displayPopularMovies() {
  const { results } = await fetchAPIData('movie/popular');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('card');
    div.innerHTML = `
          <a href="movie-details.html?id=${movie.id}">
            ${
              movie.poster_path
                ? `<img
              src="https://image.tmdb.org/t/p/w500${movie.poster_path}"
              class="card-img-top"
              alt="${movie.title}"
            />`
                : `<img
            src="../images/no-image.jpg"
            class="card-img-top"
            alt="${movie.title}"
          />`
            }
          </a>
          <div class="card-body">
            <h5 class="card-title">${movie.title}</h5>
            <p class="card-text">
              <small class="text-muted">Release: ${movie.release_date}</small>
            </p>
          </div>
        `;

    document.querySelector('#popular-movies').appendChild(div);
  });
}

// Display 20 most popular tv shows
async function displayPopularShows() {
  const { results } = await fetchAPIData('tv/popular');

  results.forEach((show) => {
    const div = document.createElement('div');
    div.classList.add('card');
    div.innerHTML = `
          <a href="tv-details.html?id=${show.id}">
            ${
              show.poster_path
                ? `<img
              src="https://image.tmdb.org/t/p/w500${show.poster_path}"
              class="card-img-top"
              alt="${show.name}"
            />`
                : `<img
            src="../images/no-image.jpg"
            class="card-img-top"
            alt="${show.name}"
          />`
            }
          </a>
          <div class="card-body">
            <h5 class="card-title">${show.name}</h5>
            <p class="card-text">
              <small class="text-muted">Air Date: ${show.first_air_date}</small>
            </p>
          </div>
        `;

    document.querySelector('#popular-shows').appendChild(div);
  });
}

// Display Movie Details
async function displayMovieDetails() {
  const movieId = window.location.search.split('=')[1];

  const movie = await fetchAPIData(`movie/${movieId}`);

  // Overlay for background image
  displayBackgroundImage('movie', movie.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    movie.poster_path
      ? `<img
    src="https://image.tmdb.org/t/p/w500${movie.poster_path}"
    class="card-img-top"
    alt="${movie.title}"
  />`
      : `<img
  src="../images/no-image.jpg"
  class="card-img-top"
  alt="${movie.title}"
/>`
  }
  </div>
  <div>
    <h2>${movie.title}</h2>
    <p>
      <i class="fas fa-star text-primary"></i>
      ${movie.vote_average.toFixed(1)} / 10
    </p>
    <p class="text-muted">Release Date: ${movie.release_date}</p>
    <p>
      ${movie.overview}
    </p>
    <h5>Genres</h5>
    <ul class="list-group">
      ${movie.genres.map((genre) => `<li>${genre.name}</li>`).join('')}
    </ul>
    <a href="${
      movie.homepage
    }" target="_blank" class="btn">Visit Movie Homepage</a>
  </div>
</div>
<div class="details-bottom">
  <h2>Movie Info</h2>
  <ul>
    <li><span class="text-secondary">Budget:</span> $${addCommasToNumber(
      movie.budget
    )}</li>
    <li><span class="text-secondary">Revenue:</span> $${addCommasToNumber(
      movie.revenue
    )}</li>
    <li><span class="text-secondary">Runtime:</span> ${
      movie.runtime
    } minutes</li>
    <li><span class="text-secondary">Status:</span> ${movie.status}</li>
  </ul>
  <h4>Production Companies</h4>
  <div class="list-group">
    ${movie.production_companies
      .map((company) => `<span>${company.name}</span>`)
      .join(', ')}
  </div>
</div>
  `;

  document.querySelector('#movie-details').appendChild(div);
}

// Display Show Details
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];

  const show = await fetchAPIData(`tv/${showId}`);

  // Overlay for background image
  displayBackgroundImage('tv', show.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    show.poster_path
      ? `<img
    src="https://image.tmdb.org/t/p/w500${show.poster_path}"
    class="card-img-top"
    alt="${show.name}"
  />`
      : `<img
  src="../images/no-image.jpg"
  class="card-img-top"
  alt="${show.name}"
/>`
  }
  </div>
  <div>
    <h2>${show.name}</h2>
    <p>
      <i class="fas fa-star text-primary"></i>
      ${show.vote_average.toFixed(1)} / 10
    </p>
    <p class="text-muted">Last Air Date: ${show.last_air_date}</p>
    <p>
      ${show.overview}
    </p>
    <h5>Genres</h5>
    <ul class="list-group">
      ${show.genres.map((genre) => `<li>${genre.name}</li>`).join('')}
    </ul>
    <a href="${
      show.homepage
    }" target="_blank" class="btn">Visit show Homepage</a>
  </div>
</div>
<div class="details-bottom">
  <h2>Show Info</h2>
  <ul>
    <li><span class="text-secondary">Number of Episodes:</span> ${
      show.number_of_episodes
    }</li>
    <li><span class="text-secondary">Last Episode To Air:</span> ${
      show.last_episode_to_air.name
    }</li>
    <li><span class="text-secondary">Status:</span> ${show.status}</li>
  </ul>
  <h4>Production Companies</h4>
  <div class="list-group">
    ${show.production_companies
      .map((company) => `<span>${company.name}</span>`)
      .join(', ')}
  </div>
</div>
  `;

  document.querySelector('#show-details').appendChild(div);
}

// Display Backdrop On Details Pages
function displayBackgroundImage(type, backgroundPath) {
  const overlayDiv = document.createElement('div');
  overlayDiv.style.backgroundImage = `url(https://image.tmdb.org/t/p/original/${backgroundPath})`;
  overlayDiv.style.backgroundSize = 'cover';
  overlayDiv.style.backgroundPosition = 'center';
  overlayDiv.style.backgroundRepeat = 'no-repeat';
  overlayDiv.style.height = '100vh';
  overlayDiv.style.width = '100vw';
  overlayDiv.style.position = 'absolute';
  overlayDiv.style.top = '0';
  overlayDiv.style.left = '0';
  overlayDiv.style.zIndex = '-1';
  overlayDiv.style.opacity = '0.1';

  if (type === 'movie') {
    document.querySelector('#movie-details').appendChild(overlayDiv);
  } else {
    document.querySelector('#show-details').appendChild(overlayDiv);
  }
}

// ✨ ============================================
// ✨ FUNGSI BARU: Display Slider Movies
// ✨ ============================================
async function displaySlider() {
  const { results } = await fetchAPIData('movie/now_playing');

  results.forEach((movie) => {
    const div = document.createElement('div');
    div.classList.add('swiper-slide');

    div.innerHTML = `
      <a href="movie-details.html?id=${movie.id}">
        <img src="https://image.tmdb.org/t/p/w500${movie.poster_path}" alt="${movie.title}" />
      </a>
      <h4 class="swiper-rating">
        <i class="fas fa-star text-secondary"></i> ${movie.vote_average} / 10
      </h4>
    `;

    document.querySelector('.swiper-wrapper').appendChild(div);
  });
  
  initSwiper();
}

// ✨ ============================================
// ✨ FUNGSI BARU: Initialize Swiper
// ✨ ============================================
function initSwiper() {
  const swiper = new Swiper('.swiper', {
    slidesPerView: 1,
    spaceBetween: 30,
    freeMode: true,
    loop: true,
    autoplay: {
      delay: 4000,
      disableOnInteraction: false,
    },
    breakpoints: {
      500: {
        slidesPerView: 2,
      },
      700: {
        slidesPerView: 3,
      },
      1200: {
        slidesPerView: 4,
      },
    },
  });
}

// Fetch data from TMDB API
async function fetchAPIData(endpoint) {
  // Register your key at https://www.themoviedb.org/settings/api and enter here
  // Only use this for development or very small projects. You should store your key and make requests from a server
  const API_KEY = '';
  const API_URL = 'https://api.themoviedb.org/3/';

  showSpinner();

  const response = await fetch(
    `${API_URL}${endpoint}?api_key=${API_KEY}&language=en-US`
  );

  const data = await response.json();

  hideSpinner();

  return data;
}

function showSpinner() {
  document.querySelector('.spinner').classList.add('show');
}

function hideSpinner() {
  document.querySelector('.spinner').classList.remove('show');
}

// Highlight active link
function highlightActiveLink() {
  const links = document.querySelectorAll('.nav-link');
  links.forEach((link) => {
    if (link.getAttribute('href') === global.currentPage) {
      link.classList.add('active');
    }
  });
}

function addCommasToNumber(number) {
  return number.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ',');
}

// Init App
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displaySlider();          // ✨ TAMBAHKAN BARIS INI
      displayPopularMovies();
      break;
    case '/shows.html':
      displayPopularShows();
      break;
    case '/movie-details.html':
      displayMovieDetails();
      break;
    case '/tv-details.html':
      displayShowDetails();
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}

document.addEventListener('DOMContentLoaded', init);
```

---

## 📌 Ringkasan Perubahan

### ✅ Yang Ditambahkan:

1. ✨ **Fungsi `displaySlider()`**
   - Fetch data dari `movie/now_playing`
   - Loop dan buat slide untuk setiap film
   - Append slide ke `.swiper-wrapper`

2. ✨ **Fungsi `initSwiper()`**
   - Inisialisasi Swiper dengan konfigurasi lengkap
   - Setup autoplay, loop, free mode
   - Konfigurasi breakpoints untuk responsive

3. ✨ **Update `init()`**
   - Panggil `displaySlider()` di homepage

### 🔧 File yang Dimodifikasi:
- ✅ `script.js` (2 fungsi baru + 1 modifikasi)
- ✅ `index.html` (sudah ada struktur yang benar)

---

## 💡 Tips dan Catatan

### ⚠️ Hal Penting yang Perlu Diingat:

1. **Urutan Pemanggilan**
   - ❌ SALAH: Panggil `initSwiper()` sebelum slide ditambahkan
   - ✅ BENAR: Panggil `initSwiper()` SETELAH semua slide ada di DOM

2. **Class CSS yang Wajib**
   - `.swiper` - Container utama
   - `.swiper-wrapper` - Pembungkus slide
   - `.swiper-slide` - Setiap slide individual

3. **API Key**
   - Jangan lupa isi `API_KEY` di fungsi `fetchAPIData()`
   - Daftar di: https://www.themoviedb.org/settings/api

### 🎯 Pengembangan Lebih Lanjut:

Anda bisa mengembangkan slider ini dengan:
- 📺 Membuat slider untuk TV Shows
- 🎨 Menambahkan navigasi button (prev/next)
- 📍 Menambahkan pagination dots
- 🖼️ Mengubah ukuran gambar sesuai kebutuhan
- ⚡ Menyesuaikan kecepatan autoplay

---

## 🎓 Kesimpulan

Selamat! 🎉 Anda telah berhasil mengimplementasikan Swiper Slider untuk menampilkan film "Now Playing" dengan fitur:

✅ Autoplay otomatis  
✅ Loop tanpa henti    
✅ Responsive di semua device  
✅ Free mode untuk navigasi manual  
✅ Rating di setiap slide  
✅ Link ke halaman detail

**Swiper** adalah library yang sangat powerful dan mudah digunakan. Dokumentasi lengkap bisa Anda lihat di [https://swiperjs.com/](https://swiperjs.com/)

---

## 📚 Referensi

- **Swiper Official Docs**: https://swiperjs.com/
- **TMDB API Docs**: https://developers.themoviedb.org/3
- **Swiper Version**: 8

---

## 🙋 FAQ

**Q: Kenapa slider tidak muncul?**  
A: Pastikan Anda sudah:
- Include file swiper.css dan swiper.js
- Struktur HTML sudah benar (swiper > swiper-wrapper > swiper-slide)
- Memanggil `initSwiper()` setelah slide ditambahkan

**Q: Kenapa gambar tidak muncul?**  
A: Periksa API_KEY sudah diisi dengan benar di fungsi `fetchAPIData()`

**Q: Bagaimana cara mengubah jumlah slide yang ditampilkan?**  
A: Ubah nilai `slidesPerView` di konfigurasi Swiper atau di breakpoints

**Q: Bagaimana cara mematikan autoplay?**  
A: Hapus atau comment bagian `autoplay` di konfigurasi Swiper

---

**Happy Coding! 🚀**

> Dibuat dengan ❤️ untuk pembelajaran pribadi
