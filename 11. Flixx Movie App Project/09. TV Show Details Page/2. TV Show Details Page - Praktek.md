# 📺 Tutorial: Membuat Halaman Detail TV Show

> **Dokumentasi Pribadi - Belajar JavaScript untuk Pemula**  
> Tutorial ini akan memandu Anda membuat halaman detail TV show langkah demi langkah

---

## 📑 Daftar Isi

- [Pendahuluan](#pendahuluan)
- [Persiapan Awal](#persiapan-awal)
- [Langkah 1: Copy Fungsi displayMovieDetails](#langkah-1-copy-fungsi-displaymoviedetails)
- [Langkah 2: Rename Fungsi dan Variabel](#langkah-2-rename-fungsi-dan-variabel)
- [Langkah 3: Ubah Endpoint API](#langkah-3-ubah-endpoint-api)
- [Langkah 4: Update displayBackgroundImage](#langkah-4-update-displaybackgroundimage)
- [Langkah 5: Ubah Target DOM di Bagian Bawah](#langkah-5-ubah-target-dom-di-bagian-bawah)
- [Langkah 6: Panggil Fungsi di Init](#langkah-6-panggil-fungsi-di-init)
- [Langkah 7: Hapus HTML Statis di tv-details.html](#langkah-7-hapus-html-statis-di-tv-detailshtml)
- [Langkah 8: Ganti Semua 'movie' ke 'show'](#langkah-8-ganti-semua-movie-ke-show)
- [Langkah 9: Ubah title Menjadi name](#langkah-9-ubah-title-menjadi-name)
- [Langkah 10: Debug dengan Console Log](#langkah-10-debug-dengan-console-log)
- [Langkah 11: Ubah release_date ke last_air_date](#langkah-11-ubah-release_date-ke-last_air_date)
- [Langkah 12: Ubah Movie Info ke Show Info](#langkah-12-ubah-movie-info-ke-show-info)
- [Langkah 13: Ubah Budget ke Number of Episodes](#langkah-13-ubah-budget-ke-number-of-episodes)
- [Langkah 14: Ubah Revenue ke Last Episode To Air](#langkah-14-ubah-revenue-ke-last-episode-to-air)
- [Langkah 15: Hapus Runtime](#langkah-15-hapus-runtime)
- [Langkah 16: Fix Last Episode To Air - Tambah .name](#langkah-16-fix-last-episode-to-air---tambah-name)
- [Langkah 17: Hapus Console Log](#langkah-17-hapus-console-log)
- [Testing dan Hasil Akhir](#testing-dan-hasil-akhir)
- [Ringkasan](#ringkasan)

---

<a name="pendahuluan"></a>
## 🎯 Pendahuluan

Dalam tutorial ini, kita akan membuat halaman detail untuk TV Show dengan cara **menyalin dan memodifikasi** fungsi yang sudah ada (`displayMovieDetails`). 

**Apa yang akan kita pelajari:**
- 📚 Cara menggunakan fungsi yang sudah ada sebagai template
- 📚 Perbedaan data struktur Movie vs TV Show di API
- 📚 Debugging menggunakan console.log
- 📚 Manipulasi DOM dinamis dengan JavaScript

**Data yang akan ditampilkan:**
- ✅ Nama show
- ✅ Rating
- ✅ Last air date (tanggal tayang terakhir)
- ✅ Overview (deskripsi)
- ✅ Genres
- ✅ Number of episodes
- ✅ Last episode to air
- ✅ Status
- ✅ Production companies

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

**File yang akan kita edit:**
1. ✅ `script.js` - Menambahkan fungsi baru
2. ✅ `tv-details.html` - Menghapus HTML statis

**Pastikan sudah ada:**
- Fungsi `displayMovieDetails()` yang sudah berfungsi
- Fungsi `fetchAPIData()` yang bisa mengambil data dari TMDB API
- Fungsi `displayBackgroundImage()` yang bisa menampilkan backdrop
- File `tv-details.html` dengan struktur HTML dasar

**Kondisi awal di `script.js`:**
```javascript
case '/tv-details.html':
  console.log('TV Details');  // ❌ Hanya console log
  break;
```

---

<a name="langkah-1-copy-fungsi-displaymoviedetails"></a>
## 📋 Langkah 1: Copy Fungsi displayMovieDetails

### 🎬 Apa yang kita lakukan?
Kita akan menyalin **seluruh fungsi** `displayMovieDetails()` sebagai dasar untuk fungsi baru kita.

### 💻 Langkah-langkahnya:

**1. Cari fungsi `displayMovieDetails` di script.js:**

```javascript
// Display Movie Details
async function displayMovieDetails() {
  const movieId = window.location.search.split('=')[1];

  const movie = await fetchAPIData(`movie/${movieId}`);

  // Overlay for background image
  displayBackgroundImage('movie', movie.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
    // ... banyak kode HTML di sini
  </div>
  <div class="details-bottom">
    // ... banyak kode HTML di sini
  </div>
  `;

  document.querySelector('#movie-details').appendChild(div);
}
```

**2. Copy seluruh fungsi tersebut**

**3. Paste di bawah fungsi `displayMovieDetails()` di script.js**

Sekarang Anda punya fungsi yang sama sebanyak 2 kali!

---

<a name="langkah-2-rename-fungsi-dan-variabel"></a>
## 🏷️ Langkah 2: Rename Fungsi dan Variabel

### 🎬 Apa yang kita lakukan?
Mengubah nama fungsi dan variabel agar sesuai dengan TV Show

### 💻 Perubahan:

**SEBELUM:**
```javascript
// Display Movie Details
async function displayMovieDetails() {
  const movieId = window.location.search.split('=')[1];
  const movie = await fetchAPIData(`movie/${movieId}`);
```

**SESUDAH:**
```javascript
// ✨ Display Show Details
async function displayShowDetails() {  // 🔄 Ubah nama fungsi
  const showId = window.location.search.split('=')[1];  // 🔄 movieId → showId
  const show = await fetchAPIData(`movie/${showId}`);  // 🔄 movie → show
```

**📌 Yang berubah:**
- `displayMovieDetails` → `displayShowDetails`
- `movieId` → `showId`
- `movie` → `show`

---

<a name="langkah-3-ubah-endpoint-api"></a>
## 🔌 Langkah 3: Ubah Endpoint API

### 🎬 Apa yang kita lakukan?
Mengubah endpoint dari `movie/` menjadi `tv/` karena kita mengambil data TV show

### 💻 Perubahan:

**SEBELUM:**
```javascript
const show = await fetchAPIData(`movie/${showId}`);  // ❌ Masih 'movie'
```

**SESUDAH:**
```javascript
const show = await fetchAPIData(`tv/${showId}`);  // ✅ Ubah ke 'tv'
```

**📌 Penjelasan:**
- TMDB API memiliki endpoint berbeda untuk movie dan TV show
- Movie: `movie/{id}`
- TV Show: `tv/{id}`

---

<a name="langkah-4-update-displaybackgroundimage"></a>
## 🖼️ Langkah 4: Update displayBackgroundImage

### 🎬 Apa yang kita lakukan?
Mengubah parameter pada fungsi `displayBackgroundImage()` agar sesuai dengan TV show

### 💻 Perubahan:

**SEBELUM:**
```javascript
displayBackgroundImage('movie', movie.backdrop_path);  // ❌ Masih 'movie'
```

**SESUDAH:**
```javascript
displayBackgroundImage('tv', show.backdrop_path);  // ✅ Ubah ke 'tv' dan 'show'
```

**📌 Penjelasan:**
- Parameter pertama: `'movie'` → `'tv'` (tipe konten)
- Parameter kedua: `movie.backdrop_path` → `show.backdrop_path` (variabel show)

---

<a name="langkah-5-ubah-target-dom-di-bagian-bawah"></a>
## 🎯 Langkah 5: Ubah Target DOM di Bagian Bawah

### 🎬 Apa yang kita lakukan?
Mengubah target DOM dari `#movie-details` menjadi `#show-details`

### 💻 Perubahan:

Di **bagian paling bawah** fungsi `displayShowDetails()`:

**SEBELUM:**
```javascript
document.querySelector('#movie-details').appendChild(div);  // ❌ Salah target
```

**SESUDAH:**
```javascript
document.querySelector('#show-details').appendChild(div);  // ✅ Target yang benar
```

**📌 Penjelasan:**
File `tv-details.html` memiliki element dengan id `show-details`, bukan `movie-details`

---

<a name="langkah-6-panggil-fungsi-di-init"></a>
## 🚀 Langkah 6: Panggil Fungsi di Init

### 🎬 Apa yang kita lakukan?
Memanggil fungsi `displayShowDetails()` ketika halaman tv-details.html dibuka

### 💻 Perubahan di fungsi `init()`:

**SEBELUM:**
```javascript
function init() {
  switch (global.currentPage) {
    // ... kode lainnya
    case '/tv-details.html':
      console.log('TV Details');  // ❌ Hanya console log
      break;
  }
}
```

**SESUDAH:**
```javascript
function init() {
  switch (global.currentPage) {
    // ... kode lainnya
    case '/tv-details.html':
      displayShowDetails();  // ✅ Panggil fungsi baru
      break;
  }
}
```

**📌 Coba save dan test di browser!**
Anda akan melihat backdrop muncul tapi data masih error karena belum diubah semua.

---

<a name="langkah-7-hapus-html-statis-di-tv-detailshtml"></a>
## 🗑️ Langkah 7: Hapus HTML Statis di tv-details.html

### 🎬 Apa yang kita lakukan?
Menghapus HTML yang hard-coded di file `tv-details.html` agar JavaScript bisa mengisi data dinamis

### 💻 Edit file `tv-details.html`:

**SEBELUM:**
```html
<div id="show-details">
  <div class="details-top">
    <!-- Ada banyak HTML statis di sini -->
    <div>
      <img src="...">
    </div>
    <div>
      <h2>Show Title</h2>
      <!-- ... dll -->
    </div>
  </div>
  <div class="details-bottom">
    <!-- Ada banyak HTML statis di sini -->
  </div>
</div>
```

**SESUDAH:**
```html
<div id="show-details">
  <!-- ✨ Kosongkan semuanya - JavaScript akan mengisi -->
</div>
```

**📌 Kenapa dihapus?**
JavaScript kita akan membuat semua HTML secara dinamis dengan data dari API!

---

<a name="langkah-8-ganti-semua-movie-ke-show"></a>
## 🔄 Langkah 8: Ganti Semua 'movie' ke 'show'

### 🎬 Apa yang kita lakukan?
Mengganti semua variabel `movie` menjadi `show` di dalam template HTML

### 💻 Cara cepat (di text editor):

**Gunakan Find & Replace (Ctrl/Cmd + D atau Find):**
- Find: `movie`
- Replace: `show`
- Tapi hanya di dalam fungsi `displayShowDetails()` saja!

**Contoh perubahan:**

**SEBELUM:**
```javascript
div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    movie.poster_path  // ❌ movie
      ? `<img
    src="https://image.tmdb.org/t/p/w500${movie.poster_path}"  // ❌ movie
```

**SESUDAH:**
```javascript
div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    show.poster_path  // ✅ show
      ? `<img
    src="https://image.tmdb.org/t/p/w500${show.poster_path}"  // ✅ show
```

**⚠️ Perhatian:**
Jangan ganti:
- Nama fungsi lain seperti `displayBackgroundImage`
- String literal seperti `'movie-details'` yang sudah kita ubah ke `'show-details'`

---

<a name="langkah-9-ubah-title-menjadi-name"></a>
## 🏷️ Langkah 9: Ubah title Menjadi name

### 🎬 Apa yang kita lakukan?
Movie menggunakan `title`, tapi TV Show menggunakan `name`

### 💻 Perubahan (ada di 3 tempat):

**SEBELUM:**
```javascript
${
  show.poster_path
    ? `<img
  src="https://image.tmdb.org/t/p/w500${show.poster_path}"
  class="card-img-top"
  alt="${show.title}"  // ❌ 1. Masih .title
/>`
    : `<img
src="../images/no-image.jpg"
class="card-img-top"
alt="${show.title}"  // ❌ 2. Masih .title
/>`
}
</div>
<div>
  <h2>${show.title}</h2>  // ❌ 3. Masih .title
```

**SESUDAH:**
```javascript
${
  show.poster_path
    ? `<img
  src="https://image.tmdb.org/t/p/w500${show.poster_path}"
  class="card-img-top"
  alt="${show.name}"  // ✅ 1. Ubah ke .name
/>`
    : `<img
src="../images/no-image.jpg"
class="card-img-top"
alt="${show.name}"  // ✅ 2. Ubah ke .name
/>`
}
</div>
<div>
  <h2>${show.name}</h2>  // ✅ 3. Ubah ke .name
```

**📌 Total 3 tempat yang diubah!**

---

<a name="langkah-10-debug-dengan-console-log"></a>
## 🐛 Langkah 10: Debug dengan Console Log

### 🎬 Apa yang kita lakukan?
Melihat struktur data yang diterima dari API agar tahu field apa saja yang tersedia

### 💻 Tambahkan console.log:

```javascript
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];
  const show = await fetchAPIData(`tv/${showId}`);

  console.log(show);  // ✨ TAMBAHKAN INI

  displayBackgroundImage('tv', show.backdrop_path);
  // ... kode lainnya
}
```

### 🔍 Cara melihatnya:

1. **Buka browser** (Chrome/Firefox)
2. **Tekan F12** untuk membuka Developer Tools
3. **Klik tab Console**
4. **Refresh halaman** tv-details
5. **Lihat data yang muncul**

**Contoh output:**
```javascript
{
  name: "Breaking Bad",
  last_air_date: "2013-09-29",
  number_of_episodes: 62,
  last_episode_to_air: {
    name: "Felina",
    air_date: "2013-09-29"
  },
  overview: "...",
  genres: [...],
  // ... banyak field lainnya
}
```

**📌 Catatan:** Kita akan menghapus console.log ini nanti!

---

<a name="langkah-11-ubah-release_date-ke-last_air_date"></a>
## 📅 Langkah 11: Ubah release_date ke last_air_date

### 🎬 Apa yang kita lakukan?
Movie punya `release_date`, TV Show punya `last_air_date`

### 💻 Perubahan:

**SEBELUM:**
```javascript
<p class="text-muted">Release Date: ${show.release_date}</p>  // ❌ Field tidak ada
```

**SESUDAH:**
```javascript
<p class="text-muted">Last Air Date: ${show.last_air_date}</p>  // ✅ Field yang benar
```

**📌 Yang berubah:**
- Label text: "Release Date" → "Last Air Date"
- Field: `release_date` → `last_air_date`

---

<a name="langkah-12-ubah-movie-info-ke-show-info"></a>
## ℹ️ Langkah 12: Ubah Movie Info ke Show Info

### 🎬 Apa yang kita lakukan?
Mengubah header dari "Movie Info" menjadi "Show Info"

### 💻 Perubahan:

Di bagian `details-bottom`:

**SEBELUM:**
```javascript
<div class="details-bottom">
  <h2>Movie Info</h2>  // ❌ Masih "Movie Info"
  <ul>
```

**SESUDAH:**
```javascript
<div class="details-bottom">
  <h2>Show Info</h2>  // ✅ Ubah ke "Show Info"
  <ul>
```

---

<a name="langkah-13-ubah-budget-ke-number-of-episodes"></a>
## 💰➡️📊 Langkah 13: Ubah Budget ke Number of Episodes

### 🎬 Apa yang kita lakukan?
Movie punya `budget`, TV Show punya `number_of_episodes`

### 💻 Perubahan:

**SEBELUM:**
```javascript
<li><span class="text-secondary">Budget:</span> $${addCommasToNumber(
  show.budget  // ❌ Field tidak ada
)}</li>
```

**SESUDAH:**
```javascript
<li><span class="text-secondary">Number of Episodes:</span> ${
  show.number_of_episodes  // ✅ Langsung tanpa addCommas
}</li>
```

**📌 Perubahan:**
1. Label: "Budget" → "Number of Episodes"
2. Hapus simbol `$` (bukan uang)
3. Hapus `addCommasToNumber()` (tidak perlu untuk jumlah episode)
4. Field: `budget` → `number_of_episodes`

---

<a name="langkah-14-ubah-revenue-ke-last-episode-to-air"></a>
## 💵➡️📺 Langkah 14: Ubah Revenue ke Last Episode To Air

### 🎬 Apa yang kita lakukan?
Movie punya `revenue`, TV Show punya `last_episode_to_air`

### 💻 Perubahan:

**SEBELUM:**
```javascript
<li><span class="text-secondary">Revenue:</span> $${addCommasToNumber(
  show.revenue  // ❌ Field tidak ada
)}</li>
```

**SESUDAH:**
```javascript
<li><span class="text-secondary">Last Episode To Air:</span> ${
  show.last_episode_to_air  // ⚠️ Ini masih salah!
}</li>
```

**⚠️ PERHATIAN:** Ini masih akan error! Kita akan fix di langkah berikutnya.

**📌 Perubahan:**
1. Label: "Revenue" → "Last Episode To Air"
2. Hapus simbol `$`
3. Hapus `addCommasToNumber()`
4. Field: `revenue` → `last_episode_to_air`

---

<a name="langkah-15-hapus-runtime"></a>
## ⏱️ Langkah 15: Hapus Runtime

### 🎬 Apa yang kita lakukan?
TV Show tidak memiliki `runtime` tunggal (setiap episode bisa berbeda), jadi kita hapus bagian ini

### 💻 Perubahan:

**SEBELUM:**
```javascript
<ul>
  <li><span class="text-secondary">Number of Episodes:</span> ${
    show.number_of_episodes
  }</li>
  <li><span class="text-secondary">Last Episode To Air:</span> ${
    show.last_episode_to_air
  }</li>
  <li><span class="text-secondary">Runtime:</span> ${  // ❌ HAPUS MULAI SINI
    show.runtime
  } minutes</li>  // ❌ SAMPAI SINI
  <li><span class="text-secondary">Status:</span> ${show.status}</li>
</ul>
```

**SESUDAH:**
```javascript
<ul>
  <li><span class="text-secondary">Number of Episodes:</span> ${
    show.number_of_episodes
  }</li>
  <li><span class="text-secondary">Last Episode To Air:</span> ${
    show.last_episode_to_air
  }</li>
  <li><span class="text-secondary">Status:</span> ${show.status}</li>  // ✅ Langsung ke Status
</ul>
```

**📌 Catatan:** Hapus seluruh `<li>` yang berisi runtime!

---

<a name="langkah-16-fix-last-episode-to-air---tambah-name"></a>
## 🔧 Langkah 16: Fix Last Episode To Air - Tambah .name

### 🎬 Apa yang kita lakukan?
`last_episode_to_air` adalah **object**, kita perlu akses property `.name` nya

### 💻 Perubahan:

**SEBELUM:**
```javascript
<li><span class="text-secondary">Last Episode To Air:</span> ${
  show.last_episode_to_air  // ❌ Akan tampil [object Object]
}</li>
```

**SESUDAH:**
```javascript
<li><span class="text-secondary">Last Episode To Air:</span> ${
  show.last_episode_to_air.name  // ✅ Akses property .name
}</li>
```

### 📚 Penjelasan Struktur Data:

```javascript
// Data dari API:
last_episode_to_air: {
  name: "Felina",          // ← Ini yang kita mau
  air_date: "2013-09-29",
  episode_number: 16,
  season_number: 5
}

// Jika kita akses:
show.last_episode_to_air        // → [object Object] ❌
show.last_episode_to_air.name   // → "Felina" ✅
```

**📌 Save dan test! Sekarang seharusnya sudah tampil dengan benar.**

---

<a name="langkah-17-hapus-console-log"></a>
## 🧹 Langkah 17: Hapus Console Log

### 🎬 Apa yang kita lakukan?
Membersihkan kode debugging

### 💻 Perubahan:

**SEBELUM:**
```javascript
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];
  const show = await fetchAPIData(`tv/${showId}`);
  
  console.log(show);  // ❌ Hapus ini
  
  displayBackgroundImage('tv', show.backdrop_path);
  // ... kode lainnya
}
```

**SESUDAH:**
```javascript
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];
  const show = await fetchAPIData(`tv/${showId}`);
  
  // ✅ Console.log sudah dihapus
  
  displayBackgroundImage('tv', show.backdrop_path);
  // ... kode lainnya
}
```

**📌 Atau bisa di-comment saja untuk nanti dipakai lagi:**
```javascript
// console.log(show);  // Untuk debugging
```

---

<a name="testing-dan-hasil-akhir"></a>
## ✅ Testing dan Hasil Akhir

### 🧪 Cara Testing:

1. **Save semua file** yang sudah diubah
2. **Buka browser** dan refresh halaman
3. **Buka halaman shows** (http://localhost/shows.html atau sesuai setup Anda)
4. **Klik salah satu TV show**
5. **Periksa halaman detail yang muncul**

### ✨ Apa yang harus terlihat:

- ✅ Background backdrop show
- ✅ Poster show
- ✅ Nama show (bukan title)
- ✅ Rating (bintang dan angka)
- ✅ Last air date
- ✅ Overview/deskripsi
- ✅ Genres (list)
- ✅ Link ke homepage show
- ✅ Number of episodes
- ✅ Last episode to air (nama episode)
- ✅ Status (Returning Series, Ended, dll)
- ✅ Production companies

### 🔍 Test beberapa show berbeda:

Coba klik beberapa TV show untuk memastikan semuanya bekerja dengan berbagai data.

---

## 🎉 Hasil Akhir Kode Lengkap

### ✨ Fungsi displayShowDetails() Lengkap:

```javascript
// Display Show Details
async function displayShowDetails() {
  const showId = window.location.search.split('=')[1];

  const show = await fetchAPIData(`tv/${showId}`);

  // Overlay for background image
  displayBackgroundImage('tv', show.backdrop_path);

  const div = document.createElement('div');

  div.innerHTML = `
  <div class="details-top">
  <div>
  ${
    show.poster_path
      ? `<img
    src="https://image.tmdb.org/t/p/w500${show.poster_path}"
    class="card-img-top"
    alt="${show.name}"
  />`
      : `<img
  src="../images/no-image.jpg"
  class="card-img-top"
  alt="${show.name}"
/>`
  }
  </div>
  <div>
    <h2>${show.name}</h2>
    <p>
      <i class="fas fa-star text-primary"></i>
      ${show.vote_average.toFixed(1)} / 10
    </p>
    <p class="text-muted">Last Air Date: ${show.last_air_date}</p>
    <p>
      ${show.overview}
    </p>
    <h5>Genres</h5>
    <ul class="list-group">
      ${show.genres.map((genre) => `<li>${genre.name}</li>`).join('')}
    </ul>
    <a href="${
      show.homepage
    }" target="_blank" class="btn">Visit show Homepage</a>
  </div>
</div>
<div class="details-bottom">
  <h2>Show Info</h2>
  <ul>
    <li><span class="text-secondary">Number of Episodes:</span> ${
      show.number_of_episodes
    }</li>
    <li><span class="text-secondary">Last Episode To Air:</span> ${
      show.last_episode_to_air.name
    }</li>
    <li><span class="text-secondary">Status:</span> ${show.status}</li>
  </ul>
  <h4>Production Companies</h4>
  <div class="list-group">
    ${show.production_companies
      .map((company) => `<span>${company.name}</span>`)
      .join(', ')}
  </div>
</div>
  `;

  document.querySelector('#show-details').appendChild(div);
}
```

### ✨ Update di fungsi init():

```javascript
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      displayPopularMovies();
      break;
    case '/shows.html':
      displayPopularShows();
      break;
    case '/movie-details.html':
      displayMovieDetails();
      break;
    case '/tv-details.html':
      displayShowDetails();  // ✅ Fungsi baru
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}
```

---

<a name="ringkasan"></a>
## 📊 Ringkasan Perubahan Movie vs TV Show

| No | Aspek | Movie | TV Show | Alasan |
|----|-------|-------|---------|--------|
| 1 | **Nama** | `title` | `name` | API menggunakan field berbeda |
| 2 | **Tanggal** | `release_date` | `last_air_date` | Show punya tanggal tayang terakhir |
| 3 | **Budget** | `budget` | `number_of_episodes` | Show tidak punya budget publik |
| 4 | **Revenue** | `revenue` | `last_episode_to_air.name` | Show tidak punya revenue |
| 5 | **Runtime** | `runtime` | *(tidak ada)* | Tiap episode berbeda durasinya |
| 6 | **Endpoint** | `movie/{id}` | `tv/{id}` | API berbeda untuk movie dan TV |
| 7 | **DOM Target** | `#movie-details` | `#show-details` | Element ID berbeda di HTML |

---

## 🎓 Apa yang Sudah Dipelajari?

Selamat! Anda telah berhasil menyelesaikan tutorial ini. Berikut yang sudah Anda pelajari:

### ✅ Skill Teknis:
1. **Reuse Code** - Menggunakan fungsi yang sudah ada sebagai template
2. **API Integration** - Memahami perbedaan endpoint API (movie vs tv)
3. **Data Mapping** - Memetakan field yang berbeda antar struktur data
4. **Debugging** - Menggunakan `console.log()` untuk melihat struktur data
5. **DOM Manipulation** - Membuat HTML dinamis dengan JavaScript
6. **Template Literals** - Menggunakan backtick (`) untuk string multi-line
7. **Nested Objects** - Mengakses data dalam object bertingkat (`show.last_episode_to_air.name`)

### ✅ Konsep Programming:
- **DRY Principle** (Don't Repeat Yourself) - Copy, modify, reuse!
- **Dynamic Content** - HTML yang dibuat oleh JavaScript
- **Async/Await** - Menunggu data dari API
- **Conditional Rendering** - Tampilkan gambar jika ada, jika tidak tampilkan placeholder

---

## 💡 Tips & Best Practices

> **💡 Tip #1: Console.log adalah Teman Terbaik**  
> Selalu gunakan `console.log()` untuk melihat struktur data dari API sebelum menggunakannya. Ini akan menghemat banyak waktu debugging!

> **💡 Tip #2: Perhatikan Nested Objects**  
> Jika melihat `[object Object]` di browser, berarti Anda perlu mengakses property yang lebih dalam (misalnya `.name`)

> **💡 Tip #3: Read the Docs**  
> Dokumentasi TMDB API sangat lengkap. Selalu cek struktur data di: https://developers.themoviedb.org/3

> **💡 Tip #4: Test dengan Data Berbeda**  
> Jangan hanya test dengan 1 show. Test dengan beberapa show berbeda untuk memastikan kode Anda robust!

> **⚠️ Perhatian: Hapus HTML Statis**  
> Jangan lupa menghapus HTML hard-coded di file HTML, karena akan bentrok dengan JavaScript!

---

## 🚀 Langkah Selanjutnya

Setelah menyelesaikan tutorial ini, Anda bisa melanjutkan ke:

1. **🎨 Slider dengan Swiper** - Membuat carousel untuk now playing movies
2. **🔍 Search Feature** - Menambahkan fitur pencarian
3. **📄 Pagination** - Menambahkan halaman untuk hasil yang banyak
4. **⭐ Rating System** - Menampilkan rating dengan visual yang lebih menarik
5. **🎬 Video Trailers** - Embed video trailer dari YouTube

---

## 🐛 Troubleshooting - Masalah Umum

### ❌ Problem: "Cannot read property 'name' of undefined"

**Penyebab:** Field tidak ada di data API

**Solusi:**
```javascript
// Gunakan optional chaining
${show.last_episode_to_air?.name || 'N/A'}
```

### ❌ Problem: Muncul "[object Object]"

**Penyebab:** Anda menampilkan object, bukan string

**Solusi:**
```javascript
// Salah
${show.last_episode_to_air}

// Benar
${show.last_episode_to_air.name}
```

### ❌ Problem: Backdrop tidak muncul

**Penyebab:** Parameter `type` salah di `displayBackgroundImage()`

**Solusi:**
```javascript
// Pastikan parameter pertama adalah 'tv', bukan 'movie'
displayBackgroundImage('tv', show.backdrop_path);
```

### ❌ Problem: Data tidak muncul sama sekali

**Checklist:**
- ✅ Sudah hapus HTML statis di `tv-details.html`?
- ✅ Sudah panggil `displayShowDetails()` di `init()`?
- ✅ API key sudah diisi?
- ✅ Internet connection aktif?

---

## 📚 Referensi & Resource

### 🔗 Dokumentasi:
- **TMDB API Docs**: https://developers.themoviedb.org/3
- **TV Show Endpoint**: https://developers.themoviedb.org/3/tv
- **MDN JavaScript**: https://developer.mozilla.org/en-US/docs/Web/JavaScript

### 🎯 Konsep Penting:
- **Async/Await**: https://javascript.info/async-await
- **Template Literals**: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals
- **DOM Manipulation**: https://javascript.info/modifying-document

---

## 📝 Checklist Akhir

Pastikan semua sudah dikerjakan:

- [ ] ✅ Copy fungsi `displayMovieDetails`
- [ ] ✅ Rename menjadi `displayShowDetails`
- [ ] ✅ Ubah `movieId` → `showId`
- [ ] ✅ Ubah endpoint `movie/` → `tv/`
- [ ] ✅ Ubah parameter `displayBackgroundImage` ke `'tv'`
- [ ] ✅ Ubah DOM target ke `#show-details`
- [ ] ✅ Panggil fungsi di `init()`
- [ ] ✅ Hapus HTML statis di `tv-details.html`
- [ ] ✅ Ganti semua `movie` → `show`
- [ ] ✅ Ubah `title` → `name` (3 tempat)
- [ ] ✅ Tambah `console.log(show)` untuk debugging
- [ ] ✅ Ubah `release_date` → `last_air_date`
- [ ] ✅ Ubah "Movie Info" → "Show Info"
- [ ] ✅ Ubah `budget` → `number_of_episodes`
- [ ] ✅ Ubah `revenue` → `last_episode_to_air`
- [ ] ✅ Hapus `runtime`
- [ ] ✅ Tambah `.name` pada `last_episode_to_air`
- [ ] ✅ Hapus `console.log()`
- [ ] ✅ Test di browser dengan beberapa show berbeda
- [ ] ✅ Pastikan tidak ada error di console

---

## 🎊 Penutup

**Selamat!** 🎉 Anda telah berhasil membuat halaman detail TV Show dari awal hingga akhir!

Anda telah belajar bagaimana:
- 🔄 Menggunakan kembali kode yang sudah ada
- 🔌 Bekerja dengan API eksternal
- 🎨 Membuat konten dinamis dengan JavaScript
- 🐛 Men-debug kode dengan console.log
- 📊 Memahami perbedaan struktur data

**Keep learning, keep coding!** 💻✨

---

## 📌 Catatan Penting untuk Pemula

> **🌟 Jangan Takut Mencoba!**  
> Programming adalah tentang trial and error. Jika ada error, jangan panik! Baca error message dengan teliti, cek di mana masalahnya, dan perbaiki sedikit demi sedikit.

> **🔍 Console adalah Sahabat Anda**  
> Developer profesional juga sering menggunakan `console.log()`. Tidak ada yang salah dengan itu!

> **📖 Dokumentasi adalah Kunci**  
> Biasakan membaca dokumentasi API. Di awal memang terlihat sulit, tapi semakin sering dibaca akan semakin mudah dipahami.

> **💪 Practice Makes Perfect**  
> Coba modifikasi kode ini! Tambahkan fitur baru, ubah tampilannya, eksperimen dengan data lain. Semakin banyak praktek, semakin mahir Anda!

---

**Happy Coding!** 🚀

*Dokumentasi ini dibuat untuk pembelajaran pribadi. Silakan modifikasi dan kembangkan sesuai kebutuhan Anda!*

---

**📅 Dibuat:** Untuk latihan pribadi  
**🎯 Tujuan:** Memahami cara membuat halaman detail TV Show dengan JavaScript  
**💻 Tech Stack:** Vanilla JavaScript, TMDB API, HTML5, CSS3  
**📝 Versi:** 1.0
