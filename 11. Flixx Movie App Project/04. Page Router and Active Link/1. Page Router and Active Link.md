# 🎬 Dokumentasi Simple Router Vanilla JavaScript untuk Pemula

> 📚 Dokumentasi pribadi untuk belajar membuat router sederhana menggunakan Vanilla JavaScript

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Router?](#apa-itu-router)
- [Persiapan Awal](#persiapan-awal)
- [Membuat Global State](#membuat-global-state)
- [Membuat Fungsi Init App](#membuat-fungsi-init-app)
- [Membuat Switch Statement untuk Router](#membuat-switch-statement-untuk-router)
- [Menambahkan Route untuk Setiap Halaman](#menambahkan-route-untuk-setiap-halaman)
- [Menambahkan Multiple Conditions](#menambahkan-multiple-conditions)
- [Membuat Fungsi Highlight Active Link](#membuat-fungsi-highlight-active-link)
- [Menjalankan Aplikasi dengan Event Listener](#menjalankan-aplikasi-dengan-event-listener)
- [Kode Lengkap](#kode-lengkap)
- [Cara Kerja Keseluruhan](#cara-kerja-keseluruhan)
- [Referensi](#referensi)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan belajar membuat **simple router** menggunakan Vanilla JavaScript. Router ini akan membantu kita menjalankan fungsi-fungsi JavaScript yang spesifik hanya pada halaman tertentu.

### 🤔 Kenapa Perlu Router?

Ketika kita menggunakan framework seperti **React** atau **Vue**, router sudah tersedia secara otomatis. Namun dengan **Vanilla JavaScript**, kita harus membuatnya sendiri - dan ini adalah **pengalaman belajar yang sangat baik**! 💪

---

<a name="apa-itu-router"></a>
## 📖 Apa itu Router?

**Router** adalah sistem yang membantu kita menjalankan kode JavaScript yang berbeda berdasarkan halaman yang sedang aktif. 

### Contoh Penggunaan:
- 🏠 Di halaman **Movies**, tampilkan daftar film
- 📺 Di halaman **TV Shows**, tampilkan daftar acara TV
- 🔍 Di halaman **Movie Details**, ambil ID dari URL dan fetch data film tertentu

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

### File yang Dibutuhkan:
- ✅ `script.js` (file JavaScript kosong)
- ✅ Console browser (untuk testing)
- ✅ HTML pages: `index.html`, `shows.html`, `movie-details.html`, `tv-details.html`, `search.html`

### 🔍 Mengecek Location Path

Kita bisa menggunakan `window.location.pathname` untuk mengetahui halaman mana yang sedang aktif:

```javascript
console.log(window.location.pathname);
```

**Hasilnya:**
- Halaman home: `/` atau `/index.html`
- Halaman TV Shows: `/shows.html`
- Halaman TV Details: `/tv-details.html`

---

<a name="membuat-global-state"></a>
## 🌍 Membuat Global State

Kita akan membuat **object global** untuk menyimpan informasi halaman yang sedang aktif.

### 📝 Langkah-langkah:

**1. Buat object global di bagian atas file:**

```javascript
const global = {
  currentPage: window.location.pathname,
};
```

**Penjelasan:**
- 📦 `global` = object untuk menyimpan state aplikasi
- 🔖 `currentPage` = property yang menyimpan path halaman saat ini
- 🌐 `window.location.pathname` = mengambil path URL saat ini

**2. Test dengan console.log:**

```javascript
console.log(global.currentPage);
```

**Hasil:**
- Di halaman TV Shows → `/shows.html` ✅

---

<a name="membuat-fungsi-init-app"></a>
## 🚀 Membuat Fungsi Init App

Fungsi `init()` adalah fungsi yang akan **menginisialisasi** atau **memulai** aplikasi kita.

### 📝 Kode:

```javascript
// Init App
function init() {
  // Di sini kita akan cek halaman mana yang aktif
}
```

**Penjelasan:**
- 🎬 Fungsi ini akan dijalankan saat halaman dimuat
- 📍 Di dalamnya kita akan cek `global.currentPage`
- ⚡ Lalu jalankan fungsi yang sesuai dengan halaman tersebut

---

<a name="membuat-switch-statement-untuk-router"></a>
## 🔀 Membuat Switch Statement untuk Router

Kita akan menggunakan **switch statement** untuk membuat router. Ini lebih bersih daripada banyak **if-else**!

### 📝 Kode Dasar:

```javascript
function init() {
  switch (global.currentPage) {
    case '/':
      console.log('Home');
      break;
  }
}
```

**Penjelasan:**
- 🎯 `switch` = menguji nilai dari `global.currentPage`
- 📌 `case` = kondisi yang akan dicocokkan
- ⏹️ `break` = menghentikan eksekusi setelah case cocok
- 💡 Lebih bersih daripada banyak `if-else`!

---

<a name="menambahkan-route-untuk-setiap-halaman"></a>
## 📄 Menambahkan Route untuk Setiap Halaman

Sekarang kita tambahkan **case** untuk setiap halaman yang ada di aplikasi.

### 📝 Kode Lengkap:

```javascript
function init() {
  switch (global.currentPage) {
    case '/':
      console.log('Home');
      break;
    case '/shows.html':
      console.log('Shows');
      break;
    case '/movie-details.html':
      console.log('Movie Details');
      break;
    case '/tv-details.html':
      console.log('TV Details');
      break;
    case '/search.html':
      console.log('Search');
      break;
  }
}
```

**Penjelasan setiap case:**
- 🏠 `case '/'` → Halaman home/movies
- 📺 `case '/shows.html'` → Halaman TV shows
- 🎬 `case '/movie-details.html'` → Halaman detail film
- 📺 `case '/tv-details.html'` → Halaman detail TV show
- 🔍 `case '/search.html'` → Halaman pencarian

**Testing:**
- Buka halaman Shows → Console menampilkan "Shows" ✅
- Buka halaman Movie Details → Console menampilkan "Movie Details" ✅

---

<a name="menambahkan-multiple-conditions"></a>
## 🔄 Menambahkan Multiple Conditions

Halaman home bisa diakses dengan **dua cara**:
- `/` (root path)
- `/index.html` (explicit path)

Kita perlu menangani **kedua kondisi** ini!

### 📝 Kode:

```javascript
switch (global.currentPage) {
  case '/':
  case '/index.html':
    console.log('Home');
    break;
  // ... case lainnya
}
```

**Penjelasan:**
- 🎯 Dua `case` tanpa `break` di antaranya
- ✅ Jika salah satu cocok, kode yang sama akan dijalankan
- 🏠 Baik `/` maupun `/index.html` akan menampilkan "Home"

**Cara kerja:**
1. Cek apakah path = `/` → Jika TIDAK, lanjut ke case berikutnya
2. Cek apakah path = `/index.html` → Jika YA, jalankan `console.log('Home')`
3. Kemudian `break` keluar dari switch

---

<a name="membuat-fungsi-highlight-active-link"></a>
## ✨ Membuat Fungsi Highlight Active Link

Kita akan membuat fitur agar **link navigasi yang aktif** akan ter-highlight dengan warna kuning! 🌟

### 🎯 Tujuan:
- Di halaman Movies → Link "Movies" berwarna kuning
- Di halaman TV Shows → Link "TV Shows" berwarna kuning

### 📝 Struktur HTML (Referensi):

```html
<nav>
  <a href="/" class="nav-link">Movies</a>
  <a href="/shows.html" class="nav-link">TV Shows</a>
</nav>
```

**Catatan:**
- Setiap link punya class `.nav-link`
- Class `.active` akan membuat link berwarna kuning (sudah ada di CSS)

### 📝 Kode Fungsi:

```javascript
// Highlight active link
function highlightActiveLink() {
  const links = document.querySelectorAll('.nav-link');
  links.forEach((link) => {
    if (link.getAttribute('href') === global.currentPage) {
      link.classList.add('active');
    }
  });
}
```

### 🔍 Penjelasan Detail:

**1. Ambil semua nav link:**
```javascript
const links = document.querySelectorAll('.nav-link');
```
- 📌 `querySelectorAll()` = ambil SEMUA element dengan class `.nav-link`
- 📦 Hasilnya: NodeList berisi semua link navigasi

**2. Loop setiap link:**
```javascript
links.forEach((link) => {
  // ...
});
```
- 🔄 Loop melalui setiap link yang ada

**3. Cek apakah href cocok dengan halaman saat ini:**
```javascript
if (link.getAttribute('href') === global.currentPage) {
  link.classList.add('active');
}
```
- 🔍 `getAttribute('href')` = ambil nilai atribut href dari link
- ✅ Jika href sama dengan `global.currentPage`, tambahkan class `active`
- 🎨 Class `active` membuat link berwarna kuning dan bold

### 📝 Menambahkan ke Init:

```javascript
function init() {
  switch (global.currentPage) {
    // ... semua case
  }

  highlightActiveLink(); // ⬅️ Panggil di sini!
}
```

**⚠️ Penting:**
- Panggil `highlightActiveLink()` di **luar switch**
- Tapi masih **dalam fungsi init**
- Sehingga akan dijalankan di **setiap halaman**

---

<a name="menjalankan-aplikasi-dengan-event-listener"></a>
## 🎬 Menjalankan Aplikasi dengan Event Listener

Kita perlu menjalankan fungsi `init()` saat halaman selesai dimuat.

### 📝 Kode:

```javascript
document.addEventListener('DOMContentLoaded', init);
```

**Penjelasan:**
- 📄 `DOMContentLoaded` = event yang dipicu saat HTML selesai dimuat
- ⚡ Kita panggil fungsi `init` saat event ini terjadi
- ✅ Ini memastikan semua element HTML sudah tersedia sebelum JavaScript dijalankan

### 🔄 Alur Kerja:

1. Browser memuat halaman HTML
2. Event `DOMContentLoaded` dipicu
3. Fungsi `init()` dijalankan
4. Switch statement mengecek halaman mana yang aktif
5. Fungsi `highlightActiveLink()` menandai link yang aktif
6. Halaman siap digunakan! 🎉

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap

```javascript
const global = {
  currentPage: window.location.pathname,
};

// Highlight active link
function highlightActiveLink() {
  const links = document.querySelectorAll('.nav-link');
  links.forEach((link) => {
    if (link.getAttribute('href') === global.currentPage) {
      link.classList.add('active');
    }
  });
}

// Init App
function init() {
  switch (global.currentPage) {
    case '/':
    case '/index.html':
      console.log('Home');
      break;
    case '/shows.html':
      console.log('Shows');
      break;
    case '/movie-details.html':
      console.log('Movie Details');
      break;
    case '/tv-details.html':
      console.log('TV Details');
      break;
    case '/search.html':
      console.log('Search');
      break;
  }

  highlightActiveLink();
}

document.addEventListener('DOMContentLoaded', init);
```

---

<a name="cara-kerja-keseluruhan"></a>
## 🎯 Cara Kerja Keseluruhan

### 📊 Flow Diagram:

```
1. Halaman dimuat
   ↓
2. Event DOMContentLoaded dipicu
   ↓
3. Fungsi init() dijalankan
   ↓
4. Switch mengecek global.currentPage
   ↓
5. Menjalankan kode sesuai halaman
   ↓
6. highlightActiveLink() menandai link aktif
   ↓
7. Aplikasi siap! ✅
```

### 🔑 Konsep Penting:

**1. File JavaScript ini terhubung ke SEMUA halaman**
- ✅ `init()` akan dijalankan di setiap halaman
- ✅ Tapi hanya kode di case yang sesuai yang dijalankan

**2. highlightActiveLink() selalu dijalankan**
- ✅ Di luar switch, jadi dijalankan di semua halaman
- ✅ Memastikan link yang aktif selalu ter-highlight

**3. Siap untuk pengembangan**
- 📝 Tambahkan fungsi lain di dalam case untuk setiap halaman
- 📝 Contoh: fetch data, render UI, handle form, dll

---

<a name="referensi"></a>
## 📚 Referensi

### 🔗 Link GitHub:
[Flixx App - Commit Router](https://github.com/bradtraversy/flixx-app/commit/0161305214d990cc17277fb9065ec39fd57bafe2)

### 📖 Metode yang Digunakan:

| Metode | Kegunaan |
|--------|----------|
| `window.location.pathname` | Mendapatkan path URL saat ini |
| `document.querySelectorAll()` | Mengambil semua element yang cocok |
| `forEach()` | Loop melalui array/NodeList |
| `getAttribute()` | Mengambil nilai atribut HTML |
| `classList.add()` | Menambahkan class CSS ke element |
| `addEventListener()` | Mendengarkan event pada element |

### ✅ Checklist Pembelajaran:

- [x] Memahami konsep router
- [x] Membuat global state
- [x] Menggunakan switch statement
- [x] Menangani multiple conditions
- [x] Manipulasi DOM untuk highlight link
- [x] Menggunakan event listener

---

## 🎉 Selamat!

Kamu sudah berhasil membuat **simple router** dengan Vanilla JavaScript! Ini adalah fondasi penting untuk membuat aplikasi web yang lebih kompleks. 

### 🚀 Langkah Selanjutnya:
- Tambahkan fungsi untuk fetch data API
- Render UI dinamis berdasarkan data
- Tambahkan interaktivitas lebih lanjut

**Keep learning and happy coding!** 💻✨
