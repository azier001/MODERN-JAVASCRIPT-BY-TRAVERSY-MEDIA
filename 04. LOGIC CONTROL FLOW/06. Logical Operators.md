# 🎓 Dokumentasi Logical Operators JavaScript untuk Pemula

> 📚 Dokumentasi pribadi untuk memahami operator logika di JavaScript secara bertahap

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Operator AND (&&)](#operator-and)
  - [Cara Kerja Dasar AND](#cara-kerja-dasar-and)
  - [Nilai Return dari AND](#nilai-return-dari-and)
  - [Contoh Praktis AND](#contoh-praktis-and)
- [Operator OR (||)](#operator-or)
  - [Cara Kerja Dasar OR](#cara-kerja-dasar-or)
  - [Nilai Return dari OR](#nilai-return-dari-or)
- [Operator Nullish Coalescing (??)](#operator-nullish-coalescing)
- [Ringkasan](#ringkasan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Logical operators (operator logika) adalah operator yang digunakan untuk menggabungkan atau memanipulasi nilai boolean. Dalam JavaScript, ada tiga operator logika utama yang akan kita pelajari:

- **`&&`** (AND) - Double Ampersand
- **`||`** (OR) - Double Pipes
- **`??`** (Nullish Coalescing) - Double Question Mark

Mari kita pelajari satu per satu! 🚀

---

<a name="operator-and"></a>
## ⚡ Operator AND (`&&`)

<a name="cara-kerja-dasar-and"></a>
### 📌 Cara Kerja Dasar AND

Operator `&&` akan menghasilkan `true` **HANYA jika SEMUA kondisi bernilai true**.

```javascript
// ✅ Semua kondisi true = hasil true
console.log(10 < 20 && 30 > 15 && 40 > 30); 
// Output: true

// ❌ Ada satu kondisi false = hasil false
console.log(10 > 20 && 30 > 15 && 40 > 30); 
// Output: false (karena 10 > 20 adalah false)
```

**💡 Penjelasan:**
- `10 < 20` → true ✅
- `30 > 15` → true ✅
- `40 > 30` → true ✅
- **Semua true**, maka hasilnya **true**

---

<a name="nilai-return-dari-and"></a>
### 🎁 Nilai Return dari AND

> **Aturan Penting:** Operator `&&` akan mengembalikan **nilai falsy pertama** ATAU **nilai terakhir** jika semua truthy.

#### 🔍 Apa itu Falsy?
Nilai-nilai yang dianggap "false" dalam JavaScript:
- `0`
- `''` (string kosong)
- `null`
- `undefined`
- `NaN`
- `false`

#### 📝 Contoh Kode:

```javascript
let a;

// Contoh 1: Semua nilai truthy
a = 10 && 20;
console.log(a); // Output: 20 (nilai terakhir)

// Contoh 2: Lebih banyak nilai truthy
a = 10 && 20 && 30;
console.log(a); // Output: 30 (nilai terakhir)

// Contoh 3: Ada nilai falsy (0)
a = 10 && 0 && 30;
console.log(a); // Output: 0 (falsy pertama ditemukan!)

// Contoh 4: Ada string kosong (falsy)
a = 10 && '' && 0 && 30;
console.log(a); // Output: '' (falsy pertama ditemukan!)
```

**🎯 Kesimpulan:**
- Jika **semua truthy** → return **nilai terakhir**
- Jika **ada falsy** → return **falsy pertama**

---

<a name="contoh-praktis-and"></a>
### 💼 Contoh Praktis AND

Operator `&&` sangat berguna untuk **mengecek kondisi sebelum menjalankan kode**:

```javascript
const posts = ['Post One', 'Post Two'];

// Cek apakah array memiliki isi sebelum menampilkan
posts.length > 0 && console.log(posts[0]);
// Output: "Post One"

// Jika array kosong:
const emptyPosts = [];
emptyPosts.length > 0 && console.log(emptyPosts[0]);
// Output: (tidak ada output, karena kondisi false)
```

**✨ Kegunaan:**
- Menghindari error saat mengakses data yang mungkin tidak ada
- Sangat umum digunakan di React untuk conditional rendering
- Membuat kode lebih ringkas tanpa `if` statement

---

<a name="operator-or"></a>
## 🔥 Operator OR (`||`)

<a name="cara-kerja-dasar-or"></a>
### 📌 Cara Kerja Dasar OR

Operator `||` akan menghasilkan `true` **jika MINIMAL SATU kondisi bernilai true**.

```javascript
// ✅ Salah satu true = hasil true
console.log(10 > 20 || 30 > 15); 
// Output: true (karena 30 > 15 adalah true)

// ❌ Semua false = hasil false
console.log(10 > 20 || 30 < 15); 
// Output: false (karena semua false)
```

**💡 Penjelasan:**
- `10 > 20` → false ❌
- `30 > 15` → true ✅
- **Ada yang true**, maka hasilnya **true**

---

<a name="nilai-return-dari-or"></a>
### 🎁 Nilai Return dari OR

> **Aturan Penting:** Operator `||` akan mengembalikan **nilai truthy pertama** ATAU **nilai terakhir** jika semua falsy.

#### 📝 Contoh Kode:

```javascript
let b;

// Contoh 1: Nilai pertama truthy
b = 10 || 20;
console.log(b); // Output: 10 (truthy pertama)

// Contoh 2: Nilai pertama falsy, kedua truthy
b = 0 || 20;
console.log(b); // Output: 20 (truthy pertama yang ditemukan)

// Contoh 3: Semua falsy
b = 0 || null || '' || undefined;
console.log(b); // Output: undefined (nilai terakhir)
```

**🎯 Kesimpulan:**
- Jika **ada truthy** → return **truthy pertama**
- Jika **semua falsy** → return **nilai terakhir**

**✨ Kegunaan:**
- Memberikan nilai default jika nilai utama falsy
- Contoh: `let username = inputUser || 'Guest';`

---

<a name="operator-nullish-coalescing"></a>
## 🌟 Operator Nullish Coalescing (`??`)

### 📌 Cara Kerja

Operator `??` mengembalikan **nilai di sebelah kanan** HANYA jika nilai di sebelah kiri adalah `null` atau `undefined`.

> ⚠️ **Perbedaan dengan `||`:** Operator `??` hanya peduli dengan `null` dan `undefined`, bukan semua falsy values!

#### 📝 Contoh Kode:

```javascript
let c;

// Contoh 1: Nilai kiri bukan null/undefined
c = 10 ?? 20;
console.log(c); // Output: 10

// Contoh 2: Nilai kiri adalah null
c = null ?? 20;
console.log(c); // Output: 20

// Contoh 3: Nilai kiri adalah undefined
c = undefined ?? 30;
console.log(c); // Output: 30

// Contoh 4: Nilai kiri adalah 0 (BUKAN null/undefined)
c = 0 ?? 30;
console.log(c); // Output: 0 (karena 0 bukan null/undefined!)

// Contoh 5: Nilai kiri adalah string kosong
c = '' ?? 30;
console.log(c); // Output: '' (karena '' bukan null/undefined!)
```

### 🆚 Perbandingan `||` vs `??`

| Nilai Kiri | `\|\|` Result | `??` Result |
|------------|---------------|-------------|
| `0` | Nilai kanan | `0` |
| `''` | Nilai kanan | `''` |
| `false` | Nilai kanan | `false` |
| `null` | Nilai kanan | Nilai kanan |
| `undefined` | Nilai kanan | Nilai kanan |

**✨ Kegunaan:**
- Ketika `0`, `''`, atau `false` adalah nilai yang valid
- Hanya ingin mengganti jika benar-benar `null` atau `undefined`

---

<a name="ringkasan"></a>
## 📊 Ringkasan

### 🎯 Cheat Sheet

| Operator | Nama | Return Value | Kapan Digunakan |
|----------|------|--------------|-----------------|
| `&&` | AND | Falsy pertama ATAU nilai terakhir | Cek semua kondisi true, conditional execution |
| `\|\|` | OR | Truthy pertama ATAU nilai terakhir | Nilai default untuk falsy values |
| `??` | Nullish Coalescing | Nilai kanan jika kiri null/undefined | Nilai default hanya untuk null/undefined |

### ✅ Tips Penting

1. **`&&` untuk validasi:** Gunakan untuk memastikan semua kondisi terpenuhi
2. **`||` untuk fallback:** Gunakan untuk memberikan nilai alternatif
3. **`??` untuk strict null check:** Gunakan jika 0, '', false adalah nilai valid
4. **Praktik terus:** Coba berbagai kombinasi untuk memahami lebih dalam!

### 🚀 Langkah Selanjutnya

Setelah memahami logical operators, kamu bisa:
- ✨ Mempelajari logical assignment operators (`&&=`, `||=`, `??=`)
- 🎯 Menggunakan operators ini dalam conditional rendering (React)
- 💪 Kombinasikan dengan ternary operator untuk kode yang lebih ringkas

---

**💙 Selamat Belajar!** Jangan ragu untuk mencoba dan eksperimen dengan kode-kode di atas! 🎉
