# 🎨 Tutorial Web Animations API untuk Pemula

> 📚 Dokumentasi Pribadi - Belajar Animasi JavaScript Step by Step

---

## 📋 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Web Animations API?](#apa-itu-web-animations-api)
- [Persiapan Project](#persiapan-project)
  - [File HTML](#file-html)
  - [File CSS](#file-css)
- [Memahami Animasi CSS (Sebelum JavaScript)](#memahami-animasi-css)
- [Migrasi ke JavaScript](#migrasi-ke-javascript)
  - [Langkah 1: Setup Awal](#langkah-1-setup-awal)
  - [Langkah 2: Membuat Array Animasi](#langkah-2-membuat-array-animasi)
  - [Langkah 3: Menambahkan Options](#langkah-3-menambahkan-options)
  - [Langkah 4: Menjalankan Animasi](#langkah-4-menjalankan-animasi)
- [Menambahkan Kontrol Animasi](#menambahkan-kontrol-animasi)
  - [Langkah 5: Play dan Pause](#langkah-5-play-dan-pause)
  - [Langkah 6: Reverse](#langkah-6-reverse)
  - [Langkah 7: Speed Up dan Slow Down](#langkah-7-speed-up-dan-slow-down)
- [Kode Lengkap](#kode-lengkap)
- [Tantangan Extra](#tantangan-extra)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan belajar cara membuat animasi bola yang berputar dan berubah warna menggunakan **Web Animations API**. 

**Yang akan kita pelajari:**
- ✅ Mengubah animasi CSS menjadi JavaScript
- ✅ Mengontrol animasi (play, pause, reverse)
- ✅ Mengatur kecepatan animasi
- ✅ Memahami `playbackRate` dan properti animasi lainnya

---

<a name="apa-itu-web-animations-api"></a>
## 📖 Apa itu Web Animations API?

**Web Animations API** adalah cara untuk membuat dan mengontrol animasi menggunakan JavaScript, mirip seperti CSS animations tapi dengan kontrol yang lebih fleksibel!

**Keuntungan:**
- 🎮 Kontrol penuh atas animasi (play, pause, reverse, speed)
- 💪 Lebih powerful daripada CSS animations
- 🔄 Bisa diubah secara dinamis saat runtime

---

<a name="persiapan-project"></a>
## 🛠️ Persiapan Project

<a name="file-html"></a>
### 📄 File HTML (`index.html`)

Buat file HTML dengan struktur berikut:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Web Animations API</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="style.css" />
    <script src="script.js" defer></script>
  </head>
  <body>
    <div class="wrapper">
      <!-- Tombol Kontrol -->
      <div class="controls">
        <button id="play"><i class="fa-solid fa-play"></i></button>
        <button id="pause"><i class="fa-solid fa-pause"></i></button>
        <button id="reverse">
          <i class="fa-solid fa-clock-rotate-left"></i>
        </button>
        <button id="speed-up"><i class="fa-solid fa-plus"></i></button>
        <button id="slow-down"><i class="fa-solid fa-minus"></i></button>
      </div>
      
      <!-- Bola SVG -->
      <div id="ball">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="194"
          height="194"
          version="1.1"
        >
          <circle fill="#000000" cx="97" cy="97" r="97" />
          <path
            fill="#ffffff"
            d="m 94,9.2 a 88,88 0 0 0 -55,21.8 l 27,0 28,-14.4 0,-7.4 z m 6,0 0,7.4 28,14.4 27,0 a 88,88 0 0 0 -55,-21.8 z m -67.2,27.8 a 88,88 0 0 0 -20,34.2 l 16,27.6 23,-3.6 21,-36.2 -8.4,-22 -31.6,0 z m 96.8,0 -8.4,22 21,36.2 23,3.6 15.8,-27.4 a 88,88 0 0 0 -19.8,-34.4 l -31.6,0 z m -50,26 -20.2,35.2 17.8,30.8 39.6,0 17.8,-30.8 -20.2,-35.2 -34.8,0 z m -68.8,16.6 a 88,88 0 0 0 -1.8,17.4 88,88 0 0 0 10.4,41.4 l 7.4,-4.4 -1.4,-29 -14.6,-25.4 z m 172.4,0.2 -14.6,25.2 -1.4,29 7.4,4.4 a 88,88 0 0 0 10.4,-41.4 88,88 0 0 0 -1.8,-17.2 z m -106,57.2 -15.4,19 L 77.2,182.6 a 88,88 0 0 0 19.8,2.4 88,88 0 0 0 19.8,-2.4 l 15.4,-26.6 -15.4,-19 -39.6,0 z m -47.8,2.6 -7,4 A 88,88 0 0 0 68.8,180.4 l -14,-24.6 -25.4,-16.2 z m 135.2,0 -25.4,16.2 -14,24.4 a 88,88 0 0 0 46.4,-36.6 l -7,-4 z"
          />
        </svg>
      </div>
    </div>
  </body>
</html>
```

**📌 Penjelasan:**
- `<div class="controls">`: Berisi tombol-tombol kontrol
- `<div id="ball">`: Berisi SVG bola yang akan dianimasi
- `<script src="script.js" defer>`: Memuat JavaScript kita

---

<a name="file-css"></a>
### 🎨 File CSS (`style.css`)

Buat file CSS untuk styling dasar:

```css
body {
  background: #000;
  color: #fff;
}

html,
body {
  height: 100%;
}

.wrapper {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

#ball {
  /* animation: roll infinite 3s linear; */ /* ⬅️ AKAN KITA COMMENT NANTI */
  color: white;
  width: 25%;
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: 0 0;
  transform: rotate(0) translate3D(-50%, -50%, 0);
  backface-visibility: hidden;
  will-change: transform, color;
}

path {
  fill: currentColor;
}

.controls {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 20px;
}

button {
  padding: 10px 20px;
  margin: 5px;
  background: #333;
  color: #fff;
  font-size: 18px;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background: blue;
}

/* 🎬 ANIMASI CSS YANG AKAN KITA GANTI KE JAVASCRIPT */
@keyframes roll {
  0% {
    transform: rotate(0) translate3D(-50%, -50%, 0);
    color: white;
  }
  30% {
    color: blue;
  }
  100% {
    transform: rotate(360deg) translate3D(-50%, -50%, 0);
    color: white;
  }
}
```

---

<a name="memahami-animasi-css"></a>
## 🎬 Memahami Animasi CSS (Sebelum JavaScript)

Sebelum kita migrasi ke JavaScript, mari pahami dulu animasi CSS yang ada:

```css
@keyframes roll {
  0% {
    transform: rotate(0) translate3D(-50%, -50%, 0);
    color: white;
  }
  30% {
    color: blue;
  }
  100% {
    transform: rotate(360deg) translate3D(-50%, -50%, 0);
    color: white;
  }
}
```

**📖 Penjelasan:**
- **0%**: Bola mulai putaran (0°) dengan warna putih
- **30%**: Warna berubah menjadi biru
- **100%**: Bola berputar penuh (360°) dan kembali putih

**💡 Tujuan kita:** Membuat animasi yang sama persis, tapi pakai JavaScript!

---

<a name="migrasi-ke-javascript"></a>
## ⚙️ Migrasi ke JavaScript

Sekarang mari kita buat animasi yang sama menggunakan JavaScript!

<a name="langkah-1-setup-awal"></a>
### 🔧 Langkah 1: Setup Awal

**Pertama**, comment dulu CSS animation di file `style.css`:

```css
#ball {
  /* animation: roll infinite 3s linear; */ /* ⬅️ COMMENT BARIS INI */
  color: white;
  /* ... kode lainnya ... */
}
```

**Kedua**, buat file `script.js` dan ambil semua elemen yang kita butuhkan:

```javascript
// 🎯 Ambil elemen bola
const ball = document.getElementById('ball');

// 🎮 Ambil semua tombol kontrol
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const reverse = document.getElementById('reverse');
const speedUp = document.getElementById('speed-up');
const slowDown = document.getElementById('slow-down');
```

**📌 Penjelasan:**
- `getElementById()`: Mengambil elemen HTML berdasarkan ID
- Kita simpan dalam variabel agar mudah digunakan nanti

---

<a name="langkah-2-membuat-array-animasi"></a>
### 🎨 Langkah 2: Membuat Array Animasi

Sekarang kita buat array yang berisi keyframe animasi:

```javascript
const rollAnimation = [
  {
    transform: 'rotate(0) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
  {
    color: 'blue',
    offset: 0.3,
  },
  {
    transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
];
```

**📌 Penjelasan:**

**Keyframe 1** (0%):
```javascript
{
  transform: 'rotate(0) translate3D(-50%, -50%, 0)',
  color: 'white',
}
```
- Posisi awal, putaran 0°, warna putih

**Keyframe 2** (30%):
```javascript
{
  color: 'blue',
  offset: 0.3,  // ⬅️ 0.3 = 30%
}
```
- Di tengah animasi (30%), warna jadi biru
- `offset: 0.3` artinya terjadi di 30% dari total durasi

**Keyframe 3** (100%):
```javascript
{
  transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
  color: 'white',
}
```
- Akhir animasi, putaran 360°, warna kembali putih

---

<a name="langkah-3-menambahkan-options"></a>
### ⚙️ Langkah 3: Menambahkan Options

Buat object untuk konfigurasi animasi:

```javascript
const rollOptions = {
  duration: 3000,        // ⬅️ Durasi 3000ms = 3 detik
  iterations: Infinity,  // ⬅️ Loop tak terbatas
};
```

**📌 Penjelasan:**
- `duration: 3000`: Animasi berjalan selama 3 detik
- `iterations: Infinity`: Animasi berulang terus-menerus

**💡 Tips:** Kamu bisa coba ubah `3000` jadi `1000` (lebih cepat) atau `5000` (lebih lambat)

---

<a name="langkah-4-menjalankan-animasi"></a>
### 🚀 Langkah 4: Menjalankan Animasi

Sekarang jalankan animasinya dengan method `.animate()`:

```javascript
const roll = ball.animate(rollAnimation, rollOptions);
```

**📌 Penjelasan:**
- `ball.animate()`: Method untuk membuat animasi pada elemen `ball`
- Parameter 1: `rollAnimation` (array keyframes)
- Parameter 2: `rollOptions` (konfigurasi)
- Hasilnya disimpan di variabel `roll` agar bisa dikontrol nanti

**🎉 Selamat!** Sekarang bola sudah berotasi dengan JavaScript!

---

<a name="menambahkan-kontrol-animasi"></a>
## 🎮 Menambahkan Kontrol Animasi

Sekarang kita tambahkan tombol-tombol kontrol!

<a name="langkah-5-play-dan-pause"></a>
### ▶️ Langkah 5: Play dan Pause

Tambahkan event listener untuk tombol pause dan play:

```javascript
// ⏸️ Tombol PAUSE
pause.addEventListener('click', () => {
  roll.pause();
});

// ▶️ Tombol PLAY
play.addEventListener('click', () => {
  roll.playbackRate = 1;  // ⬅️ Set kecepatan normal
  roll.play();
});
```

**📌 Penjelasan:**
- `addEventListener('click', ...)`: Mendengarkan klik pada tombol
- `roll.pause()`: Menghentikan animasi
- `roll.play()`: Melanjutkan animasi
- `roll.playbackRate = 1`: Mengatur kecepatan ke normal (penting untuk reverse)

**✨ Coba sekarang:** Klik pause untuk stop, klik play untuk lanjut!

---

<a name="langkah-6-reverse"></a>
### 🔄 Langkah 6: Reverse

Tambahkan fungsi reverse (putar balik):

```javascript
// 🔄 Tombol REVERSE
reverse.addEventListener('click', () => {
  roll.reverse();
});
```

**📌 Penjelasan:**
- `roll.reverse()`: Membalikkan arah animasi

**🤔 Cara kerja Reverse:**
1. Bola berputar 1x ➡️ klik reverse ➡️ putar balik 1x
2. Bola berputar 2x ➡️ klik reverse ➡️ putar balik 2x
3. Klik reverse lagi ➡️ berfungsi seperti toggle (maju-mundur-maju)

**💡 Tips:** Jika reverse tidak bisa di-play, pastikan kode play sudah ada `roll.playbackRate = 1`

---

<a name="langkah-7-speed-up-dan-slow-down"></a>
### ⚡ Langkah 7: Speed Up dan Slow Down

Terakhir, tambahkan kontrol kecepatan:

```javascript
// ⚡ Tombol SPEED UP (Percepat)
speedUp.addEventListener('click', () => {
  roll.playbackRate = roll.playbackRate * 2;
});

// 🐌 Tombol SLOW DOWN (Perlambat)
slowDown.addEventListener('click', () => {
  roll.playbackRate = roll.playbackRate * 0.5;
});
```

**📌 Penjelasan:**

**Speed Up:**
- `roll.playbackRate * 2`: Kecepatan dikali 2
- Contoh: 1 → 2 → 4 → 8 → 16 (semakin cepat!)

**Slow Down:**
- `roll.playbackRate * 0.5`: Kecepatan dikali 0.5 (setengahnya)
- Contoh: 1 → 0.5 → 0.25 → 0.125 (semakin lambat!)

**✨ Coba sekarang:** 
- Klik + beberapa kali = super cepat! 🚀
- Klik - beberapa kali = super lambat! 🐌

---

<a name="kode-lengkap"></a>
## 📝 Kode Lengkap

Berikut kode JavaScript lengkap (`script.js`):

```javascript
// 🎯 Ambil elemen
const ball = document.getElementById('ball');
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const reverse = document.getElementById('reverse');
const speedUp = document.getElementById('speed-up');
const slowDown = document.getElementById('slow-down');

// 🎨 Array animasi (keyframes)
const rollAnimation = [
  {
    transform: 'rotate(0) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
  {
    color: 'blue',
    offset: 0.3,
  },
  {
    transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
];

// ⚙️ Konfigurasi animasi
const rollOptions = {
  duration: 3000,
  iterations: Infinity,
};

// 🚀 Jalankan animasi
const roll = ball.animate(rollAnimation, rollOptions);

// 🎮 Event Listeners untuk kontrol

// ▶️ Play
play.addEventListener('click', () => {
  roll.playbackRate = 1;
  roll.play();
});

// ⏸️ Pause
pause.addEventListener('click', () => {
  roll.pause();
});

// 🔄 Reverse
reverse.addEventListener('click', () => {
  roll.reverse();
});

// ⚡ Speed Up
speedUp.addEventListener('click', () => {
  roll.playbackRate = roll.playbackRate * 2;
});

// 🐌 Slow Down
slowDown.addEventListener('click', () => {
  roll.playbackRate = roll.playbackRate * 0.5;
});
```

---

<a name="tantangan-extra"></a>
## 🏆 Tantangan Extra

Sekarang kamu sudah paham dasar-dasarnya! Coba tantangan ini:

### 💪 Level 1: Tampilkan Kecepatan
Tambahkan elemen HTML untuk menampilkan kecepatan saat ini:

```html
<div id="speed-display">Speed: 1x</div>
```

```javascript
// Update display setiap kali speed berubah
function updateSpeedDisplay() {
  const speedDisplay = document.getElementById('speed-display');
  speedDisplay.textContent = `Speed: ${roll.playbackRate}x`;
}

// Panggil di event speed up dan slow down
speedUp.addEventListener('click', () => {
  roll.playbackRate = roll.playbackRate * 2;
  updateSpeedDisplay(); // ⬅️ Tambahkan ini
});
```

### 💪 Level 2: Reset Button
Buat tombol untuk reset kecepatan ke normal (1x)

### 💪 Level 3: Slider Kecepatan
Gunakan `<input type="range">` untuk kontrol kecepatan lebih smooth!

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah belajar:

✅ Apa itu **Web Animations API**  
✅ Cara membuat **keyframe animations** dengan JavaScript  
✅ Mengontrol animasi dengan **play, pause, reverse**  
✅ Mengatur kecepatan dengan **playbackRate**  

**🔑 Konsep Penting yang Dipelajari:**
- `.animate(keyframes, options)` - Method utama untuk animasi
- `playbackRate` - Properti untuk mengatur kecepatan
- `.play()`, `.pause()`, `.reverse()` - Method kontrol animasi

**📚 Next Steps:**
- Coba buat animasi lain (fade in/out, slide, dll)
- Kombinasikan beberapa animasi sekaligus
- Pelajari tentang `Animation.finished` promise
- Eksplorasi CSS easing functions

---

**💡 Tips Belajar:**
- Ubah-ubah nilai `duration` dan lihat perbedaannya
- Tambahkan keyframe baru di array `rollAnimation`
- Eksperimen dengan properti CSS lainnya (opacity, scale, dll)

**Happy Coding! 🚀**

---

*Dokumentasi dibuat untuk pembelajaran pribadi*  
*Last updated: 2026*
