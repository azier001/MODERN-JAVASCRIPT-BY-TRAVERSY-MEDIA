# 🎨 Dokumentasi Web Animations API - Tutorial Lengkap untuk Pemula

---

## 📚 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Web Animations API?](#apa-itu-web-animations-api)
- [Persiapan Awal](#persiapan-awal)
  - [File HTML](#file-html)
  - [File CSS](#file-css)
- [Dari CSS ke JavaScript](#dari-css-ke-javascript)
  - [Memahami CSS Animation](#memahami-css-animation)
  - [Menghapus CSS Animation](#menghapus-css-animation)
- [Membuat Animasi dengan JavaScript](#membuat-animasi-dengan-javascript)
  - [Step 1: Mengambil Element](#step-1-mengambil-element)
  - [Step 2: Membuat Array Animasi](#step-2-membuat-array-animasi)
  - [Step 3: Mengatur Options](#step-3-mengatur-options)
  - [Step 4: Menjalankan Animasi](#step-4-menjalankan-animasi)
- [Menambahkan Kontrol](#menambahkan-kontrol)
  - [Play dan Pause](#play-dan-pause)
  - [Reverse (Mundur)](#reverse-mundur)
  - [Speed Up (Mempercepat)](#speed-up-mempercepat)
  - [Slow Down (Memperlambat)](#slow-down-memperlambat)
- [Kode Lengkap](#kode-lengkap)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Selamat datang di dokumentasi **Web Animations API**! 🎉

Dalam tutorial ini, kita akan belajar cara membuat animasi yang tadinya dibuat dengan **CSS** menjadi animasi yang dikontrol dengan **JavaScript**. Kita akan menganimasikan sebuah **bola yang berputar dan berubah warna**, plus menambahkan tombol kontrol seperti:

- ▶️ **Play** (Mainkan)
- ⏸️ **Pause** (Jeda)
- 🔄 **Reverse** (Putar Balik)
- ⚡ **Speed Up** (Percepat)
- 🐌 **Slow Down** (Perlambat)

---

<a name="apa-itu-web-animations-api"></a>
## 📖 Apa itu Web Animations API?

**Web Animations API** adalah cara untuk membuat dan mengontrol animasi menggunakan JavaScript, bukan CSS. 

🔑 **Kenapa pakai JavaScript?**
- Lebih **fleksibel** - kita bisa kontrol animasi dengan tombol
- Lebih **interaktif** - bisa pause, play, reverse kapan saja
- Lebih **powerful** - bisa ubah kecepatan secara dinamis

---

<a name="persiapan-awal"></a>
## 🛠️ Persiapan Awal

<a name="file-html"></a>
### 📄 File HTML

Berikut adalah struktur HTML yang kita gunakan:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Web Animations API</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="style.css" />
    <script src="script.js" defer></script>
  </head>
  <body>
    <div class="wrapper">
      <div class="controls">
        <button id="play"><i class="fa-solid fa-play"></i></button>
        <button id="pause"><i class="fa-solid fa-pause"></i></button>
        <button id="reverse">
          <i class="fa-solid fa-clock-rotate-left"></i>
        </button>
        <button id="speed-up"><i class="fa-solid fa-plus"></i></button>
        <button id="slow-down"><i class="fa-solid fa-minus"></i></button>
      </div>
      <div id="ball">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="194"
          height="194"
          version="1.1"
        >
          <circle fill="#000000" cx="97" cy="97" r="97" />
          <path
            fill="#ffffff"
            d="m 94,9.2 a 88,88 0 0 0 -55,21.8 l 27,0 28,-14.4 0,-7.4 z m 6,0 0,7.4 28,14.4 27,0 a 88,88 0 0 0 -55,-21.8 z m -67.2,27.8 a 88,88 0 0 0 -20,34.2 l 16,27.6 23,-3.6 21,-36.2 -8.4,-22 -31.6,0 z m 96.8,0 -8.4,22 21,36.2 23,3.6 15.8,-27.4 a 88,88 0 0 0 -19.8,-34.4 l -31.6,0 z m -50,26 -20.2,35.2 17.8,30.8 39.6,0 17.8,-30.8 -20.2,-35.2 -34.8,0 z m -68.8,16.6 a 88,88 0 0 0 -1.8,17.4 88,88 0 0 0 10.4,41.4 l 7.4,-4.4 -1.4,-29 -14.6,-25.4 z m 172.4,0.2 -14.6,25.2 -1.4,29 7.4,4.4 a 88,88 0 0 0 10.4,-41.4 88,88 0 0 0 -1.8,-17.2 z m -106,57.2 -15.4,19 L 77.2,182.6 a 88,88 0 0 0 19.8,2.4 88,88 0 0 0 19.8,-2.4 l 15.4,-26.6 -15.4,-19 -39.6,0 z m -47.8,2.6 -7,4 A 88,88 0 0 0 68.8,180.4 l -14,-24.6 -25.4,-16.2 z m 135.2,0 -25.4,16.2 -14,24.4 a 88,88 0 0 0 46.4,-36.6 l -7,-4 z"
          />
        </svg>
      </div>
    </div>
  </body>
</html>
```

**📌 Penjelasan:**
- Kita punya 5 tombol kontrol dengan ID: `play`, `pause`, `reverse`, `speed-up`, `slow-down`
- Ada elemen `<div id="ball">` yang berisi **SVG bola sepak** ⚽
- Font Awesome digunakan untuk icon tombol

---

<a name="file-css"></a>
### 🎨 File CSS

```css
body {
  background: #000;
  color: #fff;
}

html,
body {
  height: 100%;
}

.wrapper {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

#ball {
  /* animation: roll infinite 3s linear; */
  color: white;
  width: 25%;
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: 0 0;
  transform: rotate(0) translate3D(-50%, -50%, 0);
  backface-visibility: hidden;
  will-change: transform, color;
}

path {
  fill: currentColor;
}

.controls {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 20px;
}

button {
  padding: 10px 20px;
  margin: 5px;
  background: #333;
  color: #fff;
  font-size: 18px;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background: blue;
}

@keyframes roll {
  0% {
    transform: rotate(0) translate3D(-50%, -50%, 0);
    color: white;
  }
  30% {
    color: blue;
  }
  100% {
    transform: rotate(360deg) translate3D(-50%, -50%, 0);
    color: white;
  }
}
```

**📌 Penjelasan:**
- Background hitam dengan text putih
- Bola diposisikan di tengah layar
- Ada `@keyframes roll` yang **akan kita ganti dengan JavaScript**

---

<a name="dari-css-ke-javascript"></a>
## 🔄 Dari CSS ke JavaScript

<a name="memahami-css-animation"></a>
### 🎬 Memahami CSS Animation

Di CSS, animasi bola didefinisikan dengan **keyframes**:

```css
#ball {
  animation: roll infinite 3s linear;
}

@keyframes roll {
  0% {
    transform: rotate(0) translate3D(-50%, -50%, 0);
    color: white;
  }
  30% {
    color: blue;
  }
  100% {
    transform: rotate(360deg) translate3D(-50%, -50%, 0);
    color: white;
  }
}
```

**📝 Artinya:**
- **0%**: Mulai dengan rotasi 0° dan warna putih
- **30%**: Warna berubah menjadi biru
- **100%**: Rotasi penuh 360° dan kembali putih
- **infinite**: Animasi terus berulang
- **3s**: Durasi 3 detik
- **linear**: Kecepatan konstan

---

<a name="menghapus-css-animation"></a>
### ❌ Menghapus CSS Animation

Langkah pertama, **comment** atau hapus baris animasi CSS:

```css
#ball {
  /* animation: roll infinite 3s linear; */  /* ← Di-comment */
  color: white;
  width: 25%;
  /* ... */
}
```

✅ Sekarang bola akan **berhenti beranimasi** karena tidak ada CSS animation lagi.

---

<a name="membuat-animasi-dengan-javascript"></a>
## 💻 Membuat Animasi dengan JavaScript

<a name="step-1-mengambil-element"></a>
### 📍 Step 1: Mengambil Element

Pertama, kita ambil semua element yang dibutuhkan:

```javascript
const ball = document.getElementById('ball');
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const reverse = document.getElementById('reverse');
const speedUp = document.getElementById('speed-up');
const slowDown = document.getElementById('slow-down');
```

**📌 Penjelasan:**
- `ball` → Element bola yang akan dianimasikan
- `play`, `pause`, dll → Tombol-tombol kontrol

---

<a name="step-2-membuat-array-animasi"></a>
### 🎨 Step 2: Membuat Array Animasi

Sekarang kita buat **array yang berisi keyframes animasi**:

```javascript
const rollAnimation = [
  {
    transform: 'rotate(0) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
  {
    color: 'blue',
    offset: 0.3,
  },
  {
    transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
];
```

**📌 Penjelasan:**

🔹 **Object 1** (Keyframe 0%):
```javascript
{
  transform: 'rotate(0) translate3D(-50%, -50%, 0)',
  color: 'white',
}
```
- Rotasi mulai dari 0°
- Warna putih

🔹 **Object 2** (Keyframe 30%):
```javascript
{
  color: 'blue',
  offset: 0.3,  // ← Artinya di posisi 30% dari durasi
}
```
- Warna berubah jadi biru
- `offset: 0.3` = 30% dari durasi total

🔹 **Object 3** (Keyframe 100%):
```javascript
{
  transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
  color: 'white',
}
```
- Rotasi penuh 360°
- Warna kembali putih

> 💡 **Catatan**: Property CSS harus ditulis sebagai **string** dalam JavaScript!

---

<a name="step-3-mengatur-options"></a>
### ⚙️ Step 3: Mengatur Options

Selanjutnya, buat **object untuk mengatur durasi dan pengulangan**:

```javascript
const rollOptions = {
  duration: 3000,        // 3000 milliseconds = 3 detik
  iterations: Infinity,  // Animasi terus berulang
};
```

**📌 Penjelasan:**
- `duration: 3000` → Durasi animasi **3 detik** (dalam milidetik)
- `iterations: Infinity` → Animasi **berulang terus** (seperti `infinite` di CSS)

---

<a name="step-4-menjalankan-animasi"></a>
### ▶️ Step 4: Menjalankan Animasi

Terakhir, **jalankan animasi** dengan method `.animate()`:

```javascript
const roll = ball.animate(rollAnimation, rollOptions);
```

**📌 Penjelasan:**
- `ball.animate()` → Method untuk membuat animasi
- Parameter 1: `rollAnimation` → Array keyframes
- Parameter 2: `rollOptions` → Object konfigurasi
- Hasil disimpan di variable `roll` → Kita bisa kontrol animasi ini nanti!

🎉 **Sekarang bola sudah beranimasi dengan JavaScript!**

---

<a name="menambahkan-kontrol"></a>
## 🎮 Menambahkan Kontrol

<a name="play-dan-pause"></a>
### ▶️⏸️ Play dan Pause

Tambahkan event listener untuk tombol Play dan Pause:

```javascript
play.addEventListener('click', () => {
  roll.playbackRate = 1;
  roll.play();
});

pause.addEventListener('click', () => roll.pause());
```

**📌 Penjelasan:**

🔹 **Tombol Play**:
```javascript
roll.playbackRate = 1;  // Set kecepatan normal (1x)
roll.play();             // Mainkan animasi
```
- `playbackRate = 1` → Pastikan kecepatan normal
- `play()` → Method untuk **memainkan** animasi

🔹 **Tombol Pause**:
```javascript
roll.pause();  // Jeda animasi
```
- `pause()` → Method untuk **menghentikan** sementara

> 💡 Mirip dengan Audio/Video API di HTML5!

---

<a name="reverse-mundur"></a>
### 🔄 Reverse (Mundur)

Tambahkan fungsi untuk **memutar balik animasi**:

```javascript
reverse.addEventListener('click', () => roll.reverse());
```

**📌 Penjelasan:**
- `reverse()` → Method untuk **membalik arah** animasi

**🤔 Cara Kerja Reverse:**

1. Jika bola sudah berputar **1 kali** → klik reverse → bola mundur **1 kali** lalu berhenti
2. Jika bola sudah berputar **2 kali** → klik reverse → bola mundur **2 kali** lalu berhenti
3. **Klik reverse lagi** → bola maju lagi (toggle)

⚠️ **Masalah**: Setelah reverse, tombol Play tidak berfungsi!

**✅ Solusi**: Update kode Play seperti ini:

```javascript
play.addEventListener('click', () => {
  roll.playbackRate = 1;  // ← Reset kecepatan ke 1 (maju)
  roll.play();
});
```

Sekarang tombol Play bisa **mengembalikan arah** setelah reverse!

---

<a name="speed-up-mempercepat"></a>
### ⚡ Speed Up (Mempercepat)

Tambahkan fungsi untuk **mempercepat animasi**:

```javascript
speedUp.addEventListener(
  'click',
  () => (roll.playbackRate = roll.playbackRate * 2)
);
```

**📌 Penjelasan:**
- `playbackRate` → Property yang mengatur **kecepatan animasi**
- `* 2` → Kecepatan **dikali 2** (2x lebih cepat)

**🔢 Contoh:**
- Kecepatan awal: `1` (normal)
- Klik 1x: `1 * 2 = 2` (2x lebih cepat)
- Klik lagi: `2 * 2 = 4` (4x lebih cepat)
- Klik lagi: `4 * 2 = 8` (8x lebih cepat) 🚀

---

<a name="slow-down-memperlambat"></a>
### 🐌 Slow Down (Memperlambat)

Tambahkan fungsi untuk **memperlambat animasi**:

```javascript
slowDown.addEventListener(
  'click',
  () => (roll.playbackRate = roll.playbackRate * 0.5)
);
```

**📌 Penjelasan:**
- `* 0.5` → Kecepatan **dikali 0.5** (setengah kecepatan)

**🔢 Contoh:**
- Kecepatan awal: `1` (normal)
- Klik 1x: `1 * 0.5 = 0.5` (setengah kecepatan)
- Klik lagi: `0.5 * 0.5 = 0.25` (seperempat kecepatan)
- Klik terus: animasi bisa **hampir berhenti** 🐢

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap

### 📄 `script.js` - Final Version

```javascript
const ball = document.getElementById('ball');
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const reverse = document.getElementById('reverse');
const speedUp = document.getElementById('speed-up');
const slowDown = document.getElementById('slow-down');

const rollAnimation = [
  {
    transform: 'rotate(0) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
  {
    color: 'blue',
    offset: 0.3,
  },
  {
    transform: 'rotate(360deg) translate3D(-50%, -50%, 0)',
    color: 'white',
  },
];

const rollOptions = {
  duration: 3000,
  iterations: Infinity,
};

const roll = ball.animate(rollAnimation, rollOptions);

play.addEventListener('click', () => {
  roll.playbackRate = 1;
  roll.play();
});

pause.addEventListener('click', () => roll.pause());

reverse.addEventListener('click', () => roll.reverse());

speedUp.addEventListener(
  'click',
  () => (roll.playbackRate = roll.playbackRate * 2)
);

slowDown.addEventListener(
  'click',
  () => (roll.playbackRate = roll.playbackRate * 0.5)
);
```

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah belajar **Web Animations API** dari nol!

### ✅ Yang Sudah Kita Pelajari:

1. ✨ **Membuat animasi dengan JavaScript** (bukan CSS)
2. 🎮 **Mengontrol animasi** dengan tombol Play, Pause, Reverse
3. ⚡ **Mengubah kecepatan** animasi secara dinamis
4. 🔧 **Menggunakan method**: `animate()`, `play()`, `pause()`, `reverse()`
5. 📊 **Menggunakan property**: `playbackRate`

### 🚀 Tantangan Tambahan (Extra Credit):

💡 **Ide**: Tambahkan tampilan kecepatan!
- Buat elemen untuk **menampilkan angka kecepatan**
- Update angkanya setiap kali `playbackRate` berubah
- Contoh: "Speed: 2x", "Speed: 0.5x"

### 📚 Ringkasan Method & Property:

| Method/Property | Fungsi |
|----------------|--------|
| `element.animate(keyframes, options)` | Membuat animasi |
| `.play()` | Mainkan animasi |
| `.pause()` | Jeda animasi |
| `.reverse()` | Balik arah animasi |
| `.playbackRate` | Atur kecepatan (1 = normal) |

---

**🎯 Sekarang kamu sudah menguasai dasar-dasar Web Animations API!** 

Selamat bereksperimen! 🚀✨

---

📝 *Dokumentasi ini dibuat untuk pembelajaran pribadi - Web Animations API Tutorial*
