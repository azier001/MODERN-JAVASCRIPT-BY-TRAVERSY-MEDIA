# 🎬 Custom Video Player - Dokumentasi Lengkap untuk Pemula

> **Dokumentasi ini akan membantu Anda memahami cara membuat video player kustom menggunakan HTML, CSS, dan JavaScript dari nol!**

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan Project](#persiapan-project)
- [Struktur HTML](#struktur-html)
  - [Mengimport Font Awesome](#mengimport-font-awesome)
  - [Element Video](#element-video)
  - [Controls Container](#controls-container)
- [Styling CSS](#styling-css)
- [JavaScript - Bagian 1: Persiapan](#javascript-bagian-1-persiapan)
  - [Mengambil Semua Element](#mengambil-semua-element)
- [JavaScript - Bagian 2: Play & Pause](#javascript-bagian-2-play-pause)
  - [Fungsi playPause()](#fungsi-playpause)
  - [Event Listener untuk Video dan Play Button](#event-listener-untuk-video-dan-play-button)
- [JavaScript - Bagian 3: Update Icon](#javascript-bagian-3-update-icon)
  - [Fungsi updateIcon()](#fungsi-updateicon)
  - [Event Listener untuk Play dan Pause](#event-listener-untuk-play-dan-pause)
- [JavaScript - Bagian 4: Stop Video](#javascript-bagian-4-stop-video)
  - [Fungsi stopVideo()](#fungsi-stopvideo)
- [JavaScript - Bagian 5: Progress Bar](#javascript-bagian-5-progress-bar)
  - [Fungsi updateProgress()](#fungsi-updateprogress)
  - [Menghitung Minutes dan Seconds](#menghitung-minutes-dan-seconds)
  - [Menambahkan Leading Zero](#menambahkan-leading-zero)
  - [Update Timestamp](#update-timestamp)
- [JavaScript - Bagian 6: Set Progress](#javascript-bagian-6-set-progress)
  - [Fungsi setProgress()](#fungsi-setprogress)
- [Kode Lengkap](#kode-lengkap)
  - [index.html](#indexhtml)
  - [style.css](#stylecss)
  - [script.js](#scriptjs)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan membuat **custom video player** yang memiliki fitur-fitur seperti:

✅ **Play/Pause** - memutar dan menjeda video  
✅ **Stop** - menghentikan video dan kembali ke awal  
✅ **Progress Bar** - menampilkan progress video yang sedang diputar  
✅ **Timestamp** - menampilkan waktu video dalam format MM:SS  
✅ **Clickable Progress Bar** - bisa klik di progress bar untuk loncat ke bagian tertentu

Project ini **sangat mirip dengan Audio Player API**, jadi jika Anda sudah familiar dengan itu, ini akan lebih mudah! 🚀

---

<a name="persiapan-project"></a>
## 📦 Persiapan Project

Sebelum memulai, pastikan Anda sudah menyiapkan:

1. **Folder project** dengan struktur seperti ini:
   ```
   custom-video-player/
   ├── index.html
   ├── style.css
   ├── script.js
   └── media/
       ├── clouds.mov (video Anda)
       └── poster.png (thumbnail video)
   ```

2. **File video** - bisa format .mov, .mp4, atau format video lainnya
3. **Poster image** - gambar yang akan muncul sebelum video diputar

---

<a name="struktur-html"></a>
## 🏗️ Struktur HTML

<a name="mengimport-font-awesome"></a>
### 📌 Mengimport Font Awesome

```html
<link
  href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
  rel="stylesheet"
/>
```

**Kenapa kita butuh Font Awesome?**  
Font Awesome menyediakan **icon-icon** seperti ▶️ (play), ⏸️ (pause), dan ⏹️ (stop) yang kita gunakan di tombol-tombol player kita.

---

<a name="element-video"></a>
### 🎥 Element Video

```html
<video
  src="media/clouds.mov"
  poster="media/poster.png"
  id="video"
  class="screen"
></video>
```

**Penjelasan:**
- `src` - lokasi file video
- `poster` - gambar thumbnail yang muncul sebelum video diputar
- `id="video"` - ID untuk diakses di JavaScript
- `class="screen"` - class untuk styling CSS

---

<a name="controls-container"></a>
### 🎮 Controls Container

```html
<div class="controls">
  <button class="btn" id="play">
    <i class="fa fa-play fa-2x"></i>
  </button>
  <button class="btn" id="stop">
    <i class="fa fa-stop fa-2x"></i>
  </button>
  <input
    type="range"
    id="progress"
    class="progress"
    min="0"
    max="100"
    step="0.1"
    value="0"
  />
  <span class="timestamp" id="timestamp">00:00</span>
</div>
```

**Penjelasan setiap bagian:**

1. **Play Button** - tombol untuk play/pause video
2. **Stop Button** - tombol untuk stop dan reset video ke awal
3. **Progress Bar** - menggunakan `<input type="range">` untuk progress bar
   - `min="0"` dan `max="100"` - range dari 0-100%
   - `step="0.1"` - pergerakan progress yang halus
4. **Timestamp** - menampilkan waktu dalam format `00:00`

---

<a name="styling-css"></a>
## 🎨 Styling CSS

CSS sudah disediakan lengkap untuk:
- ✨ Styling video player dengan border radius
- 🎨 Background color steelblue
- 🖱️ Custom range slider dengan styling khusus untuk WebKit, Firefox, dan IE
- 📱 Responsive design untuk layar kecil

**Highlight penting dari CSS:**

```css
.screen {
  cursor: pointer;
  width: 60%;
  background-color: #000 !important;
  border-top-left-radius: 10px;
  border-top-right-radius: 10px;
}

.controls {
  background: #333;
  color: #fff;
  width: 60%;
  border-bottom-left-radius: 10px;
  border-bottom-right-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 10px;
}
```

**Catatan:** CSS untuk `input[type='range']` sangat panjang karena setiap browser (Chrome, Firefox, IE) memiliki styling yang berbeda! 🎯

---

<a name="javascript-bagian-1-persiapan"></a>
## ⚙️ JavaScript - Bagian 1: Persiapan

<a name="mengambil-semua-element"></a>
### 📍 Mengambil Semua Element

Langkah pertama adalah **mengambil semua element HTML** yang akan kita gunakan:

```javascript
const video = document.getElementById('video');
const play = document.getElementById('play');
const stop = document.getElementById('stop');
const progress = document.getElementById('progress');
const timestamp = document.getElementById('timestamp');
```

**Penjelasan:**
- `document.getElementById()` - mengambil element berdasarkan ID
- Kita simpan di variable untuk digunakan nanti
- Total ada **5 element** yang kita ambil

💡 **Tips:** Pastikan ID di HTML sama dengan yang kita tulis di JavaScript!

---

<a name="javascript-bagian-2-play-pause"></a>
## ▶️ JavaScript - Bagian 2: Play & Pause

<a name="fungsi-playpause"></a>
### 🎯 Fungsi playPause()

```javascript
function playPause() {
  if (video.paused) {
    video.play();
  } else {
    video.pause();
  }
}
```

**Cara kerjanya:**
1. ✅ Cek apakah video sedang **paused** (berhenti)
2. ✅ Jika `video.paused` bernilai `true` → panggil `video.play()` untuk memutar
3. ✅ Jika `video.paused` bernilai `false` → panggil `video.pause()` untuk menjeda

**Property penting:**
- `video.paused` - property yang mengembalikan `true` atau `false`
- `video.play()` - method untuk memutar video
- `video.pause()` - method untuk menjeda video

---

<a name="event-listener-untuk-video-dan-play-button"></a>
### 🖱️ Event Listener untuk Video dan Play Button

```javascript
video.addEventListener('click', playPause);
play.addEventListener('click', playPause);
```

**Penjelasan:**
- Kita ingin video bisa diputar dengan **2 cara**:
  1. 🖱️ Klik di **video** itu sendiri
  2. 🖱️ Klik di **tombol play**
- Kedua cara ini akan memanggil fungsi `playPause()` yang sama

💡 **Ini memberikan pengalaman user yang lebih baik!**

---

<a name="javascript-bagian-3-update-icon"></a>
## 🔄 JavaScript - Bagian 3: Update Icon

<a name="fungsi-updateicon"></a>
### 🎨 Fungsi updateIcon()

```javascript
function updateIcon() {
  if (video.paused) {
    play.innerHTML = '<i class="fa fa-play fa-2x"></i>';
  } else {
    play.innerHTML = '<i class="fa fa-pause fa-2x"></i>';
  }
}
```

**Cara kerjanya:**
1. ✅ Cek apakah video sedang paused
2. ✅ Jika **paused** → tampilkan icon **PLAY** (▶️)
3. ✅ Jika **playing** → tampilkan icon **PAUSE** (⏸️)

**Teknik yang digunakan:**
- Kita menggunakan `innerHTML` untuk mengubah konten HTML di dalam tombol
- Icon menggunakan class Font Awesome:
  - `fa-play` untuk icon play
  - `fa-pause` untuk icon pause
  - `fa-2x` untuk ukuran 2x lebih besar

**📝 Catatan:** Ini berbeda dari tutorial audio player sebelumnya, tapi cara ini lebih simple!

---

<a name="event-listener-untuk-play-dan-pause"></a>
### 🎧 Event Listener untuk Play dan Pause

```javascript
video.addEventListener('play', updateIcon);
video.addEventListener('pause', updateIcon);
```

**Penjelasan:**
- Event `'play'` - dipicu ketika video **mulai diputar**
- Event `'pause'` - dipicu ketika video **dijeda**
- Kedua event ini akan memanggil `updateIcon()` untuk mengubah icon

💡 **Event ini otomatis dipicu oleh browser, kita tinggal "mendengarkan" saja!**

---

<a name="javascript-bagian-4-stop-video"></a>
## ⏹️ JavaScript - Bagian 4: Stop Video

<a name="fungsi-stopvideo"></a>
### 🛑 Fungsi stopVideo()

```javascript
function stopVideo() {
  video.currentTime = 0;
  video.pause();
}
```

**Cara kerjanya:**
1. ✅ Set `video.currentTime = 0` → **reset video ke detik 0** (awal)
2. ✅ Panggil `video.pause()` → **pause video**

**Perbedaan Stop vs Pause:**
- **Pause** → video berhenti di waktu saat ini
- **Stop** → video berhenti DAN kembali ke awal

```javascript
stop.addEventListener('click', stopVideo);
```

Ketika tombol stop diklik, fungsi `stopVideo()` akan dipanggil! 🎯

---

<a name="javascript-bagian-5-progress-bar"></a>
## 📊 JavaScript - Bagian 5: Progress Bar

<a name="fungsi-updateprogress"></a>
### 📈 Fungsi updateProgress()

```javascript
function updateProgress() {
  progress.value = (video.currentTime / video.duration) * 100;
  
  // ... kode untuk timestamp
}
```

**Cara menghitung progress:**
```
Progress (%) = (Current Time / Duration) × 100
```

**Contoh:**
- Video duration: 60 detik
- Current time: 30 detik
- Progress = (30 / 60) × 100 = **50%**

**Property penting:**
- `video.currentTime` - waktu video saat ini (dalam detik)
- `video.duration` - total durasi video (dalam detik)
- `progress.value` - nilai dari input range (0-100)

```javascript
video.addEventListener('timeupdate', updateProgress);
```

**Event `'timeupdate'`** → dipicu secara berkala saat video berjalan, perfect untuk update progress! ⏱️

---

<a name="menghitung-minutes-dan-seconds"></a>
### ⏰ Menghitung Minutes dan Seconds

```javascript
// Get minutes
let minutes = Math.floor(video.currentTime / 60);
```

**Penjelasan:**
- `video.currentTime` dalam **detik** (contoh: 125 detik)
- Dibagi 60 untuk dapat **menit** (125 / 60 = 2.08333...)
- `Math.floor()` untuk **membulatkan ke bawah** (2.08333 → 2)

```javascript
let seconds = Math.floor(video.currentTime % 60);
```

**Operator Modulus (%):**
- `%` mengambil **sisa pembagian**
- Contoh: 125 % 60 = **5** (sisa setelah 125 dibagi 60)
- `Math.floor()` untuk membulatkan ke bawah

💡 **Jadi, 125 detik = 2 menit 5 detik!**

---

<a name="menambahkan-leading-zero"></a>
### 0️⃣ Menambahkan Leading Zero

```javascript
if (minutes < 10) {
  minutes = '0' + String(minutes);
}
```

**Kenapa kita butuh ini?**
- Tanpa leading zero: `2:5` ❌
- Dengan leading zero: `02:05` ✅

**Cara kerjanya:**
1. Cek apakah angka **< 10** (single digit)
2. Jika ya, tambahkan `'0'` di depan
3. Convert ke String dan concatenate

**Contoh:**
- `5` → `'0' + '5'` → `'05'`
- `15` → tetap `'15'` (tidak perlu zero)

```javascript
if (seconds < 10) {
  seconds = '0' + String(seconds);
}
```

Sama untuk seconds! 🔢

---

<a name="update-timestamp"></a>
### 🕐 Update Timestamp

```javascript
timestamp.innerHTML = `${minutes}:${seconds}`;
```

**Template Literal:**
- Menggunakan backticks `` ` `` (bukan quote biasa)
- `${variableName}` untuk memasukkan variable
- Hasil: `"02:05"`, `"00:30"`, dll.

**Contoh output:**
- `00:00` - video di awal
- `01:30` - 1 menit 30 detik
- `05:45` - 5 menit 45 detik

---

<a name="javascript-bagian-6-set-progress"></a>
## 🎯 JavaScript - Bagian 6: Set Progress

<a name="fungsi-setprogress"></a>
### 🖱️ Fungsi setProgress()

Sekarang kita ingin bisa **klik di progress bar** untuk loncat ke bagian tertentu di video!

```javascript
function setProgress() {
  video.currentTime = (+progress.value * video.duration) / 100;
}
```

**Breakdown formula:**
```
Current Time = (Progress Value × Duration) / 100
```

**Penjelasan:**
- `progress.value` - nilai dari slider (0-100) sebagai **STRING**
- `+progress.value` - **unary plus operator** mengconvert ke NUMBER
- Kalikan dengan `video.duration` untuk dapat waktu dalam detik
- Bagi 100 karena progress dalam persen

**Contoh:**
- Video duration: 120 detik
- User klik di 50% progress bar
- Current time = (50 × 120) / 100 = **60 detik**

```javascript
progress.addEventListener('click', setProgress);
```

Ketika user klik di progress bar, video akan loncat ke posisi itu! 🎯

---

**⚠️ Catatan tentang Range Input:**

Progress bar ini menggunakan `<input type="range">`, yang bisa **sedikit wonky** (tidak presisi). 

**Alternatif lebih baik:**
- Gunakan `<div>` seperti di audio player tutorial
- Set width berdasarkan percentage
- Lebih smooth dan presisi!

Tapi untuk belajar, cara ini sudah cukup bagus! 😊

---

<a name="kode-lengkap"></a>
## 📝 Kode Lengkap

<a name="indexhtml"></a>
### 📄 index.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Custom Video Player</title>
    <link
      href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
      rel="stylesheet"
      integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="style.css" />
    <script src="script.js" defer></script>
  </head>
  <body>
    <video
      src="media/clouds.mov"
      poster="media/poster.png"
      id="video"
      class="screen"
    ></video>
    <div class="controls">
      <button class="btn" id="play">
        <i class="fa fa-play fa-2x"></i>
      </button>
      <button class="btn" id="stop">
        <i class="fa fa-stop fa-2x"></i>
      </button>
      <input
        type="range"
        id="progress"
        class="progress"
        min="0"
        max="100"
        step="0.1"
        value="0"
      />
      <span class="timestamp" id="timestamp">00:00</span>
    </div>
  </body>
</html>
```

---

<a name="stylecss"></a>
### 🎨 style.css

```css
@import url('https://fonts.googleapis.com/css?family=Questrial&display=swap');

* {
  box-sizing: border-box;
}

body {
  font-family: 'Questrial', sans-serif;
  background-color: steelblue;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  max-height: 100vh;
  margin: 30px 0;
}

h1 {
  color: #fff;
}

.screen {
  cursor: pointer;
  width: 60%;
  background-color: #000 !important;
  border-top-left-radius: 10px;
  border-top-right-radius: 10px;
}

.controls {
  background: #333;
  color: #fff;
  width: 60%;
  border-bottom-left-radius: 10px;
  border-bottom-right-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 10px;
}

.controls .btn {
  border: 0;
  background: transparent;
  cursor: pointer;
}

.controls .fa-play {
  color: #28a745;
}

.controls .fa-stop {
  color: #dc3545;
}

.controls .fa-pause {
  color: #fff;
}

.controls .timestamp {
  color: #fff;
  font-weight: bold;
  margin-left: 10px;
}

.btn:focus {
  outline: 0;
}

input[type='range'] {
  -webkit-appearance: none;
  width: 100%;
  background: transparent;
}

input[type='range']::-webkit-slider-thumb {
  -webkit-appearance: none;
}

input[type='range']:focus {
  outline: none;
}

input[type='range']::-ms-track {
  width: 100%;
  cursor: pointer;
  background: transparent;
  border-color: transparent;
  color: transparent;
}

/* Special styling for WebKit/Blink */
input[type='range']::-webkit-slider-thumb {
  -webkit-appearance: none;
  border: 1px solid #000000;
  height: 36px;
  width: 16px;
  border-radius: 3px;
  background: #ffffff;
  cursor: pointer;
  margin-top: -14px;
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
}

/* All the same stuff for Firefox */
input[type='range']::-moz-range-thumb {
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
  border: 1px solid #000000;
  height: 36px;
  width: 16px;
  border-radius: 3px;
  background: #ffffff;
  cursor: pointer;
}

/* All the same stuff for IE */
input[type='range']::-ms-thumb {
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
  border: 1px solid #000000;
  height: 36px;
  width: 16px;
  border-radius: 3px;
  background: #ffffff;
  cursor: pointer;
}

input[type='range']::-webkit-slider-runnable-track {
  width: 100%;
  height: 8.4px;
  cursor: pointer;
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
  background: #3071a9;
  border-radius: 1.3px;
  border: 0.2px solid #010101;
}

input[type='range']:focus::-webkit-slider-runnable-track {
  background: #367ebd;
}

input[type='range']::-moz-range-track {
  width: 100%;
  height: 8.4px;
  cursor: pointer;
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
  background: #3071a9;
  border-radius: 1.3px;
  border: 0.2px solid #010101;
}

input[type='range']::-ms-track {
  width: 100%;
  height: 8.4px;
  cursor: pointer;
  background: transparent;
  border-color: transparent;
  border-width: 16px 0;
  color: transparent;
}

input[type='range']::-ms-fill-lower {
  background: #2a6495;
  border: 0.2px solid #010101;
  border-radius: 2.6px;
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
}

input[type='range']:focus::-ms-fill-lower {
  background: #3071a9;
}

input[type='range']::-ms-fill-upper {
  background: #3071a9;
  border: 0.2px solid #010101;
  border-radius: 2.6px;
  box-shadow: 1px 1px 1px #000000, 0px 0px 1px #0d0d0d;
}

input[type='range']:focus::-ms-fill-upper {
  background: #367ebd;
}

@media (max-width: 800px) {
  .screen,
  .controls {
    width: 90%;
  }
}
```

---

<a name="scriptjs"></a>
### ⚡ script.js

```javascript
const video = document.getElementById('video');
const play = document.getElementById('play');
const stop = document.getElementById('stop');
const progress = document.getElementById('progress');
const timestamp = document.getElementById('timestamp');

function playPause() {
  if (video.paused) {
    video.play();
  } else {
    video.pause();
  }
}

function stopVideo() {
  video.currentTime = 0;
  video.pause();
}

function updateIcon() {
  if (video.paused) {
    play.innerHTML = '<i class="fa fa-play fa-2x"></i>';
  } else {
    play.innerHTML = '<i class="fa fa-pause fa-2x"></i>';
  }
}

function updateProgress() {
  progress.value = (video.currentTime / video.duration) * 100;

  // Get minutes
  let minutes = Math.floor(video.currentTime / 60);
  if (minutes < 10) {
    minutes = '0' + String(minutes);
  }

  let seconds = Math.floor(video.currentTime % 60);
  if (seconds < 10) {
    seconds = '0' + String(seconds);
  }

  timestamp.innerHTML = `${minutes}:${seconds}`;
}

function setProgress() {
  video.currentTime = (+progress.value * video.duration) / 100;
}

video.addEventListener('click', playPause);
video.addEventListener('play', updateIcon);
video.addEventListener('pause', updateIcon);
video.addEventListener('timeupdate', updateProgress);

play.addEventListener('click', playPause);
stop.addEventListener('click', stopVideo);
progress.addEventListener('click', setProgress);
```

---

<a name="kesimpulan"></a>
## 🎉 Kesimpulan

**Selamat!** Anda sudah berhasil membuat custom video player dengan fitur lengkap! 🎊

### 📚 Apa yang sudah kita pelajari:

✅ Cara menggunakan **HTML5 Video API**  
✅ Property penting: `currentTime`, `duration`, `paused`  
✅ Method penting: `play()`, `pause()`  
✅ Event penting: `'play'`, `'pause'`, `'timeupdate'`  
✅ Cara membuat **progress bar** dengan range input  
✅ Cara menghitung **minutes dan seconds** dari detik  
✅ Cara menambahkan **leading zero** untuk format waktu  
✅ Cara mengubah **icon** secara dinamis dengan JavaScript  
✅ Cara membuat video **interactive** (clickable)

### 🚀 Next Steps:

Anda bisa mengembangkan project ini dengan:
- 🔊 Menambahkan **volume control**
- ⏩ Menambahkan tombol **forward/backward**
- 📺 Menambahkan **fullscreen mode**
- 🎬 Membuat **playlist video**
- 📱 Meningkatkan **responsive design**

### 💡 Tips Penting:

1. **Video API** sangat mirip dengan **Audio API** - jika sudah paham satu, yang lain lebih mudah!
2. Untuk production, pertimbangkan menggunakan **`<div>` untuk progress bar** daripada range input (lebih smooth)
3. Selalu tambahkan **error handling** untuk video loading
4. Test di **berbagai browser** karena implementasi bisa berbeda

---

**Happy Coding!** 💻✨

Jangan lupa untuk **practice, practice, practice!** Cara terbaik belajar adalah dengan mencoba sendiri dan eksperimen dengan fitur-fitur baru! 🎯

---

> **Catatan:** Dokumentasi ini dibuat untuk pembelajaran pribadi. Jika ada pertanyaan atau kebingungan, coba baca ulang bagian yang belum dipahami dan praktekkan step-by-step! 📖
