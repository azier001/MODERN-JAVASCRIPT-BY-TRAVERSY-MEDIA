# 🎬 Dokumentasi HTML5 Video API untuk Pemula

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan File Media](#persiapan-file-media)
- [Struktur HTML Dasar](#struktur-html-dasar)
- [Tag Video HTML5](#tag-video-html5)
  - [Atribut Dasar Video](#atribut-dasar-video)
  - [Mengatur Lebar Video](#mengatur-lebar-video)
- [Atribut-atribut Video](#atribut-atribut-video)
  - [Controls - Menampilkan Kontrol](#controls)
  - [Autoplay - Memutar Otomatis](#autoplay)
  - [Muted - Mode Bisu](#muted)
  - [Loop - Pengulangan Video](#loop)
  - [Poster - Gambar Thumbnail](#poster)
- [Kontrol Video dengan JavaScript](#kontrol-video-dengan-javascript)
  - [Membuat Tombol Kontrol](#membuat-tombol-kontrol)
  - [Menghubungkan dengan JavaScript](#menghubungkan-dengan-javascript)
  - [Fungsi Play](#fungsi-play)
  - [Fungsi Pause](#fungsi-pause)
  - [Fungsi Stop](#fungsi-stop)
  - [Menampilkan Waktu Berjalan](#menampilkan-waktu-berjalan)
- [Kode Lengkap](#kode-lengkap)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

**HTML5 Video API** adalah teknologi yang memungkinkan kita untuk memutar video di halaman web tanpa perlu plugin tambahan seperti Flash. API ini sangat mirip dengan Audio API, dan memberikan kontrol penuh terhadap video melalui HTML dan JavaScript.

> 💡 **Catatan Penting**: Video API bekerja sangat mirip dengan Audio API yang mungkin sudah Anda pelajari sebelumnya.

---

<a name="persiapan-file-media"></a>
## 📁 Persiapan File Media

Sebelum memulai, pastikan Anda memiliki folder `media` yang berisi:

- **clouds.mov** - File video awan bergerak (durasi sekitar 15 detik)
- **poster.png** - Gambar thumbnail untuk video

```
project/
├── index.html
├── script.js
└── media/
    ├── clouds.mov
    └── poster.png
```

---

<a name="struktur-html-dasar"></a>
## 🏗️ Struktur HTML Dasar

Mari kita mulai dengan struktur HTML dasar:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Video API</title>
    <script src="script.js" defer></script>
  </head>
  <body>
    <!-- Konten akan ditambahkan di sini -->
  </body>
</html>
```

---

<a name="tag-video-html5"></a>
## 🎥 Tag Video HTML5

<a name="atribut-dasar-video"></a>
### ✨ Atribut Dasar Video

Sama seperti ada tag `<audio>`, HTML5 juga memiliki tag `<video>`. Berikut cara penggunaannya:

```html
<video src="media/clouds.mov"></video>
```

**Hasil**: Video akan ditampilkan, tetapi **memenuhi seluruh layar** dan **tidak ada kontrol** apapun.

<a name="mengatur-lebar-video"></a>
### 📏 Mengatur Lebar Video

Untuk membatasi ukuran video, tambahkan atribut `width`:

```html
<video src="media/clouds.mov" width="500"></video>
```

**Hasil**: Video sekarang memiliki lebar 500px dan hanya menampilkan **frame pertama** dari video.

> ⚠️ **Catatan**: Tanpa atribut tambahan, video tidak akan bisa diputar karena tidak ada kontrol.

---

<a name="atribut-atribut-video"></a>
## ⚙️ Atribut-atribut Video

<a name="controls"></a>
### 🎛️ Controls - Menampilkan Kontrol

Atribut `controls` menampilkan kontrol video standar (play, pause, volume, dll).

```html
<video src="media/clouds.mov" width="500" controls></video>
```

**Hasil**: Video sekarang memiliki **player standar** dengan tombol play/pause, volume, dan progress bar.

✅ **Kegunaan**: User dapat memutar, menjeda, dan mengatur volume video.

---

<a name="autoplay"></a>
### ▶️ Autoplay - Memutar Otomatis

Atribut `autoplay` membuat video otomatis diputar saat halaman dimuat.

```html
<video src="media/clouds.mov" width="500" controls autoplay></video>
```

> ⚠️ **Penting di Chrome**: Browser Chrome **tidak mengizinkan autoplay** dengan suara. Video harus dalam mode `muted` agar autoplay berfungsi.

---

<a name="muted"></a>
### 🔇 Muted - Mode Bisu

Atribut `muted` membuat video tidak bersuara (bisu).

```html
<video src="media/clouds.mov" width="500" controls autoplay muted></video>
```

**Alasan**: 
- Mencegah pengalaman user yang buruk (video tiba-tiba berbunyi saat halaman dibuka)
- **Wajib** untuk fitur autoplay di Chrome

✅ **Hasil**: Video sekarang **otomatis diputar** saat halaman dimuat tanpa suara.

---

<a name="loop"></a>
### 🔁 Loop - Pengulangan Video

Atribut `loop` membuat video otomatis diulang dari awal setelah selesai.

```html
<video src="media/clouds.mov" width="500" controls autoplay muted loop></video>
```

**Hasil**: Ketika video mencapai akhir, video akan **otomatis mulai dari awal lagi**.

---

<a name="poster"></a>
### 🖼️ Poster - Gambar Thumbnail

Atribut `poster` menampilkan gambar placeholder sebelum video diputar.

```html
<video src="media/clouds.mov" width="500" poster="media/poster.png"></video>
```

**Kegunaan**: 
- Menampilkan thumbnail yang menarik
- Memberi gambaran isi video sebelum diputar
- Lebih profesional daripada frame pertama video

✅ **Hasil**: Gambar `poster.png` ditampilkan sampai user menekan tombol play.

---

<a name="kontrol-video-dengan-javascript"></a>
## 💻 Kontrol Video dengan JavaScript

Sekarang kita akan membuat **kontrol video custom** menggunakan JavaScript, tanpa menggunakan atribut `controls`, `autoplay`, `muted`, atau `loop`.

<a name="membuat-tombol-kontrol"></a>
### 🔘 Membuat Tombol Kontrol

Pertama, kita buat HTML untuk video dan tombol-tombol kontrolnya:

```html
<video id="video" src="media/clouds.mov" width="500" poster="media/poster.png"></video>

<div>
  <button id="play">Play</button>
  <button id="pause">Pause</button>
  <button id="stop">Stop</button>
  <div id="current-time"></div>
</div>
```

**Penjelasan**:
- ✅ Hanya menggunakan atribut `width` dan `poster`
- ✅ Menambahkan `id="video"` untuk akses JavaScript
- ✅ Membuat 3 tombol: Play, Pause, Stop
- ✅ Membuat div untuk menampilkan waktu berjalan video

---

<a name="menghubungkan-dengan-javascript"></a>
### 🔗 Menghubungkan dengan JavaScript

Di file `script.js`, kita ambil semua elemen yang diperlukan:

```javascript
const video = document.getElementById('video');
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const stop = document.getElementById('stop');
const currentTime = document.getElementById('current-time');
```

**Penjelasan**:
- 📌 `video` - Elemen video yang akan dikontrol
- 📌 `play` - Tombol untuk memutar video
- 📌 `pause` - Tombol untuk menjeda video
- 📌 `stop` - Tombol untuk menghentikan video
- 📌 `currentTime` - Elemen untuk menampilkan waktu

---

<a name="fungsi-play"></a>
### ▶️ Fungsi Play

Menambahkan event listener untuk tombol play:

```javascript
play.addEventListener('click', () => video.play());
```

**Cara Kerja**:
1. Ketika tombol Play diklik
2. Method `video.play()` dipanggil
3. Video mulai diputar

✅ **Hasil**: Klik tombol Play → Video mulai berjalan

---

<a name="fungsi-pause"></a>
### ⏸️ Fungsi Pause

Menambahkan event listener untuk tombol pause:

```javascript
pause.addEventListener('click', () => video.pause());
```

**Cara Kerja**:
1. Ketika tombol Pause diklik
2. Method `video.pause()` dipanggil
3. Video dijeda pada posisi saat ini

✅ **Hasil**: Klik tombol Pause → Video berhenti sementara

---

<a name="fungsi-stop"></a>
### ⏹️ Fungsi Stop

Untuk fungsi Stop, kita perlu **menggabungkan pause dengan reset waktu**:

```javascript
stop.addEventListener('click', () => {
  video.pause();
  video.currentTime = 0;
});
```

**Penjelasan**:
- ⚠️ **Tidak ada method `stop()` dalam Video API**
- ✅ Solusi: Gunakan `pause()` + set `currentTime` ke 0
- 📌 `video.currentTime = 0` mengembalikan video ke detik ke-0

**Cara Kerja**:
1. Video dijeda dengan `pause()`
2. Waktu video dikembalikan ke awal (0 detik)
3. Poster kembali ditampilkan

✅ **Hasil**: Klik tombol Stop → Video kembali ke awal

---

<a name="menampilkan-waktu-berjalan"></a>
### ⏱️ Menampilkan Waktu Berjalan

Untuk menampilkan waktu video yang sedang berjalan, gunakan event `timeupdate`:

```javascript
video.addEventListener(
  'timeupdate',
  () => (currentTime.innerText = video.currentTime)
);
```

**Penjelasan**:
- 📌 Event `timeupdate` dipicu **setiap kali waktu video berubah**
- 📌 `video.currentTime` memberikan waktu saat ini dalam **detik**
- 📌 Nilai ditampilkan di elemen `currentTime`

✅ **Hasil**: Saat video berjalan, angka waktu terus berubah (contoh: 0.523, 1.045, 2.789, dst.)

---

<a name="kode-lengkap"></a>
## 📝 Kode Lengkap

### File: `index.html`

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Video API</title>
    <script src="script.js" defer></script>
  </head>
  <body>
    <video id="video" src="media/clouds.mov" width="500" poster="media/poster.png"></video>

    <div>
      <button id="play">Play</button>
      <button id="pause">Pause</button>
      <button id="stop">Stop</button>
      <div id="current-time"></div>
    </div>
  </body>
</html>
```

### File: `script.js`

```javascript
const video = document.getElementById('video');
const play = document.getElementById('play');
const pause = document.getElementById('pause');
const stop = document.getElementById('stop');
const currentTime = document.getElementById('current-time');

play.addEventListener('click', () => video.play());
pause.addEventListener('click', () => video.pause());
stop.addEventListener('click', () => {
  video.pause();
  video.currentTime = 0;
});

video.addEventListener(
  'timeupdate',
  () => (currentTime.innerText = video.currentTime)
);
```

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

### Apa yang Sudah Dipelajari:

✅ **Tag `<video>`** - Cara menampilkan video di HTML5  
✅ **Atribut Video** - `controls`, `autoplay`, `muted`, `loop`, `poster`  
✅ **Kontrol dengan JavaScript** - Play, Pause, Stop  
✅ **Method Video API** - `play()`, `pause()`, `currentTime`  
✅ **Event `timeupdate`** - Menampilkan waktu video berjalan  

### Poin Penting:

> 💡 Video API sangat mirip dengan Audio API  
> ⚠️ Autoplay di Chrome memerlukan atribut `muted`  
> ⚠️ Tidak ada method `stop()`, gunakan `pause()` + `currentTime = 0`  
> 🎯 Event `timeupdate` untuk tracking waktu video  

### Langkah Selanjutnya:

Di tutorial berikutnya, kita akan membuat **custom video player** yang lebih cantik dengan:
- 🎨 Desain yang lebih menarik
- 📊 Progress bar yang interaktif
- 🎛️ Kontrol yang lebih lengkap

---

**📚 Selamat Belajar! Keep Coding! 🚀**
