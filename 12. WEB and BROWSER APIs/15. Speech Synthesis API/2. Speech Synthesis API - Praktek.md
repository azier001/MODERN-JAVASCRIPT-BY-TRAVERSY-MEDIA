# 🎤 Dokumentasi Web Speech API - Speech Synthesis

> **Dokumentasi Pribadi untuk Pemula**  
> Belajar membuat aplikasi text-to-speech menggunakan Web Speech API

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Web Speech API?](#apa-itu-web-speech-api)
- [Apa itu Speech Synthesis?](#apa-itu-speech-synthesis)
- [Persiapan Project](#persiapan-project)
- [Langkah 1: Membuat HTML](#langkah-1-membuat-html)
- [Langkah 2: Styling dengan CSS](#langkah-2-styling-dengan-css)
- [Langkah 3: JavaScript - Inisialisasi](#langkah-3-javascript-inisialisasi)
- [Langkah 4: Membuat Fungsi Submit](#langkah-4-membuat-fungsi-submit)
- [Langkah 5: Membuat Utterance](#langkah-5-membuat-utterance)
- [Langkah 6: Testing Pertama](#langkah-6-testing-pertama)
- [Langkah 7: Menambahkan Voices](#langkah-7-menambahkan-voices)
- [Langkah 8: Mengisi Select Box](#langkah-8-mengisi-select-box)
- [Langkah 9: Implementasi Pemilihan Voice](#langkah-9-implementasi-pemilihan-voice)
- [Langkah 10: Testing Final](#langkah-10-testing-final)
- [Kode Lengkap](#kode-lengkap)
- [Tips dan Catatan](#tips-dan-catatan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam tutorial ini, kita akan belajar membuat aplikasi **Text-to-Speech** sederhana menggunakan **Web Speech API**. Aplikasi ini akan:

✅ Mengubah teks menjadi suara  
✅ Menyediakan berbagai pilihan suara  
✅ Mendukung berbagai aksen dan bahasa  

**Cocok untuk:**
- Aplikasi aksesibilitas
- Tool untuk orang non-verbal
- Pembelajaran bahasa
- Dan masih banyak lagi!

---

<a name="apa-itu-web-speech-api"></a>
## 📖 Apa itu Web Speech API?

**Web Speech API** adalah API bawaan browser yang memungkinkan kita untuk:

1. 🎙️ **Speech Recognition** - Mengubah suara menjadi teks
2. 🔊 **Speech Synthesis** - Mengubah teks menjadi suara

Dalam tutorial ini, kita fokus pada **Speech Synthesis** (Text-to-Speech).

---

<a name="apa-itu-speech-synthesis"></a>
## 📖 Apa itu Speech Synthesis?

**Speech Synthesis** adalah teknologi yang mengubah teks tertulis menjadi suara yang bisa didengar. 

**Contoh penggunaan:**
- Pembaca layar untuk tunanetra
- Asisten virtual (Siri, Google Assistant)
- GPS dan navigasi
- Alat bantu komunikasi

---

<a name="persiapan-project"></a>
## 🛠️ Persiapan Project

Buat folder baru dengan struktur berikut:

```
my-speech-app/
├── index.html
├── style.css
└── script.js
```

---

<a name="langkah-1-membuat-html"></a>
## 📄 Langkah 1: Membuat HTML

### 🎯 Tujuan
Membuat struktur HTML dasar dengan form input dan select box.

### 📝 Kode HTML

Buat file `index.html`:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Web Speech API</title>
    <script src="script.js" defer></script>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <h1>Type some text</h1>
      <form id="form">
        <div>
          <textarea id="text-input" cols="30" rows="10"></textarea>
        </div>
        <div>
          <select id="voice-select"></select>
        </div>
        <button type="submit" class="btn">Speak</button>
      </form>
    </div>
  </body>
</html>
```

### 💡 Penjelasan

| Element | ID | Fungsi |
|---------|-----|--------|
| `<form>` | `form` | Menampung semua input |
| `<textarea>` | `text-input` | Tempat user menulis teks |
| `<select>` | `voice-select` | Dropdown pilihan suara |
| `<button>` | - | Tombol untuk memicu speech |

**⚠️ Penting:** Perhatikan atribut `defer` pada script tag - ini memastikan JavaScript dijalankan setelah HTML selesai dimuat.

---

<a name="langkah-2-styling-dengan-css"></a>
## 🎨 Langkah 2: Styling dengan CSS

### 🎯 Tujuan
Membuat tampilan aplikasi lebih menarik dan user-friendly.

### 📝 Kode CSS

Buat file `style.css`:

```css
@import url('https://fonts.googleapis.com/css?family=Poppins:400,500,600,700,800,900&display=swap');

body {
  font-family: 'Poppins', sans-serif;
  font-size: 18px;
  background: salmon;
}

.container {
  width: 100%;
  max-width: 600px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

#text-input,
#voice-select {
  display: block;
  width: 600px;
  border-radius: 10px;
  background: #fafafa;
  margin-bottom: 20px;
  font-size: inherit;
  padding: 8px;
  outline: none;
}

#voice-select {
  height: 40px;
}

.btn {
  display: inline-block;
  padding: 10px 20px;
  border-radius: 10px;
  font-size: 18px;
  width: 100%;
  background: #333;
  border: 1px solid #000;
  text-decoration: none;
  color: #fff;
  font-weight: 600;
  cursor: pointer;
}

.btn:hover {
  background: #000;
}
```

### 💡 Penjelasan

- 🎨 **Background salmon** - Warna latar belakang yang menarik
- 📱 **Max-width 600px** - Membatasi lebar untuk tampilan yang nyaman
- ✨ **Border-radius 10px** - Sudut melengkung untuk estetika modern
- 🖱️ **Hover effect** - Interaktif saat mouse di atas tombol

---

<a name="langkah-3-javascript-inisialisasi"></a>
## ⚙️ Langkah 3: JavaScript - Inisialisasi

### 🎯 Tujuan
Menginisialisasi variabel-variabel yang akan digunakan.

### 📝 Langkah-langkah

**Buka file `script.js` dan mulai dengan:**

#### 1️⃣ Inisialisasi Speech Synthesis

```javascript
const synth = window.speechSynthesis;
```

**💡 Penjelasan:**
- `window.speechSynthesis` adalah objek bawaan browser
- Kita simpan dalam variabel `synth` agar mudah digunakan

#### 2️⃣ Inisialisasi Voice Select Element

```javascript
const voiceSelect = document.getElementById('voice-select');
```

**💡 Penjelasan:**
- Mengambil element `<select>` dari HTML
- Akan digunakan untuk menampilkan pilihan suara

#### 3️⃣ Deklarasi Variabel Voices

```javascript
let voices;
```

**💡 Penjelasan:**
- Menggunakan `let` karena nilainya akan diubah nanti
- Akan menyimpan daftar suara yang tersedia

### ✅ Kode Lengkap Inisialisasi

```javascript
const synth = window.speechSynthesis;
const voiceSelect = document.getElementById('voice-select');
let voices;
```

---

<a name="langkah-4-membuat-fungsi-submit"></a>
## 🎬 Langkah 4: Membuat Fungsi Submit

### 🎯 Tujuan
Membuat fungsi yang akan dijalankan saat form di-submit.

### 📝 Langkah-langkah

#### 1️⃣ Buat Fungsi onSubmit

```javascript
function onSubmit(e) {
  e.preventDefault();
  
  const textInput = document.getElementById('text-input');
  
  console.log(textInput.value);
}
```

**💡 Penjelasan:**

| Kode | Fungsi |
|------|--------|
| `e.preventDefault()` | Mencegah page reload saat submit |
| `document.getElementById('text-input')` | Mengambil element textarea |
| `console.log(textInput.value)` | Menampilkan teks di console (untuk testing) |

#### 2️⃣ Tambahkan Event Listener

```javascript
document.getElementById('form').addEventListener('submit', onSubmit);
```

**💡 Penjelasan:**
- Mendengarkan event `submit` pada form
- Memanggil fungsi `onSubmit` saat form di-submit

### ✅ Testing Langkah Ini

1. Buka `index.html` di browser
2. Ketik "Hello" di textarea
3. Klik tombol "Speak"
4. Buka Console (F12) → Seharusnya muncul "Hello"

---

<a name="langkah-5-membuat-utterance"></a>
## 🗣️ Langkah 5: Membuat Utterance

### 🎯 Tujuan
Membuat objek utterance yang akan diucapkan oleh browser.

### 📝 Apa itu Utterance?

**SpeechSynthesisUtterance** adalah objek yang merepresentasikan:
- ✅ Teks yang akan diucapkan
- ✅ Voice (suara) yang digunakan
- ✅ Kecepatan, pitch, dan volume

### 📝 Kode

Modifikasi fungsi `onSubmit`:

```javascript
function onSubmit(e) {
  e.preventDefault();

  const textInput = document.getElementById('text-input');

  // Membuat utterance baru
  const utterThis = new SpeechSynthesisUtterance(textInput.value);

  // Meminta browser untuk berbicara
  synth.speak(utterThis);
}
```

**💡 Penjelasan:**

```javascript
new SpeechSynthesisUtterance(textInput.value)
```
- Membuat objek utterance baru
- Parameter: teks yang akan diucapkan

```javascript
synth.speak(utterThis)
```
- Meminta browser untuk mengucapkan utterance

---

<a name="langkah-6-testing-pertama"></a>
## 🧪 Langkah 6: Testing Pertama

### 🎯 Tujuan
Memastikan text-to-speech sudah berfungsi.

### ✅ Cara Testing

1. **Refresh browser**
2. **Ketik teks:** "Hello World"
3. **Klik tombol "Speak"**
4. **🔊 Dengarkan!** Browser akan membacakan teks

### 🎉 Selamat!

Aplikasi text-to-speech dasar sudah jalan! Tapi semua masih menggunakan suara default. Mari kita tambahkan pilihan suara.

---

<a name="langkah-7-menambahkan-voices"></a>
## 🎙️ Langkah 7: Menambahkan Voices

### 🎯 Tujuan
Mendapatkan daftar suara yang tersedia di browser.

### 📝 Membuat Fungsi addVoicesToSelect

#### 1️⃣ Buat Fungsi Kosong

```javascript
function addVoicesToSelect() {
  // Kode akan ditambahkan di sini
}
```

#### 2️⃣ Dapatkan Daftar Voices

```javascript
function addVoicesToSelect() {
  voices = synth.getVoices();
}
```

**💡 Penjelasan:**
- `synth.getVoices()` mengembalikan array berisi semua suara yang tersedia
- Disimpan ke variabel global `voices`

---

<a name="langkah-8-mengisi-select-box"></a>
## 📋 Langkah 8: Mengisi Select Box

### 🎯 Tujuan
Menampilkan semua suara yang tersedia di dropdown.

### 📝 Langkah-langkah

#### 1️⃣ Loop Melalui Voices

```javascript
function addVoicesToSelect() {
  voices = synth.getVoices();

  for (let i = 0; i < voices.length; i++) {
    // Kode di sini
  }
}
```

#### 2️⃣ Buat Element Option

```javascript
for (let i = 0; i < voices.length; i++) {
  const option = document.createElement('option');
  option.textContent = `${voices[i].name} - ${voices[i].lang}`;
}
```

**💡 Penjelasan:**
- Membuat element `<option>` untuk setiap suara
- Menampilkan nama dan bahasa suara

#### 3️⃣ Tandai Suara Default

```javascript
for (let i = 0; i < voices.length; i++) {
  const option = document.createElement('option');
  option.textContent = `${voices[i].name} - ${voices[i].lang}`;

  if (voices[i].default) {
    option.textContent += ' - DEFAULT';
  }
}
```

**💡 Penjelasan:**
- Jika suara adalah default, tambahkan label "- DEFAULT"

#### 4️⃣ Set Data Attributes

```javascript
for (let i = 0; i < voices.length; i++) {
  const option = document.createElement('option');
  option.textContent = `${voices[i].name} - ${voices[i].lang}`;

  if (voices[i].default) {
    option.textContent += ' - DEFAULT';
  }

  option.setAttribute('data-lang', voices[i].lang);
  option.setAttribute('data-name', voices[i].name);
}
```

**💡 Penjelasan:**
- Menyimpan data bahasa dan nama di attribute
- Akan digunakan saat memilih suara

#### 5️⃣ Tambahkan ke Select Box

```javascript
for (let i = 0; i < voices.length; i++) {
  const option = document.createElement('option');
  option.textContent = `${voices[i].name} - ${voices[i].lang}`;

  if (voices[i].default) {
    option.textContent += ' - DEFAULT';
  }

  option.setAttribute('data-lang', voices[i].lang);
  option.setAttribute('data-name', voices[i].name);
  voiceSelect.appendChild(option);
}
```

**💡 Penjelasan:**
- `appendChild()` menambahkan option ke select box

### ✅ Fungsi Lengkap

```javascript
function addVoicesToSelect() {
  voices = synth.getVoices();

  for (let i = 0; i < voices.length; i++) {
    const option = document.createElement('option');
    option.textContent = `${voices[i].name} - ${voices[i].lang}`;

    if (voices[i].default) {
      option.textContent += ' - DEFAULT';
    }

    option.setAttribute('data-lang', voices[i].lang);
    option.setAttribute('data-name', voices[i].name);
    voiceSelect.appendChild(option);
  }
}
```

#### 6️⃣ Panggil Fungsi

Tambahkan di akhir file:

```javascript
addVoicesToSelect();

if (speechSynthesis.onvoiceschanged !== undefined) {
  speechSynthesis.onvoiceschanged = addVoicesToSelect;
}
```

**💡 Penjelasan:**

| Kode | Fungsi |
|------|--------|
| `addVoicesToSelect()` | Panggil fungsi pertama kali |
| `onvoiceschanged` | Event handler untuk saat daftar suara berubah |

### 🧪 Testing

Refresh browser → Select box seharusnya sudah terisi dengan pilihan suara!

---

<a name="langkah-9-implementasi-pemilihan-voice"></a>
## 🎛️ Langkah 9: Implementasi Pemilihan Voice

### 🎯 Tujuan
Membuat aplikasi menggunakan suara yang dipilih user.

### 📝 Langkah-langkah

#### 1️⃣ Dapatkan Suara yang Dipilih

Modifikasi fungsi `onSubmit`, tambahkan sebelum `synth.speak()`:

```javascript
function onSubmit(e) {
  e.preventDefault();

  const textInput = document.getElementById('text-input');
  const utterThis = new SpeechSynthesisUtterance(textInput.value);

  // Dapatkan nama suara yang dipilih
  const selectedOption =
    voiceSelect.selectedOptions[0].getAttribute('data-name');
}
```

**💡 Penjelasan:**
- `selectedOptions[0]` → Option yang sedang dipilih
- `getAttribute('data-name')` → Ambil nama suara

#### 2️⃣ Cari dan Set Voice

```javascript
function onSubmit(e) {
  e.preventDefault();

  const textInput = document.getElementById('text-input');
  const utterThis = new SpeechSynthesisUtterance(textInput.value);

  const selectedOption =
    voiceSelect.selectedOptions[0].getAttribute('data-name');
  
  // Loop untuk mencari voice yang sesuai
  for (let i = 0; i < voices.length; i++) {
    if (voices[i].name === selectedOption) {
      utterThis.voice = voices[i];
    }
  }

  synth.speak(utterThis);
}
```

**💡 Penjelasan:**
- Loop melalui array `voices`
- Jika nama cocok, set `utterThis.voice`

### ✅ Fungsi onSubmit Lengkap

```javascript
function onSubmit(e) {
  e.preventDefault();

  const textInput = document.getElementById('text-input');

  const utterThis = new SpeechSynthesisUtterance(textInput.value);

  const selectedOption =
    voiceSelect.selectedOptions[0].getAttribute('data-name');
  for (let i = 0; i < voices.length; i++) {
    if (voices[i].name === selectedOption) {
      utterThis.voice = voices[i];
    }
  }

  synth.speak(utterThis);
}
```

---

<a name="langkah-10-testing-final"></a>
## 🎊 Langkah 10: Testing Final

### ✅ Cara Testing Lengkap

1. **Refresh browser**
2. **Ketik teks:** "Hello World"
3. **Pilih suara** dari dropdown (misalnya "Anna", "Fred", "Diego")
4. **Klik "Speak"**
5. **🔊 Dengarkan perbedaan suara!**

### 🎮 Eksperimen

Coba berbagai kombinasi:

| Suara | Bahasa | Aksen |
|-------|--------|-------|
| Anna | en-US | Amerika |
| Diego | es-MX | Meksiko |
| Ellen | en-US | Amerika |
| Fred | en-US | Amerika |
| Juan | es-MX | Meksiko |

### 🎉 Selamat!

Aplikasi Text-to-Speech sudah selesai dan berfungsi penuh!

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap

### 📄 index.html

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Web Speech API</title>
    <script src="script.js" defer></script>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <h1>Type some text</h1>
      <form id="form">
        <div>
          <textarea id="text-input" cols="30" rows="10"></textarea>
        </div>
        <div>
          <select id="voice-select"></select>
        </div>
        <button type="submit" class="btn">Speak</button>
      </form>
    </div>
  </body>
</html>
```

### 🎨 style.css

```css
@import url('https://fonts.googleapis.com/css?family=Poppins:400,500,600,700,800,900&display=swap');

body {
  font-family: 'Poppins', sans-serif;
  font-size: 18px;
  background: salmon;
}

.container {
  width: 100%;
  max-width: 600px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

#text-input,
#voice-select {
  display: block;
  width: 600px;
  border-radius: 10px;
  background: #fafafa;
  margin-bottom: 20px;
  font-size: inherit;
  padding: 8px;
  outline: none;
}

#voice-select {
  height: 40px;
}

.btn {
  display: inline-block;
  padding: 10px 20px;
  border-radius: 10px;
  font-size: 18px;
  width: 100%;
  background: #333;
  border: 1px solid #000;
  text-decoration: none;
  color: #fff;
  font-weight: 600;
  cursor: pointer;
}

.btn:hover {
  background: #000;
}
```

### ⚙️ script.js

```javascript
const voiceSelect = document.getElementById('voice-select');

const synth = window.speechSynthesis;
let voices;

function addVoicesToSelect() {
  voices = synth.getVoices();

  for (let i = 0; i < voices.length; i++) {
    const option = document.createElement('option');
    option.textContent = `${voices[i].name} - ${voices[i].lang}`;

    if (voices[i].default) {
      option.textContent += ' - DEFAULT';
    }

    option.setAttribute('data-lang', voices[i].lang);
    option.setAttribute('data-name', voices[i].name);
    voiceSelect.appendChild(option);
  }
}

function onSubmit(e) {
  e.preventDefault();

  const textInput = document.getElementById('text-input');

  const utterThis = new SpeechSynthesisUtterance(textInput.value);

  const selectedOption =
    voiceSelect.selectedOptions[0].getAttribute('data-name');
  for (let i = 0; i < voices.length; i++) {
    if (voices[i].name === selectedOption) {
      utterThis.voice = voices[i];
    }
  }

  synth.speak(utterThis);
}

addVoicesToSelect();
if (speechSynthesis.onvoiceschanged !== undefined) {
  speechSynthesis.onvoiceschanged = addVoicesToSelect;
}

document.getElementById('form').addEventListener('submit', onSubmit);
```

---

<a name="tips-dan-catatan"></a>
## 💡 Tips dan Catatan

### ✨ Tips Pengembangan

1. **🎚️ Volume & Pitch**
   ```javascript
   utterThis.volume = 1; // 0 to 1
   utterThis.pitch = 1;  // 0 to 2
   utterThis.rate = 1;   // 0.1 to 10
   ```

2. **🔄 Pause & Resume**
   ```javascript
   synth.pause();
   synth.resume();
   ```

3. **🛑 Stop Speech**
   ```javascript
   synth.cancel();
   ```

### 🌟 Ide Pengembangan Lanjutan

- ➕ Tambahkan slider untuk volume dan kecepatan
- 🎵 Tambahkan kontrol pitch
- 💾 Simpan pengaturan suara favorit
- 📱 Buat responsive untuk mobile
- 🌐 Tambahkan support multi-bahasa

### ⚠️ Penting untuk Diketahui

| Platform | Catatan |
|----------|---------|
| **Windows** | Memiliki suara yang berbeda dari Mac |
| **Mac** | Punya lebih banyak pilihan suara berkualitas |
| **Mobile** | Support terbatas tergantung browser |
| **Browser** | Pastikan menggunakan Chrome/Firefox modern |

### 🎯 Kegunaan Praktis

**Aplikasi ini cocok untuk:**

1. 🦽 **Aksesibilitas**
   - Membantu orang dengan disabilitas visual
   - Tool komunikasi untuk non-verbal

2. 📚 **Pendidikan**
   - Pembelajaran bahasa
   - Pembaca buku digital

3. 🎮 **Hiburan**
   - Game narration
   - Interactive storytelling

### 🐛 Troubleshooting

**Masalah:** Suara tidak muncul
- ✅ Cek volume komputer
- ✅ Pastikan browser support (Chrome/Firefox)
- ✅ Refresh halaman

**Masalah:** Select box kosong
- ✅ Tunggu beberapa detik (voices loading)
- ✅ Cek console untuk error

**Masalah:** Voice tidak berubah
- ✅ Pastikan kode pemilihan voice sudah benar
- ✅ Cek apakah voices array terisi

---

## 🎓 Kesimpulan

Selamat! 🎉 Kamu telah berhasil membuat aplikasi **Text-to-Speech** menggunakan **Web Speech API**!

### 📝 Yang Sudah Dipelajari:

✅ Cara menggunakan `window.speechSynthesis`  
✅ Membuat `SpeechSynthesisUtterance`  
✅ Mendapatkan dan menampilkan daftar suara  
✅ Mengimplementasikan pemilihan suara  
✅ Manipulasi DOM untuk dynamic content  

### 🚀 Langkah Selanjutnya:

1. Coba modifikasi styling sesuai selera
2. Tambahkan fitur-fitur baru (volume, speed control)
3. Eksplorasi Speech Recognition untuk membuat chat bot
4. Deploy ke GitHub Pages untuk share ke teman!

---

## 📚 Referensi

- [MDN Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)
- [SpeechSynthesis Interface](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis)
- [SpeechSynthesisUtterance](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisUtterance)

---

**Made with ❤️ for learning**  
*Keep coding, keep learning!* 🚀
