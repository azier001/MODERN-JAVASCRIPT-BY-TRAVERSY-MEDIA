# 🥁 Dokumentasi Drum Machine - Proyek JavaScript untuk Pemula

---

## 📑 Daftar Isi

- [Pengenalan Proyek](#pengenalan-proyek)
- [Apa yang Akan Kita Buat?](#apa-yang-akan-kita-buat)
- [Struktur File Proyek](#struktur-file-proyek)
- [Persiapan HTML](#persiapan-html)
  - [Struktur Dasar HTML](#struktur-dasar-html)
  - [Menambahkan Data Attribute](#menambahkan-data-attribute)
  - [Menambahkan Audio Elements](#menambahkan-audio-elements)
- [Styling dengan CSS](#styling-dengan-css)
  - [CSS Dasar](#css-dasar)
  - [Styling untuk Keys](#styling-untuk-keys)
  - [Efek Animasi Playing](#efek-animasi-playing)
- [JavaScript - Membuat Interaktivitas](#javascript-membuat-interaktivitas)
  - [Event Listener Keydown](#event-listener-keydown)
  - [Fungsi playSound](#fungsi-playsound)
  - [Mendapatkan Audio Element](#mendapatkan-audio-element)
  - [Mendapatkan Key Element](#mendapatkan-key-element)
  - [Validasi Audio Element](#validasi-audio-element)
  - [Memutar Audio](#memutar-audio)
  - [Menambahkan Animasi](#menambahkan-animasi)
- [Cara Kerja Keseluruhan](#cara-kerja-keseluruhan)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan-proyek"></a>
## 🎯 Pengenalan Proyek

Selamat datang di dokumentasi **Drum Machine**! 🎉

Ini adalah proyek kecil yang sangat menarik menggunakan **Audio API** dari JavaScript. Yang mengejutkan adalah kita hanya membutuhkan **sedikit sekali kode JavaScript** untuk membuat drum machine yang interaktif!

> 💡 **Catatan Penting**: Proyek ini cocok untuk pemula yang ingin belajar tentang event handling, DOM manipulation, dan Audio API di JavaScript.

---

<a name="apa-yang-akan-kita-buat"></a>
## 🎵 Apa yang Akan Kita Buat?

Kita akan membuat sebuah **drum machine interface** sederhana dengan fitur:

- ✨ Tombol keyboard A sampai K (satu baris tengah keyboard)
- 🔊 Setiap tombol akan memutar suara drum yang berbeda
- 🎨 Animasi visual ketika tombol ditekan
- 🎶 8 jenis suara drum: clap, hihat, kick, openhat, boom, ride, snare, tom

**Cara Kerja:**
Ketika kita menekan salah satu tombol (A, S, D, F, G, H, J, K), akan:
1. Memutar suara drum yang sesuai
2. Menampilkan animasi pada tombol yang ditekan

---

<a name="struktur-file-proyek"></a>
## 📁 Struktur File Proyek

Proyek ini memiliki struktur file yang sangat sederhana:

```
drum-machine/
│
├── index.html          # File HTML utama
├── style.css           # File CSS untuk styling
├── script.js           # File JavaScript untuk interaktivitas
│
└── sounds/             # Folder berisi file audio
    ├── clap.wav
    ├── hihat.wav
    ├── kick.wav
    ├── openhat.wav
    ├── boom.wav
    ├── ride.wav
    ├── snare.wav
    └── tom.wav
```

---

<a name="persiapan-html"></a>
## 🏗️ Persiapan HTML

<a name="struktur-dasar-html"></a>
### 📄 Struktur Dasar HTML

Kita mulai dengan struktur HTML yang sederhana:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="style.css" />
    <script src="script.js" defer></script>
    <title>Drum Machine</title>
  </head>
  <body>
    <header>
      <h1>Drum Machine</h1>
    </header>
    <!-- Konten akan ditambahkan di sini -->
  </body>
</html>
```

**Penjelasan:**
- `<link rel="stylesheet" href="style.css" />` → Menghubungkan file CSS
- `<script src="script.js" defer></script>` → Menghubungkan file JavaScript dengan `defer` agar JavaScript dijalankan setelah HTML selesai dimuat

---

<a name="menambahkan-data-attribute"></a>
### 🔑 Menambahkan Data Attribute

Sekarang kita akan membuat div untuk setiap tombol drum dengan **custom attribute** bernama `data-key`.

> 💡 **Apa itu Key Code?**  
> Key code adalah angka unik untuk setiap tombol di keyboard. Contoh:
> - Tombol **A** = Key code **65**
> - Tombol **S** = Key code **83**
> - Tombol **D** = Key code **68**
> - Dan seterusnya...

**HTML untuk Keys:**

```html
<div class="keys">
  <!-- Tombol A dengan key code 65 -->
  <div class="key" data-key="65">
    <kbd>A</kbd>
    <span class="sound">clap</span>
  </div>
  
  <!-- Tombol S dengan key code 83 -->
  <div class="key" data-key="83">
    <kbd>S</kbd>
    <span class="sound">hihat</span>
  </div>
  
  <!-- Tombol D dengan key code 68 -->
  <div class="key" data-key="68">
    <kbd>D</kbd>
    <span class="sound">kick</span>
  </div>
  
  <!-- Tombol F dengan key code 70 -->
  <div class="key" data-key="70">
    <kbd>F</kbd>
    <span class="sound">openhat</span>
  </div>
  
  <!-- Tombol G dengan key code 71 -->
  <div class="key" data-key="71">
    <kbd>G</kbd>
    <span class="sound">boom</span>
  </div>
  
  <!-- Tombol H dengan key code 72 -->
  <div class="key" data-key="72">
    <kbd>H</kbd>
    <span class="sound">ride</span>
  </div>
  
  <!-- Tombol J dengan key code 74 -->
  <div class="key" data-key="74">
    <kbd>J</kbd>
    <span class="sound">snare</span>
  </div>
  
  <!-- Tombol K dengan key code 75 -->
  <div class="key" data-key="75">
    <kbd>K</kbd>
    <span class="sound">tom</span>
  </div>
</div>
```

**Penjelasan Setiap Bagian:**
- `<div class="key" data-key="65">` → Div pembungkus dengan attribute `data-key` yang menyimpan key code
- `<kbd>A</kbd>` → Tag keyboard untuk menampilkan huruf tombol
- `<span class="sound">clap</span>` → Menampilkan nama suara drum

---

<a name="menambahkan-audio-elements"></a>
### 🔊 Menambahkan Audio Elements

Setiap tombol harus memiliki **audio element** yang sesuai. Kita letakkan sebelum penutup tag `</body>`:

```html
<!-- Audio untuk tombol A (key code 65) -->
<audio data-key="65" src="sounds/clap.wav"></audio>

<!-- Audio untuk tombol S (key code 83) -->
<audio data-key="83" src="sounds/hihat.wav"></audio>

<!-- Audio untuk tombol D (key code 68) -->
<audio data-key="68" src="sounds/kick.wav"></audio>

<!-- Audio untuk tombol F (key code 70) -->
<audio data-key="70" src="sounds/openhat.wav"></audio>

<!-- Audio untuk tombol G (key code 71) -->
<audio data-key="71" src="sounds/boom.wav"></audio>

<!-- Audio untuk tombol H (key code 72) -->
<audio data-key="72" src="sounds/ride.wav"></audio>

<!-- Audio untuk tombol J (key code 74) -->
<audio data-key="74" src="sounds/snare.wav"></audio>

<!-- Audio untuk tombol K (key code 75) -->
<audio data-key="75" src="sounds/tom.wav"></audio>
```

**Penjelasan:**
- `<audio data-key="65" src="sounds/clap.wav">` → Audio element dengan:
  - `data-key="65"` → Attribute yang sama dengan div key, untuk mencocokkan tombol dengan suaranya
  - `src="sounds/clap.wav"` → Path ke file suara

> ⚠️ **Penting!** Attribute `data-key` pada audio harus **sama persis** dengan `data-key` pada div agar bisa dicocokkan nanti di JavaScript.

---

<a name="styling-dengan-css"></a>
## 🎨 Styling dengan CSS

<a name="css-dasar"></a>
### 🖌️ CSS Dasar

Mari kita mulai dengan styling dasar:

```css
@import url('https://fonts.googleapis.com/css?family=Poppins:300,400,700');

html,
body {
  font-family: 'Poppins', sans-serif;
  margin: 0;
  padding: 0;
}

header {
  background: #f4f4f4;
}

h1 {
  margin: 0;
  padding: 0;
  text-align: center;
}
```

**Penjelasan:**
- `@import url(...)` → Mengimpor font Poppins dari Google Fonts
- `font-family: 'Poppins', sans-serif;` → Menggunakan font Poppins untuk seluruh halaman
- `margin: 0; padding: 0;` → Menghapus margin dan padding default browser

---

<a name="styling-untuk-keys"></a>
### 🎹 Styling untuk Keys

Sekarang kita styling tombol-tombol drum:

```css
.keys {
  font-size: 40px;
  width: 500px;
  margin: 100px auto;
}

.keys kbd {
  display: inline-block;
  padding: 10px 20px;
  margin: 0 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  background: #eee;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
  font-size: 20px;
  font-weight: 300;
}
```

**Penjelasan Detail:**
- `.keys` → Container untuk semua tombol
  - `width: 500px` → Lebar container 500px
  - `margin: 100px auto` → Margin atas-bawah 100px, kiri-kanan auto (untuk center)

- `.keys kbd` → Styling untuk setiap tombol keyboard
  - `display: inline-block` → Agar tombol bisa berdiri sejajar
  - `padding: 10px 20px` → Ruang dalam tombol
  - `margin: 0 10px` → Jarak antar tombol
  - `border: 1px solid #ccc` → Garis tepi abu-abu
  - `border-radius: 5px` → Sudut melengkung
  - `background: #eee` → Warna latar abu-abu terang
  - `box-shadow: 0 0 5px rgba(0, 0, 0, 0.1)` → Bayangan halus

---

<a name="efek-animasi-playing"></a>
### ✨ Efek Animasi Playing

Ini adalah bagian **penting** untuk animasi ketika tombol ditekan:

```css
.keys .playing kbd {
  transform: scale(1.2);
  transition: all 0.07s;
}
```

**Penjelasan:**
- `.keys .playing kbd` → Ketika div key memiliki class `playing`
  - `transform: scale(1.2)` → Membesar 1.2x (120% dari ukuran normal)
  - `transition: all 0.07s` → Animasi perubahan selama 0.07 detik

> 💡 **Cara Kerja:** Nanti di JavaScript, kita akan menambahkan class `playing` ketika tombol ditekan, dan menghapusnya setelah 100 milidetik. Inilah yang menciptakan efek "flash" atau kedipan.

---

<a name="javascript-membuat-interaktivitas"></a>
## ⚡ JavaScript - Membuat Interaktivitas

Sekarang bagian paling seru! Kita akan membuat drum machine menjadi **interaktif** dengan JavaScript.

<a name="event-listener-keydown"></a>
### 👂 Event Listener Keydown

Mari kita mulai dengan mendengarkan event ketika tombol keyboard ditekan:

```javascript
window.addEventListener('keydown', playSound);
```

**Penjelasan:**
- `window.addEventListener('keydown', playSound)` → Menambahkan event listener pada window
  - `'keydown'` → Event yang didengarkan (ketika tombol ditekan)
  - `playSound` → Fungsi yang akan dipanggil ketika event terjadi

> 💡 **Kenapa window?** Karena kita ingin mendengarkan semua tombol keyboard di seluruh halaman, bukan hanya pada element tertentu.

---

<a name="fungsi-playsound"></a>
### 🎵 Fungsi playSound

Sekarang kita buat fungsi `playSound` yang akan dijalankan setiap kali tombol ditekan:

```javascript
function playSound(e) {
  // Kode akan kita tambahkan bertahap di sini
}
```

**Penjelasan:**
- `function playSound(e)` → Fungsi dengan parameter `e` (event object)
- `e` → Berisi informasi tentang event yang terjadi, termasuk tombol apa yang ditekan

---

<a name="mendapatkan-audio-element"></a>
### 🎧 Mendapatkan Audio Element

Mari kita dapatkan audio element yang sesuai dengan tombol yang ditekan:

```javascript
function playSound(e) {
  const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);
}
```

**Penjelasan Detail:**
- `e.keyCode` → Mendapatkan key code dari tombol yang ditekan (misal: 65 untuk A)
- `document.querySelector(...)` → Mencari element di halaman
- `` `audio[data-key="${e.keyCode}"]` `` → Selector untuk mencari:
  - Element `audio` 
  - Yang memiliki attribute `data-key` 
  - Dengan nilai sama dengan `e.keyCode`

**Contoh:**
Jika kita tekan tombol **A** (key code 65), maka selector menjadi:
```javascript
document.querySelector(`audio[data-key="65"]`)
```
Ini akan menemukan: `<audio data-key="65" src="sounds/clap.wav">`

---

<a name="mendapatkan-key-element"></a>
### 🔲 Mendapatkan Key Element

Sekarang kita juga perlu mendapatkan div key untuk animasi:

```javascript
function playSound(e) {
  const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);
  const key = document.querySelector(`.key[data-key="${e.keyCode}"]`);
}
```

**Penjelasan:**
- `document.querySelector(`.key[data-key="${e.keyCode}"]`)` → Mencari:
  - Element dengan class `key`
  - Yang memiliki attribute `data-key` sama dengan key code yang ditekan

**Contoh:**
Jika tekan **S** (key code 83):
```javascript
document.querySelector(`.key[data-key="83"]`)
```
Menemukan: `<div class="key" data-key="83">...</div>`

---

<a name="validasi-audio-element"></a>
### ✅ Validasi Audio Element

Kita perlu memastikan audio element ditemukan (tombol yang ditekan ada di daftar drum kita):

```javascript
function playSound(e) {
  const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);
  const key = document.querySelector(`.key[data-key="${e.keyCode}"]`);

  if (!audio) return;
}
```

**Penjelasan:**
- `if (!audio) return;` → Jika audio tidak ditemukan (null), hentikan fungsi
- Ini mencegah error ketika kita tekan tombol yang **bukan** A-K (misalnya R, T, Y, dll)

**Contoh:**
- Tekan **A** → audio ditemukan → lanjut
- Tekan **R** → audio tidak ditemukan (null) → fungsi berhenti

---

<a name="memutar-audio"></a>
### ▶️ Memutar Audio

Sekarang kita set waktu audio ke 0 dan putar:

```javascript
function playSound(e) {
  const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);
  const key = document.querySelector(`.key[data-key="${e.keyCode}"]`);

  if (!audio) return;

  audio.currentTime = 0;
  audio.play();
}
```

**Penjelasan:**
- `audio.currentTime = 0;` → Set waktu pemutaran ke detik ke-0 (awal)
  - **Kenapa?** Agar setiap kali tombol ditekan, suara selalu dimulai dari awal
  - Jika tidak diset 0, kita harus menunggu audio selesai sebelum bisa memutar lagi
  
- `audio.play();` → Memutar audio

> 💡 **Tips:** Setting `currentTime = 0` memungkinkan kita menekan tombol berkali-kali dengan cepat dan suaranya tetap akan diputar dari awal setiap kali!

---

<a name="menambahkan-animasi"></a>
### 🎬 Menambahkan Animasi

Terakhir, kita tambahkan dan hapus class `playing` untuk animasi:

```javascript
function playSound(e) {
  const audio = document.querySelector(`audio[data-key="${e.keyCode}"]`);
  const key = document.querySelector(`.key[data-key="${e.keyCode}"]`);

  if (!audio) return;

  audio.currentTime = 0;
  audio.play();

  key.classList.add('playing');

  setTimeout(() => {
    key.classList.remove('playing');
  }, 100);
}
```

**Penjelasan Detail:**

1. **Menambahkan Class:**
   ```javascript
   key.classList.add('playing');
   ```
   - Menambahkan class `playing` ke div key
   - Ini membuat tombol **membesar** sesuai CSS yang kita buat tadi

2. **Menghapus Class dengan Delay:**
   ```javascript
   setTimeout(() => {
     key.classList.remove('playing');
   }, 100);
   ```
   - `setTimeout(...)` → Menjalankan fungsi setelah delay tertentu
   - `() => {...}` → Arrow function yang akan dijalankan
   - `key.classList.remove('playing')` → Menghapus class `playing`
   - `100` → Delay dalam milidetik (0.1 detik)

**Cara Kerja Animasi:**
1. Tombol ditekan → class `playing` ditambahkan → tombol membesar
2. Setelah 100ms → class `playing` dihapus → tombol kembali normal
3. Hasilnya: Efek "flash" atau kedipan yang cepat! ✨

---

<a name="cara-kerja-keseluruhan"></a>
## 🔄 Cara Kerja Keseluruhan

Mari kita lihat alur kerja lengkap dari awal hingga akhir:

### 📋 Langkah demi Langkah:

**1. User Menekan Tombol** (misalnya tombol **A**) ⌨️
   ```
   Event keydown terpicu → e.keyCode = 65
   ```

**2. Event Listener Menangkap Event** 👂
   ```javascript
   window.addEventListener('keydown', playSound);
   // Fungsi playSound dipanggil dengan parameter e
   ```

**3. Mencari Audio dan Key Element** 🔍
   ```javascript
   const audio = document.querySelector(`audio[data-key="65"]`);
   // Ditemukan: <audio data-key="65" src="sounds/clap.wav">
   
   const key = document.querySelector(`.key[data-key="65"]`);
   // Ditemukan: <div class="key" data-key="65">...</div>
   ```

**4. Validasi** ✅
   ```javascript
   if (!audio) return;
   // Audio ditemukan, lanjut!
   ```

**5. Memutar Audio** 🔊
   ```javascript
   audio.currentTime = 0;  // Set ke awal
   audio.play();           // Putar suara clap
   ```

**6. Menambahkan Animasi** 🎬
   ```javascript
   key.classList.add('playing');
   // Tombol A membesar (scale 1.2)
   ```

**7. Menghapus Animasi Setelah 100ms** ⏱️
   ```javascript
   setTimeout(() => {
     key.classList.remove('playing');
     // Tombol A kembali normal
   }, 100);
   ```

**Hasil Akhir:** Suara drum "clap" terdengar dan tombol A berkedip! 🎉

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

Selamat! 🎊 Kamu telah berhasil membuat **Drum Machine** yang interaktif!

### 📝 Yang Sudah Kita Pelajari:

1. ✅ **HTML Structure**
   - Penggunaan `data-key` attribute untuk mencocokkan tombol dengan audio
   - Tag `<kbd>` untuk menampilkan tombol keyboard
   - Tag `<audio>` untuk file suara

2. ✅ **CSS Styling**
   - Styling tombol dengan border, padding, dan box-shadow
   - Membuat animasi dengan `transform: scale()` dan `transition`
   - Class `.playing` untuk efek visual

3. ✅ **JavaScript Interactivity**
   - Event listener `keydown` untuk mendeteksi tombol
   - `querySelector()` dengan attribute selector
   - Mengontrol audio dengan `currentTime` dan `play()`
   - Manipulasi class dengan `classList.add()` dan `classList.remove()`
   - `setTimeout()` untuk delay

### 💪 Kelebihan Proyek Ini:

- ⚡ **Kode yang Sangat Sedikit** - Hanya beberapa baris JavaScript!
- 🎯 **Konsep yang Jelas** - Mudah dipahami langkah demi langkah
- 🎨 **Visual yang Menarik** - Animasi sederhana tapi efektif
- 🔊 **Audio API** - Belajar cara kerja audio di web

### 🎮 Ide Pengembangan:

Kamu bisa mengembangkan proyek ini dengan:
- 🎵 Menambahkan lebih banyak suara drum
- 📱 Membuat responsive untuk mobile
- 🎹 Menambahkan fitur record dan playback
- 🌈 Mengubah warna tombol berdasarkan jenis suara
- 🎤 Menambahkan visualizer audio

---

## 🎉 Selamat Berkreasi!

Jangan lupa untuk **bereksperimen** dan **bermain-main** dengan kode ini! Coding itu seperti bermain drum - semakin sering latihan, semakin mahir! 🥁

> 💡 **Tips Akhir:** Coba tekan tombol dengan cepat dan buat ritme drum sendiri. Have fun! 🎶

---

**Dibuat dengan ❤️ untuk pembelajaran JavaScript**
