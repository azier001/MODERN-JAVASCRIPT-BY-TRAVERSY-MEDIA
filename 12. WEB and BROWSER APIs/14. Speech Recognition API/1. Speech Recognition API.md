# 🎤 Web Speech API - Speech Recognition Tutorial

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Web Speech API?](#apa-itu-web-speech-api)
- [Speech Recognition vs Speech Synthesis](#speech-recognition-vs-speech-synthesis)
- [Kegunaan Speech Recognition](#kegunaan-speech-recognition)
- [Persiapan Project](#persiapan-project)
- [Struktur File HTML](#struktur-file-html)
- [Membuat Script JavaScript](#membuat-script-javascript)
  - [Inisialisasi Speech Recognition](#inisialisasi-speech-recognition)
  - [Mengatasi Kompatibilitas Browser](#mengatasi-kompatibilitas-browser)
  - [Mengatur Properti Recognition](#mengatur-properti-recognition)
  - [Memulai Listening](#memulai-listening)
  - [Menangani Hasil Speech Recognition](#menangani-hasil-speech-recognition)
  - [Memahami Struktur Data Hasil](#memahami-struktur-data-hasil)
  - [Menggunakan Loop untuk Multiple Results](#menggunakan-loop-untuk-multiple-results)
  - [Normalisasi Text (Lowercase & Trim)](#normalisasi-text-lowercase-trim)
  - [Validasi dengan Array Warna](#validasi-dengan-array-warna)
  - [Mengubah Background Color](#mengubah-background-color)
- [Kode Lengkap](#kode-lengkap)
- [Cara Menggunakan Aplikasi](#cara-menggunakan-aplikasi)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Selamat datang di tutorial **Web Speech API**! 🚀

Dalam tutorial ini, kita akan belajar membuat aplikasi sederhana yang bisa **mendengarkan suara kita** dan **mengubah warna background** sesuai dengan warna yang kita ucapkan. Misalnya, jika kita bilang "red", background akan berubah menjadi merah! 🔴

---

<a name="apa-itu-web-speech-api"></a>
## 📖 Apa itu Web Speech API?

**Web Speech API** adalah teknologi yang memungkinkan browser untuk:
- 🎙️ **Mendengarkan** apa yang kita katakan (Speech to Text)
- 🔊 **Berbicara** atau membacakan teks (Text to Speech)

API ini terdiri dari **dua bagian utama**:
1. **Speech Recognition** - Mengubah suara menjadi teks
2. **Speech Synthesis** - Mengubah teks menjadi suara

---

<a name="speech-recognition-vs-speech-synthesis"></a>
## 🔄 Speech Recognition vs Speech Synthesis

| **Speech Recognition** 🎤 | **Speech Synthesis** 🔊 |
|---------------------------|-------------------------|
| Suara → Teks | Teks → Suara |
| Kita bicara, komputer menulis | Kita ketik, komputer membaca |
| Contoh: Voice typing | Contoh: Text-to-speech reader |

Dalam tutorial ini, kita fokus pada **Speech Recognition** (Suara ke Teks).

---

<a name="kegunaan-speech-recognition"></a>
## 💡 Kegunaan Speech Recognition

Speech Recognition sangat berguna untuk:

✅ **Aplikasi untuk penyandang disabilitas**
- Contoh: Aplikasi komunikasi untuk anak non-verbal yang bisa menekan tombol untuk menyuarakan kebutuhan mereka (seperti "lapar" atau "lelah")

✅ **Voice typing**
- Berbicara dan kata-kata langsung muncul di layar

✅ **Voice commands**
- Mengontrol aplikasi dengan suara

✅ **Transkripsi otomatis**
- Menyimpan percakapan ke database

---

<a name="persiapan-project"></a>
## 🛠️ Persiapan Project

Kita akan membuat project sederhana dengan 3 file:

```
📁 project-folder/
  ├── 📄 index.html
  ├── 📄 style.css
  └── 📄 script.js
```

**Tujuan Project**: Membuat aplikasi yang bisa mendengarkan nama warna dan mengubah background sesuai warna yang disebutkan! 🎨

---

<a name="struktur-file-html"></a>
## 📄 Struktur File HTML

File `index.html` sangat sederhana:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Web Speech API</title>
    <script src="script.js" defer></script>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <h1>Say A Color</h1>
  </body>
</html>
```

**Penjelasan:**
- 📌 Ada heading `<h1>` dengan teks "Say A Color"
- 📌 File CSS untuk styling sederhana (mengatur font dan posisi teks ke tengah)
- 📌 File JavaScript yang akan kita buat selanjutnya

---

<a name="membuat-script-javascript"></a>
## 💻 Membuat Script JavaScript

Sekarang kita mulai membuat file `script.js` langkah demi langkah! 👇

---

<a name="inisialisasi-speech-recognition"></a>
### 1️⃣ Inisialisasi Speech Recognition

Pertama, kita perlu membuat **object Speech Recognition** baru:

```javascript
const rec = new SpeechRecognition();
```

**Penjelasan:**
- 📌 Variabel `rec` adalah singkatan dari **recognition**
- 📌 Kita membuat instance baru dari `SpeechRecognition`
- 📌 Object ini yang akan kita gunakan untuk mendengarkan suara

---

<a name="mengatasi-kompatibilitas-browser"></a>
### 2️⃣ Mengatasi Kompatibilitas Browser

Tidak semua browser mendukung `SpeechRecognition` dengan nama yang sama. Beberapa browser lama menggunakan prefix `webkit`. Jadi kita perlu membuat fallback:

```javascript
const SpeechRecognition =
  window.SpeechRecognition || window.webkitSpeechRecognition;

const rec = new SpeechRecognition();
```

**Penjelasan:**
- 📌 `window.SpeechRecognition` - Untuk browser modern
- 📌 `||` (OR operator) - Jika tidak ada, gunakan yang sebelah kanan
- 📌 `window.webkitSpeechRecognition` - Untuk browser lama (dengan webkit prefix)
- 📌 Sekarang `SpeechRecognition` akan bekerja di kedua jenis browser

---

<a name="mengatur-properti-recognition"></a>
### 3️⃣ Mengatur Properti Recognition

Ada beberapa properti yang bisa kita atur:

#### 🌍 **Bahasa (Language)**

```javascript
rec.lang = 'en-US';
```

**Penjelasan:**
- 📌 `'en-US'` = Bahasa Inggris (Amerika)
- 📌 Bisa diganti dengan bahasa lain sesuai kebutuhan
- 📌 Contoh: `'id-ID'` untuk Bahasa Indonesia

#### ♾️ **Continuous (Mendengarkan Terus-menerus)**

```javascript
rec.continuous = true;
```

**Penjelasan:**

| `continuous = false` ❌ | `continuous = true` ✅ |
|------------------------|----------------------|
| Dengar sekali, lalu berhenti | Terus mendengarkan |
| Cocok untuk 1 perintah | Cocok untuk multiple perintah |
| Contoh: "red" → stop | Contoh: "red" "blue" "green" → terus dengar |

Karena kita ingin bisa menyebut banyak warna berturut-turut, kita set ke **`true`**!

---

<a name="memulai-listening"></a>
### 4️⃣ Memulai Listening

Untuk mulai mendengarkan, kita panggil method `start()`:

```javascript
rec.start();
```

**Penjelasan:**
- 📌 Setelah kode ini dijalankan, browser akan meminta izin menggunakan microphone
- 📌 Kamu akan melihat **icon microphone** atau **red dot** di tab browser
- 📌 🔴 = Browser sedang listening!

⚠️ **Catatan Penting:** Kamu harus punya microphone agar ini bisa bekerja!

---

<a name="menangani-hasil-speech-recognition"></a>
### 5️⃣ Menangani Hasil Speech Recognition

Sekarang kita perlu membuat **handler** untuk menangani apa yang kita ucapkan:

```javascript
rec.onresult = function (e) {
  console.log(e.results);
};
```

**Penjelasan:**
- 📌 `onresult` - Event yang dipanggil saat ada hasil recognition
- 📌 `e` - Event object yang berisi data hasil
- 📌 `e.results` - Array yang berisi apa yang kita ucapkan

#### 🧪 **Testing**

Jika kita reload halaman dan bilang **"blue"**, di console akan muncul object seperti ini:

```
SpeechRecognitionResultList
  └─ 0: SpeechRecognitionResult
      └─ 0: SpeechRecognitionAlternative
          ├─ transcript: "blue"  ← Ini yang kita ucapkan!
          └─ confidence: 0.95
```

---

<a name="memahami-struktur-data-hasil"></a>
### 6️⃣ Memahami Struktur Data Hasil

Mari kita pahami struktur data yang dikembalikan:

```javascript
e.results[0][0].transcript
```

**Penjelasan struktur:**
- 📌 `e.results` - Array utama
- 📌 `[0]` - Index pertama (hasil pertama)
- 📌 `[0]` - Alternative pertama (biasanya yang paling akurat)
- 📌 `.transcript` - Teks yang dikenali

#### 🔍 **Contoh dengan continuous = true:**

Jika kita bilang: "blue" → "red" → "green"

```
e.results[0][0].transcript = "blue"
e.results[1][0].transcript = "red"
e.results[2][0].transcript = "green"
```

Setiap kali kita berhenti bicara sebentar, akan dibuat entry baru!

---

<a name="menggunakan-loop-untuk-multiple-results"></a>
### 7️⃣ Menggunakan Loop untuk Multiple Results

Karena kita ingin menangani semua hasil yang masuk, kita perlu loop:

```javascript
rec.onresult = function (e) {
  for (let i = e.resultIndex; i < e.results.length; i++) {
    const script = e.results[i][0].transcript;
    console.log(script);
  }
};
```

**Penjelasan:**
- 📌 `e.resultIndex` - Index hasil yang baru (mulai dari hasil baru, bukan dari awal)
- 📌 `e.results.length` - Total jumlah hasil
- 📌 `i++` - Increment untuk iterasi
- 📌 Loop ini akan mengambil semua hasil baru yang masuk

#### 🧪 **Testing:**

Jika kita bilang: "red" → "blue" → "green"

Console akan menampilkan:
```
red
blue
green
```

Sempurna! 🎉

---

<a name="normalisasi-text-lowercase-trim"></a>
### 8️⃣ Normalisasi Text (Lowercase & Trim)

Ada masalah: kadang hasil recognition punya:
- ❌ Spasi di awal/akhir: `" red"`
- ❌ Huruf kapital: `"Red"` atau `"RED"`

Kita perlu normalisasi:

```javascript
const script = e.results[i][0].transcript.toLowerCase().trim();
```

**Penjelasan:**
- 📌 `.toLowerCase()` - Ubah semua huruf jadi kecil
  - `"Red"` → `"red"`
  - `"BLUE"` → `"blue"`
- 📌 `.trim()` - Hapus spasi di awal dan akhir
  - `" green "` → `"green"`
  - `"  yellow"` → `"yellow"`

Sekarang hasil akan selalu konsisten! ✅

---

<a name="validasi-dengan-array-warna"></a>
### 9️⃣ Validasi dengan Array Warna

Kita perlu membatasi warna yang diterima. Buat array warna yang valid:

```javascript
const acceptedColors = [
  'red',
  'blue',
  'green',
  'yellow',
  'pink',
  'brown',
  'purple',
  'orange',
  'black',
  'white',
];
```

**Penjelasan:**
- 📌 Array ini berisi nama-nama warna yang kita terima
- 📌 Semua dalam huruf kecil (lowercase) untuk matching
- 📌 Kamu bisa tambah warna lain sesuai keinginan!

#### ✅ **Validasi dengan `.includes()`**

```javascript
if (acceptedColors.includes(script)) {
  // Warna valid! ✅
} else {
  // Warna tidak valid! ❌
}
```

**Penjelasan:**
- 📌 `.includes(script)` - Cek apakah `script` ada di dalam array
- 📌 Return `true` jika ada, `false` jika tidak ada
- 📌 Contoh:
  - `acceptedColors.includes('red')` → `true` ✅
  - `acceptedColors.includes('test')` → `false` ❌

---

<a name="mengubah-background-color"></a>
### 🔟 Mengubah Background Color

Sekarang bagian yang seru! Ubah warna background:

```javascript
if (acceptedColors.includes(script)) {
  document.body.style.backgroundColor = script;
} else {
  alert('Please say a color');
}
```

**Penjelasan:**
- 📌 `document.body` - Element `<body>` di HTML
- 📌 `.style.backgroundColor` - Properti CSS background color
- 📌 `= script` - Set nilai warna sesuai yang diucapkan
- 📌 `alert()` - Tampilkan pesan jika warna tidak valid

#### 🎨 **Cara Kerja:**
1. Ucapkan "red" → Background jadi merah 🔴
2. Ucapkan "blue" → Background jadi biru 🔵
3. Ucapkan "test" → Muncul alert "Please say a color" ⚠️

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap

Berikut adalah kode lengkap file `script.js`:

```javascript
// 1. Mengatasi kompatibilitas browser
const SpeechRecognition =
  window.SpeechRecognition || window.webkitSpeechRecognition;

// 2. Membuat instance Speech Recognition
const rec = new SpeechRecognition();

// 3. Mengatur properti
rec.lang = 'en-US';        // Bahasa Inggris
rec.continuous = true;      // Terus mendengarkan

// 4. Handler untuk hasil recognition
rec.onresult = function (e) {
  // Array warna yang diterima
  const acceptedColors = [
    'red',
    'blue',
    'green',
    'yellow',
    'pink',
    'brown',
    'purple',
    'orange',
    'black',
    'white',
  ];

  // Loop untuk semua hasil yang masuk
  for (let i = e.resultIndex; i < e.results.length; i++) {
    // Ambil transcript dan normalisasi
    const script = e.results[i][0].transcript.toLowerCase().trim();

    // Validasi dan ubah warna
    if (acceptedColors.includes(script)) {
      document.body.style.backgroundColor = script;
    } else {
      alert('Please say a color');
    }
  }
};

// 5. Mulai listening
rec.start();
```

---

<a name="cara-menggunakan-aplikasi"></a>
## 🎮 Cara Menggunakan Aplikasi

1. **Buka file `index.html` di browser** 🌐
2. **Izinkan akses microphone** 🎤
   - Browser akan meminta izin
   - Klik "Allow" atau "Izinkan"
3. **Lihat red dot di tab** 🔴
   - Artinya browser sedang listening
4. **Ucapkan nama warna!** 🗣️
   - Contoh: "red" → Background merah
   - Contoh: "blue" → Background biru
   - Contoh: "green" → Background hijau
5. **Ucapkan multiple warna!** 🎨
   - "red" → "blue" → "green" → "orange"
   - Background akan terus berubah!
6. **Ucapkan kata yang bukan warna** ❌
   - Contoh: "test" atau "hello"
   - Akan muncul alert "Please say a color"

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

Selamat! 🎉 Kamu sudah belajar:

✅ Apa itu **Web Speech API**  
✅ Perbedaan **Speech Recognition** dan **Speech Synthesis**  
✅ Cara membuat **Speech Recognition object**  
✅ Mengatasi **kompatibilitas browser**  
✅ Mengatur **properti** (language, continuous)  
✅ Menangani **hasil recognition** dengan event handler  
✅ Memahami **struktur data** results  
✅ Menggunakan **loop** untuk multiple results  
✅ **Normalisasi** text dengan toLowerCase dan trim  
✅ **Validasi** input dengan array  
✅ **Mengubah background color** dengan JavaScript  

### 🚀 Ide Pengembangan Lebih Lanjut:

- 🎨 Tambahkan lebih banyak warna
- 📝 Tampilkan nama warna di layar
- 🎵 Tambahkan suara/efek saat warna berubah
- 🌈 Buat animasi transisi warna
- 📊 Rekam history warna yang sudah disebutkan
- 🎮 Buat game tebak warna dengan voice

### 📚 Next Step:

Di video berikutnya, kita akan belajar tentang **Speech Synthesis** (Text to Speech) - kebalikan dari Speech Recognition! 🔊

---

**Happy Coding!** 💻✨

*Dokumentasi ini dibuat untuk pembelajaran pribadi - Tutorial Web Speech API untuk Pemula*
