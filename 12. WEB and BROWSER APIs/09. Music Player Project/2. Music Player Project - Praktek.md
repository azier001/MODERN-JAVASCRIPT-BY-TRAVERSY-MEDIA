# 🎵 Dokumentasi Music Player - Panduan Lengkap untuk Pemula

> 📚 Dokumentasi ini dibuat untuk membantu pemula memahami cara membuat Music Player dengan JavaScript langkah demi langkah.

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa yang Akan Kita Buat?](#apa-yang-akan-kita-buat)
- [Persiapan Project](#persiapan-project)
- [Struktur File](#struktur-file)
- [HTML Structure (Sudah Tersedia)](#html-structure-sudah-tersedia)
- [CSS Styling (Sudah Tersedia)](#css-styling-sudah-tersedia)
- [JavaScript - Mulai Coding! 🚀](#javascript---mulai-coding-)
  - [Langkah 1: Mengambil Elemen HTML](#langkah-1-mengambil-elemen-html)
  - [Langkah 2: Membuat Array Lagu](#langkah-2-membuat-array-lagu)
  - [Langkah 3: Membuat Fungsi Load Song](#langkah-3-membuat-fungsi-load-song)
  - [Langkah 4: Membuat Fungsi Play Song](#langkah-4-membuat-fungsi-play-song)
  - [Langkah 5: Membuat Fungsi Pause Song](#langkah-5-membuat-fungsi-pause-song)
  - [Langkah 6: Menambahkan Event Listener Play/Pause](#langkah-6-menambahkan-event-listener-playpause)
  - [Langkah 7: Membuat Fungsi Previous Song](#langkah-7-membuat-fungsi-previous-song)
  - [Langkah 8: Membuat Fungsi Next Song](#langkah-8-membuat-fungsi-next-song)
  - [Langkah 9: Menambahkan Event Listener Prev/Next](#langkah-9-menambahkan-event-listener-prevnext)
  - [Langkah 10: Membuat Fungsi Update Progress](#langkah-10-membuat-fungsi-update-progress)
  - [Langkah 11: Membuat Fungsi Set Progress](#langkah-11-membuat-fungsi-set-progress)
  - [Langkah 12: Menambahkan Event Listener Progress](#langkah-12-menambahkan-event-listener-progress)
- [Kode Lengkap](#kode-lengkap)
- [Testing dan Troubleshooting](#testing-dan-troubleshooting)
- [Penutup](#penutup)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Selamat datang! Dalam dokumentasi ini, kita akan belajar membuat **Music Player** yang keren menggunakan **HTML**, **CSS**, dan **JavaScript**. 

### 💡 Yang Akan Kamu Pelajari:
- ✅ Cara memanipulasi elemen HTML dengan JavaScript
- ✅ Cara menggunakan Audio API
- ✅ Cara membuat animasi dengan class CSS
- ✅ Cara menangani event (click, timeupdate)
- ✅ Cara mengupdate progress bar secara real-time

---

<a name="apa-yang-akan-kita-buat"></a>
## 🎨 Apa yang Akan Kita Buat?

Music Player dengan fitur-fitur berikut:

| Fitur | Deskripsi |
|-------|-----------|
| ▶️ **Play/Pause** | Memutar dan menjeda lagu |
| ⏮️ **Previous** | Kembali ke lagu sebelumnya |
| ⏭️ **Next** | Lanjut ke lagu berikutnya |
| 🎨 **Animasi** | Cover berputar saat lagu diputar |
| 📊 **Progress Bar** | Menampilkan progress lagu |
| 🖱️ **Clickable Progress** | Klik progress bar untuk skip ke bagian tertentu |
| 🔄 **Auto Loop** | Otomatis putar lagu berikutnya saat selesai |

---

<a name="persiapan-project"></a>
## 🛠️ Persiapan Project

### 📦 Yang Kamu Butuhkan:

1. **Text Editor** (VS Code, Sublime Text, atau lainnya)
2. **Browser** (Chrome, Firefox, dll)
3. **File Audio** (format .mp3)
4. **File Gambar** (format .jpg untuk cover lagu)

### ⚠️ Penting!
- File audio (.mp3) dan gambar (.jpg) harus memiliki **nama yang sama**
- Contoh: `hey.mp3` → `hey.jpg`

---

<a name="struktur-file"></a>
## 📁 Struktur File

Buat struktur folder seperti ini:

```
music-player/
│
├── index.html
├── style.css
├── script.js
│
├── music/
│   ├── hey.mp3
│   ├── summer.mp3
│   └── ukulele.mp3
│
└── images/
    ├── hey.jpg
    ├── summer.jpg
    └── ukulele.jpg
```

---

<a name="html-structure-sudah-tersedia"></a>
## 📄 HTML Structure (Sudah Tersedia)

File `index.html` sudah tersedia dengan struktur berikut:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.2/css/all.min.css"
    />
    <link rel="stylesheet" href="style.css" />
    <script src="script.js" defer></script>
    <title>Music Player</title>
  </head>
  <body>
    <h1>Music Player</h1>

    <div class="music-container" id="music-container">
      <div class="music-info">
        <h4 id="title"></h4>
        <div class="progress-container" id="progress-container">
          <div class="progress" id="progress"></div>
        </div>
      </div>

      <audio src="music/ukulele.mp3" id="audio"></audio>

      <div class="img-container">
        <img src="images/ukulele.jpg" alt="music-cover" id="cover" />
      </div>
      
      <div class="navigation">
        <button id="prev" class="action-btn">
          <i class="fas fa-backward"></i>
        </button>
        <button id="play" class="action-btn action-btn-big">
          <i class="fas fa-play"></i>
        </button>
        <button id="next" class="action-btn">
          <i class="fas fa-forward"></i>
        </button>
      </div>
    </div>
  </body>
</html>
```

### 🔍 Penjelasan Elemen Penting:

| ID | Fungsi |
|-----|--------|
| `#music-container` | Container utama (untuk animasi) |
| `#title` | Menampilkan judul lagu |
| `#progress-container` | Container progress bar |
| `#progress` | Progress bar itu sendiri |
| `#audio` | Elemen audio HTML5 |
| `#cover` | Gambar cover lagu |
| `#prev` | Tombol previous |
| `#play` | Tombol play/pause |
| `#next` | Tombol next |

---

<a name="css-styling-sudah-tersedia"></a>
## 🎨 CSS Styling (Sudah Tersedia)

File `style.css` sudah tersedia dengan styling lengkap. 

### ✨ Fitur CSS Penting:

#### 1️⃣ **Animasi Rotasi**
```css
@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

#### 2️⃣ **Class `.play` untuk Animasi**
Ketika class `play` ditambahkan ke `#music-container`:
- ✅ Gambar cover akan berputar
- ✅ Info lagu akan muncul dari bawah

```css
.music-container.play .img-container img {
  animation-play-state: running;
}

.music-container.play .music-info {
  opacity: 1;
  transform: translateY(-100%);
}
```

---

<a name="javascript---mulai-coding-"></a>
## 🚀 JavaScript - Mulai Coding!

Sekarang kita mulai coding JavaScript! Buka file `script.js` yang masih kosong.

---

<a name="langkah-1-mengambil-elemen-html"></a>
### 📌 Langkah 1: Mengambil Elemen HTML

**🎯 Tujuan:** Mengambil semua elemen HTML yang akan kita gunakan.

**💻 Kode:**

```javascript
const musicContainer = document.getElementById('music-container');
const playBtn = document.getElementById('play');
const prevBtn = document.getElementById('prev');
const nextBtn = document.getElementById('next');
const audio = document.getElementById('audio');
const progress = document.getElementById('progress');
const progressContainer = document.getElementById('progress-container');
const title = document.getElementById('title');
const cover = document.getElementById('cover');
```

**📖 Penjelasan:**
- `document.getElementById()` digunakan untuk mengambil elemen berdasarkan ID
- Kita simpan setiap elemen dalam variabel agar mudah digunakan nanti
- `const` digunakan karena variabel ini tidak akan diubah

**✅ Checkpoint:** Simpan file dan cek di browser console (`F12`). Ketik `musicContainer` untuk melihat apakah elemen berhasil diambil.

---

<a name="langkah-2-membuat-array-lagu"></a>
### 📌 Langkah 2: Membuat Array Lagu

**🎯 Tujuan:** Membuat daftar lagu dan melacak lagu yang sedang diputar.

**💻 Kode:**

```javascript
// Song titles
const songs = ['hey', 'summer', 'ukulele'];

// Keep track of song
let songIndex = 2;
```

**📖 Penjelasan:**
- `songs` adalah array berisi nama-nama lagu (tanpa ekstensi .mp3)
- `songIndex` melacak lagu mana yang sedang dimainkan
- Index dimulai dari 0: `hey = 0`, `summer = 1`, `ukulele = 2`
- Kita set `songIndex = 2` (ukulele) sebagai lagu awal
- Menggunakan `let` karena nilai `songIndex` akan berubah-ubah

**💡 Tips:** Kamu bisa menambah lagu dengan menambahkan nama ke array `songs`!

---

<a name="langkah-3-membuat-fungsi-load-song"></a>
### 📌 Langkah 3: Membuat Fungsi Load Song

**🎯 Tujuan:** Membuat fungsi untuk memuat detail lagu (judul, audio, cover).

**💻 Kode:**

```javascript
// Initially load song details
loadSong(songs[songIndex]);

function loadSong(song) {
  title.innerText = song;
  audio.src = `music/${song}.mp3`;
  cover.src = `images/${song}.jpg`;
}
```

**📖 Penjelasan:**

1. **`loadSong(songs[songIndex])`** 
   - Memanggil fungsi pertama kali dengan lagu index 2 (ukulele)
   
2. **`title.innerText = song`**
   - Mengubah teks judul dengan nama lagu
   
3. **`audio.src = `music/${song}.mp3` `**
   - Mengatur source file audio
   - Menggunakan template literal (` `) untuk gabungkan string
   - Contoh hasil: `music/ukulele.mp3`
   
4. **`cover.src = `images/${song}.jpg` `**
   - Mengatur source gambar cover
   - Contoh hasil: `images/ukulele.jpg`

**✅ Checkpoint:** Refresh browser, kamu akan melihat judul "ukulele" dan cover-nya muncul!

---

<a name="langkah-4-membuat-fungsi-play-song"></a>
### 📌 Langkah 4: Membuat Fungsi Play Song

**🎯 Tujuan:** Membuat fungsi untuk memutar lagu dengan animasi.

**💻 Kode:**

```javascript
function playSong() {
  musicContainer.classList.add('play');
  playBtn.querySelector('i.fas').classList.remove('fa-play');
  playBtn.querySelector('i.fas').classList.add('fa-pause');
  audio.play();
}
```

**📖 Penjelasan:**

1. **`musicContainer.classList.add('play')`**
   - Menambahkan class `play` ke container
   - Ini memicu animasi CSS (rotasi & popup info)
   
2. **`playBtn.querySelector('i.fas').classList.remove('fa-play')`**
   - Mencari icon Font Awesome di dalam tombol play
   - Menghapus class `fa-play` (icon ▶️)
   
3. **`playBtn.querySelector('i.fas').classList.add('fa-pause')`**
   - Menambahkan class `fa-pause` (icon ⏸️)
   - Tombol berubah dari play menjadi pause
   
4. **`audio.play()`**
   - Method bawaan HTML5 Audio untuk memutar audio

**💡 Yang Terjadi:**
```
Sebelum: ▶️ Cover diam
Setelah: ⏸️ Cover berputar + musik mulai 🎵
```

---

<a name="langkah-5-membuat-fungsi-pause-song"></a>
### 📌 Langkah 5: Membuat Fungsi Pause Song

**🎯 Tujuan:** Membuat fungsi untuk menjeda lagu (kebalikan dari play).

**💻 Kode:**

```javascript
function pauseSong() {
  musicContainer.classList.remove('play');
  playBtn.querySelector('i.fas').classList.add('fa-play');
  playBtn.querySelector('i.fas').classList.remove('fa-pause');
  audio.pause();
}
```

**📖 Penjelasan:**

1. **`musicContainer.classList.remove('play')`**
   - Menghapus class `play`
   - Animasi berhenti (cover tidak berputar)
   
2. **`classList.add('fa-play')`** & **`classList.remove('fa-pause')`**
   - Mengembalikan icon ke play (▶️)
   
3. **`audio.pause()`**
   - Method untuk menjeda audio

**💡 Yang Terjadi:**
```
Sebelum: ⏸️ Cover berputar + musik main 🎵
Setelah: ▶️ Cover berhenti + musik pause 🔇
```

---

<a name="langkah-6-menambahkan-event-listener-playpause"></a>
### 📌 Langkah 6: Menambahkan Event Listener Play/Pause

**🎯 Tujuan:** Membuat tombol play/pause berfungsi dengan mendeteksi klik.

**💻 Kode:**

```javascript
playBtn.addEventListener('click', () => {
  const isPlaying = musicContainer.classList.contains('play');

  if (isPlaying) {
    pauseSong();
  } else {
    playSong();
  }
});
```

**📖 Penjelasan:**

1. **`addEventListener('click', () => {...})`**
   - Mendengarkan event click pada tombol play
   - Menggunakan arrow function `() => {}`
   
2. **`const isPlaying = musicContainer.classList.contains('play')`**
   - Mengecek apakah class `play` ada di container
   - Jika ada → lagu sedang diputar (true)
   - Jika tidak → lagu tidak diputar (false)
   
3. **`if (isPlaying) { pauseSong() } else { playSong() }`**
   - Jika sedang main → pause
   - Jika tidak main → play

**🔄 Logika:**
```
Klik 1: tidak main → play() → main ✅
Klik 2: sedang main → pause() → pause ✅
Klik 3: tidak main → play() → main ✅
```

**✅ Checkpoint:** Klik tombol play/pause, musik seharusnya bisa dimainkan dan dijeda!

---

<a name="langkah-7-membuat-fungsi-previous-song"></a>
### 📌 Langkah 7: Membuat Fungsi Previous Song

**🎯 Tujuan:** Membuat fungsi untuk pindah ke lagu sebelumnya.

**💻 Kode:**

```javascript
function prevSong() {
  songIndex--;

  if (songIndex < 0) {
    songIndex = songs.length - 1;
  }

  loadSong(songs[songIndex]);
  playSong();
}
```

**📖 Penjelasan:**

1. **`songIndex--`**
   - Mengurangi index sebanyak 1
   - Contoh: dari 2 → 1, dari 1 → 0
   
2. **`if (songIndex < 0) { songIndex = songs.length - 1 }`**
   - Jika index kurang dari 0 (di bawah lagu pertama)
   - Pindah ke lagu terakhir
   - `songs.length - 1` → `3 - 1 = 2` (ukulele)
   
3. **`loadSong(songs[songIndex])`**
   - Memuat lagu baru berdasarkan index yang baru
   
4. **`playSong()`**
   - Langsung putar lagu baru

**🔄 Contoh Flow:**
```
Lagu: Ukulele (index 2)
Klik Prev → Summer (index 1)
Klik Prev → Hey (index 0)
Klik Prev → Ukulele (index 2) ← Loop ke akhir!
```

---

<a name="langkah-8-membuat-fungsi-next-song"></a>
### 📌 Langkah 8: Membuat Fungsi Next Song

**🎯 Tujuan:** Membuat fungsi untuk pindah ke lagu berikutnya.

**💻 Kode:**

```javascript
function nextSong() {
  songIndex++;

  if (songIndex > songs.length - 1) {
    songIndex = 0;
  }

  loadSong(songs[songIndex]);
  playSong();
}
```

**📖 Penjelasan:**

1. **`songIndex++`**
   - Menambah index sebanyak 1
   - Contoh: dari 0 → 1, dari 1 → 2
   
2. **`if (songIndex > songs.length - 1) { songIndex = 0 }`**
   - Jika index melebihi lagu terakhir
   - Kembali ke lagu pertama (index 0)
   - `songs.length - 1` → `3 - 1 = 2`
   
3. **`loadSong()` & `playSong()`**
   - Sama seperti prevSong

**🔄 Contoh Flow:**
```
Lagu: Hey (index 0)
Klik Next → Summer (index 1)
Klik Next → Ukulele (index 2)
Klik Next → Hey (index 0) ← Loop ke awal!
```

---

<a name="langkah-9-menambahkan-event-listener-prevnext"></a>
### 📌 Langkah 9: Menambahkan Event Listener Prev/Next

**🎯 Tujuan:** Membuat tombol previous dan next berfungsi.

**💻 Kode:**

```javascript
prevBtn.addEventListener('click', prevSong);
nextBtn.addEventListener('click', nextSong);
```

**📖 Penjelasan:**

- Ketika tombol `prev` diklik → panggil fungsi `prevSong()`
- Ketika tombol `next` diklik → panggil fungsi `nextSong()`
- Kita tidak perlu `()` karena hanya passing reference fungsi

**✅ Checkpoint:** Coba klik tombol prev dan next, lagu seharusnya berganti!

---

<a name="langkah-10-membuat-fungsi-update-progress"></a>
### 📌 Langkah 10: Membuat Fungsi Update Progress

**🎯 Tujuan:** Membuat progress bar yang update otomatis saat lagu diputar.

**💻 Kode:**

```javascript
function updateProgress(e) {
  const { duration, currentTime } = e.srcElement;
  const progressPercent = (currentTime / duration) * 100;
  progress.style.width = `${progressPercent}%`;
}
```

**📖 Penjelasan:**

1. **`const { duration, currentTime } = e.srcElement`**
   - Destructuring: mengambil property dari elemen audio
   - `duration`: total durasi lagu (detik)
   - `currentTime`: waktu saat ini (detik)
   
2. **`const progressPercent = (currentTime / duration) * 100`**
   - Menghitung persentase progress
   - Contoh: `(30 / 120) * 100 = 25%`
   
3. **`progress.style.width = `${progressPercent}%` `**
   - Mengubah lebar progress bar sesuai persentase
   - Contoh: `width: 25%`

**🧮 Contoh Perhitungan:**
```
Duration: 180 detik (3 menit)
Current: 90 detik (1.5 menit)
Progress: (90 / 180) × 100 = 50%
```

---

<a name="langkah-11-membuat-fungsi-set-progress"></a>
### 📌 Langkah 11: Membuat Fungsi Set Progress

**🎯 Tujuan:** Membuat progress bar bisa diklik untuk skip ke bagian tertentu.

**💻 Kode:**

```javascript
function setProgress(e) {
  const width = this.clientWidth;
  const clickX = e.offsetX;
  const duration = audio.duration;

  audio.currentTime = (clickX / width) * duration;
}
```

**📖 Penjelasan:**

1. **`const width = this.clientWidth`**
   - Mengambil lebar total progress container
   - `this` merujuk ke `progressContainer`
   
2. **`const clickX = e.offsetX`**
   - Posisi klik dari kiri (dalam pixel)
   - Contoh: klik di tengah bar = 150px
   
3. **`const duration = audio.duration`**
   - Total durasi lagu
   
4. **`audio.currentTime = (clickX / width) * duration`**
   - Menghitung waktu berdasarkan posisi klik
   - Set `currentTime` audio ke waktu tersebut

**🧮 Contoh Perhitungan:**
```
Width: 300px
ClickX: 150px (tengah)
Duration: 180 detik

CurrentTime = (150 / 300) × 180 = 90 detik
→ Skip ke menit ke-1.5! ✅
```

---

<a name="langkah-12-menambahkan-event-listener-progress"></a>
### 📌 Langkah 12: Menambahkan Event Listener Progress

**🎯 Tujuan:** Mengaktifkan update progress dan click progress.

**💻 Kode:**

```javascript
audio.addEventListener('timeupdate', updateProgress);
progressContainer.addEventListener('click', setProgress);
```

**📖 Penjelasan:**

1. **`audio.addEventListener('timeupdate', updateProgress)`**
   - Event `timeupdate` dipicu setiap kali waktu audio berubah
   - Otomatis update progress bar secara real-time
   
2. **`progressContainer.addEventListener('click', setProgress)`**
   - Ketika progress bar diklik
   - Panggil fungsi `setProgress` untuk skip

**✅ Checkpoint Final:** 
- ✅ Progress bar bergerak saat musik main
- ✅ Klik progress bar untuk skip
- ✅ Semua fitur bekerja!

---

<a name="kode-lengkap"></a>
## 📜 Kode Lengkap

Berikut adalah kode JavaScript lengkap dari `script.js`:

```javascript
const musicContainer = document.getElementById('music-container');
const playBtn = document.getElementById('play');
const prevBtn = document.getElementById('prev');
const nextBtn = document.getElementById('next');
const audio = document.getElementById('audio');
const progress = document.getElementById('progress');
const progressContainer = document.getElementById('progress-container');
const title = document.getElementById('title');
const cover = document.getElementById('cover');

// Song titles
const songs = ['hey', 'summer', 'ukulele'];

// Keep track of song
let songIndex = 2;

// Initially load song details
loadSong(songs[songIndex]);

function loadSong(song) {
  title.innerText = song;
  audio.src = `music/${song}.mp3`;
  cover.src = `images/${song}.jpg`;
}

function playSong() {
  musicContainer.classList.add('play');
  playBtn.querySelector('i.fas').classList.remove('fa-play');
  playBtn.querySelector('i.fas').classList.add('fa-pause');
  audio.play();
}

function pauseSong() {
  musicContainer.classList.remove('play');
  playBtn.querySelector('i.fas').classList.add('fa-play');
  playBtn.querySelector('i.fas').classList.remove('fa-pause');
  audio.pause();
}

function prevSong() {
  songIndex--;

  if (songIndex < 0) {
    songIndex = songs.length - 1;
  }

  loadSong(songs[songIndex]);
  playSong();
}

function nextSong() {
  songIndex++;

  if (songIndex > songs.length - 1) {
    songIndex = 0;
  }

  loadSong(songs[songIndex]);
  playSong();
}

function updateProgress(e) {
  const { duration, currentTime } = e.srcElement;
  const progressPercent = (currentTime / duration) * 100;
  progress.style.width = `${progressPercent}%`;
}

function setProgress(e) {
  const width = this.clientWidth;
  const clickX = e.offsetX;
  const duration = audio.duration;

  audio.currentTime = (clickX / width) * duration;
}

// Event listeners
playBtn.addEventListener('click', () => {
  const isPlaying = musicContainer.classList.contains('play');

  if (isPlaying) {
    pauseSong();
  } else {
    playSong();
  }
});

prevBtn.addEventListener('click', prevSong);
nextBtn.addEventListener('click', nextSong);
audio.addEventListener('timeupdate', updateProgress);
progressContainer.addEventListener('click', setProgress);
```

---

<a name="testing-dan-troubleshooting"></a>
## 🧪 Testing dan Troubleshooting

### ✅ Checklist Testing:

- [ ] Lagu bisa diputar dengan klik tombol play
- [ ] Lagu bisa dijeda dengan klik tombol pause
- [ ] Cover berputar saat lagu diputar
- [ ] Info lagu muncul dari bawah saat play
- [ ] Tombol previous berfungsi
- [ ] Tombol next berfungsi
- [ ] Loop dari lagu terakhir ke pertama (dan sebaliknya)
- [ ] Progress bar bergerak saat lagu diputar
- [ ] Klik progress bar untuk skip

### ❌ Masalah Umum:

| Problem | Solution |
|---------|----------|
| Audio tidak muncul | Cek path file di folder `music/` |
| Cover tidak muncul | Cek path file di folder `images/` |
| Console error | Periksa penulisan ID di HTML dan JS |
| Progress bar tidak gerak | Pastikan event `timeupdate` terpasang |
| Animasi tidak jalan | Cek apakah class `play` ditambahkan |

### 🔍 Debug dengan Console:

```javascript
// Tambahkan di fungsi untuk debug
console.log('Current song:', songs[songIndex]);
console.log('Is playing:', musicContainer.classList.contains('play'));
console.log('Progress:', progressPercent + '%');
```

---

<a name="penutup"></a>
## 🎉 Penutup

### 🏆 Selamat!

Kamu telah berhasil membuat **Music Player** yang fully functional! 

### 📚 Apa yang Sudah Dipelajari:

| Konsep | Penjelasan |
|--------|------------|
| **DOM Manipulation** | Mengambil & mengubah elemen HTML |
| **Event Listeners** | Mendengarkan interaksi user |
| **Audio API** | Kontrol audio dengan JavaScript |
| **Array & Index** | Mengelola daftar lagu |
| **Class Manipulation** | Menambah/hapus class untuk animasi |
| **Template Literals** | Menggabungkan string dengan `` ` `` |
| **Destructuring** | Mengambil property dengan `{...}` |
| **Arrow Functions** | Fungsi modern `() => {}` |

### 🚀 Tantangan Selanjutnya:

1. 🔊 Tambahkan kontrol volume
2. 🔀 Tambahkan tombol shuffle
3. 🔁 Tambahkan tombol repeat
4. ⏱️ Tampilkan waktu (00:00 / 03:45)
5. 📱 Buat responsive untuk mobile
6. 🎨 Ubah tema warna
7. 📝 Buat playlist yang bisa di-customize
8. 💾 Simpan last played song di localStorage

### 💡 Tips Belajar:

- ✅ Ketik kode sendiri (jangan copy-paste!)
- ✅ Coba modifikasi kode
- ✅ Baca dokumentasi MDN untuk konsep yang belum paham
- ✅ Gunakan console.log() untuk debug
- ✅ Practice makes perfect!

### 📚 Resources:

- [MDN Web Audio API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API)
- [JavaScript.info](https://javascript.info/)
- [W3Schools JavaScript](https://www.w3schools.com/js/)

---

> 💪 **Keep Learning, Keep Coding!**
>
> "The only way to learn a new programming language is by writing programs in it." - Dennis Ritchie

---

**📅 Dokumentasi dibuat:** 2026  
**✍️ Tujuan:** Pembelajaran pribadi  
**🎯 Level:** Pemula  

---

🌟 **Happy Coding!** 🌟
