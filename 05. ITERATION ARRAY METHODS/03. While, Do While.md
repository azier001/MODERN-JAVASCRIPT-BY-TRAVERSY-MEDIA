# 🔄 Dokumentasi While & Do-While Loop JavaScript

> 📚 **Dokumentasi Pribadi untuk Pemula** - Belajar loop step-by-step dengan mudah!

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [While Loop](#while-loop)
  - [Apa itu While Loop?](#apa-itu-while-loop)
  - [Sintaks Dasar While Loop](#sintaks-dasar-while-loop)
  - [Kapan Menggunakan While Loop?](#kapan-menggunakan-while-loop)
  - [While Loop untuk Array](#while-loop-untuk-array)
  - [Nested While Loop](#nested-while-loop)
- [Do-While Loop](#do-while-loop)
  - [Apa itu Do-While Loop?](#apa-itu-do-while-loop)
  - [Sintaks Dasar Do-While Loop](#sintaks-dasar-do-while-loop)
  - [Perbedaan While vs Do-While](#perbedaan-while-vs-do-while)
- [Tips & Peringatan](#tips-peringatan)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

**While loop** dan **do-while loop** adalah dua cara lain untuk melakukan iterasi (perulangan) dalam JavaScript, selain for loop. Meskipun sekarang ada metode modern seperti `forEach()`, `for...of`, dan `for...in`, **penting untuk memahami while dan do-while** karena:

✅ **Fundamental** - Konsep dasar yang ada di hampir semua bahasa pemrograman  
✅ **Fleksibel** - Cocok untuk kasus di mana jumlah iterasi tidak diketahui  
✅ **Sering dipakai** - Masih banyak digunakan dalam kode production  

---

<a name="while-loop"></a>
## 🔁 While Loop

<a name="apa-itu-while-loop"></a>
### 📖 Apa itu While Loop?

**While loop** adalah perulangan yang akan **terus berjalan selama kondisi bernilai `true`**. Berbeda dengan for loop, pada while loop:

- ✏️ **Inisialisasi** variabel dilakukan di luar loop
- 🔍 **Kondisi** ditulis di dalam tanda kurung `while()`
- ➕ **Increment/decrement** ditulis di dalam body loop

---

<a name="sintaks-dasar-while-loop"></a>
### 🛠️ Sintaks Dasar While Loop

**Format Penulisan:**
```javascript
// 1️⃣ Inisialisasi variabel (di luar loop)
let i = 0;

// 2️⃣ While loop dengan kondisi
while (i <= 20) {
  // 3️⃣ Kode yang akan dijalankan
  console.log('Number ' + i);
  
  // 4️⃣ Increment (WAJIB! Atau terjadi infinite loop)
  i++;
}
```

**📊 Output:**
```
Number 0
Number 1
Number 2
...
Number 20
```

**🔍 Penjelasan Step-by-Step:**

1. **`let i = 0;`** → Kita buat variabel `i` dengan nilai awal 0
2. **`while (i <= 20)`** → Loop akan jalan **selama** `i` kurang dari atau sama dengan 20
3. **`console.log('Number ' + i);`** → Cetak nilai `i` saat ini
4. **`i++;`** → **PENTING!** Tambah nilai `i` sebesar 1 (jika lupa, loop tidak akan berhenti!)

---

<a name="kapan-menggunakan-while-loop"></a>
### 🤔 Kapan Menggunakan While Loop?

| 🔄 For Loop | 🔁 While Loop |
|------------|---------------|
| Gunakan ketika **tahu persis** berapa kali loop akan berjalan | Gunakan ketika **tidak tahu** berapa kali loop akan berjalan |
| Contoh: Loop 10 kali, loop array dengan panjang tetap | Contoh: Loop sampai user input valid, loop sampai kondisi tertentu terpenuhi |

**💡 Contoh Kasus While Loop:**
```javascript
// Menunggu input user yang valid
let password = '';
while (password !== 'secret123') {
  password = prompt('Masukkan password:');
}

// Loop sampai angka random mencapai target
let randomNum = 0;
while (randomNum < 0.9) {
  randomNum = Math.random();
  console.log(randomNum);
}
```

---

<a name="while-loop-untuk-array"></a>
### 📦 While Loop untuk Array

While loop juga bisa digunakan untuk **loop melalui elemen array**, sama seperti for loop!

**Kode:**
```javascript
const arr = [10, 20, 30, 40, 50];

let i = 0; // ① Mulai dari index 0

while (i < arr.length) { // ② Selama i kurang dari panjang array
  console.log(arr[i]); // ③ Ambil elemen array di index i
  i++; // ④ Pindah ke index berikutnya
}
```

**📊 Output:**
```
10
20
30
40
50
```

**🔍 Penjelasan:**
- `arr.length` → Panjang array (dalam kasus ini = 5)
- `arr[i]` → Mengakses elemen array di index `i`
- Loop berhenti ketika `i = 5` (karena `5 < 5` adalah `false`)

---

<a name="nested-while-loop"></a>
### 🎭 Nested While Loop (While di dalam While)

**Nested loop** artinya **loop di dalam loop**. Sangat berguna untuk membuat tabel, pola, atau operasi 2 dimensi.

**Contoh: Tabel Perkalian**
```javascript
let i = 1; // ① Outer loop variable

while (i <= 5) { // ② Loop angka 1-5
  console.log('Number ' + i);

  let j = 1; // ③ Inner loop variable (harus reset setiap kali!)
  
  while (j <= 5) { // ④ Loop perkalian 1-5
    console.log(`${i} * ${j} = ${i * j}`);
    j++; // ⑤ Increment inner loop
  }

  i++; // ⑥ Increment outer loop
}
```

**📊 Output:**
```
Number 1
1 * 1 = 1
1 * 2 = 2
1 * 3 = 3
1 * 4 = 4
1 * 5 = 5
Number 2
2 * 1 = 2
2 * 2 = 4
2 * 3 = 6
... (dan seterusnya)
```

**🔍 Cara Kerja:**
1. **Outer loop** (`i`) berjalan dari 1-5
2. Untuk **setiap nilai `i`**, **inner loop** (`j`) berjalan dari 1-5
3. Total iterasi = 5 × 5 = **25 kali**

**⚠️ PENTING:** Jangan lupa increment **kedua variabel** (`i++` dan `j++`), jika tidak akan terjadi infinite loop!

---

<a name="do-while-loop"></a>
## 🔂 Do-While Loop

<a name="apa-itu-do-while-loop"></a>
### 📖 Apa itu Do-While Loop?

**Do-while loop** adalah jenis loop yang **PASTI dijalankan minimal 1 kali**, bahkan jika kondisinya `false` dari awal!

**🎯 Karakteristik:**
- ✅ Kode dijalankan **dulu**, kondisi dicek **kemudian**
- ✅ **Selalu jalan minimal 1x** (berbeda dengan while loop biasa)
- ✅ Cocok untuk kasus yang **butuh eksekusi pertama sebelum validasi**

---

<a name="sintaks-dasar-do-while-loop"></a>
### 🛠️ Sintaks Dasar Do-While Loop

**Format Penulisan:**
```javascript
let i = 1;

do {
  // ① Kode ini PASTI dijalankan minimal 1x
  console.log('Number ' + i);
  i++; // ② Increment
} while (i <= 20); // ③ Kondisi dicek di sini (setelah eksekusi)
```

**📊 Output:**
```
Number 1
Number 2
Number 3
...
Number 20
```

**🔍 Urutan Eksekusi:**
1. **JALANKAN** kode di dalam `do { }`
2. **CEK** kondisi di `while (kondisi)`
3. Jika `true` → Ulangi dari langkah 1
4. Jika `false` → Berhenti

---

<a name="perbedaan-while-vs-do-while"></a>
### ⚖️ Perbedaan While vs Do-While

**🧪 Percobaan 1: Kondisi Langsung False**

**While Loop Biasa:**
```javascript
let i = 21; // Nilai awal sudah tidak memenuhi kondisi

while (i <= 20) {
  console.log('Number ' + i);
  i++;
}

// 📊 Output: (TIDAK ADA - tidak dijalankan sama sekali!)
```

**Do-While Loop:**
```javascript
let i = 21; // Nilai awal sudah tidak memenuhi kondisi

do {
  console.log('Number ' + i);
  i++;
} while (i <= 20);

// 📊 Output: Number 21 (TETAP JALAN 1x meskipun kondisi false!)
```

**📝 Kesimpulan Perbedaan:**

| Aspek | While Loop | Do-While Loop |
|-------|------------|---------------|
| **Pengecekan kondisi** | Di **awal** sebelum eksekusi | Di **akhir** setelah eksekusi |
| **Eksekusi minimal** | 0x (jika kondisi false dari awal) | 1x (pasti jalan sekali) |
| **Kapan dipakai** | Loop normal dengan validasi dulu | Butuh eksekusi minimal 1x |

**💡 Contoh Kasus Do-While:**
```javascript
// Menu interaktif - HARUS tampil minimal 1x
let choice;
do {
  console.log('=== MENU ===');
  console.log('1. Option A');
  console.log('2. Option B');
  console.log('0. Exit');
  choice = prompt('Pilihan Anda:');
} while (choice !== '0');
```

---

<a name="tips-peringatan"></a>
## ⚠️ Tips & Peringatan

### 🚨 **BAHAYA: Infinite Loop!**

Lupa increment adalah **kesalahan paling umum** dan menyebabkan **infinite loop** (loop tidak pernah berhenti)!

**❌ SALAH (Infinite Loop):**
```javascript
let i = 0;
while (i <= 20) {
  console.log(i);
  // ❌ LUPA i++! Loop tidak akan pernah berhenti!
}
```

**✅ BENAR:**
```javascript
let i = 0;
while (i <= 20) {
  console.log(i);
  i++; // ✅ JANGAN LUPA INCREMENT!
}
```

---

### 💡 **Tips Praktis**

**1️⃣ Gunakan Break & Continue**
```javascript
let i = 0;
while (i <= 10) {
  i++;
  
  if (i === 5) continue; // Skip angka 5
  if (i === 8) break; // Berhenti di angka 8
  
  console.log(i);
}
// Output: 1, 2, 3, 4, 6, 7
```

**2️⃣ Selalu Pastikan Kondisi Bisa Tercapai**
```javascript
// ❌ BAD: Kondisi tidak akan pernah true
let x = 0;
while (x > 10) { // x dimulai dari 0, tidak akan pernah > 10
  console.log(x);
  x++;
}

// ✅ GOOD: Kondisi jelas dan bisa tercapai
let x = 0;
while (x < 10) {
  console.log(x);
  x++;
}
```

**3️⃣ Gunakan Variabel dengan Nama Jelas**
```javascript
// ❌ Kurang jelas
let i = 0;
while (i < users.length) {
  console.log(users[i]);
  i++;
}

// ✅ Lebih jelas
let userIndex = 0;
while (userIndex < users.length) {
  console.log(users[userIndex]);
  userIndex++;
}
```

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

### 📌 Rangkuman Penting

| Loop Type | Kapan Dipakai | Eksekusi Minimal |
|-----------|---------------|------------------|
| **For Loop** | Tahu berapa kali loop berjalan | 0x |
| **While Loop** | Tidak tahu berapa kali loop berjalan | 0x |
| **Do-While Loop** | Butuh eksekusi minimal 1x | 1x |

### ✅ Yang Harus Diingat:

1. **While loop** → Kondisi dicek **SEBELUM** eksekusi
2. **Do-while loop** → Kondisi dicek **SETELAH** eksekusi (pasti jalan 1x)
3. **Jangan lupa increment** → Atau terjadi infinite loop! 🔥
4. **Break & Continue** → Bisa dipakai di semua jenis loop
5. **Nested loop** → Loop di dalam loop (hati-hati dengan performance!)

### 🎯 Next Steps:

- ✅ Praktik dengan membuat program sederhana menggunakan while loop
- ✅ Coba buat nested loop untuk membuat pola atau tabel
- ✅ Pelajari `for...of` dan `for...in` sebagai alternatif modern
- ✅ Eksplorasi high-order array methods: `forEach()`, `map()`, `filter()`

---

**📚 Selamat belajar dan terus berlatih! 💪**

> *"Practice makes perfect - semakin sering latihan, semakin paham!"* 🚀
