# 📚 Dokumentasi Call Stack JavaScript
## 🎯 Untuk Pemula - Belajar Step by Step

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Call Stack?](#apa-itu-call-stack)
- [Konsep LIFO (Last In First Out)](#konsep-lifo)
- [Cara Kerja Call Stack](#cara-kerja-call-stack)
- [Contoh 1: Function Berurutan](#contoh-1-function-berurutan)
- [Contoh 2: Nested Function Calls](#contoh-2-nested-function-calls)
- [Praktek dengan Browser DevTools](#praktek-dengan-browser-devtools)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Selamat datang di dokumentasi **Call Stack JavaScript**! 🎉

Dokumentasi ini akan membantu kamu memahami konsep **Call Stack** dengan cara yang mudah dipahami. Call Stack adalah salah satu konsep penting dalam JavaScript yang akan sangat berguna ketika kamu mulai belajar konsep yang lebih advanced.

> 💡 **Catatan**: Tenang saja! Kita akan belajar perlahan dan dengan contoh yang mudah dipahami.

---

<a name="apa-itu-call-stack"></a>
## 📖 Apa itu Call Stack?

**Call Stack** adalah sebuah mekanisme yang digunakan JavaScript untuk mengelola **eksekusi function**. 

### 🔑 Poin Penting:

- ✅ **Stack of functions** → Tumpukan function yang akan dieksekusi
- ✅ **Manages execution contexts** → Mengelola konteks eksekusi
- ✅ **LIFO principle** → Menggunakan prinsip Last In First Out

Bayangkan Call Stack seperti **tumpukan piring**: 🍽️
- Piring yang terakhir ditumpuk akan diambil **pertama kali**
- Piring yang pertama ditumpuk akan diambil **terakhir**

---

<a name="konsep-lifo"></a>
## 🔄 Konsep LIFO (Last In First Out)

**LIFO** adalah prinsip dasar dari Stack:

```
📦 LIFO = Last In First Out
   
   Yang TERAKHIR MASUK → Yang PERTAMA KELUAR
```

### 📊 Visualisasi:

```
┌─────────────┐
│   third()   │ ← Masuk Terakhir (Push)
├─────────────┤
│  second()   │
├─────────────┤
│   first()   │ ← Masuk Pertama
├─────────────┤
│  Global EC  │ ← Selalu di bawah
└─────────────┘

Keluar dengan urutan:
1. third()  → Pop! ❌
2. second() → Pop! ❌
3. first()  → Pop! ❌
```

> 🎯 **Global EC** = Global Execution Context (selalu ada di paling bawah stack)

---

<a name="cara-kerja-call-stack"></a>
## ⚙️ Cara Kerja Call Stack

### 📝 Proses Dasar:

1. **Push** 📥 → Function ditambahkan ke stack saat dipanggil
2. **Execute** ⚡ → Function dijalankan
3. **Pop** 📤 → Function dihapus dari stack setelah selesai

```javascript
// Ketika function dipanggil:
function myFunction() {
  console.log('Hello!');
}

myFunction(); // ← Function di-PUSH ke stack
              // ← Function dieksekusi
              // ← Function di-POP dari stack
```

---

<a name="contoh-1-function-berurutan"></a>
## 📌 Contoh 1: Function Berurutan (Sequential)

### 💻 Kode:

```javascript
function first() {
  console.log('first...');
}

function second() {
  console.log('second...');
}

function third() {
  console.log('third...');
}

// Pemanggilan berurutan
first();
second();
third();
```

### 🎬 Alur Call Stack:

#### **Step 1**: `first()` dipanggil
```
┌─────────────┐
│   first()   │ ← PUSH
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 2**: `first()` selesai
```
┌─────────────┐
│  Global EC  │ ← first() sudah POP
└─────────────┘
```

#### **Step 3**: `second()` dipanggil
```
┌─────────────┐
│  second()   │ ← PUSH
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 4**: `second()` selesai
```
┌─────────────┐
│  Global EC  │ ← second() sudah POP
└─────────────┘
```

#### **Step 5**: `third()` dipanggil
```
┌─────────────┐
│   third()   │ ← PUSH
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 6**: `third()` selesai
```
┌─────────────┐
│  Global EC  │ ← third() sudah POP
└─────────────┘
```

### 📊 Output Console:
```
first...
second...
third...
```

> 💡 **Penjelasan**: Karena function dipanggil berurutan dan tidak ada yang nested (bersarang), masing-masing function di-push, dieksekusi, dan di-pop secara terpisah.

---

<a name="contoh-2-nested-function-calls"></a>
## 📌 Contoh 2: Nested Function Calls (Bersarang)

### 💻 Kode:

```javascript
function first() {
  console.log('first...');
  second(); // ← Memanggil second di dalam first
}

function second() {
  console.log('second...');
  third(); // ← Memanggil third di dalam second
}

function third() {
  console.log('third...');
}

// Hanya memanggil first
first();
```

### 🎬 Alur Call Stack:

#### **Step 1**: `first()` dipanggil
```
┌─────────────┐
│   first()   │ ← PUSH (belum selesai)
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 2**: Di dalam `first()`, `second()` dipanggil
```
┌─────────────┐
│  second()   │ ← PUSH (second masuk)
├─────────────┤
│   first()   │ ← Masih di stack (belum selesai)
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 3**: Di dalam `second()`, `third()` dipanggil
```
┌─────────────┐
│   third()   │ ← PUSH (third masuk)
├─────────────┤
│  second()   │ ← Masih di stack (belum selesai)
├─────────────┤
│   first()   │ ← Masih di stack (belum selesai)
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 4**: `third()` selesai
```
┌─────────────┐
│  second()   │ ← third() sudah POP ✅
├─────────────┤
│   first()   │
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 5**: `second()` selesai
```
┌─────────────┐
│   first()   │ ← second() sudah POP ✅
├─────────────┤
│  Global EC  │
└─────────────┘
```

#### **Step 6**: `first()` selesai
```
┌─────────────┐
│  Global EC  │ ← first() sudah POP ✅
└─────────────┘
```

### 📊 Output Console:
```
first...
second...
third...
```

> 💡 **Penjelasan**: Function saling memanggil (nested), sehingga mereka menumpuk di stack. Function yang dipanggil terakhir (`third`) akan selesai pertama (LIFO).

---

<a name="praktek-dengan-browser-devtools"></a>
## 🔍 Praktek dengan Browser DevTools

Kamu bisa melihat Call Stack secara real-time menggunakan **Browser DevTools**! 🛠️

### 📋 Langkah-langkah:

1. **Buka file HTML** dengan JavaScript di browser
2. **Buka DevTools**: 
   - Windows/Linux: `F12` atau `Ctrl + Shift + I`
   - Mac: `Cmd + Option + I`
3. **Pergi ke tab "Sources"** 📂
4. **Pilih file JavaScript** kamu (misalnya `script.js`)
5. **Klik nomor baris** untuk menambahkan **breakpoint** 🔴
6. **Reload halaman** (F5)
7. **Lihat tab "Call Stack"** di panel kanan bawah 👀

### 🎯 Tips Debugging:

```javascript
// Contoh: Letakkan breakpoint di baris ini
first(); // ← Klik nomor baris di sini untuk breakpoint
```

#### 🔘 Tombol-tombol penting:

- ▶️ **Step Over** → Jalankan baris saat ini, lanjut ke baris berikutnya
- ⬇️ **Step Into** → Masuk ke dalam function yang dipanggil
- ⬆️ **Step Out** → Keluar dari function saat ini
- ▶️ **Resume** → Lanjutkan eksekusi sampai breakpoint berikutnya

### 📸 Apa yang akan kamu lihat:

```
Call Stack Panel:
┌──────────────────┐
│ third            │ ← Function paling atas (sedang dieksekusi)
│ second           │
│ first            │
│ (anonymous)      │ ← Global Execution Context
└──────────────────┘
```

> 🎉 **Selamat!** Sekarang kamu bisa melihat Call Stack bekerja secara langsung!

---

<a name="rangkuman"></a>
## 📝 Rangkuman

### ✨ Poin-poin Penting:

1. **Call Stack** adalah mekanisme untuk mengelola eksekusi function
2. Menggunakan prinsip **LIFO** (Last In First Out)
3. Function yang dipanggil → **PUSH** ke stack
4. Function yang selesai → **POP** dari stack
5. **Global Execution Context** selalu ada di dasar stack
6. Bisa dilihat langsung di Browser DevTools (tab Sources → Call Stack)

### 🎯 Analogi Sederhana:

```
Call Stack = Tumpukan Piring 🍽️

Piring Terakhir Ditumpuk     ↔️  Function Terakhir Dipanggil
Diambil Pertama Kali         ↔️  Selesai Pertama Kali
```

### 🔑 Key Terms:

| Term | Penjelasan |
|------|------------|
| **Push** | Menambahkan function ke stack |
| **Pop** | Menghapus function dari stack |
| **LIFO** | Last In First Out (terakhir masuk, pertama keluar) |
| **Global EC** | Global Execution Context (konteks global) |
| **Stack** | Struktur data tumpukan |

---

## 🎓 Apa Selanjutnya?

Setelah memahami Call Stack, kamu siap untuk belajar:
- ⏭️ Control Flow & Logic
- ⏭️ Conditionals (if/else)
- ⏭️ Asynchronous JavaScript
- ⏭️ Event Loop
- ⏭️ Callback Functions

---

## 💪 Latihan

Coba jalankan kode berikut dan tebak urutan output-nya:

```javascript
function a() {
  console.log('A start');
  b();
  console.log('A end');
}

function b() {
  console.log('B start');
  c();
  console.log('B end');
}

function c() {
  console.log('C');
}

a();
```

<details>
<summary>👁️ Lihat Jawaban</summary>

Output:
```
A start
B start
C
B end
A end
```

Penjelasan:
1. `a()` dipanggil → Print "A start"
2. `b()` dipanggil dari `a()` → Print "B start"
3. `c()` dipanggil dari `b()` → Print "C"
4. `c()` selesai, kembali ke `b()` → Print "B end"
5. `b()` selesai, kembali ke `a()` → Print "A end"

</details>

---

## 🙌 Penutup

Selamat! 🎉 Kamu sudah memahami konsep **Call Stack** dalam JavaScript. Konsep ini sangat fundamental dan akan sangat membantu kamu dalam memahami bagaimana JavaScript bekerja di balik layar.

> 💡 **Tips**: Terus latihan dengan membuat berbagai kombinasi function calls dan lihat hasilnya di DevTools!

**Happy Coding!** 👨‍💻👩‍💻

---

📅 **Dibuat untuk**: Dokumentasi Pribadi  
🎯 **Level**: Pemula  
📚 **Topik**: JavaScript Fundamentals - Call Stack
