# 📚 Panduan Lengkap JavaScript Functions untuk Pemula

> 🎓 **Dokumentasi Pribadi** - Catatan pembelajaran JavaScript tentang Functions dan Scope

---

## 📋 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Function?](#apa-itu-function)
- [Cara Membuat Function](#cara-membuat-function)
- [Function Sederhana](#function-sederhana)
- [Parameters vs Arguments](#parameters-vs-arguments)
- [Istilah Penting dalam Function](#istilah-penting-dalam-function)
- [Mengembalikan Nilai (Return)](#mengembalikan-nilai-return)
- [Kapan Menggunakan Return](#kapan-menggunakan-return)
- [Scope dalam Function](#scope-dalam-function)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dokumentasi ini dibuat untuk memahami konsep **Functions** dalam JavaScript dari nol. Materi disusun secara bertahap agar mudah dipahami oleh pemula yang baru belajar programming.

---

<a name="apa-itu-function"></a>
## 📖 Apa itu Function?

**Function** adalah cara untuk **mengelompokkan kode** yang bisa kita jalankan kapan saja dan sebanyak yang kita mau.

### ✨ Keuntungan Menggunakan Function:
- 🔄 **Reusable** - Bisa digunakan berulang kali
- 📖 **Readable** - Membuat kode lebih mudah dibaca
- 🎯 **Organized** - Kode lebih terstruktur dan rapi

---

<a name="cara-membuat-function"></a>
## 🛠️ Cara Membuat Function

Ada beberapa cara membuat function di JavaScript. Untuk pemula, kita mulai dengan cara **paling dasar** menggunakan keyword `function`.

### 📝 Struktur Dasar Function:

```javascript
function namaFunction() {
  // Function body - kode yang akan dijalankan
}
```

**Penjelasan:**
- `function` → Keyword untuk membuat function
- `namaFunction` → Nama function (boleh apa saja)
- `()` → Parentheses (kurung bulat) untuk parameters
- `{}` → Curly braces (kurung kurawal) untuk function body

---

<a name="function-sederhana"></a>
## 🌟 Function Sederhana

### Contoh 1: Function Tanpa Parameter

```javascript
// Define a function (Mendefinisikan/membuat function)
function sayHello() {
  console.log('Hello World');
}

// Invoke, execute or call a function (Menjalankan function)
sayHello();
```

**💡 Output:**
```
Hello World
```

**📌 Catatan Penting:**
- ⚠️ Jika hanya menulis `function sayHello() {...}` tanpa `sayHello()`, function **TIDAK akan berjalan**
- ✅ Function harus **dipanggil/dijalankan** dengan menulis `sayHello()`

---

<a name="parameters-vs-arguments"></a>
## 🔀 Parameters vs Arguments

Ini adalah konsep penting yang sering membingungkan pemula!

### 🎯 Perbedaan Parameters dan Arguments

| **Parameters** | **Arguments** |
|----------------|---------------|
| Variabel saat **MEMBUAT** function | Nilai saat **MEMANGGIL** function |
| Ada di dalam `()` saat definisi | Ada di dalam `()` saat pemanggilan |

### Contoh 2: Function dengan Parameters

```javascript
// Define a function with parameters
// num1 dan num2 adalah PARAMETERS
function add(num1, num2) {
  console.log(num1 + num2);
}

// Invoke function with arguments
// 5 dan 10 adalah ARGUMENTS
add(5, 10);
```

**💡 Output:**
```
15
```

**📝 Penjelasan:**
- `num1` dan `num2` → **Parameters** (placeholder untuk menerima data)
- `5` dan `10` → **Arguments** (nilai aktual yang dikirim)
- Function akan menjumlahkan kedua nilai dan menampilkannya

---

<a name="istilah-penting-dalam-function"></a>
## 📚 Istilah Penting dalam Function

### 🏷️ Vocabulary yang Perlu Diketahui:

1. **Defining / Declaring a Function** (Membuat Function)
   ```javascript
   function sayHello() {
     console.log('Hello');
   }
   ```

2. **Invoking / Calling / Executing a Function** (Menjalankan Function)
   ```javascript
   sayHello();
   ```

3. **Parameters** (Saat membuat function)
   ```javascript
   function greet(name) { // 'name' adalah parameter
     console.log('Hello ' + name);
   }
   ```

4. **Arguments** (Saat memanggil function)
   ```javascript
   greet('John'); // 'John' adalah argument
   ```

**💬 Catatan:**
> Banyak orang menggunakan istilah "parameter" dan "argument" secara bergantian, dan itu tidak masalah! Yang penting kita paham konsepnya.

---

<a name="mengembalikan-nilai-return"></a>
## 🔄 Mengembalikan Nilai (Return)

Biasanya function tidak hanya menjalankan kode, tapi juga **mengembalikan nilai** yang bisa kita gunakan.

### Contoh 3: Function dengan Return

```javascript
// Returning a value
function subtract(num1, num2) {
  return num1 - num2;

  console.log('After the return'); // ⚠️ Tidak akan dijalankan!
}
```

**⚡ Fakta Penting tentang `return`:**
- ✅ Mengembalikan nilai dari function
- 🛑 **Menghentikan** eksekusi function
- ❌ Kode setelah `return` **TIDAK akan dijalankan**

### Contoh 4: Menggunakan Nilai Return

```javascript
// Assign the returned value to a variable
const result = subtract(10, 2);

console.log(result); // Output: 8
console.log(subtract(20, 5)); // Output: 15
```

**💡 Penjelasan:**
- `result` menyimpan nilai yang dikembalikan dari `subtract(10, 2)`
- Kita bisa langsung console.log function tanpa menyimpannya ke variabel
- Nilai return bisa digunakan untuk operasi lebih lanjut

---

<a name="kapan-menggunakan-return"></a>
## 🤔 Kapan Menggunakan Return?

### ✅ Function DENGAN Return Value

```javascript
function multiply(a, b) {
  return a * b; // Mengembalikan hasil
}

const result = multiply(5, 3);
console.log(result); // 15
```

**Kapan digunakan:**
- Ketika butuh hasil perhitungan
- Ketika ingin menggunakan nilai di tempat lain
- Ketika perlu menyimpan nilai ke variabel

### 🔹 Function TANPA Return Value

```javascript
function logMessage(message) {
  console.log(message); // Hanya menampilkan
  // Tidak ada return
}

logMessage('Hello!');
```

**Kapan digunakan:**
- Hanya menampilkan informasi
- Update data ke database/API tanpa perlu nilai balik
- Melakukan action tanpa perlu hasil

### 🆓 Return Tanpa Nilai

```javascript
function checkAge(age) {
  if (age < 18) {
    return; // Keluar dari function tanpa nilai
  }
  console.log('Akses diberikan');
}
```

**Kapan digunakan:**
- Keluar dari function lebih awal
- Tidak perlu mengembalikan nilai apapun

---

<a name="scope-dalam-function"></a>
## 🎯 Scope dalam Function

**Scope** adalah "jangkauan" dimana variabel bisa diakses.

### 📦 Function Scope vs Global Scope

```javascript
// ❌ SALAH - Tidak bisa membuat variabel sama di global scope
const num1 = 5;
const num1 = 10; // ERROR!

// ✅ BENAR - Bisa menggunakan nama sama di function berbeda
function add(num1, num2) {
  return num1 + num2;
}

function subtract(num1, num2) {
  return num1 - num2;
}
```

**💡 Penjelasan:**
- `num1` di function `add` berbeda dengan `num1` di function `subtract`
- Setiap function punya **scope sendiri**
- Parameters dalam function **terisolasi** dan tidak bentrok

---

<a name="rangkuman"></a>
## 📊 Rangkuman

### ✅ Checklist Pembelajaran:

- [x] Function adalah cara mengelompokkan kode yang bisa dijalankan kapan saja
- [x] Cara membuat function: `function namaFunction() { ... }`
- [x] Cara menjalankan function: `namaFunction()`
- [x] **Parameters** = variabel saat membuat function
- [x] **Arguments** = nilai saat memanggil function
- [x] **Return** = mengembalikan nilai dari function
- [x] Kode setelah `return` tidak akan dijalankan
- [x] Setiap function punya scope sendiri

### 🎯 Contoh Lengkap:

```javascript
// ✨ FULL EXAMPLE

// 1. Function sederhana
function sayHello() {
  console.log('Hello World');
}
sayHello(); // Output: Hello World

// 2. Function dengan parameters
function add(num1, num2) {
  console.log(num1 + num2);
}
add(5, 10); // Output: 15

// 3. Function dengan return
function subtract(num1, num2) {
  return num1 - num2;
}
const result = subtract(10, 2);
console.log(result); // Output: 8
console.log(subtract(20, 5)); // Output: 15
```

---

## 🎓 Tips Belajar

1. 💪 **Practice Makes Perfect** - Latihan terus membuat function
2. 🧪 **Experiment** - Coba berbagai kombinasi parameters
3. 🐛 **Debug** - Jangan takut error, justru dari error kita belajar
4. 📝 **Take Notes** - Catat hal-hal penting seperti dokumentasi ini

---

## 🚀 Next Steps

Setelah memahami dasar-dasar function, topik selanjutnya yang bisa dipelajari:
- 🔸 Arrow Functions (`=>`)
- 🔸 Default Parameters
- 🔸 Rest Parameters (`...`)
- 🔸 Function Expressions
- 🔸 Callback Functions

---

> 💡 **Remember:** Belajar programming itu seperti belajar bahasa baru. Butuh waktu dan praktek terus-menerus. Jangan menyerah! 💪

**Happy Coding! 🎉**
