# 🚀 Memahami Execution Context di JavaScript

> **Dokumentasi Pribadi untuk Pemula** - Belajar JavaScript Step by Step

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Execution Context?](#apa-itu-execution-context)
- [Memory Creation Phase](#memory-creation-phase)
- [Execution Phase](#execution-phase)
- [Kode Praktek](#kode-praktek)
- [Debugging dengan Browser DevTools](#debugging-dengan-browser-devtools)
- [Function Execution Context](#function-execution-context)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Dalam JavaScript, memahami bagaimana kode dijalankan "di balik layar" sangat penting. Dokumen ini akan membantu kamu memahami konsep **Execution Context** dengan cara yang sederhana dan praktis.

### 🎓 Apa yang Akan Dipelajari?
- ✅ Bagaimana JavaScript menjalankan kode
- ✅ Memory Creation Phase vs Execution Phase
- ✅ Cara debugging menggunakan Browser DevTools
- ✅ Function Execution Context

---

<a name="apa-itu-execution-context"></a>
## 📖 Apa itu Execution Context?

**Execution Context** adalah "lingkungan" di mana kode JavaScript dijalankan. Setiap kali kode JavaScript berjalan, ia menciptakan execution context.

### 🔍 Dua Fase Penting:

1. **Memory Creation Phase** (Fase Pembuatan Memori)
2. **Execution Phase** (Fase Eksekusi)

> 💡 **Analogi Sederhana**: Bayangkan kamu akan memasak. Memory Creation Phase seperti menyiapkan semua bahan dan alat di meja dapur. Execution Phase adalah ketika kamu mulai memasak dengan bahan-bahan tersebut.

---

<a name="memory-creation-phase"></a>
## 🧠 Memory Creation Phase

Ini adalah **fase pertama** yang terjadi **sebelum** kode dijalankan.

### ✨ Apa yang Terjadi?

1. **Variabel** → Disimpan di memori dengan nilai `undefined`
2. **Fungsi** → Disimpan di memori dengan **seluruh kode fungsi**

### 📝 Contoh:

```javascript
const x = 100;
const y = 50;

function getSum(n1, n2) {
  const sum = n1 + n2;
  return sum;
}
```

**Saat Memory Creation Phase:**
- `x` → `undefined`
- `y` → `undefined`
- `getSum` → **[Function: getSum]** (seluruh kode fungsi disimpan)

> ⚠️ **Penting**: Variabel yang dideklarasikan dengan `const` atau `let` **tidak** masuk ke global scope. Tapi jika menggunakan `var`, variabel akan masuk ke global scope.

---

<a name="execution-phase"></a>
## ⚡ Execution Phase

Ini adalah **fase kedua** di mana kode **benar-benar dijalankan** baris per baris.

### 🔄 Apa yang Terjadi?

1. **Variabel** → Mendapat nilai sebenarnya
2. **Fungsi** → Dijalankan ketika dipanggil

### 📝 Contoh Proses:

```javascript
const x = 100;  // ✅ x sekarang = 100 (bukan undefined lagi)
const y = 50;   // ✅ y sekarang = 50 (bukan undefined lagi)
```

> 🚀 **Fun Fact**: Execution phase biasanya terjadi sangat cepat! Tapi kita bisa memperlambatnya menggunakan debugger.

---

<a name="kode-praktek"></a>
## 💻 Kode Praktek

Berikut adalah kode lengkap yang akan kita pelajari:

```javascript
const x = 100;
const y = 50;

function getSum(n1, n2) {
  const sum = n1 + n2;
  return sum;
}

const sum1 = getSum(x, y);
const sum2 = getSum(10, 5);

console.log(sum1, sum2);
```

### 📊 Penjelasan Kode:

| Baris | Kode | Penjelasan |
|-------|------|------------|
| 1 | `const x = 100;` | Membuat variabel `x` dengan nilai 100 |
| 2 | `const y = 50;` | Membuat variabel `y` dengan nilai 50 |
| 4-7 | `function getSum(n1, n2) {...}` | Mendefinisikan fungsi untuk menjumlahkan dua angka |
| 9 | `const sum1 = getSum(x, y);` | Memanggil fungsi dengan x dan y (hasil: 150) |
| 10 | `const sum2 = getSum(10, 5);` | Memanggil fungsi dengan 10 dan 5 (hasil: 15) |
| 12 | `console.log(sum1, sum2);` | Menampilkan hasil: 150 15 |

---

<a name="debugging-dengan-browser-devtools"></a>
## 🔧 Debugging dengan Browser DevTools

### 🛠️ Langkah-langkah Debugging:

#### 1️⃣ Buka DevTools Sources Tab
- Tekan `F12` atau klik kanan → Inspect
- Pilih tab **"Sources"**
- Cari file `script.js`

#### 2️⃣ Pasang Breakpoint
- Klik pada **nomor baris** di sebelah kiri kode
- Muncul tanda biru → artinya breakpoint sudah terpasang

> 💡 **Breakpoint**: Titik berhenti sementara untuk kode. Kode akan berhenti di baris tersebut.

#### 3️⃣ Reload Halaman
- Tekan `F5` atau klik reload
- Kode akan **berhenti** di breakpoint

#### 4️⃣ Lihat Memory Creation Phase
Di bagian bawah DevTools, kamu akan melihat:

```
▼ Local
  x: undefined
  y: undefined
  sum1: undefined
  sum2: undefined

▼ Global
  getSum: ƒ getSum(n1, n2)
```

**Penjelasan:**
- ✅ Semua variabel sudah ada di memori dengan nilai `undefined`
- ✅ Fungsi `getSum` sudah tersimpan lengkap dengan kodenya

#### 5️⃣ Step Through Code
Gunakan tombol **Step Over** (▶️ dengan garis di atasnya) untuk menjalankan kode baris per baris.

**Setiap klik, kamu akan melihat:**

| Klik | Yang Terjadi | Tampilan |
|------|--------------|----------|
| 1 | Baris `const x = 100;` dijalankan | `x: 100` |
| 2 | Baris `const y = 50;` dijalankan | `y: 50` |
| 3 | Fungsi `getSum(x, y)` dipanggil | Masuk ke function context |
| 4 | `const sum = n1 + n2;` dijalankan | `sum: 150` |

---

<a name="function-execution-context"></a>
## 🎪 Function Execution Context

### 🌟 Apa itu Function Execution Context?

Setiap kali **fungsi dipanggil**, JavaScript membuat **execution context baru** khusus untuk fungsi tersebut.

### 📦 Contoh: Memanggil `getSum(x, y)`

```javascript
const sum1 = getSum(x, y);  // Memanggil fungsi pertama kali
```

**Yang Terjadi:**

1️⃣ **Membuat Function Execution Context Baru**
```
▼ Local (Function Context)
  n1: 100
  n2: 50
  sum: undefined
```

2️⃣ **Menjalankan Kode di Dalam Fungsi**
```javascript
const sum = n1 + n2;  // 100 + 50 = 150
```

3️⃣ **Setelah Eksekusi**
```
▼ Local (Function Context)
  n1: 100
  n2: 50
  sum: 150
```

4️⃣ **Return Nilai**
```javascript
return sum;  // Mengembalikan 150
```

### 🔄 Memanggil Fungsi Kedua Kali

```javascript
const sum2 = getSum(10, 5);  // Memanggil fungsi kedua kali
```

**JavaScript membuat execution context BARU lagi:**

```
▼ Local (Function Context - Panggilan Kedua)
  n1: 10
  n2: 5
  sum: 15
```

> 🎯 **Poin Penting**: Setiap pemanggilan fungsi = execution context baru!

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

### ✅ Yang Sudah Dipelajari:

1. **Execution Context** = Lingkungan di mana kode dijalankan
2. **Memory Creation Phase** = Persiapan sebelum eksekusi
   - Variabel → `undefined`
   - Fungsi → Disimpan lengkap
3. **Execution Phase** = Kode dijalankan baris per baris
4. **Function Execution Context** = Context baru untuk setiap pemanggilan fungsi
5. **Debugging** = Cara melihat proses eksekusi dengan DevTools

### 🎯 Tips untuk Pemula:

- 💡 Jangan khawatir jika masih bingung - konsep ini butuh waktu untuk dipahami
- 🔄 Praktikkan berkali-kali dengan kode yang berbeda
- 🔍 Gunakan debugger untuk melihat apa yang terjadi "di balik layar"
- 📚 Konsep ini akan membantu memahami **hoisting**, **scope**, dan **call stack** nanti

### 🚀 Next Steps:

Setelah memahami Execution Context, topik selanjutnya yang akan dipelajari:

1. **Call Stack** - Bagaimana JavaScript melacak fungsi yang dipanggil
2. **Hoisting** - Mengapa kamu bisa memanggil fungsi sebelum dideklarasikan
3. **Scope** - Jangkauan akses variabel
4. **Conditionals & Loops** - Membuat keputusan dan pengulangan

---

## 📌 Catatan Tambahan

### 🤔 Pertanyaan Umum:

**Q: Mengapa harus memahami ini?**  
**A:** Memahami execution context membantu kamu:
- Debug kode dengan lebih baik
- Memahami error yang muncul
- Menulis kode yang lebih efisien

**Q: Apakah harus hapal semua ini?**  
**A:** Tidak perlu hapal! Yang penting paham konsepnya. Semakin sering praktik, akan semakin natural.

**Q: Kapan menggunakan debugger?**  
**A:** Gunakan debugger ketika:
- Kode tidak berjalan seperti yang diharapkan
- Ingin memahami alur eksekusi kode
- Mencari bug/error

---

### 🌈 Motivasi

> "Programming isn't about what you know; it's about what you can figure out."  
> — Chris Pine

**Selamat belajar! 🎉**

---

**📅 Dibuat untuk pembelajaran pribadi**  
**🔖 Tag**: JavaScript Fundamentals, Execution Context, Debugging
