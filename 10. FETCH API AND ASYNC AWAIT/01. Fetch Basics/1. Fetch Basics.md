# 📚 Dokumentasi Fetch API untuk Pemula

> 🎓 **Catatan Belajar Pribadi** - Memahami cara mengambil data dari server menggunakan Fetch API

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Perbandingan dengan XMLHttpRequest](#perbandingan-dengan-xmlhttprequest)
- [Apa itu Fetch API?](#apa-itu-fetch-api)
- [Cara Kerja Fetch](#cara-kerja-fetch)
- [Response Object](#response-object)
- [Fetching JSON File](#fetching-json-file)
- [Fetching Text File](#fetching-text-file)
- [Fetching dari API](#fetching-dari-api)
- [Contoh Lengkap - GitHub API](#contoh-lengkap-github-api)
- [Tips dan Best Practices](#tips-dan-best-practices)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Fetch API adalah cara **modern dan lebih sederhana** untuk membuat HTTP requests di JavaScript. Ini menggantikan cara lama menggunakan XMLHttpRequest (XHR).

### ✨ Keunggulan Fetch API:
- ✅ Lebih mudah dibaca dan dipahami
- ✅ Menggunakan Promise (lebih clean)
- ✅ Syntax yang lebih ringkas
- ✅ Built-in di browser modern

---

<a name="perbandingan-dengan-xmlhttprequest"></a>
## 🔄 Perbandingan dengan XMLHttpRequest

### 🕰️ **Cara Lama (XMLHttpRequest):**
```javascript
// Cara old school - lebih ribet
const xhr = new XMLHttpRequest();
xhr.open('GET', 'data.json');
xhr.onload = function() {
  console.log(xhr.response);
};
xhr.send();
```

### 🚀 **Cara Modern (Fetch API):**
```javascript
// Cara modern - lebih simple!
fetch('data.json')
  .then(response => response.json())
  .then(data => console.log(data));
```

> 💡 **Perbedaan Utama:** Fetch API menggunakan **Promise**, sehingga kita bisa menggunakan `.then()` untuk menangani hasilnya.

---

<a name="apa-itu-fetch-api"></a>
## 📖 Apa itu Fetch API?

Fetch adalah **method yang tersedia di window object** browser. Kita bisa mengaksesnya langsung tanpa perlu menulis `window.fetch()`.

```javascript
// Bisa ditulis seperti ini (tapi jarang dipakai)
window.fetch('data.json');

// Biasanya kita tulis seperti ini
fetch('data.json');
```

### 🎯 Kegunaan Fetch API:

1. 🌐 **Mengambil data dari API publik** (Twitter, GitHub, dll)
2. 🖥️ **Mengambil data dari backend server sendiri**
3. 📄 **Membaca file** (JSON, TXT, dll)

---

<a name="cara-kerja-fetch"></a>
## ⚙️ Cara Kerja Fetch

### 🔍 **Konsep 2 Langkah:**

Fetch API bekerja dalam **2 tahap Promise**:

```javascript
fetch('data.json')
  // TAHAP 1: Mendapat Response Object
  .then(response => response.json())
  // TAHAP 2: Mendapat Data Asli
  .then(data => console.log(data));
```

#### 📊 **Alur Kerja:**

```
1. fetch('url') 
   ↓
2. Promise pertama → Response Object
   ↓
3. response.json() atau response.text()
   ↓
4. Promise kedua → Data yang kita inginkan
```

> ⚠️ **PENTING:** Jangan bingung! Response pertama **bukan langsung data**, tapi informasi tentang request-nya.

---

<a name="response-object"></a>
## 📦 Response Object

Saat kita `fetch()`, yang pertama kali kita dapat adalah **Response Object**.

### 🔎 **Isi Response Object:**

```javascript
fetch('movies.json')
  .then(response => {
    console.log(response);
    // Output: Response object dengan properties:
    // - status: 200
    // - statusText: "OK"
    // - ok: true
    // - headers: Headers
    // - body: ReadableStream
  });
```

### 📋 **Properties Penting:**

| Property | Penjelasan | Contoh |
|----------|------------|--------|
| `status` | Kode status HTTP | `200` (sukses), `404` (not found) |
| `statusText` | Text dari status | `"OK"`, `"Not Found"` |
| `ok` | Boolean, true jika sukses | `true` atau `false` |
| `body` | Data mentah (ReadableStream) | Perlu diproses lagi |

### ✅ **Contoh Sukses (200 OK):**

```javascript
fetch('movies.json')
  .then(response => console.log(response));

// Response {
//   status: 200,
//   statusText: "OK",
//   ok: true,
//   ...
// }
```

### ❌ **Contoh Error (404 Not Found):**

```javascript
fetch('movies1.json') // file tidak ada
  .then(response => console.log(response));

// Response {
//   status: 404,
//   statusText: "Not Found",
//   ok: false,
//   ...
// }
```

---

<a name="fetching-json-file"></a>
## 📄 Fetching JSON File

### 🎬 **Contoh File: movies.json**

```json
[
  {
    "title": "Scarface",
    "release_year": "1983"
  },
  {
    "title": "The Godfather",
    "release_year": "1972"
  },
  {
    "title": "Goodfellas",
    "release_year": "1990"
  },
  {
    "title": "A Bronx Tale",
    "release_year": "1993"
  }
]
```

### 💻 **Cara Mengambil Data JSON:**

#### **Versi Lengkap:**
```javascript
fetch('./movies.json')
  .then(function(response) {
    return response.json(); // Mengubah response jadi JSON
  })
  .then(function(data) {
    console.log(data); // Data movies dalam bentuk array
  });
```

#### **Versi Ringkas (Arrow Function):**
```javascript
fetch('./movies.json')
  .then((response) => response.json())
  .then((data) => console.log(data));
```

> 💡 **Penjelasan:**
> - `.then(response => response.json())` → Mengubah response menjadi data JSON
> - `.then(data => console.log(data))` → Menampilkan data yang sudah jadi

### 📤 **Output:**

```javascript
[
  { title: "Scarface", release_year: "1983" },
  { title: "The Godfather", release_year: "1972" },
  { title: "Goodfellas", release_year: "1990" },
  { title: "A Bronx Tale", release_year: "1993" }
]
```

---

<a name="fetching-text-file"></a>
## 📝 Fetching Text File

Untuk file teks biasa (.txt), kita gunakan **`.text()`** bukan `.json()`.

### 📄 **Contoh File: test.txt**

```
This is a text file to test out with the Fetch API
```

### 💻 **Cara Mengambil Data Text:**

```javascript
fetch('./test.txt')
  .then((response) => response.text()) // Pakai .text() untuk file txt
  .then((data) => console.log(data));
```

### 📤 **Output:**

```
This is a text file to test out with the Fetch API
```

### ⚠️ **Kesalahan Umum:**

```javascript
// ❌ SALAH - Pakai .json() untuk file .txt
fetch('./test.txt')
  .then((response) => response.json())
  .then((data) => console.log(data));

// Error: Syntax error - This is not valid JSON
```

> 💡 **Ingat:**
> - File **JSON** → gunakan `.json()`
> - File **TXT** → gunakan `.text()`

---

<a name="fetching-dari-api"></a>
## 🌐 Fetching dari API

Biasanya kita fetch dari **API** (Application Programming Interface), bukan file lokal.

### 🔗 **Jenis API:**

1. 🌍 **API Publik** - Gratis untuk umum (GitHub, Twitter, Weather API, dll)
2. 🔐 **API Privat** - Butuh authentication/token
3. 🖥️ **API Sendiri** - Backend yang kita buat sendiri

### 🏗️ **Contoh Backend Technologies:**

- **Node.js** + Express
- **PHP** + Laravel
- **Python** + Django
- Dan lain-lain

> 📝 **Catatan:** Kursus ini fokus ke **frontend**, jadi kita akan pakai API publik yang sudah tersedia.

---

<a name="contoh-lengkap-github-api"></a>
## 🐙 Contoh Lengkap - GitHub API

GitHub menyediakan API gratis tanpa perlu authentication (untuk beberapa request pertama).

### 1️⃣ **Mendapat 30 User Pertama:**

```javascript
fetch('https://api.github.com/users')
  .then((response) => response.json())
  .then((data) => console.log(data));

// Output: Array berisi 30 user GitHub
```

### 2️⃣ **Mendapat User Spesifik:**

```javascript
fetch('https://api.github.com/users/bradtraversy')
  .then((response) => response.json())
  .then((data) => console.log(data));

// Output: Object dengan data user bradtraversy
```

### 📤 **Contoh Output:**

```javascript
{
  login: "bradtraversy",
  id: 5550850,
  avatar_url: "https://avatars.githubusercontent.com/u/5550850",
  name: "Brad Traversy",
  company: "Traversy Media",
  blog: "traversymedia.com",
  location: "Florida",
  // ... data lainnya
}
```

### 3️⃣ **Mendapat Repositories User:**

```javascript
fetch('https://api.github.com/users/bradtraversy/repos')
  .then((response) => response.json())
  .then((data) => console.log(data));

// Output: Array berisi 30 repositories pertama
```

### 4️⃣ **Menampilkan Data ke HTML:**

#### **HTML:**
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fetch API</title>
  </head>
  <body>
    <h1>Fetch API</h1>

    <script src="./script.js"></script>
  </body>
</html>
```

#### **JavaScript:**
```javascript
fetch('https://api.github.com/users/bradtraversy')
  .then((response) => response.json())
  .then((data) => {
    // Mengubah text H1 dengan username dari API
    document.querySelector('h1').textContent = data.login;
  });
```

#### **Hasil di Browser:**
```
bradtraversy  ← (username dari GitHub API)
```

---

<a name="tips-dan-best-practices"></a>
## 💡 Tips dan Best Practices

### ✅ **Do's (Yang Benar):**

1. **Gunakan Arrow Function untuk Ringkas:**
   ```javascript
   // ✅ Lebih clean
   fetch('url')
     .then(res => res.json())
     .then(data => console.log(data));
   ```

2. **Pahami Struktur API:**
   - Baca dokumentasi API
   - Cek endpoint yang tersedia
   - Pahami format data yang dikembalikan

3. **Handle Error (akan dipelajari nanti):**
   ```javascript
   fetch('url')
     .then(res => res.json())
     .then(data => console.log(data))
     .catch(error => console.error('Error:', error));
   ```

### ❌ **Don'ts (Yang Salah):**

1. **Jangan Lupa `.json()` atau `.text()`:**
   ```javascript
   // ❌ SALAH
   fetch('data.json')
     .then(data => console.log(data)); // Ini masih Response Object!
   
   // ✅ BENAR
   fetch('data.json')
     .then(res => res.json())
     .then(data => console.log(data));
   ```

2. **Jangan Asal Pakai Method:**
   - JSON file → `.json()`
   - Text file → `.text()`

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

### 📝 **Yang Sudah Dipelajari:**

✅ Fetch API adalah cara modern untuk HTTP requests  
✅ Fetch menggunakan **Promise** dengan 2 tahap  
✅ Response pertama adalah **Response Object**, bukan data langsung  
✅ Gunakan `.json()` untuk JSON dan `.text()` untuk text  
✅ Bisa fetch dari file lokal atau API  
✅ GitHub API bagus untuk latihan (tanpa authentication)  

### 🔜 **Selanjutnya:**

- 🎯 Project: Random User API
- 📮 Belajar POST, PUT, DELETE requests
- ⚡ Async/Await (cara lebih modern dari `.then()`)

---

### 📚 **Struktur File Project:**

```
project/
│
├── index.html          # File HTML utama
├── script.js           # JavaScript dengan Fetch API
├── movies.json         # Data JSON untuk latihan
└── test.txt           # Data text untuk latihan
```

---

### 🎯 **Quick Reference:**

```javascript
// Template Dasar Fetch
fetch('url')
  .then(response => response.json())  // atau .text()
  .then(data => {
    // Lakukan sesuatu dengan data
    console.log(data);
  });
```

---

> 💪 **Keep Learning!** Practice makes perfect. Coba berbagai API publik untuk latihan!

**Happy Coding! 🚀**
