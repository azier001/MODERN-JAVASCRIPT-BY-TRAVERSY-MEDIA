# 🎨 Random User Generator - Dokumentasi Lengkap

> **Dokumentasi Pribadi untuk Pemula** 🌱  
> Belajar Fetch API dengan Membuat Aplikasi Random User Generator

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa yang Akan Kita Buat?](#apa-yang-akan-kita-buat)
- [Struktur File Project](#struktur-file-project)
- [Langkah 1: Setup HTML](#langkah-1-setup-html)
- [Langkah 2: Membuat Spinner CSS](#langkah-2-membuat-spinner-css)
- [Langkah 3: Memahami API](#langkah-3-memahami-api)
- [Langkah 4: Fungsi Fetch User (Bagian 1)](#langkah-4-fungsi-fetch-user-bagian-1)
- [Langkah 5: Fungsi Display User](#langkah-5-fungsi-display-user)
- [Langkah 6: Menampilkan Data Dinamis](#langkah-6-menampilkan-data-dinamis)
- [Langkah 7: Event Listener Button](#langkah-7-event-listener-button)
- [Langkah 8: Fungsi Spinner](#langkah-8-fungsi-spinner)
- [Langkah 9: Testing Final](#langkah-9-testing-final)
- [Code Lengkap](#code-lengkap)
- [Catatan Penting](#catatan-penting)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Selamat datang! Di dokumentasi ini, kita akan belajar membuat aplikasi **Random User Generator** menggunakan **Fetch API**. Aplikasi ini akan mengambil data pengguna random dari API dan menampilkannya di halaman web.

### 🎓 Apa yang Akan Dipelajari?

- ✅ Menggunakan Fetch API untuk request data
- ✅ Menampilkan data dari API ke halaman web
- ✅ Manipulasi DOM dengan JavaScript
- ✅ Event Listener untuk interaksi user
- ✅ Loading spinner untuk UX yang lebih baik

---

<a name="apa-yang-akan-kita-buat"></a>
## 🚀 Apa yang Akan Kita Buat?

Kita akan membuat aplikasi yang:

1. 📸 Menampilkan foto user random
2. 📝 Menampilkan nama, email, telepon, lokasi, dan umur
3. 🎨 Background berubah warna berdasarkan gender (biru untuk pria, ungu untuk wanita)
4. ⚡ Button untuk generate user baru
5. 🔄 Loading spinner saat fetch data

---

<a name="struktur-file-project"></a>
## 📁 Struktur File Project

```
random-user-generator/
│
├── index.html      # File HTML utama
├── spinner.css     # Style untuk loading spinner
└── script.js       # File JavaScript utama
```

---

<a name="langkah-1-setup-html"></a>
## 📄 Langkah 1: Setup HTML

### 🔨 Buat File `index.html`

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="spinner.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Random User Generator</title>
  </head>
  <body class="bg-white text-white overflow-x-hidden">
    <div class="relative max-w-2xl m-auto mt-12 border border-white rounded-lg p-4">
      <div class="text-center">
        <h1 class="text-2xl">Random User Generator</h1>
        <button
          id="generate"
          class="relative bg-black text-white px-4 py-2 rounded w-full my-4"
        >
          <div class="spinner hidden"></div>
          Generate User
        </button>
      </div>

      <div id="user" class="mt-6"></div>
    </div>

    <script src="./script.js"></script>
  </body>
</html>
```

### 💡 Penjelasan Kode HTML

- `<link rel="stylesheet" href="spinner.css" />` → Link ke file CSS spinner
- `<script src="https://cdn.tailwindcss.com"></script>` → CDN Tailwind CSS untuk styling
- `<div id="user">` → Container kosong yang akan diisi data user dari API
- `<div class="spinner hidden">` → Spinner yang tersembunyi secara default
- `<button id="generate">` → Button untuk generate user baru

---

<a name="langkah-2-membuat-spinner-css"></a>
## 🎨 Langkah 2: Membuat Spinner CSS

### 🔨 Buat File `spinner.css`

```css
.spinner {
  --frame: 1s;
}

.spinner,
.spinner::before,
.spinner::after {
  border: 5px solid white;
  border-top-color: transparent;
  border-bottom-color: transparent;
  border-radius: 50%;
}

.spinner {
  margin: auto;
  font-size: 4em;
  position: absolute;
  top: 4px;
  left: 140px;
  width: 0.5em;
  height: 0.5em;
  animation: anim-spinner var(--frame) ease infinite;
}

.spinner::before,
.spinner::after {
  content: '';
  position: absolute;
}

.spinner::before {
  inset: 1px;
  animation: anim-spinner calc(var(--frame) * 4.3) ease-in infinite;
}

.spinner::after {
  inset: 7px;
  animation: anim-spinner calc(var(--frame) * 1.51) ease-out infinite;
}

@keyframes anim-spinner {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(359deg);
  }
}
```

### 💡 Penjelasan Spinner

CSS ini membuat efek loading spinner yang berputar dengan 3 layer lingkaran yang beranimasi dengan kecepatan berbeda.

---

<a name="langkah-3-memahami-api"></a>
## 🌐 Langkah 3: Memahami API

### 📡 API yang Digunakan

**URL API:** `https://randomuser.me/api`

### 📦 Struktur Data Response

```json
{
  "results": [
    {
      "gender": "male",
      "name": {
        "title": "Mr",
        "first": "John",
        "last": "Doe"
      },
      "location": {
        "city": "Sydney",
        "country": "Australia"
      },
      "email": "john.doe@example.com",
      "phone": "123-456-7890",
      "picture": {
        "large": "https://...",
        "medium": "https://...",
        "thumbnail": "https://..."
      },
      "dob": {
        "date": "1990-01-01",
        "age": 34
      }
    }
  ]
}
```

### 🔑 Data yang Akan Kita Gunakan

- ✅ `results[0]` → User pertama (dan satu-satunya)
- ✅ `gender` → Jenis kelamin
- ✅ `name.first` dan `name.last` → Nama lengkap
- ✅ `email` → Email
- ✅ `phone` → Nomor telepon
- ✅ `location.city` dan `location.country` → Lokasi
- ✅ `picture.large` → Foto besar
- ✅ `dob.age` → Umur

---

<a name="langkah-4-fungsi-fetch-user-bagian-1"></a>
## 🎯 Langkah 4: Fungsi Fetch User (Bagian 1)

### 🔨 Buat File `script.js` dan Mulai dengan Fetch Data

```javascript
function fetchUser() {
  fetch('https://randomuser.me/api')
    .then((res) => res.json())
    .then((data) => {
      console.log(data);
    });
}

// Panggil fungsi saat halaman load
fetchUser();
```

### 💡 Penjelasan Kode

1. **`function fetchUser()`** → Membuat fungsi untuk fetch data user
2. **`fetch('https://randomuser.me/api')`** → Mengirim request ke API
3. **`.then((res) => res.json())`** → Mengubah response menjadi JSON
4. **`.then((data) => { ... })`** → Menerima data JSON
5. **`console.log(data)`** → Cek data di console browser

### 🧪 Testing

Buka browser, tekan **F12** untuk membuka Console, refresh halaman. Kamu akan melihat object data dari API!

---

<a name="langkah-5-fungsi-display-user"></a>
## 🎨 Langkah 5: Fungsi Display User

### 🔨 Update `script.js` - Tambahkan Fungsi Display User

```javascript
function fetchUser() {
  fetch('https://randomuser.me/api')
    .then((res) => res.json())
    .then((data) => {
      displayUser(data.results[0]); // Kirim user pertama ke displayUser
    });
}

function displayUser(user) {
  const userDisplay = document.querySelector('#user');

  // Ubah background berdasarkan gender
  if (user.gender === 'female') {
    document.body.style.backgroundColor = 'rebeccapurple';
  } else {
    document.body.style.backgroundColor = 'steelblue';
  }

  console.log(user.gender); // Cek gender di console
}

fetchUser();
```

### 💡 Penjelasan Kode

1. **`displayUser(data.results[0])`** → Kirim user pertama dari array results
2. **`const userDisplay = document.querySelector('#user')`** → Ambil element dengan id "user"
3. **`if (user.gender === 'female')`** → Cek jika gender female
4. **`document.body.style.backgroundColor`** → Ubah warna background body

### 🎨 Warna Background

- 💜 **Female** → Rebecca Purple
- 💙 **Male** → Steel Blue

### 🧪 Testing

Refresh halaman beberapa kali, perhatikan background berubah warna!

---

<a name="langkah-6-menampilkan-data-dinamis"></a>
## 📝 Langkah 6: Menampilkan Data Dinamis

### 🔨 Update Fungsi `displayUser` - Tampilkan Semua Data User

```javascript
function displayUser(user) {
  const userDisplay = document.querySelector('#user');

  // Ubah background berdasarkan gender
  if (user.gender === 'female') {
    document.body.style.backgroundColor = 'rebeccapurple';
  } else {
    document.body.style.backgroundColor = 'steelblue';
  }

  // Tampilkan data user ke HTML
  userDisplay.innerHTML = `
    <div class="flex justify-between">
      <div class="flex">
        <img
          class="w-48 h-48 rounded-full mr-8"
          src="${user.picture.large}"
        />
        <div class="space-y-3">
          <p class="text-xl">
            <span class="font-bold">Name: </span>${user.name.first} ${user.name.last}
          </p>
          <p class="text-xl">
            <span class="font-bold">Email: </span> ${user.email}
          </p>
          <p class="text-xl">
            <span class="font-bold">Phone: </span> ${user.phone}
          </p>
          <p class="text-xl">
            <span class="font-bold">Location: </span> ${user.location.city} ${user.location.country}
          </p>
          <p class="text-xl">
            <span class="font-bold">Age: </span> ${user.dob.age}
          </p>
        </div>
      </div>
    </div>
  `;
}
```

### 💡 Penjelasan Kode

1. **`userDisplay.innerHTML = `...``** → Set HTML content dengan template literal
2. **`${user.picture.large}`** → Tampilkan foto user
3. **`${user.name.first} ${user.name.last}`** → Tampilkan nama lengkap
4. **`${user.email}`** → Tampilkan email
5. **`${user.phone}`** → Tampilkan nomor telepon
6. **`${user.location.city} ${user.location.country}`** → Tampilkan lokasi
7. **`${user.dob.age}`** → Tampilkan umur

### 🧪 Testing

Refresh halaman, sekarang kamu akan melihat data user lengkap dengan foto!

---

<a name="langkah-7-event-listener-button"></a>
## 🔘 Langkah 7: Event Listener Button

### 🔨 Update `script.js` - Tambahkan Event Listener

```javascript
function fetchUser() {
  fetch('https://randomuser.me/api')
    .then((res) => res.json())
    .then((data) => {
      displayUser(data.results[0]);
    });
}

function displayUser(user) {
  const userDisplay = document.querySelector('#user');

  if (user.gender === 'female') {
    document.body.style.backgroundColor = 'rebeccapurple';
  } else {
    document.body.style.backgroundColor = 'steelblue';
  }

  userDisplay.innerHTML = `
    <div class="flex justify-between">
      <div class="flex">
        <img
          class="w-48 h-48 rounded-full mr-8"
          src="${user.picture.large}"
        />
        <div class="space-y-3">
          <p class="text-xl">
            <span class="font-bold">Name: </span>${user.name.first} ${user.name.last}
          </p>
          <p class="text-xl">
            <span class="font-bold">Email: </span> ${user.email}
          </p>
          <p class="text-xl">
            <span class="font-bold">Phone: </span> ${user.phone}
          </p>
          <p class="text-xl">
            <span class="font-bold">Location: </span> ${user.location.city} ${user.location.country}
          </p>
          <p class="text-xl">
            <span class="font-bold">Age: </span> ${user.dob.age}
          </p>
        </div>
      </div>
    </div>
  `;
}

// Event listener untuk button
document.querySelector('#generate').addEventListener('click', fetchUser);

// Fetch user saat halaman load
fetchUser();
```

### 💡 Penjelasan Kode

1. **`document.querySelector('#generate')`** → Ambil button dengan id "generate"
2. **`.addEventListener('click', fetchUser)`** → Jalankan fungsi fetchUser saat button diklik
3. **`fetchUser()`** di baris terakhir → Fetch user pertama kali saat halaman load

### 🧪 Testing

Sekarang klik button "Generate User", user baru akan muncul tanpa perlu refresh halaman!

---

<a name="langkah-8-fungsi-spinner"></a>
## ⏳ Langkah 8: Fungsi Spinner

### 🔨 Update `script.js` - Tambahkan Fungsi Spinner

```javascript
function fetchUser() {
  showSpinner(); // Tampilkan spinner sebelum fetch
  
  fetch('https://randomuser.me/api')
    .then((res) => res.json())
    .then((data) => {
      hideSpinner(); // Sembunyikan spinner setelah data diterima
      displayUser(data.results[0]);
    });
}

function displayUser(user) {
  const userDisplay = document.querySelector('#user');

  if (user.gender === 'female') {
    document.body.style.backgroundColor = 'rebeccapurple';
  } else {
    document.body.style.backgroundColor = 'steelblue';
  }

  userDisplay.innerHTML = `
    <div class="flex justify-between">
      <div class="flex">
        <img
          class="w-48 h-48 rounded-full mr-8"
          src="${user.picture.large}"
        />
        <div class="space-y-3">
          <p class="text-xl">
            <span class="font-bold">Name: </span>${user.name.first} ${user.name.last}
          </p>
          <p class="text-xl">
            <span class="font-bold">Email: </span> ${user.email}
          </p>
          <p class="text-xl">
            <span class="font-bold">Phone: </span> ${user.phone}
          </p>
          <p class="text-xl">
            <span class="font-bold">Location: </span> ${user.location.city} ${user.location.country}
          </p>
          <p class="text-xl">
            <span class="font-bold">Age: </span> ${user.dob.age}
          </p>
        </div>
      </div>
    </div>
  `;
}

function showSpinner() {
  document.querySelector('.spinner').style.display = 'block';
}

function hideSpinner() {
  document.querySelector('.spinner').style.display = 'none';
}

document.querySelector('#generate').addEventListener('click', fetchUser);

fetchUser();
```

### 💡 Penjelasan Kode

1. **`showSpinner()`** → Fungsi untuk menampilkan spinner
2. **`hideSpinner()`** → Fungsi untuk menyembunyikan spinner
3. **`style.display = 'block'`** → Tampilkan spinner
4. **`style.display = 'none'`** → Sembunyikan spinner
5. **`showSpinner()`** dipanggil sebelum fetch
6. **`hideSpinner()`** dipanggil setelah data diterima

### 🧪 Testing

Klik button "Generate User", kamu akan melihat loading spinner sejenak sebelum user baru muncul!

---

<a name="langkah-9-testing-final"></a>
## ✅ Langkah 9: Testing Final

### 🧪 Checklist Testing

- ✅ Halaman load → User pertama muncul otomatis
- ✅ Klik button → User baru muncul
- ✅ Loading spinner → Muncul saat fetch data
- ✅ Background color → Berubah berdasarkan gender (biru/ungu)
- ✅ Data lengkap → Foto, nama, email, phone, lokasi, umur

### 🎉 Selamat!

Kamu telah berhasil membuat aplikasi Random User Generator menggunakan Fetch API!

---

<a name="code-lengkap"></a>
## 📦 Code Lengkap

### 📄 `index.html`

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="spinner.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Random User Generator</title>
  </head>
  <body class="bg-white text-white overflow-x-hidden">
    <div class="relative max-w-2xl m-auto mt-12 border border-white rounded-lg p-4">
      <div class="text-center">
        <h1 class="text-2xl">Random User Generator</h1>
        <button
          id="generate"
          class="relative bg-black text-white px-4 py-2 rounded w-full my-4"
        >
          <div class="spinner hidden"></div>
          Generate User
        </button>
      </div>

      <div id="user" class="mt-6"></div>
    </div>

    <script src="./script.js"></script>
  </body>
</html>
```

### 🎨 `spinner.css`

```css
.spinner {
  --frame: 1s;
}

.spinner,
.spinner::before,
.spinner::after {
  border: 5px solid white;
  border-top-color: transparent;
  border-bottom-color: transparent;
  border-radius: 50%;
}

.spinner {
  margin: auto;
  font-size: 4em;
  position: absolute;
  top: 4px;
  left: 140px;
  width: 0.5em;
  height: 0.5em;
  animation: anim-spinner var(--frame) ease infinite;
}

.spinner::before,
.spinner::after {
  content: '';
  position: absolute;
}

.spinner::before {
  inset: 1px;
  animation: anim-spinner calc(var(--frame) * 4.3) ease-in infinite;
}

.spinner::after {
  inset: 7px;
  animation: anim-spinner calc(var(--frame) * 1.51) ease-out infinite;
}

@keyframes anim-spinner {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(359deg);
  }
}
```

### ⚡ `script.js`

```javascript
function fetchUser() {
  showSpinner();
  
  fetch('https://randomuser.me/api')
    .then((res) => res.json())
    .then((data) => {
      hideSpinner();
      displayUser(data.results[0]);
    });
}

function displayUser(user) {
  const userDisplay = document.querySelector('#user');

  if (user.gender === 'female') {
    document.body.style.backgroundColor = 'rebeccapurple';
  } else {
    document.body.style.backgroundColor = 'steelblue';
  }

  userDisplay.innerHTML = `
    <div class="flex justify-between">
      <div class="flex">
        <img
          class="w-48 h-48 rounded-full mr-8"
          src="${user.picture.large}"
        />
        <div class="space-y-3">
          <p class="text-xl">
            <span class="font-bold">Name: </span>${user.name.first} ${user.name.last}
          </p>
          <p class="text-xl">
            <span class="font-bold">Email: </span> ${user.email}
          </p>
          <p class="text-xl">
            <span class="font-bold">Phone: </span> ${user.phone}
          </p>
          <p class="text-xl">
            <span class="font-bold">Location: </span> ${user.location.city} ${user.location.country}
          </p>
          <p class="text-xl">
            <span class="font-bold">Age: </span> ${user.dob.age}
          </p>
        </div>
      </div>
    </div>
  `;
}

function showSpinner() {
  document.querySelector('.spinner').style.display = 'block';
}

function hideSpinner() {
  document.querySelector('.spinner').style.display = 'none';
}

document.querySelector('#generate').addEventListener('click', fetchUser);

fetchUser();
```

---

<a name="catatan-penting"></a>
## 📌 Catatan Penting

### 🎯 Konsep Penting yang Dipelajari

1. **Fetch API** → Cara modern untuk request data dari server
2. **Promise & .then()** → Menangani asynchronous operation
3. **DOM Manipulation** → Mengubah content HTML dengan JavaScript
4. **Event Listener** → Menangani interaksi user
5. **Template Literals** → Cara mudah membuat string HTML dinamis

### 💡 Tips Belajar

- ✅ **Pahami flow data** → API → Fetch → Response → JSON → Display
- ✅ **Console.log adalah temanmu** → Gunakan untuk debug
- ✅ **Coba modifikasi** → Ubah warna, tambah data lain dari API
- ✅ **Praktik berulang** → Jangan hanya copy-paste, ketik sendiri!

### 🚀 Pengembangan Selanjutnya

Beberapa ide untuk upgrade aplikasi:

- ➕ Tambah error handling dengan `.catch()`
- ➕ Tampilkan multiple users sekaligus
- ➕ Tambah filter gender
- ➕ Simpan favorite users ke LocalStorage
- ➕ Tambah animasi smooth saat user berubah

---

## 🎊 Selesai!

Selamat! Kamu telah berhasil menyelesaikan project Random User Generator. Terus praktik dan jangan takut bereksperimen dengan kode! 💪

**Happy Coding!** 🚀✨
