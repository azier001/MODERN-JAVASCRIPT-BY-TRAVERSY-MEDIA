# 📚 Dokumentasi Event Object JavaScript untuk Pemula

> 🎓 **Dokumentasi Pribadi Belajar JavaScript - Event Object**  
> Panduan lengkap memahami event object dan propertinya dalam JavaScript

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Apa itu Event Object?](#apa-itu-event-object)
- [Cara Mengakses Event Object](#cara-mengakses-event-object)
- [Properti-Properti Event Object](#properti-properti-event-object)
  - [target vs currentTarget](#target-vs-currenttarget)
  - [type - Tipe Event](#type-tipe-event)
  - [timeStamp - Waktu Event](#timestamp-waktu-event)
  - [Koordinat Mouse - clientX & clientY](#koordinat-mouse-clientx-clienty)
  - [Koordinat Offset - offsetX & offsetY](#koordinat-offset-offsetx-offsety)
  - [Koordinat Page - pageX & pageY](#koordinat-page-pagex-pagey)
  - [Koordinat Screen - screenX & screenY](#koordinat-screen-screenx-screeny)
- [Method preventDefault()](#method-preventdefault)
- [Event yang Dinamis - Drag Event](#event-yang-dinamis-drag-event)
- [Praktek Kode Lengkap](#praktek-kode-lengkap)
- [Tips Belajar](#tips-belajar)
- [Kesimpulan](#kesimpulan)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

Ketika kita menambahkan event listener pada sebuah element HTML (seperti button, image, atau link), JavaScript akan memberikan kita sebuah **object spesial** yang berisi informasi lengkap tentang event yang terjadi. Object ini disebut **Event Object**.

Event Object ini sangat powerful karena berisi banyak informasi seperti:
- ✅ Element mana yang diklik
- ✅ Posisi mouse saat diklik
- ✅ Waktu event terjadi
- ✅ Tipe event apa yang terjadi
- ✅ Dan masih banyak lagi!

---

<a name="apa-itu-event-object"></a>
## 📖 Apa itu Event Object?

**Event Object** adalah sebuah object JavaScript yang **otomatis dibuat** ketika sebuah event terjadi (seperti click, drag, mouseover, dll). Object ini berisi semua informasi dan data tentang event tersebut.

### 🔍 Analogi Sederhana:
Bayangkan kamu memesan makanan online:
- 📦 **Event Object** = Paket yang berisi informasi lengkap
- 📍 Lokasi pengiriman = `target` (element yang diklik)
- ⏰ Waktu pemesanan = `timeStamp`
- 📝 Jenis makanan = `type` (click, drag, dll)

---

<a name="cara-mengakses-event-object"></a>
## 🎮 Cara Mengakses Event Object

Ada 2 cara untuk mengakses Event Object:

### 1️⃣ **Menggunakan Anonymous Function**

```javascript
const logo = document.querySelector('img');

logo.addEventListener('click', function(e) {
  console.log(e); // e adalah Event Object
});
```

### 2️⃣ **Menggunakan Named Function**

```javascript
const logo = document.querySelector('img');

function onClick(e) {
  console.log(e); // e adalah Event Object
}

logo.addEventListener('click', onClick);
```

> 💡 **Catatan:** Parameter `e` bisa dinamakan apa saja (`event`, `evt`, `e`), tapi `e` adalah yang paling umum digunakan!

---

<a name="properti-properti-event-object"></a>
## 🎨 Properti-Properti Event Object

<a name="target-vs-currenttarget"></a>
### 1. 🎯 `target` vs `currentTarget`

#### **`e.target`** 
Element yang **benar-benar diklik** oleh user

#### **`e.currentTarget`**
Element yang **memiliki event listener**

```javascript
const logo = document.querySelector('img');

function onClick(e) {
  console.log(e.target);        // Element yang diklik
  console.log(e.currentTarget); // Element dengan event listener
}

logo.addEventListener('click', onClick);
```

#### 🤔 Kapan Keduanya Berbeda?

```javascript
// Event listener dipasang di BODY
document.body.addEventListener('click', function(e) {
  console.log(e.target);        // <li> yang diklik
  console.log(e.currentTarget); // <body> (punya event listener)
});
```

> ⚠️ **Penting:** Karena konsep **Event Bubbling**, event akan "naik" dari element yang diklik ke parent-nya, hingga ke body!

#### 💪 Contoh Praktis dengan `e.target`

```javascript
logo.addEventListener('click', function(e) {
  // Ubah background element yang diklik menjadi hitam
  e.target.style.backgroundColor = 'black';
});
```

---

<a name="type-tipe-event"></a>
### 2. 📌 `type` - Tipe Event

Memberitahu **jenis event** apa yang terjadi.

```javascript
function onClick(e) {
  console.log(e.type); // Output: "click"
}

logo.addEventListener('click', onClick);
```

**Contoh tipe event lainnya:**
- `click` - Ketika diklik
- `dblclick` - Ketika double click
- `drag` - Ketika di-drag
- `mouseover` - Ketika mouse melewati element

---

<a name="timestamp-waktu-event"></a>
### 3. ⏰ `timeStamp` - Waktu Event

Memberikan **timestamp** (dalam milidetik) kapan event terjadi sejak halaman dimuat.

```javascript
function onClick(e) {
  console.log(e.timeStamp); // Output: 5234.5 (contoh)
}

logo.addEventListener('click', onClick);
```

> ⚠️ **Perhatian:** Properti ini menggunakan **huruf besar S** → `timeStamp` bukan `timestamp`

---

<a name="koordinat-mouse-clientx-clienty"></a>
### 4. 📍 Koordinat Mouse - `clientX` & `clientY`

Posisi mouse **relatif terhadap window/viewport browser**.

```javascript
function onClick(e) {
  console.log(e.clientX); // Posisi X dari kiri window
  console.log(e.clientY); // Posisi Y dari atas window
}

logo.addEventListener('click', onClick);
```

#### 📊 Penjelasan Koordinat:
- **X-axis (clientX):** Jarak horizontal dari **kiri window** (➡️)
- **Y-axis (clientY):** Jarak vertikal dari **atas window** (⬇️)

#### 🧪 Contoh:
```
Klik di pojok kiri atas gambar:
- clientX: 27  (27px dari kiri window)
- clientY: 58  (58px dari atas window)

Klik lebih ke bawah:
- clientX: 26
- clientY: 105 (lebih besar karena lebih ke bawah)
```

---

<a name="koordinat-offset-offsetx-offsety"></a>
### 5. 📐 Koordinat Offset - `offsetX` & `offsetY`

Posisi mouse **relatif terhadap element yang diklik**.

```javascript
function onClick(e) {
  console.log(e.offsetX); // Posisi X di dalam element
  console.log(e.offsetY); // Posisi Y di dalam element
}

logo.addEventListener('click', onClick);
```

#### 🎯 Perbedaan dengan `clientX/Y`:
- **clientX/Y:** Dari **window**
- **offsetX/Y:** Dari **element itu sendiri**

#### 🧪 Contoh:
```
Gambar logo berukuran 50x50px

Klik pojok kiri atas gambar:
- offsetX: 10  (10px dari kiri gambar)
- offsetY: 11  (11px dari atas gambar)

Klik pojok kanan bawah gambar:
- offsetX: 42  (42px dari kiri gambar)
- offsetY: 53  (53px dari atas gambar)
```

---

<a name="koordinat-page-pagex-pagey"></a>
### 6. 📄 Koordinat Page - `pageX` & `pageY`

Posisi mouse **relatif terhadap seluruh halaman** (bukan window).

```javascript
function onClick(e) {
  console.log(e.pageX); // Posisi X dari kiri halaman
  console.log(e.pageY); // Posisi Y dari atas halaman
}

logo.addEventListener('click', onClick);
```

#### 🔍 Kapan `pageX/Y` Berbeda dari `clientX/Y`?

Ketika halaman bisa di-scroll atau ada konten yang melebihi ukuran window:

```javascript
// Halaman di-resize menjadi sangat lebar
// Logo terdorong ke kanan

// Klik logo:
clientX: 29   // Dari window
pageX: 503    // Dari halaman (lebih besar!)
```

---

<a name="koordinat-screen-screenx-screeny"></a>
### 7. 🖥️ Koordinat Screen - `screenX` & `screenY`

Posisi mouse **relatif terhadap seluruh layar monitor**.

```javascript
function onClick(e) {
  console.log(e.screenX); // Posisi X dari kiri monitor
  console.log(e.screenY); // Posisi Y dari atas monitor
}

logo.addEventListener('click', onClick);
```

#### 💻 Catatan:
Nilai ini bergantung pada ukuran monitor kamu. Monitor ultrawide akan memiliki nilai `screenX` yang sangat besar!

#### 🧪 Contoh dengan Monitor Ultrawide:
```
screenX: 2916  (2916px dari kiri monitor)
screenY: 366   (366px dari atas monitor)
```

---

## 📊 Tabel Perbandingan Koordinat

| Properti | Relatif Terhadap | Kapan Digunakan |
|----------|------------------|-----------------|
| `clientX/Y` | 🪟 Window/Viewport | Posisi di area yang terlihat |
| `offsetX/Y` | 📦 Element | Posisi di dalam element |
| `pageX/Y` | 📄 Halaman | Posisi di seluruh dokumen |
| `screenX/Y` | 🖥️ Monitor | Posisi di layar monitor |

---

<a name="method-preventdefault"></a>
## 🛑 Method `preventDefault()`

Method ini **mencegah behavior default** dari sebuah element.

### 🎯 Kapan Digunakan?

#### 1️⃣ **Mencegah Link Membuka Halaman Baru**

```javascript
document.querySelector('a').addEventListener('click', function(e) {
  e.preventDefault(); // Link tidak akan membuka halaman!
  console.log('Link was clicked');
});
```

**Tanpa `preventDefault()`:**
- Klik link Google → Browser membuka google.com ❌

**Dengan `preventDefault()`:**
- Klik link Google → Console log muncul, tapi tidak pindah halaman ✅

#### 2️⃣ **Mencegah Form Submit**

```javascript
document.querySelector('form').addEventListener('submit', function(e) {
  e.preventDefault(); // Form tidak akan submit!
  
  // Lakukan validasi atau kirim data via AJAX
  console.log('Form submitted but page did not reload');
});
```

### 💡 Use Case Umum:
- ✅ Validasi form sebelum submit
- ✅ Membuat SPA (Single Page Application)
- ✅ Mengirim data via AJAX/Fetch
- ✅ Custom behavior pada link

---

<a name="event-yang-dinamis-drag-event"></a>
## 🎪 Event yang Dinamis - Drag Event

Beberapa event property seperti `clientX` dan `clientY` bisa **berubah secara real-time**!

### 🎮 Contoh: Tracking Posisi Mouse Saat Drag

```javascript
const logo = document.querySelector('img');

function onDrag(e) {
  // Update H1 dengan posisi mouse real-time
  document.querySelector('h1').textContent = `X ${e.clientX} Y ${e.clientY}`;
}

logo.addEventListener('drag', onDrag);
```

**Hasil:**
```
Saat kamu drag logo:
H1 akan berubah menjadi → "X 245 Y 389"
                         → "X 267 Y 412"
                         → "X 289 Y 435"
(Berubah terus sesuai posisi mouse!)
```

### 🎯 Manfaat:
- 🎮 Membuat game browser
- 🖱️ Drag and drop functionality
- 📊 Interactive data visualization
- ✏️ Drawing apps

---

<a name="praktek-kode-lengkap"></a>
## 💻 Praktek Kode Lengkap

### 📄 HTML (`index.html`)

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="style.css">
  <title>Shopping List</title>
</head>
<body>
  <div class="container">
    <header>
      <img src="images/note.png" alt="Logo" />
      <h1>Shopping List</h1>
    </header>
    
    <form id="item-form">
      <input type="text" placeholder="Enter Item" />
      <button type="submit">Add Item</button>
    </form>

    <ul id="item-list">
      <li>Apples <button class="remove-item">×</button></li>
      <li>Orange Juice <button class="remove-item">×</button></li>
    </ul>
    
    <!-- Link untuk test preventDefault -->
    <a href="https://google.com">Google</a>
  </div>

  <script src="script.js"></script>
</body>
</html>
```

### 📜 JavaScript (`script.js`)

```javascript
// =================================
// 1. MENGAKSES EVENT OBJECT
// =================================
const logo = document.querySelector('img');

function onClick(e) {
  // Tampilkan semua property event
  console.log('=== EVENT PROPERTIES ===');
  
  // Property Target & CurrentTarget
  console.log('target:', e.target);
  console.log('currentTarget:', e.currentTarget);
  
  // Property Type & TimeStamp
  console.log('type:', e.type);
  console.log('timeStamp:', e.timeStamp);
  
  // Koordinat Client
  console.log('clientX:', e.clientX);
  console.log('clientY:', e.clientY);
  
  // Koordinat Offset
  console.log('offsetX:', e.offsetX);
  console.log('offsetY:', e.offsetY);
  
  // Koordinat Page
  console.log('pageX:', e.pageX);
  console.log('pageY:', e.pageY);
  
  // Koordinat Screen
  console.log('screenX:', e.screenX);
  console.log('screenY:', e.screenY);
  
  // Contoh manipulasi element dengan e.target
  e.target.style.backgroundColor = 'black';
}

logo.addEventListener('click', onClick);


// =================================
// 2. DRAG EVENT (REAL-TIME UPDATE)
// =================================
function onDrag(e) {
  // Update H1 dengan posisi mouse secara real-time
  document.querySelector('h1').textContent = `X ${e.clientX} Y ${e.clientY}`;
}

logo.addEventListener('drag', onDrag);


// =================================
// 3. PREVENT DEFAULT
// =================================
document.querySelector('a').addEventListener('click', function(e) {
  e.preventDefault(); // Cegah link membuka halaman baru
  console.log('Link was clicked, but page did not navigate!');
});


// =================================
// 4. CONTOH EVENT BUBBLING
// =================================
// Uncomment untuk test event bubbling
/*
document.body.addEventListener('click', function(e) {
  console.log('Clicked element:', e.target);
  console.log('Event listener attached to:', e.currentTarget);
});
*/
```

### 📝 Penjelasan Kode:

#### 1. **Section Event Properties**
- Menampilkan semua property event ke console
- Mengubah background element yang diklik

#### 2. **Section Drag Event**
- Tracking posisi mouse secara real-time
- Update text H1 dengan koordinat mouse

#### 3. **Section Prevent Default**
- Mencegah link membuka halaman baru
- Console log custom message

#### 4. **Section Event Bubbling**
- Contoh perbedaan `target` vs `currentTarget`
- Uncomment untuk testing

---

<a name="tips-belajar"></a>
## 💡 Tips Belajar

### 🎯 Cara Efektif Belajar Event Object:

1. **🧪 Eksperimen Langsung**
   ```javascript
   // Coba sendiri di console browser!
   logo.addEventListener('click', (e) => console.log(e));
   ```

2. **📝 Tulis Kode "Bodoh" untuk Latihan**
   - Buat interaksi yang tidak masuk akal
   - Tujuannya: memahami konsep, bukan membuat project bagus
   - Contoh: Ubah warna background random saat click

3. **🔍 Inspect Event Object di Console**
   ```javascript
   function onClick(e) {
     console.log(e); // Expand object ini di DevTools!
   }
   ```

4. **🎮 Buat Mini Games**
   - Click counter
   - Mouse position tracker
   - Drag and drop simulator

5. **📚 Praktek Sedikit Demi Sedikit**
   - Hari 1: Pahami `target` dan `type`
   - Hari 2: Eksperimen dengan koordinat
   - Hari 3: Praktek `preventDefault()`

---

<a name="kesimpulan"></a>
## 🎓 Kesimpulan

### ✅ Yang Sudah Dipelajari:

1. **Event Object** adalah object yang berisi info lengkap tentang event
2. **Property Penting:**
   - `target` & `currentTarget` - Element yang terlibat
   - `type` - Jenis event
   - `clientX/Y`, `offsetX/Y`, `pageX/Y`, `screenX/Y` - Koordinat mouse
3. **Method `preventDefault()`** - Mencegah behavior default
4. **Event Dinamis** - Property bisa berubah real-time (contoh: drag)

### 🎯 Next Steps:

- 🔥 **Event Bubbling & Capturing** - Cara event "naik" ke parent
- ⌨️ **Keyboard Events** - Event dari keyboard
- 🖱️ **Mouse Events** - Event mouse lebih detail
- 📝 **Form Events** - Submit, focus, blur, dll

### 💪 Challenge untuk Kamu:

1. Buat click counter dengan event object
2. Buat mouse position tracker yang menampilkan semua koordinat
3. Buat form dengan validasi custom menggunakan `preventDefault()`
4. Eksperimen dengan drag event untuk membuat simple drawing app

---

## 📌 Catatan Penting

> ⚠️ **Event Object hanya tersedia di dalam function event handler!**

> 💡 **Jangan takut eksperimen - JavaScript tidak akan merusak komputer kamu!**

> 🎯 **Focus on understanding, not memorizing - Kamu bisa selalu lihat dokumentasi!**

---

**Selamat Belajar! 🚀**

*Dokumentasi ini dibuat untuk pembelajaran pribadi. Keep practicing dan jangan menyerah!* 💪✨
