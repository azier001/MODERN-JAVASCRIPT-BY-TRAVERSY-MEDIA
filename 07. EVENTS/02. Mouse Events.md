# 🖱️ Dokumentasi Mouse Events JavaScript

> **Panduan Lengkap untuk Pemula** - Belajar Event Listener pada Mouse Step by Step

---

## 📑 Daftar Isi

- [Pengenalan](#pengenalan)
- [Persiapan Project](#persiapan-project)
- [Struktur Kode Dasar](#struktur-kode-dasar)
- [Mouse Events yang Akan Dipelajari](#mouse-events-yang-akan-dipelajari)
- [Event 1: Click](#event-1-click)
- [Event 2: Double Click](#event-2-double-click)
- [Event 3: Right Click (Context Menu)](#event-3-right-click)
- [Event 4: Mouse Down](#event-4-mouse-down)
- [Event 5: Mouse Up](#event-5-mouse-up)
- [Event 6: Mouse Wheel](#event-6-mouse-wheel)
- [Event 7: Mouse Over](#event-7-mouse-over)
- [Event 8: Mouse Out](#event-8-mouse-out)
- [Event 9: Drag Start](#event-9-drag-start)
- [Event 10: Drag](#event-10-drag)
- [Event 11: Drag End](#event-11-drag-end)
- [Kode Lengkap](#kode-lengkap)
- [Tips untuk Belajar](#tips-untuk-belajar)
- [Rangkuman](#rangkuman)

---

<a name="pengenalan"></a>
## 🎯 Pengenalan

**Mouse Events** adalah cara JavaScript mendengarkan dan merespons interaksi pengguna dengan mouse. Setiap kali Anda mengklik, men-drag, atau mengarahkan mouse ke elemen HTML, JavaScript dapat mendeteksi aksi tersebut dan menjalankan kode tertentu.

### 🤔 Mengapa Penting?

- ✅ Membuat website interaktif
- ✅ Merespons aksi pengguna secara real-time
- ✅ Membuat game dan aplikasi web yang dinamis
- ✅ Meningkatkan user experience

---

<a name="persiapan-project"></a>
## 📦 Persiapan Project

### File yang Dibutuhkan:

```
project-folder/
│
├── index.html
├── style.css
├── script.js
└── images/
    └── note.png
```

### 📄 File HTML (index.html)

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css"
      integrity="sha512-1sCRPdkRXhBV2PBLUdRb4tMg1w2YPf37qatUFeS7zlBy7jJI8Lf4VHwWfZZfpXtYSLy85pkm9GaYVYMfw5BC1A=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="style.css" />
    <title>Shopping List</title>
  </head>
  <body>
    <div class="container">
      <header>
        <img src="images/note.png" alt="" />
        <h1>Shopping List</h1>
      </header>
      <form id="item-form">
        <div class="form-control">
          <input
            type="text"
            class="form-input"
            id="item-input"
            name="item"
            placeholder="Enter Item"
          />
        </div>
        <div class="form-control">
          <button type="submit" class="btn">
            <i class="fa-solid fa-plus"></i> Add Item
          </button>
        </div>
      </form>

      <div class="filter">
        <input
          type="text"
          class="form-input-filter"
          id="filter"
          placeholder="Filter Items"
        />
      </div>

      <ul id="item-list" class="items">
        <li>
          Apples
          <button class="remove-item btn-link text-red">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
        <li>
          Orange Juice
          <button class="remove-item btn-link text-red">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
        <li>
          Oreos
          <button class="remove-item btn-link text-red">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
        <li>
          Milk
          <button class="remove-item btn-link text-red">
            <i class="fa-solid fa-xmark"></i>
          </button>
        </li>
      </ul>

      <button id="clear" class="btn-clear">Clear All</button>
    </div>

    <script src="script.js"></script>
  </body>
</html>
```

---

<a name="struktur-kode-dasar"></a>
## 🏗️ Struktur Kode Dasar

### 🎨 Cara Kerja Event Listener

```javascript
// 1. Pilih elemen HTML
const element = document.querySelector('selector');

// 2. Buat fungsi yang akan dijalankan
const handleEvent = () => {
  console.log('Event terjadi!');
};

// 3. Tambahkan event listener
element.addEventListener('namaEvent', handleEvent);
```

### 📌 Dalam Tutorial Ini:

Kita akan menggunakan **logo gambar** sebagai target untuk semua mouse events:

```javascript
const logo = document.querySelector('img');
```

> 💡 **Catatan**: Kita memilih `img` karena hanya ada satu gambar di halaman ini (logo note.png)

---

<a name="mouse-events-yang-akan-dipelajari"></a>
## 🖱️ Mouse Events yang Akan Dipelajari

| No | Event | Deskripsi | Kapan Terjadi |
|----|-------|-----------|---------------|
| 1️⃣ | `click` | Klik biasa | Saat mouse diklik sekali |
| 2️⃣ | `dblclick` | Double click | Saat mouse diklik 2x cepat |
| 3️⃣ | `contextmenu` | Right click | Saat klik kanan mouse |
| 4️⃣ | `mousedown` | Mouse ditekan | Saat tombol mouse ditekan (belum dilepas) |
| 5️⃣ | `mouseup` | Mouse dilepas | Saat tombol mouse dilepas |
| 6️⃣ | `wheel` | Mouse wheel | Saat scroll mouse diputar |
| 7️⃣ | `mouseover` | Mouse masuk area | Saat mouse masuk ke elemen |
| 8️⃣ | `mouseout` | Mouse keluar area | Saat mouse keluar dari elemen |
| 9️⃣ | `dragstart` | Mulai drag | Saat mulai men-drag elemen |
| 🔟 | `drag` | Sedang drag | Saat sedang men-drag elemen |
| 1️⃣1️⃣ | `dragend` | Selesai drag | Saat selesai men-drag elemen |

---

<a name="event-1-click"></a>
## 1️⃣ Event: Click

### 📝 Penjelasan

Event **click** terjadi ketika pengguna mengklik elemen dengan mouse (klik kiri biasa).

### 💻 Kode

```javascript
// Fungsi yang akan dijalankan saat logo diklik
const onClick = () => console.log('click event');

// Tambahkan event listener
logo.addEventListener('click', onClick);
```

### 🎯 Cara Kerja

1. Pengguna mengklik logo
2. Browser mendeteksi klik tersebut
3. Fungsi `onClick()` dijalankan
4. Console menampilkan: `"click event"`

### ✨ Hasil

```
Console Output:
click event
```

---

<a name="event-2-double-click"></a>
## 2️⃣ Event: Double Click (dblclick)

### 📝 Penjelasan

Event **dblclick** terjadi ketika pengguna mengklik elemen dua kali dengan cepat.

### 💻 Kode

```javascript
const onDoubleClick = () => {
  // Cek apakah background saat ini bukan purple
  if (document.body.style.backgroundColor !== 'purple') {
    // Ubah ke purple dengan teks putih
    document.body.style.backgroundColor = 'purple';
    document.body.style.color = 'white';
  } else {
    // Kembalikan ke white dengan teks hitam
    document.body.style.backgroundColor = 'white';
    document.body.style.color = 'black';
  }
};

logo.addEventListener('dblclick', onDoubleClick);
```

### 🎯 Cara Kerja

1. Pengguna double-click logo
2. JavaScript cek warna background saat ini
3. Jika **bukan purple** → ubah ke **purple + teks putih**
4. Jika **sudah purple** → kembalikan ke **white + teks hitam**

### ✨ Hasil

- **Double-click pertama**: Background jadi purple 🟣
- **Double-click kedua**: Background jadi white ⚪
- **Bisa toggle bolak-balik!**

> ⚠️ **Penting**: Nama event adalah `dblclick` (bukan `doubleclick`)

---

<a name="event-3-right-click"></a>
## 3️⃣ Event: Right Click (contextmenu)

### 📝 Penjelasan

Event **contextmenu** terjadi ketika pengguna mengklik kanan mouse. Biasanya ini membuka menu context (menu klik kanan).

### 💻 Kode

```javascript
const onRightClick = () => console.log('right click event');

logo.addEventListener('contextmenu', onRightClick);
```

### 🎯 Cara Kerja

1. Pengguna klik kanan pada logo
2. Browser mendeteksi klik kanan
3. Menu context muncul (menu klik kanan default browser)
4. Console menampilkan: `"right click event"`

### ✨ Hasil

```
Console Output:
right click event
```

> 💡 **Catatan**: Event ini disebut `contextmenu` (bukan `rightclick`)

---

<a name="event-4-mouse-down"></a>
## 4️⃣ Event: Mouse Down

### 📝 Penjelasan

Event **mousedown** terjadi ketika tombol mouse **ditekan** (tapi belum dilepas).

### 💻 Kode

```javascript
const onMouseDown = () => console.log('mouse down event');

logo.addEventListener('mousedown', onMouseDown);
```

### 🎯 Cara Kerja

1. Pengguna menekan tombol mouse (tahan)
2. Event `mousedown` langsung terpicu
3. Console menampilkan: `"mouse down event"`

### 🔍 Perbedaan dengan Click

| Event | Kapan Terpicu |
|-------|---------------|
| `mousedown` | Saat tombol mouse **ditekan** |
| `click` | Saat tombol mouse **ditekan + dilepas** |

### ✨ Hasil

```
Console Output:
mouse down event  ← Muncul saat tombol ditekan
```

---

<a name="event-5-mouse-up"></a>
## 5️⃣ Event: Mouse Up

### 📝 Penjelasan

Event **mouseup** terjadi ketika tombol mouse **dilepas** setelah ditekan.

### 💻 Kode

```javascript
const onMouseUp = () => console.log('mouse up event');

logo.addEventListener('mouseup', onMouseUp);
```

### 🎯 Cara Kerja

1. Pengguna menekan tombol mouse → `mousedown` terpicu
2. Pengguna melepas tombol mouse → `mouseup` terpicu
3. Console menampilkan: `"mouse up event"`

### 🔄 Urutan Event Lengkap

Saat Anda melakukan **klik penuh** (tekan + lepas):

```
1. mousedown event  ← Saat ditekan
2. mouseup event    ← Saat dilepas
3. click event      ← Setelah kedua event di atas
```

### ✨ Hasil

```
Console Output (saat klik penuh):
mouse down event
mouse up event
click event
```

---

<a name="event-6-mouse-wheel"></a>
## 6️⃣ Event: Mouse Wheel

### 📝 Penjelasan

Event **wheel** terjadi ketika pengguna memutar scroll mouse (mouse wheel).

### 💻 Kode

```javascript
const onMouseWheel = () => console.log('mouse wheel event');

logo.addEventListener('wheel', onMouseWheel);
```

### 🎯 Cara Kerja

1. Pengguna mengarahkan mouse ke atas logo
2. Pengguna scroll (putar wheel mouse)
3. Event terpicu **setiap kali scroll**
4. Console menampilkan: `"mouse wheel event"`

### ⚠️ Catatan Penting

- Event **HANYA** terpicu jika mouse berada **di atas logo**
- Jika scroll di luar logo, event tidak terpicu
- Setiap gerakan scroll = 1 event

### ✨ Hasil

```
Console Output (saat scroll 3x):
mouse wheel event
mouse wheel event
mouse wheel event
```

---

<a name="event-7-mouse-over"></a>
## 7️⃣ Event: Mouse Over

### 📝 Penjelasan

Event **mouseover** terjadi ketika pointer mouse **masuk** ke area elemen.

### 💻 Kode

```javascript
const onMouseOver = () => console.log('mouse over event');

logo.addEventListener('mouseover', onMouseOver);
```

### 🎯 Cara Kerja

1. Mouse berada di luar logo
2. Mouse bergerak masuk ke area logo
3. Event `mouseover` terpicu **sekali**
4. Console menampilkan: `"mouse over event"`

### 🔁 Perilaku Event

- Event terpicu saat **mouse masuk** ke logo
- Jika mouse tetap di dalam logo → tidak ada event lagi
- Jika mouse keluar lalu masuk lagi → event terpicu lagi

### ✨ Hasil

```
Console Output:
mouse over event  ← Saat mouse masuk area logo
```

---

<a name="event-8-mouse-out"></a>
## 8️⃣ Event: Mouse Out

### 📝 Penjelasan

Event **mouseout** terjadi ketika pointer mouse **keluar** dari area elemen.

### 💻 Kode

```javascript
const onMouseOut = () => console.log('mouse out event');

logo.addEventListener('mouseout', onMouseOut);
```

### 🎯 Cara Kerja

1. Mouse berada di dalam logo
2. Mouse bergerak keluar dari area logo
3. Event `mouseout` terpicu **sekali**
4. Console menampilkan: `"mouse out event"`

### 🔄 Kombinasi dengan Mouse Over

Kedua event ini sering digunakan bersamaan untuk efek hover:

```javascript
logo.addEventListener('mouseover', onMouseOver);  // Masuk
logo.addEventListener('mouseout', onMouseOut);    // Keluar
```

### ✨ Hasil

```
Console Output:
mouse over event   ← Mouse masuk logo
mouse out event    ← Mouse keluar logo
mouse over event   ← Mouse masuk lagi
mouse out event    ← Mouse keluar lagi
```

> 💡 **Alternatif untuk Hover**: Gunakan kombinasi `mouseover` + `mouseout` untuk membuat efek hover interaktif!

---

<a name="event-9-drag-start"></a>
## 9️⃣ Event: Drag Start

### 📝 Penjelasan

Event **dragstart** terjadi ketika pengguna **mulai men-drag** sebuah elemen (klik + tahan + gerakkan).

### 💻 Kode

```javascript
const onDragStart = () => console.log('drag start event');

logo.addEventListener('dragstart', onDragStart);
```

### 🎯 Cara Kerja

1. Pengguna klik dan tahan logo
2. Pengguna mulai menggerakkan mouse (drag)
3. Event `dragstart` terpicu **sekali** di awal
4. Console menampilkan: `"drag start event"`

### 🔍 Karakteristik

- Terpicu **hanya sekali** saat mulai drag
- Tidak terpicu lagi meskipun terus di-drag
- Penanda bahwa operasi drag telah dimulai

### ✨ Hasil

```
Console Output:
drag start event  ← Muncul sekali saat mulai drag
```

---

<a name="event-10-drag"></a>
## 🔟 Event: Drag

### 📝 Penjelasan

Event **drag** terjadi **terus-menerus** selama elemen sedang di-drag.

### 💻 Kode

```javascript
const onDrag = () => console.log('drag event');

logo.addEventListener('drag', onDrag);
```

### 🎯 Cara Kerja

1. Pengguna mulai drag → `dragstart` terpicu
2. Selama logo di-drag → `drag` terpicu **berkali-kali**
3. Setiap gerakan kecil = event baru
4. Console menampilkan: `"drag event"` berulang-ulang

### ⚡ Karakteristik

- Terpicu **sangat sering** (puluhan kali per detik)
- Terpicu selama elemen masih dipegang dan digerakkan
- Berhenti saat mouse dilepas

### ✨ Hasil

```
Console Output (saat drag):
drag start event   ← Sekali di awal
drag event         ← Berulang-ulang
drag event
drag event
drag event
drag event
... (terus berlanjut)
```

> ⚠️ **Perhatian**: Event ini sangat sering terpicu! Pastikan kode di dalamnya efisien.

---

<a name="event-11-drag-end"></a>
## 1️⃣1️⃣ Event: Drag End

### 📝 Penjelasan

Event **dragend** terjadi ketika operasi drag **selesai** (saat mouse dilepas).

### 💻 Kode

```javascript
const onDragEnd = () => console.log('drag end event');

logo.addEventListener('dragend', onDragEnd);
```

### 🎯 Cara Kerja

1. Pengguna sedang drag logo
2. Pengguna melepas tombol mouse
3. Event `dragend` terpicu **sekali**
4. Console menampilkan: `"drag end event"`

### 🔄 Urutan Lengkap Drag Events

Berikut urutan event saat melakukan drag lengkap:

```
1. dragstart  ← Mulai drag (sekali)
2. drag       ← Sedang drag (berkali-kali)
3. drag
4. drag
5. ...
6. dragend    ← Selesai drag (sekali)
```

### ✨ Hasil

```
Console Output (urutan lengkap):
drag start event
drag event
drag event
drag event
... (berulang selama drag)
drag end event  ← Muncul saat mouse dilepas
```

---

<a name="kode-lengkap"></a>
## 📦 Kode Lengkap (script.js)

Berikut adalah kode lengkap yang menggabungkan **semua mouse events**:

```javascript
// 🎯 Pilih elemen logo
const logo = document.querySelector('img');

// 📝 FUNGSI-FUNGSI EVENT HANDLER

// 1️⃣ Click Event
const onClick = () => console.log('click event');

// 2️⃣ Double Click Event - Toggle Background
const onDoubleClick = () => {
  if (document.body.style.backgroundColor !== 'purple') {
    document.body.style.backgroundColor = 'purple';
    document.body.style.color = 'white';
  } else {
    document.body.style.backgroundColor = 'white';
    document.body.style.color = 'black';
  }
};

// 3️⃣ Right Click Event
const onRightClick = () => console.log('right click event');

// 4️⃣ Mouse Down Event
const onMouseDown = () => console.log('mouse down event');

// 5️⃣ Mouse Up Event
const onMouseUp = () => console.log('mouse up event');

// 6️⃣ Mouse Wheel Event
const onMouseWheel = () => console.log('mouse wheel event');

// 7️⃣ Mouse Over Event
const onMouseOver = () => console.log('mouse over event');

// 8️⃣ Mouse Out Event
const onMouseOut = () => console.log('mouse out event');

// 9️⃣ Drag Start Event
const onDragStart = () => console.log('drag start event');

// 🔟 Drag Event
const onDrag = () => console.log('drag event');

// 1️⃣1️⃣ Drag End Event
const onDragEnd = () => console.log('drag end event');

// 🔗 EVENT LISTENERS
// Semua event listener ditambahkan di bagian bawah
logo.addEventListener('click', onClick);
logo.addEventListener('dblclick', onDoubleClick);
logo.addEventListener('contextmenu', onRightClick);
logo.addEventListener('mousedown', onMouseDown);
logo.addEventListener('mouseup', onMouseUp);
logo.addEventListener('wheel', onMouseWheel);
logo.addEventListener('mouseover', onMouseOver);
logo.addEventListener('mouseout', onMouseOut);
logo.addEventListener('dragstart', onDragStart);
logo.addEventListener('drag', onDrag);
logo.addEventListener('dragend', onDragEnd);
```

### 🎨 Struktur Kode yang Baik

```
1. Pilih elemen HTML
   ↓
2. Buat semua fungsi handler di atas
   ↓
3. Tambahkan semua event listener di bawah
```

> 💡 **Best Practice**: Letakkan fungsi di atas dan event listeners di bawah agar kode lebih mudah dibaca!

---

<a name="tips-untuk-belajar"></a>
## 💡 Tips untuk Belajar

### 🎓 Cara Berlatih Efektif

1. **Coba Satu Per Satu**
   - Mulai dengan 1 event
   - Pahami cara kerjanya
   - Baru lanjut ke event berikutnya

2. **Eksperimen dengan Kode**
   - Ubah pesan console.log
   - Tambahkan efek visual lainnya
   - Coba kombinasi event

3. **Buat Proyek Kecil**
   ```javascript
   // Contoh: Ubah warna saat hover
   logo.addEventListener('mouseover', () => {
     logo.style.border = '5px solid red';
   });
   
   logo.addEventListener('mouseout', () => {
     logo.style.border = 'none';
   });
   ```

4. **Gunakan Console.log**
   - Selalu cek console browser (F12)
   - Lihat kapan event terpicu
   - Debug jika ada error

### 🎯 Latihan Tambahan

**Tantangan 1**: Buat counter yang bertambah setiap klik
```javascript
let clickCount = 0;
const onClick = () => {
  clickCount++;
  console.log(`Klik ke-${clickCount}`);
};
```

**Tantangan 2**: Ubah ukuran logo saat scroll
```javascript
const onMouseWheel = (e) => {
  const currentWidth = logo.offsetWidth;
  if (e.deltaY > 0) {
    logo.style.width = (currentWidth - 10) + 'px';
  } else {
    logo.style.width = (currentWidth + 10) + 'px';
  }
};
```

**Tantangan 3**: Buat efek shadow saat drag
```javascript
const onDragStart = () => {
  logo.style.boxShadow = '0 10px 20px rgba(0,0,0,0.5)';
};

const onDragEnd = () => {
  logo.style.boxShadow = 'none';
};
```

---

<a name="rangkuman"></a>
## 📊 Rangkuman

### 🎯 Apa yang Sudah Dipelajari

| Event | Nama JavaScript | Kapan Terjadi |
|-------|-----------------|---------------|
| Klik | `click` | Klik sekali |
| Double Klik | `dblclick` | Klik 2x cepat |
| Klik Kanan | `contextmenu` | Klik kanan mouse |
| Mouse Ditekan | `mousedown` | Tombol ditekan |
| Mouse Dilepas | `mouseup` | Tombol dilepas |
| Scroll Mouse | `wheel` | Wheel diputar |
| Mouse Masuk | `mouseover` | Pointer masuk elemen |
| Mouse Keluar | `mouseout` | Pointer keluar elemen |
| Mulai Drag | `dragstart` | Drag dimulai |
| Sedang Drag | `drag` | Sedang di-drag |
| Selesai Drag | `dragend` | Drag selesai |

### 🔑 Poin Penting

✅ **Event Listener** menghubungkan event dengan fungsi

✅ **Fungsi Handler** berisi kode yang dijalankan saat event terjadi

✅ Event ditulis dalam **huruf kecil semua** (lowercase)

✅ Satu elemen bisa memiliki **banyak event listener**

✅ Beberapa event terpicu **berkali-kali** (seperti `drag`)

### 🚀 Langkah Selanjutnya

Setelah menguasai mouse events, Anda bisa lanjut mempelajari:

1. **Event Object** - Mendapatkan informasi detail tentang event
2. **Keyboard Events** - Event untuk keyboard (keydown, keyup, dll)
3. **Form Events** - Event untuk form (submit, input, change)
4. **Event Delegation** - Teknik efisien untuk menangani event
5. **Custom Events** - Membuat event sendiri

### 🎉 Selamat!

Anda telah mempelajari **11 mouse events** dalam JavaScript! Teruslah berlatih dan eksperimen dengan kode untuk memperdalam pemahaman Anda.

---

> 📚 **Dokumentasi Pribadi** - Disusun untuk pembelajaran step-by-step
> 
> 💻 **Happy Coding!** 🚀
