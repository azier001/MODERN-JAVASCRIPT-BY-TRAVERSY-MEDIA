# 📚 Dokumentasi JSON untuk Pemula

> **Catatan Pribadi**: Dokumentasi ini dibuat untuk memahami JSON secara bertahap dan mudah dipahami.

---

## 📋 Daftar Isi

- [Pengenalan JSON](#pengenalan-json)
- [Apa itu JSON?](#apa-itu-json)
- [Perbedaan JSON vs JavaScript Object](#perbedaan-json-vs-javascript-object)
- [Contoh API yang Menggunakan JSON](#contoh-api-yang-menggunakan-json)
- [Membuat File JSON](#membuat-file-json)
- [Mengubah JavaScript Object ke JSON String](#mengubah-javascript-object-ke-json-string)
- [Mengubah JSON String ke JavaScript Object](#mengubah-json-string-ke-javascript-object)
- [Bekerja dengan Array of Objects](#bekerja-dengan-array-of-objects)
- [Hal Penting yang Perlu Diingat](#hal-penting-yang-perlu-diingat)
- [Ringkasan](#ringkasan)

---

<a name="pengenalan-json"></a>
## 🎯 Pengenalan JSON

JSON adalah format data yang **sangat sering** digunakan dalam pengembangan web modern, terutama saat bekerja dengan API (Application Programming Interface). Sebagai developer JavaScript, kamu akan **sering sekali** berinteraksi dengan JSON!

---

<a name="apa-itu-json"></a>
## 📖 Apa itu JSON?

**JSON** adalah singkatan dari **JavaScript Object Notation** (dibaca: "Jason" atau "J-S-O-N").

### ✨ Karakteristik JSON:

- 📦 **Lightweight data interchange format** - Format pertukaran data yang ringan
- 📝 **Human-readable** - Mudah dibaca oleh manusia
- 🔄 **Data exchange** - Digunakan untuk mengirim dan menerima data dari/ke server
- 🌐 **Standard modern** - Menggantikan XML sebagai standar dalam beberapa tahun terakhir

### 💡 Kenapa JSON Penting?

Ketika kamu bekerja dengan API (misalnya mengambil data user, posting data form, dll), data yang dikirim/diterima biasanya dalam format JSON. Dulu, XML adalah standarnya, tapi sekarang JSON lebih populer karena lebih sederhana!

---

<a name="perbedaan-json-vs-javascript-object"></a>
## 🔍 Perbedaan JSON vs JavaScript Object

Meskipun JSON mirip dengan JavaScript Object, ada beberapa **perbedaan penting**:

| 🟦 JavaScript Object | 🟩 JSON |
|---------------------|---------|
| Key **tanpa** tanda kutip | Key **harus** pakai **double quotes** `"` |
| String bisa pakai `'` atau `"` | String **harus** pakai **double quotes** `"` |
| Bisa punya function/method | **Tidak** bisa punya function |
| Number tidak perlu tanda kutip | Number tidak perlu tanda kutip ✅ |

### 📌 Contoh Perbandingan:

**JavaScript Object:**
```javascript
const user = {
  name: 'John',    // ← key tanpa quotes
  age: 30
};
```

**JSON:**
```json
{
  "name": "John",  // ← key dengan double quotes
  "age": 30
}
```

---

<a name="contoh-api-yang-menggunakan-json"></a>
## 🌐 Contoh API yang Menggunakan JSON

GitHub menyediakan API publik yang bisa kita gunakan untuk belajar!

### 🔗 Endpoint GitHub API:

**1. Mendapatkan daftar users:**
```
https://api.github.com/users
```
☝️ Mengembalikan array berisi 20 user pertama GitHub

**2. Mendapatkan info user tertentu:**
```
https://api.github.com/users/bradtraversy
```
☝️ Mengembalikan informasi detail tentang user `bradtraversy`

**3. Mendapatkan repositories user:**
```
https://api.github.com/users/bradtraversy/repos
```
☝️ Mengembalikan array berisi semua repository milik user tersebut

### 📊 Struktur Response API:

Response dari API biasanya berupa:
- **Array of objects** (daftar data)
- **Single object** (data tunggal)

Setiap object memiliki **key-value pairs** seperti:
```json
{
  "login": "bradtraversy",
  "id": 123456,
  "avatar_url": "https://...",
  "repos_url": "https://api.github.com/users/bradtraversy/repos"
}
```

---

<a name="membuat-file-json"></a>
## 📄 Membuat File JSON

Kamu bisa membuat file `.json` sendiri!

### 📝 Contoh: `todo.json`

```json
[
  {
    "id": "1",
    "title": "Take out trash"
  }
]
```

### ⚠️ Aturan Penting:

1. ✅ Key **HARUS** pakai **double quotes** `"`
2. ✅ String value **HARUS** pakai **double quotes** `"`
3. ✅ Number **TIDAK** perlu quotes
4. ❌ Tidak boleh ada trailing comma (koma di akhir)

### 🔧 Validasi JSON:

VS Code akan otomatis memvalidasi file `.json` kamu:
- Jika ada error, akan muncul garis merah
- Hover mouse untuk melihat detail error
- Contoh error: `"Property keys must be double quoted"`

---

<a name="mengubah-javascript-object-ke-json-string"></a>
## 🔄 Mengubah JavaScript Object ke JSON String

Gunakan method **`JSON.stringify()`** untuk mengubah JavaScript object menjadi JSON string.

### 📌 Contoh Kode:

```javascript
// 1️⃣ Membuat JavaScript Object
const post = {
  id: 1,
  title: 'Post One',
  body: 'This is the body',
};

console.log(post);
// Output: {id: 1, title: 'Post One', body: 'This is the body'}

// 2️⃣ Convert ke JSON String
const str = JSON.stringify(post);

console.log(str);
// Output: '{"id":1,"title":"Post One","body":"This is the body"}'
```

### 💡 Penjelasan:

- **`post`** adalah JavaScript object biasa (bisa diklik/expand di console)
- **`str`** adalah JSON string (tampil sebagai text biasa, tidak bisa diklik)
- JSON string inilah yang **dikirim ke server** saat kamu POST data!

### 🎯 Kapan Menggunakan `JSON.stringify()`?

1. **Saat menyimpan data ke Local Storage** (Local Storage hanya bisa menyimpan string)
2. **Saat mengirim data ke API** (meski fetch API sering melakukannya otomatis)
3. **Saat ingin mem-print object sebagai string**

---

<a name="mengubah-json-string-ke-javascript-object"></a>
## 🔙 Mengubah JSON String ke JavaScript Object

Gunakan method **`JSON.parse()`** untuk mengubah JSON string kembali menjadi JavaScript object.

### 📌 Contoh Kode:

```javascript
// 1️⃣ Kita punya JSON String (dari stringify sebelumnya)
const str = '{"id":1,"title":"Post One","body":"This is the body"}';

// 2️⃣ Parse JSON String ke Object
const obj = JSON.parse(str);

console.log(obj);
// Output: {id: 1, title: 'Post One', body: 'This is the body'}

// 3️⃣ Sekarang kita bisa akses propertinya
console.log(obj.id);      // Output: 1
console.log(obj.title);   // Output: 'Post One'
```

### ⚠️ Perbedaan Penting:

```javascript
// ❌ TIDAK BISA akses property dari JSON String
console.log(str.id);
// Output: undefined

// ✅ BISA akses property setelah di-parse
console.log(obj.id);
// Output: 1
```

### 🎯 Kapan Menggunakan `JSON.parse()`?

1. **Saat mengambil data dari Local Storage** (data tersimpan sebagai string)
2. **Saat menerima response dari API** (meski fetch API sering melakukannya otomatis)
3. **Saat ingin bekerja dengan JSON string sebagai object**

---

<a name="bekerja-dengan-array-of-objects"></a>
## 📚 Bekerja dengan Array of Objects

JSON tidak hanya untuk single object, tapi juga untuk **array of objects**!

### 📌 Contoh Kode:

```javascript
// 1️⃣ Membuat Array of Objects
const posts = [
  {
    id: 1,
    title: 'Post One',
    body: 'This is the body',
  },
  {
    id: 2,
    title: 'Post Two',
    body: 'This is the body',
  },
];

// 2️⃣ Convert Array ke JSON String
const str2 = JSON.stringify(posts);

console.log(str2);
// Output: '[{"id":1,"title":"Post One","body":"This is the body"},{"id":2,"title":"Post Two","body":"This is the body"}]'
```

### 💡 Penjelasan:

- Array tetap menggunakan **bracket** `[]`
- Setiap object di dalam array dipisahkan dengan **comma** `,`
- Proses stringify dan parse **sama** seperti single object

### 🔄 Parse Array JSON:

```javascript
// Parse kembali ke JavaScript Array
const parsedPosts = JSON.parse(str2);

console.log(parsedPosts[0]);     // Object pertama
console.log(parsedPosts[0].id);  // Output: 1
console.log(parsedPosts[1].title); // Output: 'Post Two'
```

---

<a name="hal-penting-yang-perlu-diingat"></a>
## ⚠️ Hal Penting yang Perlu Diingat

### 🚫 JANGAN Lakukan Ini:

```javascript
const str = '{"id":1,"title":"Post One"}';

// ❌ Akses property dari JSON String = undefined
console.log(str.id);        // undefined
console.log(str.title);     // undefined
```

### ✅ LAKUKAN Ini:

```javascript
const str = '{"id":1,"title":"Post One"}';

// ✅ Parse dulu, baru akses property
const obj = JSON.parse(str);
console.log(obj.id);        // 1
console.log(obj.title);     // 'Post One'
```

### 📝 Aturan Emas JSON:

1. **Double quotes** untuk key dan string value
2. **No trailing comma** (koma terakhir)
3. **No functions** atau methods
4. **No comments** dalam file JSON
5. **Parse sebelum akses** property

---

<a name="ringkasan"></a>
## 📝 Ringkasan

### 🎯 Dua Method Utama:

| Method | Fungsi | Input | Output |
|--------|--------|-------|--------|
| `JSON.stringify()` | Object → String | JavaScript Object/Array | JSON String |
| `JSON.parse()` | String → Object | JSON String | JavaScript Object/Array |

### 🔄 Flow Kerja JSON:

```
JavaScript Object
      ↓ JSON.stringify()
   JSON String (kirim ke server/simpan ke storage)
      ↓ JSON.parse()
JavaScript Object (bisa diakses propertinya)
```

### 💼 Kapan Menggunakan JSON:

- ✅ **API Communication** - Kirim/terima data dari server
- ✅ **Local Storage** - Simpan data di browser
- ✅ **Configuration Files** - File config aplikasi
- ✅ **Data Transfer** - Transfer data antar sistem

### 🚀 Next Steps:

Setelah memahami JSON, langkah selanjutnya adalah:
1. Belajar **Fetch API** untuk mengambil data dari server
2. Belajar **Local Storage** untuk menyimpan data di browser
3. Belajar **REST API** untuk komunikasi client-server
4. Praktik membuat aplikasi yang konsumsi API

---

## 🎓 Latihan Tambahan

Coba praktikkan ini untuk memahami lebih dalam:

```javascript
// 1. Buat object student
const student = {
  name: 'Budi',
  age: 20,
  grade: 'A'
};

// 2. Convert ke JSON string
const studentStr = JSON.stringify(student);
console.log(studentStr);

// 3. Parse kembali ke object
const studentObj = JSON.parse(studentStr);
console.log(studentObj.name); // 'Budi'

// 4. Coba dengan array
const students = [student, {name: 'Ani', age: 19, grade: 'B'}];
const studentsStr = JSON.stringify(students);
console.log(studentsStr);
```

---

## 🎉 Selamat!

Kamu sekarang sudah memahami dasar-dasar JSON! Terus praktik dan explore API-API gratis untuk meningkatkan skill kamu. Happy coding! 🚀

---

**📅 Terakhir diupdate**: Dokumentasi pribadi untuk belajar JSON dengan JavaScript

**🔖 Tags**: #JSON #JavaScript #Pemula #WebDevelopment #API
