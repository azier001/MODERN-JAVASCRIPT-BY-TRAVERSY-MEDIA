# 📚 Panduan Array Methods JavaScript untuk Pemula

## 🎯 Pengantar

Setelah mempelajari dasar-dasar array (cara membuat dan mengakses nilai berdasarkan index), sekarang kita akan belajar berbagai **method** (fungsi bawaan) yang bisa digunakan untuk memanipulasi array.

---

## 🔧 Persiapan Awal

Mari kita buat array sederhana untuk praktek:

```javascript
let x;
const arr = [28, 38, 44, 29, 109];
```

### 💡 Prototype Chain
Setiap array memiliki **prototype chain** yang berisi semua method yang bisa digunakan. Anda bisa melihatnya dengan `console.log(arr)` dan klik arrow di console browser.

---

## 📝 Method untuk Mengubah Array

### 1️⃣ `push()` - Menambah di Akhir

> ✨ **Fungsi**: Menambahkan nilai baru di **akhir** array

```javascript
arr.push(100);
console.log(arr); // [28, 38, 44, 29, 109, 100]
```

**Penjelasan**: Method `push()` menambahkan angka `100` ke posisi terakhir array.

---

### 2️⃣ `pop()` - Menghapus dari Akhir

> ✨ **Fungsi**: Menghapus dan mengembalikan elemen **terakhir**

```javascript
arr.pop();
console.log(arr); // [28, 38, 44, 29, 109]
```

**Penjelasan**: Method `pop()` menghapus elemen terakhir (100 yang baru saja ditambahkan). Tidak perlu parameter.

---

### 3️⃣ `unshift()` - Menambah di Awal

> ✨ **Fungsi**: Menambahkan nilai baru di **awal** array

```javascript
arr.unshift(99);
console.log(arr); // [99, 28, 38, 44, 29, 109]
```

**Penjelasan**: Method `unshift()` menambahkan `99` ke index 0 (posisi pertama).

---

### 4️⃣ `shift()` - Menghapus dari Awal

> ✨ **Fungsi**: Menghapus dan mengembalikan elemen **pertama**

```javascript
arr.shift();
console.log(arr); // [28, 38, 44, 29, 109]
```

**Penjelasan**: Method `shift()` menghapus elemen pertama (99 yang baru ditambahkan).

---

### 5️⃣ `reverse()` - Membalik Urutan

> ✨ **Fungsi**: Membalik urutan seluruh elemen array

```javascript
arr.reverse();
console.log(arr); // [109, 29, 44, 38, 28]
```

⚠️ **PENTING**: Method ini **mengubah array asli** (mutates original array).

---

## 🔍 Method untuk Mencari Informasi

### 6️⃣ `includes()` - Mengecek Keberadaan Nilai

> ✨ **Fungsi**: Mengecek apakah nilai tertentu ada dalam array

```javascript
x = arr.includes(445);
console.log(x); // false (445 tidak ada dalam array)

x = arr.includes(28);
console.log(x); // true (28 ada dalam array)
```

**Penjelasan**: 
- Mengembalikan `true` jika nilai **ditemukan**
- Mengembalikan `false` jika nilai **tidak ditemukan**

---

### 7️⃣ `indexOf()` - Mencari Posisi Index

> ✨ **Fungsi**: Mengembalikan **index** dari nilai yang dicari

```javascript
x = arr.indexOf(28);
console.log(x); // 0 (atau posisi lain tergantung urutan array)

x = arr.indexOf(340);
console.log(x); // -1 (tidak ditemukan)
```

**Penjelasan**:
- Mengembalikan **index** (angka 0, 1, 2, dst) jika nilai ditemukan
- Mengembalikan **-1** jika nilai tidak ditemukan

💡 **Tips**: Sering digunakan dalam kondisi `if` untuk validasi.

---

## 🔄 Method untuk Konversi Format

### 8️⃣ `toString()` - Ubah ke String

> ✨ **Fungsi**: Mengubah array menjadi **string**

```javascript
x = arr.toString();
console.log(x); // "28,38,44,29,109"
```

---

### 9️⃣ `join()` - Ubah ke String dengan Pemisah

> ✨ **Fungsi**: Mengubah array ke string dengan pemisah kustom

```javascript
x = arr.join();
console.log(x); // "28,38,44,29,109" (default: koma)

x = arr.join(' - ');
console.log(x); // "28 - 38 - 44 - 29 - 109"
```

---

## ✂️ Method untuk Mengambil Sebagian Array

### 🔟 `slice()` - Salin Sebagian (Tidak Mengubah Array Asli)

> ✨ **Fungsi**: Mengambil sebagian elemen **TANPA** mengubah array asli

```javascript
const arr = [28, 38, 44, 29, 109];
x = arr.slice(1, 4);

console.log(x);   // [38, 44, 29]
console.log(arr); // [28, 38, 44, 29, 109] (TIDAK BERUBAH)
```

**Cara Kerja**:
- Parameter 1: **Index awal** (mulai dari sini)
- Parameter 2: **Index akhir** (berhenti **sebelum** index ini)

**Contoh Visual**:
```
Array: [28, 38, 44, 29, 109]
Index:  0   1   2   3   4

slice(1, 4) mengambil index 1, 2, 3
Hasil: [38, 44, 29]
```

✅ **Keuntungan**: Array asli tetap aman!

---

### 1️⃣1️⃣ `splice()` - Potong Sebagian (Mengubah Array Asli)

> ✨ **Fungsi**: Mengambil sebagian elemen **DAN MENGUBAH** array asli

```javascript
const arr = [28, 38, 44, 29, 109];
x = arr.splice(1, 4);

console.log(x);   // [38, 44, 29, 109]
console.log(arr); // [28] (ARRAY BERUBAH!)
```

**Cara Kerja**:
- Parameter 1: **Index awal** (mulai dari sini)
- Parameter 2: **Jumlah elemen** yang ingin diambil

**Contoh Visual**:
```
Array Awal: [28, 38, 44, 29, 109]
Index:       0   1   2   3   4

splice(1, 4) → Ambil 4 elemen mulai dari index 1
Hasil x:     [38, 44, 29, 109]
Array sisa:  [28]
```

⚠️ **PERINGATAN**: Method ini **mengubah array asli**!

---

### 🎯 Menghapus 1 Elemen Spesifik

> ✨ **Use Case**: Menghapus satu elemen tertentu dari array

```javascript
const arr = [28, 38, 44, 29, 109];
x = arr.splice(3, 1); // Hapus 1 elemen di index 3

console.log(x);   // [29] (elemen yang dihapus)
console.log(arr); // [28, 38, 44, 109] (29 sudah hilang)
```

**Penjelasan**: `splice(3, 1)` artinya "Mulai dari index 3, hapus 1 elemen"

---

## 🔗 Method Chaining (Menggabungkan Method)

> ✨ **Konsep**: Menjalankan beberapa method sekaligus secara berurutan

```javascript
const arr = [28, 38, 44, 29, 109];
x = arr.slice(1, 4).reverse().toString().charAt(0);

console.log(x); // "2"
```

### 📖 Penjelasan Step-by-Step:

```javascript
// Step 1: slice(1, 4)
[38, 44, 29]

// Step 2: .reverse()
[29, 44, 38]

// Step 3: .toString()
"29,44,38"

// Step 4: .charAt(0)
"2"
```

### 🔑 Aturan Chaining:
1. ✅ Method harus mengembalikan nilai yang bisa di-chain
2. ✅ `reverse()` bisa di-chain karena return array
3. ✅ `toString()` bisa di-chain karena return string
4. ✅ `charAt()` berfungsi pada string
5. ❌ Tidak semua method bisa di-chain!

**Contoh Error**:
```javascript
// ❌ SALAH - charAt() tidak bisa digunakan pada array
x = arr.reverse().charAt(0); // Error!

// ✅ BENAR - Harus diubah ke string dulu
x = arr.reverse().toString().charAt(0); // OK!
```

---

## 📊 Tabel Ringkasan Method

| Method | Fungsi | Ubah Array? | Return Value |
|--------|--------|-------------|--------------|
| `push()` | Tambah di akhir | ✅ Ya | Panjang array baru |
| `pop()` | Hapus dari akhir | ✅ Ya | Elemen yang dihapus |
| `unshift()` | Tambah di awal | ✅ Ya | Panjang array baru |
| `shift()` | Hapus dari awal | ✅ Ya | Elemen yang dihapus |
| `reverse()` | Balik urutan | ✅ Ya | Array terbalik |
| `includes()` | Cek keberadaan | ❌ Tidak | `true`/`false` |
| `indexOf()` | Cari posisi | ❌ Tidak | Index atau -1 |
| `toString()` | Ubah ke string | ❌ Tidak | String |
| `join()` | Ubah ke string + pemisah | ❌ Tidak | String |
| `slice()` | Salin sebagian | ❌ Tidak | Array baru |
| `splice()` | Potong sebagian | ✅ Ya | Array yang dipotong |

---

## 💡 Tips Belajar

### 🎯 Kapan Pakai `slice()` vs `splice()`?

**Gunakan `slice()` jika**:
- ✅ Ingin menyalin sebagian array
- ✅ Ingin array asli tetap utuh
- ✅ Hanya butuh membaca data

**Gunakan `splice()` jika**:
- ✅ Ingin menghapus elemen dari array
- ✅ Ingin mengubah array asli
- ✅ Ingin "memotong" bagian array

---

## 🚀 Latihan Mandiri

Coba praktek sendiri dengan kode ini:

```javascript
// Buat array sendiri
const myArray = [10, 20, 30, 40, 50];

// 1. Tambahkan 60 di akhir
// 2. Tambahkan 5 di awal
// 3. Cek apakah ada angka 30
// 4. Cari posisi angka 40
// 5. Ambil elemen index 1-3 tanpa mengubah array
// 6. Hapus elemen index 2

console.log(myArray);
```

<details>
<summary>🔍 <strong>Klik di sini untuk melihat jawaban</strong></summary>
  
```javascript
// Buat array sendiri
const myArray = [10, 20, 30, 40, 50];

// 1. Tambahkan 60 di akhir
myArray.push(60);
console.log('Setelah push(60):', myArray); 
// [10, 20, 30, 40, 50, 60]

// 2. Tambahkan 5 di awal
myArray.unshift(5);
console.log('Setelah unshift(5):', myArray); 
// [5, 10, 20, 30, 40, 50, 60]

// 3. Cek apakah ada angka 30
const ada30 = myArray.includes(30);
console.log('Apakah ada 30?', ada30); 
// true

// 4. Cari posisi angka 40
const posisi40 = myArray.indexOf(40);
console.log('Posisi angka 40 di index:', posisi40); 
// 4

// 5. Ambil elemen index 1-3 tanpa mengubah array
const sebagian = myArray.slice(1, 4);
console.log('Elemen index 1-3:', sebagian); 
// [10, 20, 30]
console.log('Array asli tetap utuh:', myArray); 
// [5, 10, 20, 30, 40, 50, 60]

// 6. Hapus elemen index 2
const dihapus = myArray.splice(2, 1);
console.log('Elemen yang dihapus:', dihapus); 
// [20]
console.log('Array setelah splice:', myArray); 
// [5, 10, 30, 40, 50, 60]

```

### 📝 Penjelasan Step-by-Step:

1. **push(60)**: Menambah 60 di akhir → `[10, 20, 30, 40, 50, 60]`
2. **unshift(5)**: Menambah 5 di awal → `[5, 10, 20, 30, 40, 50, 60]`
3. **includes(30)**: Mengecek apakah ada 30 → `true`
4. **indexOf(40)**: Mencari posisi 40 → index `4`
5. **slice(1, 4)**: Salin index 1-3 → `[10, 20, 30]` (array asli tidak berubah)
6. **splice(2, 1)**: Hapus 1 elemen di index 2 → menghapus `20`

**💡 Hasil Akhir Array**: `[5, 10, 30, 40, 50, 60]`

</details>

---

## 📚 Materi Selanjutnya

Di video berikutnya, kita akan belajar:
- 🔹 **Nesting Array** (Array di dalam array)
- 🔹 **Concatenation** (Menggabungkan array)
- 🔹 **Spread Operator** (`...`)
- 🔹 Dan lebih banyak lagi! 🎉

---

## ✨ Kesimpulan

Selamat! 🎊 Kamu sudah mempelajari berbagai method array JavaScript yang paling sering digunakan. Ingat:

1. ⚡ Beberapa method **mengubah array asli** (push, pop, splice, reverse)
2. 🛡️ Beberapa method **tidak mengubah** array asli (slice, includes, indexOf)
3. 🔗 Method bisa di-**chain** jika return valuenya mendukung
4. 📖 Praktik adalah kunci! Coba semua method di console browser

**Happy Coding!** 💻✨
