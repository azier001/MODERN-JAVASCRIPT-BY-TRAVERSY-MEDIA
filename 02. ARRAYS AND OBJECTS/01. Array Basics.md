# 📚 Dokumentasi Array JavaScript untuk Pemula

## 🎯 Apa itu Array?

**Array** adalah struktur data khusus di JavaScript yang dapat menyimpan **multiple values** (banyak nilai) dalam satu variabel. Array termasuk tipe objek spesial yang sangat berguna untuk mengelola kumpulan data.

---

## 🔧 Cara Membuat Array

### 1️⃣ **Array Literal** (Cara Paling Umum)

Array literal menggunakan **tanda kurung siku `[]`** untuk membungkus nilai-nilainya.

```javascript
const numbers = [12, 45, 33, 29, 39, 102];
```

> 💡 **Penjelasan**: Ini adalah cara paling sederhana dan paling sering digunakan untuk membuat array.

---

### 2️⃣ **Array Constructor**

Menggunakan kata kunci `new Array()` untuk membuat array.

```javascript
const fruits = new Array('apple', 'grape', 'orange');
```

> 💡 **Penjelasan**: Meskipun bisa digunakan, cara ini kurang populer karena lebih panjang. Hasilnya sama dengan array literal.

---

## 📦 Elemen dalam Array

Nilai-nilai yang ada di dalam array disebut **elements** (elemen). Setiap elemen bisa berisi berbagai tipe data:

```javascript
const mixed = [12, 'Hello', true, null];
```

| Index | Nilai | Tipe Data |
|-------|-------|-----------|
| 0 | 12 | Number |
| 1 | 'Hello' | String |
| 2 | true | Boolean |
| 3 | null | Null |

> ⚠️ **Penting**: Array bisa menampung berbagai tipe data sekaligus dalam satu array!

---

## 🔢 Index dalam Array

Array menggunakan sistem **zero-based indexing**, artinya elemen pertama dimulai dari index **0**, bukan 1.

```javascript
const numbers = [12, 45, 33, 29, 39, 102];
//              [0]  [1] [2] [3] [4]  [5]
```

### ✨ Cara Mengakses Elemen

```javascript
let x;

x = numbers[0];  // Hasil: 12 (elemen pertama)
x = numbers[3];  // Hasil: 29 (elemen keempat)
```

> 💡 **Ingat**: Index 0 = elemen pertama, Index 1 = elemen kedua, dan seterusnya!

---

## 🧮 Operasi dengan Elemen Array

### ➕ Operasi Matematika

```javascript
x = numbers[0] + numbers[3];  // 12 + 29 = 41
```

### 📝 Menggunakan dalam Template Literal

```javascript
x = `My favorite fruit is an ${fruits[2]}`;
// Hasil: "My favorite fruit is an orange"
```

> 💡 **Penjelasan**: `fruits[2]` adalah elemen ketiga (index 2) yaitu 'orange'

---

## 📏 Property `length`

Property `length` mengembalikan **jumlah elemen** dalam array.

```javascript
x = numbers.length;  // Hasil: 6
```

> 🎯 **Kegunaan**: Sangat berguna untuk mengetahui berapa banyak item dalam array, terutama saat bekerja dengan data dinamis.

---

## ✏️ Mengubah Nilai Elemen

Array bersifat **mutable** (dapat diubah). Kita bisa mengubah nilai elemen kapan saja:

```javascript
fruits[2] = 'pear';
// Sebelum: ['apple', 'grape', 'orange']
// Sesudah:  ['apple', 'grape', 'pear']
```

> ⚡ **Penting**: Meskipun menggunakan `const`, isi array tetap bisa diubah!

---

## ➕ Menambahkan Elemen Baru

### Cara 1: Menggunakan Index Tertentu

```javascript
fruits[3] = 'strawberry';
// Hasil: ['apple', 'grape', 'pear', 'strawberry']
```

### Cara 2: Menggunakan `length` sebagai Index ⭐

Ini adalah trik pintar untuk menambahkan elemen di akhir array:

```javascript
fruits[fruits.length] = 'blueberry';
fruits[fruits.length] = 'peach';
```

> 💡 **Mengapa ini bekerja?**
> - Jika array punya 3 elemen, `length` = 3
> - Index terakhir adalah 2 (karena dimulai dari 0)
> - Maka `fruits[3]` akan menambah elemen baru di posisi keempat
> - `length` selalu 1 lebih besar dari index terakhir!

**Visualisasi:**
```
Sebelum: ['apple', 'grape', 'pear']
         [   0   ] [   1  ] [  2  ]
         length = 3

fruits[fruits.length] = 'blueberry'
         ↓
fruits[3] = 'blueberry'

Sesudah: ['apple', 'grape', 'pear', 'blueberry']
         [   0   ] [   1  ] [  2  ] [     3     ]
         length = 4
```

---

## 🚫 Mengubah Length (Jarang Digunakan)

Property `length` **tidak read-only**, artinya bisa diubah:

```javascript
// ⚠️ Hati-hati! Ini akan memotong array
// fruits.length = 2;
// Hasil: ['apple', 'grape'] - elemen lainnya hilang!
```

> ⚠️ **Peringatan**: Biasanya tidak disarankan karena bisa menghilangkan data secara tidak sengaja.

---

## 📝 Kode Lengkap

```javascript
let x;

// Array Literal
const numbers = [12, 45, 33, 29, 39, 102];
const mixed = [12, 'Hello', true, null];

// Array Constructor
const fruits = new Array('apple', 'grape', 'orange');

// Get value by index
x = numbers[0];

x = numbers[0] + numbers[3];

x = `My favorite fruit is an ${fruits[2]}`;

x = numbers.length;

fruits[2] = 'pear';

// length is not read-only
// fruits.length = 2;

fruits[3] = 'strawberry';

// Using the length as the index will always add on the end
fruits[fruits.length] = 'blueberry';
fruits[fruits.length] = 'peach';

x = fruits;

console.log(x);
```

---

## 🎓 Rangkuman

| Konsep | Penjelasan | Contoh |
|--------|------------|--------|
| 🏗️ **Membuat Array** | Gunakan `[]` atau `new Array()` | `[1, 2, 3]` |
| 📍 **Index** | Dimulai dari 0 | `arr[0]` = elemen pertama |
| 📏 **Length** | Jumlah elemen | `arr.length` |
| ✏️ **Mengubah** | Akses index dan assign nilai baru | `arr[0] = 10` |
| ➕ **Menambah** | Gunakan `arr[arr.length]` | `arr[arr.length] = 'new'` |

---

## 🚀 Langkah Selanjutnya

Di video berikutnya, kita akan belajar tentang **Array Methods** seperti:
- `push()` - cara lebih mudah menambah elemen
- `pop()` - menghapus elemen terakhir
- `shift()` - menghapus elemen pertama
- `unshift()` - menambah di awal
- Dan masih banyak lagi!

---

## 💪 Latihan

Coba buat array sendiri dengan:
1. 5 nama teman
2. Ubah nama kedua
3. Tambahkan nama baru di akhir menggunakan `length`
4. Tampilkan hasilnya dengan `console.log()`

**Selamat belajar! 🎉**
